# M1 — 串口通信（SerialPort）

> **优先级定位**：🔴 必学 · 串口 RS232/485（JD 主干必会）
> **技术来源**：🟩 .NET 类库 `System.IO.Ports`（.NET 8 需 `dotnet add package System.IO.Ports`；它是微软官方 BCL 扩展，不是第三方，装了就行、随 .NET 版本）。
> **给简历加的能力**：真实 / 虚拟串口设备接入 —— 上位机对接下位机、仪表、传感器的**最普遍**方式。
> **前置**：M0（类 / 事件 / 并发）。M1 是第一次把"后台收数 → 事件 → UI"串成真实链路。

> ⏱️ **阅读路径**(按时间预算选入口)
> - **3 分钟**:看「模块目标」+ 「Day 1 一句话讲清楚」— 知道串口收发流程
> - **30 分钟**:加看 Day 1 波特率/RS232/485 对比表 + 写一个 Hello World 收发
> - **3 小时**:全文精读 + Day 1-2 串口采集 + Day 3 RawLog 联调
> - 🎯 **面试高频**:RS232 vs RS485(差分/距离/总线)/ 9600-8-N-1 四参数 / DataReceived 跨线程
> - 🔁 **配套复习**:[速记卡 Q7-Q8](面试高频知识点_速记卡.md) · [代码肌肉 B3 IDevice 实现](代码肌肉训练手册_30天刷题版.md) · [间隔重复表](记忆与复习机制_间隔重复版.md)

> 📚 **前置语法**(M1 用到的,陌生请查 [C# 语法速查 — 前端视角](CSharp语法速查_前端视角.md))
> - `byte[]` / `byte n = 0x41` — 字节数组与十六进制字面量
> - `event EventHandler<T>?` / `E?.Invoke(this, args)` — 事件发布订阅,速查 §7
> - `Action<T>` / `Func<T,TResult>` — 委托类型,速查 §7
> - `=> x => x * 2` — Lambda,速查 §7
> - `async Task` / `await` — 异步,速查 §8
> - `using var port = ...` — 资源释放(SerialPort 必须释放),速查 §13

> 📦 **前置类型**(本模块示例代码用到的核心自定义类型)
> M1 示例引用 `DeviceBase` / `IDevice` / `SensorPoint` 等类型 — 这些在 [📦 前置类型定义 · 学员粘贴版](前置类型定义_学员粘贴版.md) **集中定义**。**遇到"找不到类型 XXX"报错,先去那份文档复制对应类型**,在项目里建 `_PredefinedTypes.cs` 粘进去就能跑。本模块还会**新建** `SerialDevice : DeviceBase` 和 `FrameParser`,跟着 Day 1-3 敲即可。

## 模块目标
写出一个串口采集器：配置波特率 → 打开端口 → 接收字节流 → 解析成业务数据 → 通过 `DataReceived` 事件抛给 UI → 显示在 DAQ Monitor。

## Day 1 — 串口基础 + 收发第一个字节

### 一句话讲清楚
串口 = 一条"按字节排队"的双向数据线。上位机和下位机约定好"语速"(波特率)和"帧格式"(数据位 / 停止位 / 校验)，然后一个发字节、一个收字节。

### 前端类比秒懂
| 串口概念 | 前端类比 | 说明 |
|---|---|---|
| `SerialPort` | `WebSocket` / Node `serialport` | 建立连接通道 |
| 波特率 9600 / 115200 | 带宽 / 速率 | 双方必须一致 |
| `DataReceived` 事件 | `onmessage` | 收到数据回调 |
| 字节流 `byte[]` | `ArrayBuffer` | 裸二进制，要自己解析 |
| 虚拟串口 | `localhost` 回环 | 没硬件也能练 |

### 分点精讲
**① 装包 + 建立端口**（🟩）

> 🔧 **必装 NuGet**(在 `src/DaqMonitor.Core/` 目录执行,不是解决方案根!):
> ```bash
> cd src/DaqMonitor.Core        # 切到 Core 项目目录
> dotnet add package System.IO.Ports
> ```
> 这是微软官方包(🟩 不是第三方),但 .NET 8 把它从框架里拆出来了,所以要单独装。
> 装完跑 `dotnet build`,应该 0 错误。

> 📂 **代码放哪**:这段示例是"语法演示",可以直接粘到 `DaqMonitor.Core` 项目里一个新建的 `Playground.cs` 测试。**真实工程**里请放到 `DaqMonitor.Core/Devices/SerialDevice.cs`(见 Day 1 末「🏗️ 项目任务」)。

```csharp
using System;
using System.IO.Ports;          // 装了 System.IO.Ports 包后才能 using

var port = new SerialPort("COM3", 9600, Parity.None, 8, StopBits.One);
port.Open();
port.WriteLine("AT\r\n");                 // 发字符串
int n = port.BytesToRead;
byte[] buf = new byte[n];
port.Read(buf, 0, n);                    // 收字节
port.Close();
```

**② 异步收数（事件驱动）**（🟩 + 🟦）
```csharp
port.DataReceived += (s, e) =>
{
    var sp = (SerialPort)s!;
    int n = sp.BytesToRead;
    byte[] data = new byte[n];
    sp.Read(data, 0, n);
    Console.WriteLine($"收到 {n} 字节");   // ⚠️ 这个回调在【后台线程】！
};
```
> ⚠️ 串口 `DataReceived` 是后台线程触发的（呼应 M0 Day 7）。要在 WPF 改界面必须用 `Application.Current.Dispatcher.Invoke(...)`。

**③ 虚拟串口练手（无硬件方案）**
- Windows 装 `com0com` 或 `VSPD`，虚拟出 `COM3<->COM4` 一对；DAQ Monitor 开 COM3，另写个小发送器开 COM4 发数据，就能闭环测试，零硬件成本。

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ 波特率双方一致 | 下位机 9600、上位机 115200，收的全是乱码 |
| ⭐ `DataReceived` 是后台线程 | 跨线程改 UI 必崩，用 `Dispatcher.Invoke` |
| 🔥 字节流要自己分包 | 串口不保证"一次发一次收"，可能半包 / 粘包，要按协议帧(头+长度)拼 |
| 🔥 中文乱码 | 下位机常用 GBK，上位机默认 UTF-8，要 `port.Encoding = Encoding.GetEncoding("GBK")` |
| 🔥 忘记 `Close()` | 端口被占用，下次 `Open()` 抛"拒绝访问" |

### ✏️ 练习
开一个 `SerialPort`，订阅 `DataReceived`，把收到的字节数与十六进制打印出来；用虚拟串口工具从对端发一串数据验证。

**✅ 答案**
```csharp
var port = new SerialPort("COM3", 9600);
port.DataReceived += (s, e) =>
{
    var sp = (SerialPort)s!;
    int n = sp.BytesToRead;
    byte[] buf = new byte[n];
    sp.Read(buf, 0, n);
    Console.WriteLine($"收到 {n} 字节：{BitConverter.ToString(buf)}");
};
port.Open();
Console.WriteLine("按任意键退出"); Console.ReadKey(); port.Close();
```

**🏗️ 项目任务**：

> 📂 在 `DaqMonitor.Core/Devices/SerialDevice.cs` · namespace `DaqMonitor.Core.Devices`
> 🔧 已装 `System.IO.Ports`(本 Day ① 已装)
> 💡 继承 `DeviceBase`(M0 Day 5) + 用 `SensorPoint`/`DeviceState`(M0 Day 2)

新建 `SerialDevice : DeviceBase`,`Connect()` 里 `new SerialPort` 并 `Open()`;`DataReceived` 里把字节喂给"帧解析器",解析出的点用 `RaiseData` 抛给 UI。先在测试工程里用虚拟串口跑通。

> 🔗 **本项目已含参考实现（可直接读，不必从零）**：`DaqMonitor.Core/Devices/` 下已有 `ISerialChannel`/`RealSerialChannel`/`LoopbackSerialChannel`/`SerialDevice`（含 `FrameParser.Build` 双向建帧 + CRC 校验），并配 5 个单测全绿（单帧/粘包/半包/坏CRC/管道集成）。**建议**：先读这份实现理清思路 → 自己照着重写一遍加深记忆 → 跑 `dotnet test` 看那 5 个绿。这就是"边做边学"：你手里的工程已经是能跑的真实代码。

**✅ 打卡[ ]**

---

### 🎭 拟人秒懂:RS232 vs RS485(面试高频考点画面感记忆)

> 把串口通信介质想象成"两种不同的电话"。

| 维度 | **RS232**(单车道电话) | **RS485**(对讲机总线) |
|---|---|---|
| **信号方式** | 单端(信号 vs 地线)— 像"电压喊话" | **差分**(两根线 A/B,看电压差)— 像"两人举旗方向" |
| **抗干扰** | 弱 — 电磁干扰直接进入信号 | **强** — 干扰同时打到两根线上,**电压差不变** |
| **传输距离** | < 15 米(短距离) | **< 1200 米**(工厂车间够用) |
| **拓扑** | 点对点(1 对 1) | **总线**(1 主多从,可挂 32 个设备) |
| **速率** | 9600-115200 bps | 同样,但长距离会降速 |
| **典型场景** | PC ↔ 单台仪表、调试口 | PLC ↔ 多台变频器、车间总线 |

**为什么工厂 90% 用 RS485**:
- **差分信号抗干扰**:工厂车间电磁干扰强(电机启停、变频器、电焊机),RS232 一干扰就乱码,RS485 两根线一起被干扰但**电压差不变**,信号保真
- **总线拓扑**:一根 RS485 线串 32 个设备(每个有唯一从站地址),省线材 — 工厂动辄几十台仪表,RS232 一台一根线不现实
- **长距离**:15 米连车间都过不去,1200 米能跑全厂

**记忆口诀**:
- **232 = 短、点对点、抗干扰弱**(像家用电话)— PC 调试用
- **485 = 长、总线、抗干扰强**(像军用对讲机)— 车间生产用

**9600-8-N-1 四参数是什么**:
- **9600**:波特率(语速)— 双方必须一致,否则听不懂
- **8**:数据位(每个字几位)— 8 位 = 1 byte
- **N**:校验位(None/Even/Odd)— N=不校验,E=偶校验,O=奇校验
- **1**:停止位(标志字节结束)— 1 位或 2 位
- 口诀:**"语速 9600 / 每字 8 位 / 无校验 / 1 位停止"** — 这是 90% 工业设备默认配置

**新手 100% 翻车场景**:波特率不匹配 → 收的全是乱码。**调试第一招:用万用表/示波器测波特率,或抓包看字节是不是合理范围**(ASCII 设备应在 0x20-0x7E 范围内,出现大量 0x80+ 多半是波特率错了)。

---

## Day 2 — 协议帧解析（把字节流变成业务数据）

### 一句话讲清楚
裸字节没意义，必须按"协议帧"切包：每个帧有【帧头 + 长度 + 数据 + 校验】，像处理 TCP 粘包一样拼包、校验、提取。

### 前端类比秒懂
| 协议解析 | 前端类比 |
|---|---|
| 帧头 `0xAA 0x55` | 自定义二进制协议的分隔符 |
| 长度字段 | `Content-Length` / WebSocket 帧头 |
| CRC16 校验 | 数据完整性校验（类比哈希） |

### 分点精讲
**① 拼包缓冲（解决半包 / 粘包）**（🟦）
```csharp
private readonly List<byte> _buffer = new();
private void OnBytes(byte[] chunk)
{
    _buffer.AddRange(chunk);
    while (TryTakeFrame(out var frame))   // 循环拆出所有完整帧
        HandleFrame(frame);
}
```

**② CRC16 校验**（🟦，Modbus 也会用，先打好基础）
```csharp
public static class Crc16
{
    public static ushort Modbus(byte[] data)
    {
        ushort crc = 0xFFFF;
        foreach (var b in data)
        {
            crc ^= b;
            for (int i = 0; i < 8; i++)
                crc = (crc & 1) != 0 ? (ushort)((crc >> 1) ^ 0xA001) : (ushort)(crc >> 1);
        }
        return crc;
    }
}
```

**③ 完整帧示例**：`AA 55 | Len | Payload... | CRC_L CRC_H`

### 🔬 掰开揉碎：CRC16 到底在算什么（新手最卡的一关）
代码只有 8 行，但「为什么是 0xA001」「为什么初始 0xFFFF」「右移/异或在干啥」——不讲清楚你永远只会抄。
- **它算的是「数据指纹」**：发送方把整帧(除 CRC 本身)算出一个 16 位校验值附在末尾；接收方用**同样算法**重算一遍，比对一致才说明传输没出错（类比：你给文件算个 MD5，对方再算一次对不对得上）。
- **初始值 0xFFFF**：所有位先置 1 再开始异或，是为了让「全 0 数据」也能产生非零校验（否则 0 数据校验也是 0，分不清是没传还是传错了）。
- **右移 + 异或 0xA001（每一步在干啥）**：
  ```
  把当前字节和 crc 低 8 位异或 → 看 crc 最低位是不是 1：
    是 1 → 右移一位 再 异或 0xA001   （0xA001 是多项式 0x8005 的「按位反转」形式，Modbus 用反转写法）
    是 0 → 只右移一位
  重复 8 次 = 处理完 1 个字节，循环每个字节
  ```
  > 直觉理解：**每右移一次，就把数据「喂」进校验寄存器一位**；碰到 1 就「搅拌」一下多项式，保证任何 1 个 bit 翻转都会导致最终结果不同。
- **线序坑（本项目测试踩过）**：算出的寄存器值是 `0x0A84`，但 Modbus **先传低字节**`84`、再传高字节`0A`，所以线上字节顺序是 `84 0A`，人们常写成「0x840A」。**寄存器真值是 0x0A84**，本项目 `Crc16.Modbus` 返回的就是这个（测试断言 `0x0A84`，不是 `0x840A`）。
- **查表法为什么快**：上面是「计算法」每字节循环 8 次；工业用「256 项预计算表」，每字节直接查表异或，快一个数量级。本项目 `FrameParser` 用计算法够用，真高频场景再换表。

### 🧪 手算验证例（自己算一遍才算会，面试硬要求）
> 用本模块的 `Crc16.Modbus` 跑一遍，确认输出和手搓帧对得上。

```csharp
// 算 Modbus 读取请求 01 03 00 00 00 02 的 CRC
ushort crc = Crc16.Modbus(new byte[] { 0x01, 0x03, 0x00, 0x00, 0x00, 0x02 });
byte lo = (byte)(crc & 0xFF);   // 低字节 → C4
byte hi = (byte)(crc >> 8);     // 高字节 → 0B
Console.WriteLine($"{lo:X2} {hi:X2}");   // 运行结果：C4 0B
```
**运行结果：`C4 0B`** —— 和 M2 手搓请求帧补齐后的 `01 03 00 00 00 02 C4 0B` 一致 ✓。
> ⚠️ **连「标准答案」都可能错**：某份流传的 Modbus 讲义写「`01 06 00 01 00 64` 的 CRC 是 `19 98`」——**实测是 `D9 E1`**（用上面同样的函数一算便知）。所以 CRC 必须自己会手算/会跑代码验证，不能抄别人的结论。

### ✏️ CRC 三档练习
- 🟢 **基础题**：用 `Crc16.Modbus` 算 `01 06 00 01 00 64`（写寄存器 40002 = 100）的 CRC，打印低/高字节。
  **✅ 答案**：`D9 E1`（不是网上有些资料写的 `19 98`）。
- 🟡 **进阶题**：写一个 `VerifyCrc(byte[] frame)`：拆出末尾 2 字节与 `Crc16.Modbus(数据部分)` 比对，返回 `bool`。
- 🔴 **挑战题**：把 `Crc16.Modbus` 改成**查表法**（预生成 256 项表），和原实现跑同一组输入，断言结果一致。

### 🔬 掰开揉碎：为什么会有「半包 / 粘包」（现场最难查的 bug 源头）
串口是**字节流**，不保证「你发一次、我收一次」：
- **半包**：下位机一次发 20 字节，你 `DataReceived` 可能先收到 5 字节、再 15 字节（系统缓冲没攒够就通知你了）。
- **粘包**：下位机连发两帧，你一次收到 40 字节（两帧粘一起）。
- **后果**：直接按「收到=一帧」解析 = 偶发乱码/丢数据，**现场几周才复现一次**，极难查。
- **正解（本项目 FrameParser 的做法）**：维护一个 `_buffer`，来啥都 `AddRange` 进去，然后**循环尝试拆帧**（`TryTakeFrame` 找帧头→读长度→够长才取出），拆不出就**留着等下一波**；注意：永远拆不出完整帧时（比如收到垃圾头）要**清空缓冲**，否则缓冲无限增长 = 内存泄漏。

### 🔬 掰开揉碎：SerialPort 真实项目里的「坑」（光会 Open/Read 不够）
- **`DataReceived` 在某些机器/某些 USB 转串口上不触发**：这是 .NET `SerialPort` 的已知老毛病（基于 Win32 事件，不稳定）。**企业级写法**是用 `BaseStream.ReadAsync` 自己开一个读循环，不依赖 `DataReceived`：
  ```csharp
  // 现代/稳健写法：不依赖 DataReceived 事件
  var buf = new byte[1024];
  while (!ct.IsCancellationRequested)
  {
      int n = await port.BaseStream.ReadAsync(buf, 0, buf.Length, ct); // 阻塞等数据，异步不卡 UI
      OnBytes(buf.AsMemory(0, n));   // 喂给帧解析器
  }
  ```
- **硬件流控 DTR/RTS**：有些下位机要求上位机拉高 `DtrEnable`/`RtsEnable` 才肯通信，连不上时检查这个。
- **跨线程 `Close()` 崩溃**：`DataReceived` 在后台线程，若在 UI 线程直接 `port.Close()` 可能和后台读冲突。统一用 `CancellationToken` 先停止读循环、再 `Close()`。
- **`BytesToRead` 一次性读不完**：循环读直到 `BytesToRead == 0`，别只 `Read` 一次。

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ 一定要处理半包 / 粘包 | 不处理 = 偶尔丢数据，现场最难查的 bug |
| ⭐ CRC 用查表法更快 | 上面是计算法，工业可用查表优化 |
| 🔥 大小端 | 下位机可能是小端，读 `int` 用 `BinaryPrimitives.ReadInt32LittleEndian` |
| 🔥 缓冲无限增长 | 永远拆不出完整帧时要清空缓冲，防内存泄漏 |

### ✏️ 练习
实现 `TryTakeFrame`：从 `_buffer` 找 `AA 55` 开头、按第 3 字节长度取出完整帧并移除缓冲；校验 CRC。

**✅ 答案**（骨架）
```csharp
private bool TryTakeFrame(out byte[] frame)
{
    frame = Array.Empty<byte>();
    int idx = _buffer.IndexOf(0xAA);
    if (idx < 0 || _buffer.Count < idx + 3) return false;
    if (_buffer[idx + 1] != 0x55) { _buffer.RemoveAt(idx); return false; }
    int len = _buffer[idx + 2];
    int total = 3 + len + 2;                 // 头+长+载荷+CRC
    if (_buffer.Count < idx + total) return false;
    frame = _buffer.GetRange(idx, total).ToArray();
    _buffer.RemoveRange(0, idx + total);
    return true;
}
```

**🏗️ 项目任务**：把帧解析接到 `SerialDevice`，解析出 `SensorPoint` 后 `RaiseData`。DAQ Monitor 现在能从"虚拟串口"收到**有结构**的数据了。

**✅ 打卡[ ]**

## 📌 温故知新（跨模块联动）
- **M0 Day7 后台线程 → 这里直接用上了**：`DataReceived`/读循环在后台线程跑，要改 UI 必须用 `Dispatcher.Invoke`（M5 还会用 `Channel<T>` 彻底解耦）。
- **M0 Day2 事件 → 这里触发 `RaiseData`**：串口字节 → 帧解析 → `SensorPoint` → 事件抛给 UI，正是 M0 说的「事件解耦」。
- **前瞻 M4/M9**：帧解析出的点最终会进 `PointStore`(M4) 和统一采集管道 `AcquisitionPipeline`(M9)，别各自造轮子。

## 📚 延伸阅读（卡点时点开）
- `SerialPort` 官方类参考：https://learn.microsoft.com/dotnet/api/system.io.ports.serialport
- CRC 原理（维基，讲多项式/初始值）：https://en.wikipedia.org/wiki/Cyclic_redundancy_check
- com0com 虚拟串口（无硬件练手）：https://sourceforge.net/projects/com0com/
- 全部模块外链汇总见 `外部链接索引.md`
- 📎 **没有硬件？看 `硬件替代方案与讲解_深度版.md`**：RS232/485、4-20mA、接线抗干扰科普 + 虚拟串口/Modbus 从站/PLCSIM 四层替代方案

## 模块交付清单（M1）
- [ ] `SerialDevice` 实现 `Connect/Disconnect/Read/Write`
- [ ] `DataReceived` → 字节缓冲 → 帧解析 → `RaiseData`
- [ ] `Crc16` 工具类
- [ ] 虚拟串口闭环跑通（无硬件也能演示）
- [ ] 中文乱码 / 跨线程 / 半包处理都验证过
