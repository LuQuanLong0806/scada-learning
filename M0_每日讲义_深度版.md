# DAQ Monitor · M0 每日讲义（日课级）：C#/.NET 系统热身 + 工程骨架

> **优先级定位**：🔴 必学 · 地基起点 · C#/WPF/并发采集闭环（整个学习路线的地基，没它后面全空中楼阁）
> **你的角色**：中高级前端工程师，有扎实的 JS/TS 直觉。这份讲义把你已有的前端知识**当作杠杆**，每学一个 C# 概念先对标你已经懂的前端概念，再补上位机特有的坑。
> **技术来源标注**（全程）：🟦 C# 语法（不装包）/ 🟩 .NET 类库 BCL（默认随 .NET，不装包）/ 🟧 第三方 NuGet（要 `dotnet add package`）。口诀：**语法天生物 · BCL 随 .NET · 第三方要装包**。
> **符号**：🟢 入门难度　🟡 进阶难度　🔴 硬核难度　⭐ 面试高频　🔥 新手必踩坑　🏗️ 落到 DAQ Monitor 项目

---

> ⏱️ **阅读路径**(按时间预算选入口)
> - **3 分钟**:看下方「优先级路线总览」表 — 知道 22 个模块哪些必学/缓学/再学
> - **30 分钟**:精读 Day 1-3(C# 类型 / LINQ / 类)— 4-5 年前端能快速迁移
> - **3 小时**:全文精读 + **Day 7 多线程/async/锁(重头戏⭐⭐⭐)** + Day 8 立起 WPF 工程
> - 🎯 **面试高频**:**struct vs class(值类型拷贝陷阱)** / **async void 吞异常 + .Result 死锁** / **Channel<T> 生产消费** / 委托/事件
> - 🔁 **配套复习**:[C# 陷阱深度版](C#_陷阱_前端转上位机必看_深度版.md) · [代码肌肉 B2 Channel 10min / B9 RelayCommand 5min / B26 IDisposable](代码肌肉训练手册_30天刷题版.md) · [Debug C1 async void / C2 .Result 死锁 / C6 struct 拷贝](代码肌肉训练手册_30天刷题版.md) · [间隔重复表](记忆与复习机制_间隔重复版.md)

> 📚 **前置语法**(M0 自身是 C# 语法源头,但若你完全没接触过 C#,**强烈建议先扫一遍** [C# 语法速查 — 前端视角](CSharp语法速查_前端视角.md) 的 §1 类型推断 / §2 集合 / §4 属性,有了视觉记忆再读 M0,效率会高很多)
> - `var` / `new()` — 类型推断,速查 §1
> - `List<T>` / `Dictionary<K,V>` — 集合,速查 §2
> - `{ get; set; }` — 属性,速查 §4
> - `$"Hello {name}"` — 字符串插值,速查 §10
> - `async Task<T>` / `await` — 异步(Day 7 重头戏),速查 §8
> - `lock (_obj)` — 多线程互斥(Day 7),速查 §14

> 📦 **学习约定 · 跟着敲不报错(必读!)**
> - 本讲义**项目驱动**,所有示例代码属于 `DaqMonitor` 工程。完整可编译代码在仓库 `DaqMonitor.sln` 里。
> - **看到 "🏗️ 项目任务" 里写 `DaqMonitor.Core/XXX.cs` 路径** → 这是让你**在真实项目里建文件**。**还没建项目骨架?** 跳到 [Day 2 开头「📦 立项目骨架」](#day-2--类型系统值类型--引用类型--可空--struct--enum-) 跑 6 条 `dotnet` 命令(10 分钟搭好)。详细版见 [实操入门 · 从零搭建企业级工程](实操入门_从零搭建企业级工程.md)。
> - **项目类型怎么选?** Core = 类库(classlib),UI = WPF 应用,Tests = xUnit。**不要建控制台(console)**。详见 Day 2 立骨架块。
> - 示例代码引用的核心类型(`SensorPoint`/`DeviceBase`/`IDevice`/`AlarmRule` 等)都在 [📦 前置类型定义 · 学员粘贴版](前置类型定义_学员粘贴版.md) **集中定义** — 遇到"找不到类型 XXX"报错,**先去这份文档复制对应类型**,在项目里建 `_PredefinedTypes.cs` 粘进去就能跑。
> - 每个 Day 开头若有 **"📦 前置代码"** 块:本 Day 用到的类型在本块内 inline 定义,直接复制即可。
> - 若没有 "📦 前置代码" 块:说明该 Day 只用 BCL 类型(`List<int>`/`string` 等),装好 .NET SDK 直接能跑。
> - **学习姿势**:① Day 1 装环境 → ② Day 2 开头立项目骨架 → ③ Day 2-7 跟着敲代码进项目 → ④ 遇到陌生语法查 [C# 语法速查](CSharp语法速查_前端视角.md) → ⑤ 遇到找不到类型查 [前置类型定义](前置类型定义_学员粘贴版.md)。

## 📋 类型依赖总览(全 30 天会建的 15 个核心类型 · 一表看清)

> 这是小白**全局地图**——读到任何 Day,如果"不知道这个类型在哪/依赖谁",回这里查。所有类型最终都建在 `DaqMonitor.Core/` 下。

| 类型 | 类型种类 | 文件路径 | 依赖谁 | 在哪个 Day 首次建 |
|---|---|---|---|---|
| `SensorPoint` | readonly struct | `DaqMonitor.Core/Models/SensorPoint.cs` | 无 | Day 2 |
| `DeviceState` | enum | `DaqMonitor.Core/Models/DeviceState.cs` | 无 | Day 2 |
| `AlarmLevel` | enum | `DaqMonitor.Core/Models/AlarmLevel.cs` | 无 | Day 2 |
| `Device`(教学版) | class | (仅课堂演示,不入项目) | 无 | Day 4 |
| `PointValue`(教学版) | class | (仅课堂演示,不入项目) | 无 | Day 4 |
| `PointRecord`(教学版) | record | (仅课堂演示,不入项目) | 无 | Day 4 |
| `IDevice` | interface | `DaqMonitor.Core/Devices/IDevice.cs` | `SensorPoint`,`DeviceState` | Day 5 |
| `DeviceBase` | abstract class | `DaqMonitor.Core/Devices/DeviceBase.cs` | `IDevice`,`SensorPoint` | Day 5 |
| `SimulatedDevice` | class | `DaqMonitor.Core/Devices/SimulatedDevice.cs` | `DeviceBase` | Day 5 |
| `AlarmRule`(教学版) | class | (Day 7 演示,生产版见 M6) | `SensorPoint`,`AlarmLevel` | Day 7 / M6 |
| `AlarmEngine`(教学版) | class | (Day 7 演示,生产版见 M6) | `AlarmRule`,`SensorPoint`,`AlarmEvent` | Day 7 / M6 |
| `AcquisitionPipeline` | class | `DaqMonitor.Core/Pipeline/AcquisitionPipeline.cs` | `SensorPoint`,`IDevice` | Day 7 |
| `PointStore` | class | `DaqMonitor.Core/Store/PointStore.cs` | `SensorPoint` | Day 7 |
| `SensorRecord` | class(EF 实体) | `DaqMonitor.Core/Persistence/SensorRecord.cs` | 无 | M4 |
| `AlarmEvent` | class | `DaqMonitor.Core/Models/AlarmEvent.cs` | `AlarmLevel` | M6 |

**📍 落地顺序铁律**:
- Day 2 先建 SensorPoint/DeviceState/AlarmLevel(后面所有类型的基础)
- Day 5 IDevice/DeviceBase 必须在 SimulatedDevice 之前
- Day 7 AcquisitionPipeline/PointStore 需要 IDevice 已建好
- 完整类型签名(可直接复制)在 [📦 前置类型定义 · 学员粘贴版](前置类型定义_学员粘贴版.md)

---

## 🎯 优先级路线总览（必学 → 缓学 → 再学，没有天数，按自己节奏啃）

| 优先级 | 模块 | 核心能力（企业要求） | 说明 |
|---|---|---|---|
| 🔴 必学 | **M0 C#/WPF/并发** | 语言直觉 + 工程骨架 + 多线程 | 地基，没它后面全空中楼阁 |
| 🔴 必学 | M1 串口 | RS232/485 收发、帧解析、CRC | JD 主干必会 |
| 🔴 必学 | **M2 Modbus** | 工业标准协议读写寄存器、CRC 手算 | JD 主干必会 |
| 🔴 必学 | **M3 PLC(S7)** | 直连 PLC 读 DB 块 | JD 主干必会 |
| 🔴 必学 | **M4 数据库** | EF Core + 历史库 + 查询导出 | JD 主干必会 |
| 🔴 必学 | M5 实时可视化 | LiveCharts 曲线/仪表盘 | 简历演示杀手锏 |
| 🔴 必学 | M6 报警引擎 | 阈值/回滞/事件总线/日志 | 工业软件灵魂 |
| 🔴 必学 | M7 OPC UA/MQTT | 上云/对接 SCADA | 15K 加分，主流也常要 |
| 🔴 必学 | M8 收尾 | MVVM/配置/部署/安装包/简历 | 把项目收口成作品 |
| 🔴 必学 | M9 工程素养 | 单测/DI/统一采集/容错重连 | 13→15K 分水岭 |
| 🔴 必学 | M10 报表 | 聚合/导出 Excel/PDF | 调研 P0 缺口，闭环 |
| 🟡 缓学 | M11 TCP/Socket | 私有协议/心跳 | JD 也提，但主场景已被串口/Modbus/PLC 覆盖 |
| 🟡 缓学 | M13 多品牌PLC | 三菱/欧姆龙/汇川 + HslCommunication | 国内企业超常用，先吃透 S7 |
| 🟡 缓学 | M14 WinForm/控件 | 双修 + 自绘控件 | JD 常写 WinForm/WPF 双修 |
| 🟢 再学 | M12 工程量算法 | 滤波/标定/拟合 | 具体项目才用 |
| 🟢 再学 | M15 协作联调 | Git/敏捷/调试工具 | 入职自然学（软技能） |
| 🟢 再学 | M16 CAN/USB | 工业总线扩展 | 汽车/锂电等特定行业 |

> **导师说**：路线是"项目驱动"——M0 第一天就立起 `DaqMonitor` 工程骨架，之后每个模块往里加一层企业级能力，最终 = 能写进简历的工业采集监控系统。你学的是**一条线**，不是一堆散知识点。

---

## M0 目录（8 天递进）

| Day | 主题 | 难度 | 上位机为什么要学 |
|---|---|---|---|
| Day 1 | 环境 + 第一个程序（C# ≈ 强类型 TS） | 🟢 | 打通工具链 |
| Day 2 | 类型系统：值/引用/可空/struct/enum | 🟢 | 一个"点位"天生就是 struct |
| Day 3 | 集合与 LINQ | 🟢 | 每秒几百个点靠 LINQ 处理 |
| Day 4 | 类/属性/封装/static | 🟢 | 设备=类，配置=属性 |
| Day 5 | 继承/接口/泛型 | 🟡 | 多设备共用一套接口（架构核心） |
| Day 6 | 委托/事件/Lambda | 🟡 | "数据到了→通知 UI"的解耦机制 |
| **Day 7** | **多线程/async-await/锁** | 🔴 | 采集后台、UI 前台，必须并发 |
| Day 8 | 周复习 + 立起 WPF 工程 | 🟡 | 把前 7 天串成真实项目骨架 |

---

## Day 1 — 环境搭建 + 第一个 C# 程序 🟢

**📌 技术来源**：🟦 C# 语法（`using`/`class`/`var`/字符串插值）+ 🟩 BCL（`Console`）

### 一句话讲清楚
C# 就是"编译期强类型 + 真正面向对象"的 TypeScript，跑在 .NET 上（≈ TS 跑在 Node 上）。你已有的 TS 直觉 90% 能用，只需补"值类型/可空/编译期类型"三处差异。

### 前端类比秒懂
| C# / .NET | 前端 | 说明 |
|---|---|---|
| .NET Runtime | Node.js Runtime | 代码的运行环境 |
| BCL（基础类库） | Node 内置模块（fs/http…） | 官方自带，不用装 |
| NuGet 包 | npm 包 | 第三方库 |
| Visual Studio 2022 | VS Code | IDE（VS 更重、更强，装 Community 版免费） |
| `dotnet run` | `npm start` | 编译并运行 |
| `.csproj` | `package.json` | 工程/依赖配置 |

### 分点精讲
**① 程序入口**：🟦 传统写法要 `static void Main()`，但 .NET 6+ 支持"顶级语句"，可以像 JS 一样直接写：
```csharp
// Program.cs —— 顶级语句，不用写 class/Main
Console.WriteLine("DAQ Monitor 启动");
```

**② 变量声明**：🟦 `var` 是类型推断（≠ JS 的 `var`，更像 TS 的 `let`）：
```csharp
var name = "DAQ";      // 推断为 string，之后不能赋别的类型
string title = "监控"; // 显式类型，等价于 TS: let title: string
int port = 502;        // 值类型，默认 0，不能是 undefined
```
⚠️ C# 没有 `undefined`，只有 `null`；而且默认变量**不能为 null**（要可空得写 `int?`）——这是比 JS 安全的地方。

**③ 字符串插值**：🟦 用 `$"..."`，等价于 JS 反引号模板：
```csharp
var v = 25.6;
Console.WriteLine($"温度 = {v} ℃");   // JS: `温度 = ${v} ℃`
Console.WriteLine($"{name} v1.0");
```

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ 一切皆类型 | 变量必须有明确类型（`var` 只是让编译器帮你推断，不是"任意类型"） |
| ⭐ 无 undefined | 只有 `null`，且非可空类型默认不能为 null |
| 🔥 `var` ≠ JS var | C# `var` 是块级 + 类型固定，别用 JS 直觉理解 |
| 🔥 大小写敏感 | `Console` 不是 `console`，类名/方法名首字母大写（PascalCase） |

### 🟢 基础题
新建 console 项目，输出"DAQ Monitor 启动"。

### 🟡 进阶题
用 `var name="DAQ"` 拼一句含变量+版本号的话，如 `DAQ Monitor 启动，版本 1.0`。

### 🔴 挑战题
在输出里同时展示 `int port=502` 与 `double temp=25.6`，并故意写 `int? maybe=null` 然后用 `?? 0` 兜底打印，体会可空类型。

**✅ 答案**
```csharp
Console.WriteLine("DAQ Monitor 启动");
var name = "DAQ";
var version = 1.0;
Console.WriteLine($"{name} Monitor 启动，版本 {version}");
// 挑战题
int? maybe = null;
Console.WriteLine($"端口={502}，温度={25.6}，兜底值={maybe ?? 0}");
```

**🏗️ 项目任务**：确认 `dotnet --version` 输出 `8.x`；规划 `DAQMonitor/src` 下的 `DaqMonitor.Core` / `DaqMonitor.UI` 两个目录。**真正建工程在 Day 2 开头「📦 立项目骨架」块**(6 条 dotnet 命令照抄)。今天目标:**环境通、跑出第一行**。

**🎓 工控导师说**：我带过的前端转上位机学员，Day 1 最大的卡点不是语法，是"没有 undefined 还要判空"——C# 的 `null` 检查比 JS 严格，这是好事，逼你写出不会运行时崩的代码。

**💼 职业建议**：面试被问"你 C# 基础怎么样"时，别只说"会语法"，要讲"强类型 + 可空引用类型让工业软件更稳"——这体现你理解**为什么**工业软件偏爱 C#。

**✅ 打卡[ ]**

---

## Day 2 — 类型系统：值类型 / 引用类型 / 可空 / struct / enum 🟢

**📌 技术来源**：🟦 C# 类型系统（全部内置语法，不装包）

### 📦 立项目骨架(本 Day 项目任务的前提,必做!)

> 本 Day 末尾的"🏗️ 项目任务"会让你**在 `DaqMonitor.Core` 建 `Models/` 目录写 `SensorPoint.cs`**。但项目骨架还没搭?先在这里 10 分钟搭好,后面 Day 3-8 全部能直接落地。
>
> **项目类型怎么选?**(新手最常问):
> - `DaqMonitor.Core` → **类库(classlib)**:不是"运行起来的程序",而是"放领域类型/设备接口/采集管道"的代码集合,被 UI 和 Tests 引用。**前端类比**:`Core` 像你前端的 `src/lib/` 或 `packages/shared/`,本身不启动,被 app 引用。
> - `DaqMonitor.UI` → **WPF 应用(wpf)**:有 MainWindow、能 `dotnet run` 弹窗的真实程序。**前端类比**:像 `apps/web/` 是入口。
> - `DaqMonitor.Tests` → **xUnit 测试(xunit)**:跑单测的工程。
> - **不要选"控制台(console)"**:console 是命令行程序,我们的目标是 WPF 桌面 UI。
> - 完整版教程见 [实操入门 · 从零搭建企业级工程](实操入门_从零搭建企业级工程.md),这里是最小可跑版。

#### 🚦 前置检查(2 分钟,不做后面全卡死)

| 检查项 | 命令 | 期望 | 不达标怎么办 |
|---|---|---|---|
| .NET SDK 装了吗 | `dotnet --version` | `8.0.xxx` 或 `10.x.x` | 去 https://dotnet.microsoft.com 下载 .NET 8 SDK |
| WPF 模板能用吗 | `dotnet new wpf --dry-run` | 不报错 | SDK 没装桌面开发 workload,跑 `dotnet workload install wpf` (Windows) |
| 当前目录对吗 | `pwd` (Git Bash) 或 `cd` (CMD 无参数) | `f:\00_project\上位机学习` | 用 `cd` 切过去 |
| 是否已建过 DAQMonitor | `ls DAQMonitor` 2>/dev/null | 报"No such file"(第一次建) | 已建过 → 跳过本节,直接看「跑完看到什么」对照 |

**照抄这 6 条命令**(在 `f:\00_project\上位机学习\` 下打开 PowerShell / Git Bash):

> ⚠️ **每条单独跑,等上一条跑完再敲下一条**。一次性粘贴多条容易因为某条失败导致后面全废。
> 💡 `dotnet new classlib -o src/DaqMonitor.Core` 会**自动创建 src/ 父目录**,不用手动 `mkdir src`。

```bash
# ① 建解决方案根目录 + .sln 清单
mkdir DAQMonitor
cd DAQMonitor
dotnet new sln -n DaqMonitor

# ② 建三个子工程(Core 类库 / UI 是 WPF / Tests 是 xUnit)
dotnet new classlib -o src/DaqMonitor.Core -n DaqMonitor.Core
dotnet new wpf      -o src/DaqMonitor.UI   -n DaqMonitor.UI
dotnet new xunit    -o src/DaqMonitor.Tests -n DaqMonitor.Tests

# ③ 把三个子工程挂到解决方案
dotnet sln add src/DaqMonitor.Core/DaqMonitor.Core.csproj
dotnet sln add src/DaqMonitor.UI/DaqMonitor.UI.csproj
dotnet sln add src/DaqMonitor.Tests/DaqMonitor.Tests.csproj

# ④ 建依赖方向:UI 依赖 Core(用领域类型),Tests 依赖 Core(测领域逻辑)
dotnet add src/DaqMonitor.UI/DaqMonitor.UI.csproj       reference src/DaqMonitor.Core/DaqMonitor.Core.csproj
dotnet add src/DaqMonitor.Tests/DaqMonitor.Tests.csproj reference src/DaqMonitor.Core/DaqMonitor.Core.csproj

# ⑤ 验证骨架能编译(应该 0 错误)
dotnet build

# ⑥ git 初始化(可选,但建议)
cd ..
dotnet new gitignore      # 在资料根目录生成标准 .NET gitignore(忽略 bin/obj)
```

跑完你会得到这个结构(跟仓库 `DAQMonitor/` 真实工程一致):
```
DAQMonitor/
├── DaqMonitor.sln                    ← 解决方案清单
└── src/
    ├── DaqMonitor.Core/              ← 类库(本 Day 在这里建 Models/)
    │   ├── Class1.cs                 ← dotnet 默认生成,可删
    │   └── DaqMonitor.Core.csproj
    ├── DaqMonitor.UI/                ← WPF 应用(Day 8 主战场)
    │   ├── App.xaml
    │   ├── MainWindow.xaml
    │   └── DaqMonitor.UI.csproj
    └── DaqMonitor.Tests/             ← xUnit 测试工程(M9 主战场)
        ├── UnitTest1.cs
        └── DaqMonitor.Tests.csproj
```

> ⭐ **关键认知**:
> - **类库(classlib)不能 `dotnet run`**,只能被引用。验证它编译过没,用 `dotnet build src/DaqMonitor.Core`。
> - **WPF 工程**才能 `dotnet run --project src/DaqMonitor.UI`,弹出窗口。
> - 后续 Day 3-7 的项目任务都假设你已经完成这一步。**如果跳过,后续每 Day 的"建 XXX 到 DaqMonitor.Core" 都没法落地。**

### 一句话讲清楚
值类型 = "复印件"（赋值就拷一份，互不影响）；引用类型 = "共享文档链接"（赋值只是多一个人拿到同一份地址）。上位机的"点位/寄存器"天生是值类型，必须分清，否则多线程下数据串台。

### 前端类比秒懂
| C# | 前端 | 赋值行为 |
|---|---|---|
| 值类型 `int/double/bool/struct/enum` | primitive `number/boolean` | 拷贝值本身 |
| 引用类型 `string/class/数组/List<T>` | object/array | 共享地址 |
| 可空 `int?` | `number \| null` | 允许为 null |
| `struct` | 无直接对应（≈ 轻量 object 但按值拷贝） | 值类型 |
| `enum` | `enum`（TS 有） | 命名常量 |

### 分点精讲
**① 值 vs 引用，看这段就懂**：
```csharp
int a = 5;
int b = a;      // 拷贝
b = 99;
// a 仍是 5（互不影响）

int[] arr1 = { 1, 2, 3 };
int[] arr2 = arr1;   // 共享地址
arr2[0] = 99;
// arr1[0] 也变成 99！（同一份数据）
```

**② 可空类型**：🟦 加 `?` 表示可为 null：
```csharp
int age = null;    // ❌ 编译报错，非可空不能为 null
int? age2 = null;  // ✅ 可空
int real = age2 ?? 0;   // ?? 是空合并（JS 也有）：null 就取 0
Console.WriteLine(age2?.ToString());  // ?. 空条件调用（JS 也有）
```

**③ struct —— 上位机点位的天然载体**：🟦 轻量值类型，适合"坐标/点位/寄存器"这类小数据：
```csharp
public readonly struct SensorPoint
{
    public int Id { get; init; }              // 点位编号
    public double Value { get; init; }        // 数值
    public DateTime Timestamp { get; init; }  // 采集时刻

    public SensorPoint(int id, double value, DateTime? timestamp = null)
    {
        Id = id;
        Value = value;
        Timestamp = timestamp ?? DateTime.Now;
    }
}
```
> 💡 这里就是 DAQ Monitor 项目的**正式 SensorPoint 定义**(也是 [前置类型定义](前置类型定义_学员粘贴版.md) 里的版本),后面所有 Day/模块都用这个。struct 用 `readonly` + `init` 属性保证不可变(线程安全),构造函数给 `Timestamp` 一个默认值,所以下面 Day 3 写 `new SensorPoint(1, 10.5)` 也能跑。

**④ enum —— 状态/功能码用它**：🟦 命名常量，比 magic number 可读：
```csharp
public enum DeviceState { Offline, Connecting, Online }
public enum AlarmLevel  { Normal, Warning, Critical }
```

### 🔧 常用方法速查(基础类型,天天用)

> 这是上位机 8K→13K 的"基本功"——**学过概念不等于会用**。下面 6 张表覆盖了 90% 日常场景,**背下来或常备速查**,写代码速度直接翻倍。前端类比都用 JS/TS 对应。

**① string 常用方法**(字符串是不可变的,所有"修改"都返回新串):

| 方法 | 前端类比 | 上位机用途 | 示例 |
|---|---|---|---|
| `.Length` | `.length` | 报文长度校验 | `"abc".Length` → 3 |
| `[i]` | `[i]` | 取第 i 字节(协议解析) | `"AB"[0]` → 'A' |
| `.Substring(start, len)` | `.slice(start, start+len)` | 截报文段 | `"AA55CC".Substring(2,2)` → "55" |
| `.IndexOf(s)` / `.LastIndexOf(s)` | `.indexOf` | 定位分隔符 | `"A,B,C".IndexOf(",")` → 1 |
| `.Contains(s)` / `.StartsWith(s)` / `.EndsWith(s)` | 同名 | 协议头/尾判断 | `frame.StartsWith("AA55")` |
| `.Replace(a, b)` | `.replaceAll` | 字符替换 | `"1.5".Replace('.', ',')` → "1,5" |
| `.Split(',')` / `string.Join(",", arr)` | `.split` / `.join` | CSV 解析/拼装 | `"1,2,3".Split(',')` → ["1","2","3"] |
| `.ToUpper()` / `.ToLower()` | 同名 | 设备名规范化 | `"com3".ToUpper()` → "COM3" |
| `.Trim()` / `.TrimStart()` / `.TrimEnd()` | `.trim*` | 去掉用户输入空格 | `" 12 ".Trim()` → "12" |
| `.PadLeft(n, '0')` / `.PadRight` | `.padStart` / `.padEnd` | 报文补零 | `5.ToString().PadLeft(4,'0')` → "0005" |
| `string.IsNullOrEmpty(s)` / `string.IsNullOrWhiteSpace(s)` | `!s` / `!s.trim()` | 防御性校验 | 检查串口名是否填了 |
| `string.Format("{0:N2}", v)` / `$"{v:N2}"` | 模板字符串 | 浮点格式化(2 位小数) | `$"{3.14159:F2}"` → "3.14" |
| `.ToCharArray()` | `.split('')` | 逐字符遍历 | `"AB".ToCharArray()` → ['A','B'] |
| `string.Concat(a, b)` / `+` | `+` / concat | 拼串 | `"AA"+"55"` → "AA55" |
| `.ToString(format)` | — | 数值转字符串(必学) | `255.ToString("X2")` → "FF" |

> 🔥 **string 不可变陷阱**:`s.Replace("a","b");` 不接住返回值,`s` 不变!必须 `s = s.Replace(...)`。前端 JS 也一样,但 C# 编译器不警告你。
> 💡 **拼大量字符串用 `StringBuilder`**(参 Day 4 静态/工具类) — 10000 次 `+` 性能 100 倍差距。

**② 数值与字符串互转**(协议解析天天用):

| 操作 | 方法 | 前端类比 | 示例 |
|---|---|---|---|
| 字符串→整数(可能抛) | `int.Parse("123")` | `parseInt`(严格) | `int.Parse("123")` → 123 |
| 字符串→整数(安全) | `int.TryParse(s, out int v)` | `parseInt`(返回 NaN) | `int.TryParse("ab", out _)` → false |
| 字符串→浮点 | `double.Parse("3.14")` | `parseFloat` | 注意小数点 — 中文系统可能要 "," |
| 任意类型互转 | `Convert.ToInt32(obj)` / `Convert.ToDouble` / `Convert.ToString` | `Number()` | `Convert.ToInt32(true)` → 1 |
| 16 进制字符串→整数 | `Convert.ToInt32("FF", 16)` | `parseInt(s,16)` | 协议字节解析 |
| 整数→16 进制字符串 | `255.ToString("X2")` / `"X4"` | `Number.toString(16)` | `255.ToString("X2")` → "FF" |
| 整数→2 进制字符串 | `255.ToString("B8")` (.NET 8+) | `toString(2)` | 位状态显示 |
| 字节→整数 | `BitConverter.ToInt32(byteArr, offset)` | `DataView.getInt32` | Modbus 4 字节拼 float |

**③ DateTime / TimeSpan**(日志、采集时刻、超时):

| 属性/方法 | 前端类比 | 上位机用途 |
|---|---|---|
| `DateTime.Now` / `DateTime.UtcNow` | `new Date()` | 采集时刻、日志戳(库用 UtcNow) |
| `DateTime.Today` | `new Date().setHours(0,0,0,0)` | 当日 0 点(按天分表) |
| `.AddSeconds(n)` / `.AddMinutes(n)` / `.AddHours(n)` / `.AddDays(n)` | `+new Date()` 手算 | 心跳超时阈值 |
| `.Subtract(dt)` / `dt2 - dt1` | 两个 Date 相减 | 算耗时:返回 `TimeSpan` |
| `.ToString("yyyy-MM-dd HH:mm:ss.fff")` | `dayjs.format()` | 日志时间戳 |
| `.Ticks` | — | 100 纳秒精度(高频采集) |
| `TimeSpan.FromSeconds(n)` / `.FromMilliseconds(n)` | — | 心跳间隔 = 5 秒 |
| `ts.TotalMilliseconds` / `ts.TotalSeconds` | — | 距上次心跳多少 ms |
| `ts.TotalMilliseconds > 5000` | — | 心跳超时检测 |

**④ enum 常用方法**(状态/功能码):

| 操作 | 示例 | 用途 |
|---|---|---|
| 字符串→enum | `Enum.Parse<DeviceState>("Online")` | 配置文件读状态 |
| 安全转换 | `Enum.TryParse(s, out DeviceState st)` | 解析用户输入 |
| enum→整数 | `(int)AlarmLevel.Critical` → 2 | 协议字节 |
| 整数→enum | `(DeviceState)1` | 协议字节→状态 |
| 列出所有值 | `Enum.GetNames<DeviceState>()` | UI 下拉框 |
| Flags 枚举(位或) | `[Flags] enum Perm { Read=1, Write=2 }` | 权限组合 |
| 位检测 | `perm.HasFlag(Perm.Write)` | `(perm & Perm.Write)!=0` 简写 |

**⑤ Math 类**(静态工具,`Math.xxx`):

| 方法 | 前端类比 | 上位机用途 |
|---|---|---|
| `Math.Abs(x)` / `Math.Max(a,b)` / `Math.Min(a,b)` | `Math.abs`/`max`/`min` | 数值比较 |
| `Math.Round(x, 2)` | `Math.round` | 保留 2 位小数(银行家舍入,留意!) |
| `Math.Floor(x)` / `Math.Ceiling(x)` | `Math.floor`/`Math.ceil` | 向下/上取整 |
| `Math.Sqrt(x)` / `Math.Pow(x, n)` | `Math.sqrt`/`pow` | 工程量计算(均方根) |
| `Math.Clamp(x, min, max)` | 自己写 | 限制值域,防溢出 |
| `Math.Sign(x)` | `Math.sign` | 趋势方向(+1/-1/0) |

**⑥ 可空类型 `T?` 常用操作**(前端 `T \| null`):

| 操作 | 示例 | 用途 |
|---|---|---|
| 判空 | `if (age.HasValue)` / `if (age == null)` | 安全访问 |
| 取值 | `age.Value`(不空时) | 直接取 |
| 空合并 | `age ?? 0` | null 取默认值 |
| 空合并赋值 | `age ??= 0` | 是 null 才赋值 |
| 空条件 | `obj?.Method()` | null 时不调用(返回 null) |
| 强制抛 | `age!.Value` | 你"确信"非 null 时用 |

> 💡 通用根方法(所有类型都有,继承自 `object`):`.ToString()` / `.Equals(obj)` / `.GetHashCode()` / `.GetType()` — 前三个天天用。

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ 值 vs 引用 | 面试必考。判断标准：`struct/基础数值/enum` 是值，`class/string/数组/集合` 是引用 |
| ⭐ string 特殊 | 虽是引用类型，但**不可变**，行为像值（拼接产生新对象） |
| 🔥 struct 传参被拷贝 | 把 struct 传进方法里改，改的是副本！要改原值传 `ref` 或改用 `class` |
| 🔥 非可空默认不能 null | 忘了写 `?` 会编译报错，别慌，这是保护你 |

### 🟢 基础题
定义 `enum AlarmLevel { Normal, Warning, Critical }` 和 `struct Alarm { int PointId; AlarmLevel Level; double Value; }`。

### 🟡 进阶题
创建一个"点位 3、Critical、值 200"的报警并打印。

### 🔴 挑战题
写一个方法 `bool IsCritical(Alarm a) => a.Level == AlarmLevel.Critical;`，并构造一个 `List<Alarm>` 含 3 条，用 LINQ（Day 3 会学，先试 `Where`）筛选出 Critical 的。

**✅ 答案**
```csharp
public enum AlarmLevel { Normal, Warning, Critical }
public struct Alarm
{
    public int PointId;
    public AlarmLevel Level;
    public double Value;
}
var alarm = new Alarm { PointId = 3, Level = AlarmLevel.Critical, Value = 200 };
Console.WriteLine($"点位{alarm.PointId} 报警 {alarm.Level} 值={alarm.Value}");
```

> 📦 **前置准备**:本任务需要 `DaqMonitor.Core` 类库已存在。**还没建?** 跳到 [Day 2 开头「📦 立项目骨架」](#-立项目骨架本-day-项目任务的前提必做) 跑 6 条 dotnet 命令(10 分钟),再回来。

**🏗️ 项目任务**：在 `DaqMonitor.Core` 建 `Models/` 目录，写下核心类型骨架：`SensorPoint`、`DeviceState`、`AlarmLevel`、`Alarm`。后面模块往里填字段。这些类型在 `DaqMonitor.Core/Models/SensorPoint.cs` 真实存在，直接对齐。

> 🛠️ **怎么建文件**(新手卡死点):
> ① 用 IDE(VS/Rider/VSCode):右键 `DaqMonitor.Core` 项目 → 添加 → 新建文件夹 `Models` → 右键 `Models` → 添加 → 类 `SensorPoint.cs`
> ② 用命令行:`mkdir src/DaqMonitor.Core/Models` 然后 `code src/DaqMonitor.Core/Models/SensorPoint.cs`(VSCode 打开)
> ③ 文件命名规则:**一个类一个文件,文件名 = 类名**(`SensorPoint.cs` 放 `SensorPoint` 类,`DeviceState.cs` 放 `DeviceState` 枚举)。这是 C# 惯例,IDE 会按文件名找类。

**🎓 工控导师说**：现场最阴的 bug 之一——把点位用 `struct` 在多线程间传来传去，结果改了副本没改原值，界面数字半天不更新。记住：**跨线程共享状态优先用 class + 锁，或每次整体替换 struct**。

**💼 职业建议**：面试官问"值类型和引用类型区别"，答完定义一定要补一句"上位机点位用 struct 省内存、但多线程要小心拷贝"——这显示你有工程经验，不是背概念。

**✅ 打卡[ ]**

---

## Day 3 — 集合与 LINQ 🟢

**📌 技术来源**：🟦 集合语法 + 🟩 BCL（`List<T>`/`Dictionary<K,V>`/`LINQ`）

### 📦 前置代码:本 Day 示例用到的类型

> 本 Day 所有示例代码都基于 [Day 2 已定义的 SensorPoint](#day-2--类型基础-值引用enumstruct)。**完整可编译版本**在 [前置类型定义 · 学员粘贴版](前置类型定义_学员粘贴版.md),如果你还没建好,**复制这份到 `DaqMonitor.Core/Models/SensorPoint.cs`**:
```csharp
namespace DaqMonitor.Core.Models;

public readonly struct SensorPoint
{
    public int Id { get; init; }              // init 表示只能在构造时赋值
    public double Value { get; init; }
    public DateTime Timestamp { get; init; }

    public SensorPoint(int id, double value, DateTime? timestamp = null)
    {
        Id = id;
        Value = value;
        Timestamp = timestamp ?? DateTime.Now;
    }
}

// 后半段 LINQ 进阶示例还会用到这个类(便于演示 GroupBy):
public class Device
{
    public int DeviceId { get; init; }
    public string Name { get; set; } = "";
}
```
> 跟着敲时:① `namespace` 改成你项目的根命名空间;② `init` 关键字(C# 9+)表示"只能在对象初始化时赋值",跟 `set` 区别是 init 之后不能再改。

### 一句话讲清楚
LINQ 就是"能写在代码里的 SQL"：像 `filter/map` 一样链式处理集合，还多了 `OrderBy/GroupBy/Sum` 这些数据库级操作。上位机每秒几百个点，全靠它做实时统计。

### 前端类比秒懂
| C# LINQ | 前端数组方法 | 说明 |
|---|---|---|
| `.Where(p => ...)` | `.filter(p => ...)` | 过滤 |
| `.Select(p => ...)` | `.map(p => ...)` | 投影/转换 |
| `.OrderBy(p => ...)` | `.sort(...)` | 升序 |
| `.FirstOrDefault(...)` | `.find(...)` | 找第一个 |
| `.Any(...)` / `.All(...)` | `.some()` / `.every()` | 存在/全部满足 |
| `.Count()` / `.Sum()` / `.Average()` | `.length` / `reduce` | 聚合 |
| `.GroupBy(...)` | 无原生（要手写） | 分组（LINQ 杀手锏） |

### 分点精讲
**① 两大集合**：
```csharp
List<SensorPoint> points = new();     // new() 是简写
points.Add(new SensorPoint(1, 10));   // 构造函数写法(Timestamp 取默认 DateTime.Now)

Dictionary<int, SensorPoint> byId = new();
byId[1] = new SensorPoint(1, 10);
var p = byId[1];               // 按 Id 取
bool ok = byId.TryGetValue(2, out var p2);  // 安全取（不存在不报错）
```

**② LINQ 实战**（上位机日常）：
```csharp
var points = new List<SensorPoint>
{
    new(1, 10),   new(2, 50),
    new(3, 120),  new(4, 200),
    new(5, 30),
};
var hotIds = points.Where(p => p.Value > 100).Select(p => p.Id).ToList(); // [3, 4]
var top3 = points.OrderByDescending(p => p.Value).Take(3).ToList();
double avg = points.Average(p => p.Value);
bool hasAlarm = points.Any(p => p.Value > 150);
```
> 💡 `new(1, 10)` 是 target-typed new(目标类型 new),编译器看左边是 `SensorPoint` 就自动知道是 `new SensorPoint(1, 10)`。`Timestamp` 走构造函数默认值 `DateTime.Now`。

### 🔧 常用方法速查(集合 + LINQ,天天用)

> 上位机数据处理 90% 是"对集合做查询/聚合/转换"。下面 3 张表把最高频方法列全,**背不下来时回这里查**。前端类比都用 JS 数组方法。

**① List\<T> 常用方法**(可变数组,前端 `[]`):

| 方法 | 前端类比 | 用途 | 示例 |
|---|---|---|---|
| `.Add(item)` / `.AddRange(otherList)` | `.push` / `push(...arr)` | 追加 | `points.Add(p)` |
| `.Insert(i, item)` | `.splice(i,0,item)` | 指定位置插 | `points.Insert(0, p)` 头部插 |
| `.Remove(item)` / `.RemoveAt(i)` | 自己写 | 删元素/删指定下标 | `points.RemoveAt(0)` |
| `.RemoveAll(pred)` | `.filter` 反向 | 删所有满足条件的 | `points.RemoveAll(p => p.Id == 3)` |
| `.Clear()` | `.length = 0` | 清空 | 切换设备时 |
| `.Contains(item)` | `.includes` | 是否存在 | `points.Contains(p)` |
| `.IndexOf(item)` / `.FindIndex(pred)` | `.indexOf` / `.findIndex` | 找下标 | `points.FindIndex(p => p.Value > 100)` |
| `.Find(pred)` / `.FindLast(pred)` | `.find` / `.findLast` | 找元素 | `points.Find(p => p.Id == 3)` |
| `.FindAll(pred)` | `.filter` | 过滤(LINQ 前) | 等价 `.Where(...).ToList()` |
| `.ForEach(action)` | `.forEach` | 遍历(不能 await) | `points.ForEach(p => Console.WriteLine(p))` |
| `.Sort()` / `.Sort(comparer)` | `.sort` | **原地**排序(改原集合) | `points.Sort((a,b) => a.Value.CompareTo(b.Value))` |
| `.Reverse()` | `.reverse` | 反转 | 倒序遍历 |
| `.ToArray()` / `.ToList()` | `[...arr]` | 互转 | 接口签名需要 |
| `.Count` / `.Capacity` | `.length` | 数量/容量(扩容前) | `points.Count` |
| `[i]` | `[i]` | 索引访问 | `points[0]` |

> 🔥 **foreach 内不能 Add/Remove**:抛 `InvalidOperationException`。先 `.ToList()` 固化再改,或用倒序 for 循环 + RemoveAt。

**② Dictionary\<K,V> 常用方法**(键值对,前端 `Map`/对象):

| 方法/属性 | 前端类比 | 用途 | 示例 |
|---|---|---|---|
| `[key]` / `.Add(k,v)` / `.TryAdd(k,v)` | `map.set` / `obj[k]=v` | 加/取 | `dict[1] = p` / `dict.TryAdd(1, p)` |
| `dict[key] = value` | 同上 | 改/加 | 不存在会自动加 |
| `.Remove(key)` | `map.delete` | 删 | `dict.Remove(1)` |
| `.ContainsKey(key)` / `.ContainsValue(v)` | `map.has` | 判断存在 | 优先用 ContainsKey(O(1)) |
| `.TryGetValue(key, out V v)` | `map.get` +判空 | **安全取**(不抛) | `dict.TryGetValue(2, out var p)` |
| `.GetValueOrDefault(key, default)` (.NET 8) | `map.get ?? def` | 取不到给默认 | 兼容老代码 |
| `.Count` | `map.size` | 数量 | |
| `.Keys` / `.Values` | `map.keys()` / `map.values()` | 取所有键/值 | 遍历部分 |
| `.Clear()` | `map.clear()` | 清空 | |
| `foreach (var kv in dict)` | `for (const [k,v] of map)` | 遍历 | `kv.Key` / `kv.Value` |
| `.ToDictionary(k => ..., v => ...)` (LINQ) | 自己 reduce | List→Dict | `points.ToDictionary(p => p.Id)` |

> ⭐ **取字典优先用 TryGetValue**:直接 `dict[不存在的key]` 抛 `KeyNotFoundException`,新手最常翻车。

**③ LINQ 60+ 方法分类速查**(分 8 大类,前端记好对应):

| 类别 | 方法 | 前端类比 | 用途 |
|---|---|---|---|
| **过滤** | `.Where(pred)` / `.OfType<T>()` / `.Distinct()` / `.Skip(n)` / `.Take(n)` / `.SkipWhile` / `.TakeWhile` | `.filter` / 类型过滤 / `[...Set]` / `.slice` | 分页用 Skip+Take |
| **投影** | `.Select(pred)` / `.SelectMany(pred)` / `.Cast<T>()` | `.map` / `.flatMap` / 类型转换 | SelectMany 把 List<List> 摊平 |
| **排序** | `.OrderBy(k)` / `.OrderByDescending(k)` / `.ThenBy(k)` / `.ThenByDescending(k)` / `.Reverse()` | `.sort` | 多字段排序用 ThenBy |
| **聚合** | `.Count()` / `.LongCount()` / `.Sum()` / `.Min()` / `.Max()` / `.Average()` / `.Aggregate(seed, fn)` | `.length`/`reduce` | Aggregate = reduce,通用 |
| **元素** | `.First()` / `.FirstOrDefault()` / `.Last()` / `.LastOrDefault()` / `.Single()` / `.SingleOrDefault()` / `.ElementAt(i)` / `.ElementAtOrDefault(i)` | `.find` / `.findLast` | 空集合 First 抛,FirstOrDefault 返默认 |
| **判断** | `.Any(pred)` / `.All(pred)` / `.Contains(item)` | `.some` / `.every` / `.includes` | 是否有/全是/包含 |
| **集合运算** | `.Distinct()` / `.Union(b)` / `.Intersect(b)` / `.Except(b)` / `.Concat(b)` / `.SequenceEqual(b)` | 集合并/交/差/拼/相等 | 去重、差集 |
| **分组** | `.GroupBy(k)` / `.ToLookup(k)` / `.Join(b, ok, ik, sel)` / `.GroupJoin(...)` | 手写 reduce | GroupBy 返回 IGrouping 序列 |
| **转换固化** | `.ToArray()` / `.ToList()` / `.ToDictionary(k)` / `.ToHashSet()` / `.ToLookup(k)` | — | **触发延迟执行**(下锅炒菜) |
| **生成** | `Enumerable.Range(1,10)` / `Enumerable.Repeat(x,n)` / `Enumerable.Empty<T>()` / `.DefaultIfEmpty(x)` | `Array.from` / `Array(n).fill` | 序号生成、空集合兜底 |

**④ 真实上位机场景速写**(把上面方法串起来):

```csharp
// 报警分组统计:每个设备的报警数 + 平均值,按报警数降序前 5
var top5 = alarms
    .GroupBy(a => a.DeviceId)                              // 按设备分组
    .Select(g => new {
        DeviceId = g.Key,
        Count   = g.Count(),
        Avg     = g.Average(a => a.Value),
        Max     = g.Max(a => a.Value),
    })
    .OrderByDescending(x => x.Count)                       // 按报警数降序
    .Take(5)                                               // 取前 5
    .ToList();                                             // 固化(下锅炒菜)

// 找出"连续 3 次超限"的设备(用 Aggregate 做"状态机")
var streakDevices = points
    .GroupBy(p => p.DeviceId)
    .Where(g => g.Aggregate(0, (streak, p) =>
        p.Value > 100 ? streak + 1 : 0,                    // 累加器:超限+1,否则归零
        result => result >= 3))                            // 最终判断
    .Select(g => g.Key)
    .ToList();
```

> 🎤 **面试加分点**:被问"LINQ 延迟执行"答完定义后,补一句"**IEnumerable 是延迟,IEnumerable.ToList 才固化** — 写循环时如果 LINQ 链被多次 foreach,必须先 ToList 防重复执行"——这就是 13K vs 8K 的细节。

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ 延迟执行(lazy) | `Where/Select` **不立即计算**，直到 `.ToList()/.Count()/foreach` 才真跑 |
| 🔥 遍历时改集合 | `foreach` 里 `Add/Remove` 会抛异常。先 `.ToList()` 固化再改 |
| 🔥 重复枚举 | 一个 LINQ 查询被多次遍历会重复计算，需固化就 `.ToList()` 存起来 |
| ⭐ 方法链 vs 查询语法 | `points.Where(...).Select(...)`（推荐）；也可 `from p in points where ... select p` |

### 🎭 拟人秒懂:菜谱 vs 菜(延迟执行记忆锚点)

> 把 LINQ 查询想象成"写菜谱",把 `.ToList()` 想象成"下锅炒菜"。

- **写 LINQ 链** = 写菜谱:`points.Where(p=>p.Value>100).Select(p=>p.Id)` — 这只是**记录了"先筛选再投影"的步骤**,还没真正执行
- **`.ToList()` / `.Count()` / `foreach`** = 下锅炒菜:这一刻才**真正遍历数据**,菜谱变成菜
- **延迟执行的好处**:你可以链式拼接 5 步,最后一次遍历,中间不分配临时集合 — 高性能
- **延迟执行的坑**:
  - **重复枚举**:你把菜谱放进两个餐厅的厨房,他们各自炒一遍 → 同一份 LINQ 查询 `foreach` 两次,数据库查了两次 → 服务器爆炸
  - **数据变了**:你写菜谱时数据是 100 条,炒菜时数据已经变成 105 条 → 结果对不上

**前端类比**:跟 RxJS `observable` 一样 — `.pipe(filter, map)` 只是声明,`.subscribe()` 才执行。LINQ 也是声明式,`.ToList()` 才执行。

**新人 100% 翻车场景**:
```csharp
var query = points.Where(p => p.Value > 100);   // 写菜谱,还没炒
points.Add(new SensorPoint { Id = 99, Value = 200 });   // 加新点
var result = query.ToList();   // 炒菜 — 包含新加的点!不一定是你要的
```
**安全做法**:写完 LINQ 立刻 `.ToList()` 固化结果,后续操作基于这份"已炒好的菜",不再受原集合变化影响。

### 🎓 一句话记住
**"LINQ 是菜谱不是菜,ToList 才下锅。重复炒菜谱要警惕,数据变了菜就变。"**

### 🟢 基础题
给 5 个点（值 10/50/120/200/30），用 LINQ 找出值 >100 的 Id 列表。

### 🟡 进阶题
算全部点的平均值，并按值降序排列输出。

### 🔴 挑战题
用 `.GroupBy` 把点按"是否超限(>100)"分成两组，分别输出每组的个数和平均值（模拟报警分组统计）。

**✅ 答案**
```csharp
var hot = points.Where(p => p.Value > 100).Select(p => p.Id).ToList(); // [3,4]
var avg = points.Average(p => p.Value);                                // 82
var sorted = points.OrderByDescending(p => p.Value).ToList();          // 200,120,50,30,10
// 挑战
var groups = points.GroupBy(p => p.Value > 100 ? "超限" : "正常");
foreach (var g in groups)
    Console.WriteLine($"{g.Key}: 个数={g.Count()} 均值={g.Average(p=>p.Value):F1}");
```

> 📦 **前置准备**:需要 `DaqMonitor.Core/Models/SensorPoint.cs` 已存在(Day 2 项目任务建的)。**还没建 Core 工程?** 跳到 [Day 2 开头「📦 立项目骨架」](#-立项目骨架本-day-项目任务的前提必做)。

**🏗️ 项目任务**：在 Core 写 `Store/PointStore.cs`（工程里真实文件）：内部用 `List<SensorPoint>` + `Dictionary<int,SensorPoint>` 双索引，提供 `AddOrUpdate/Get/GetAll`，并用 LINQ 写 `GetAlarms(threshold)` 返回超阈值的点（Day 7 报警直接复用）。

**🎓 工控导师说**：某次现场，徒弟用 `foreach` 遍历点位集合时顺手 `points.Remove(...)`，程序直接抛异常崩在客户面前。记住：**改集合前先 `.ToList()` 复制一份**。

**💼 职业建议**：LINQ 是 C# 面试必考，且常考"延迟执行"——答出"LINQ 查询不立即执行，ToList 才触发"能直接拉开和 8K 候选人的差距。

**✅ 打卡[ ]**

---

## Day 4 — 类 / 属性 / 封装 / static 🟢

**📌 技术来源**：🟦 `class` / 属性 / 访问修饰符 / `static`

### 一句话讲清楚
类是"设备的蓝图"，属性是"带 getter/setter 的字段"（C# 一等公民），封装是"用 private 挡住不该被外部乱改的数据"。WPF 数据绑定**只认属性**，所以这是上位机 UI 的地基。

### 前端类比秒懂
| C# | 前端 | 说明 |
|---|---|---|
| `class Device {}` | `class Device {}` | ES6 也有 class |
| `public double Value { get; set; }` | getter/setter | C# 语法更简洁 |
| `private` / `public` / `internal` | `#private` / 约定 `_` | C# 是**编译期**访问控制 |
| `static` | `static` | 属于类而非实例 |
| 构造函数 `public Device(int id){}` | `constructor(id){}` | 初始化 |

### 分点精讲
**① 属性（Property）—— 不是字段**：🟦 `{ get; set; }` 自动属性，还能加逻辑：
```csharp
public class Device
{
    public int Id { get; }               // 只读（只有 get），构造时赋值
    public string Name { get; set; } = "未命名";  // 可读写 + 默认值
    private double _value;
    public double Value                  // 带逻辑的属性
    {
        get => _value;
        set { if (value < -273) throw new ArgumentException("温度不合理"); _value = value; }
    }
    public Device(int id) => Id = id;    // 构造函数（表达式体写法）
}
```

**② 访问修饰符**：
```csharp
public class Foo
{
    public int A;      // 任何地方可访问
    private int _b;    // 只有本类内
    internal int C;    // 同一个程序集（项目）内
    protected int D;   // 本类 + 子类
}
```

**③ static —— 工具方法/常量**：🟦
```csharp
public static class Crc16   // 后面算 Modbus 校验会用到
{
    public static ushort Compute(byte[] data) { /* ... */ return 0; }
}
// 调用：Crc16.Compute(bytes);  ≈ 你用过的 Math.max()
```

### 🔧 类与对象常用成员速查

> Day 4 介绍了 class/属性/static,**这里补"日常写类时 90% 都会用到的成员"**。前端类比都用 JS/TS。

**① 类型检查与转换(天天用,面试常考)**:

| 操作 | 前端类比 | 用途 | 示例 |
|---|---|---|---|
| `obj is Type` | `instanceof` | 类型判断,返回 bool | `if (dev is SerialDevice)` |
| `(Type)obj` | 强转 | 直接转,失败抛 `InvalidCastException` | `(SerialDevice)dev` |
| `obj as Type` | — | 安全转,失败返回 null | `var s = dev as SerialDevice; if (s != null)...` |
| `is Type x`(模式匹配) | — | **判断+赋值一步** | `if (dev is SerialDevice s) s.Open();` |
| `is { Property: val }`(属性模式) | — | 解构判断(.NET 7+) | `if (p is { Value: >100 }) ...` |
| `typeof(T)` | `T` 直接用 | 取类型本身 | `typeof(SerialDevice)` |
| `obj.GetType()` | `obj.constructor` | 运行时类型 | `dev.GetType().Name` |
| `switch` 表达式 | TS `switch(true)` | 模式匹配分发 | 见下例 |

```csharp
// 模式匹配分发(上位机"按设备类型分发解析器"天天用)
string label = dev switch
{
    SerialDevice s    => $"串口:{s.PortName}",
    ModbusDevice m    => $"Modbus:{m.UnitId}",
    TcpDevice t       => $"TCP:{t.Ip}",
    null              => "未连接",
    _                 => "未知设备",    // _ 是 default
};
```

**② object 通用方法(所有类型继承,可重写)**:

| 方法 | 前端类比 | 用途 | 上位机场景 |
|---|---|---|---|
| `.ToString()` | `.toString()` | 转字符串 | 默认返回类名 — **必须重写**才有用 |
| `.Equals(obj)` | `===` | 判等(默认比引用) | 值对象要重写:点位相同算相等 |
| `.GetHashCode()` | — | 哈希码(Dictionary 用) | 重写 Equals 必须**同时**重写 |
| `.GetType()` | `.constructor` | 取运行时类型 | 反射/序列化 |

```csharp
// 值类型语义的类(必须同时重写三个)
// 注意:这里改名 PointValue,避免和项目里 SensorPoint(struct)冲突
public class PointValue
{
    public int Id { get; }
    public PointValue(int id) => Id = id;
    public override bool Equals(object? obj) => obj is PointValue p && p.Id == Id;
    public override int GetHashCode() => Id;
    public override string ToString() => $"Point#{Id}";
}
// 不重写:两个 new PointValue(1) 用 Equals 返回 false(默认比引用地址)
```

**③ record 记录类型(C# 9+,值语义+不可变,前端无对应)**:
```csharp
// 注意:这里改名 PointRecord,避免和项目里 SensorPoint(struct)冲突
public record PointRecord(int Id, double Value);    // 一行定义
var p1 = new PointRecord(1, 10.5);
var p2 = new PointRecord(1, 10.5);
p1.Equals(p2);   // ✅ true(record 自带值相等)
var p3 = p1 with { Value = 20.0 };   // 拷贝并改一个字段(不可变修改)
```
> 💡 用途:DTO/消息/事件参数 — 不变量传递,代码极简。这是给"轻量值对象"用的语法糖;项目里真正的 `SensorPoint` 是 struct(参 Day 2 / [前置类型定义](前置类型定义_学员粘贴版.md)),二者各司其职。

**④ using / IDisposable(资源释放,Day 7 多线程+并发也用)**:
```csharp
using var stream = new FileStream("log.txt", FileMode.Append);   // 离开作用域自动 Dispose
// ≈ 前端 finally + close()
// 等价老写法:
using (var conn = new SqlConnection(connStr)) { /* ... */ }   // 块结束释放
```
> ⭐ **必须 using 的类型**:`FileStream` / `SqlConnection` / `SerialPort` / `TcpClient` / `Timer` / `DbContext`(EF Core) — 用完不释放就**句柄泄漏**,程序跑久了崩。

**⑤ nameof(重命名安全)**:
```csharp
throw new ArgumentException("温度非法", nameof(temperature));   // → "temperature"
// 重命名变量后字符串自动同步,不会漏
// WPF 数据绑定也用 OnPropertyChanged(nameof(Value));
```

**⑥ 杂项速查(基础语法补丁)**:

| 操作 | 示例 | 用途 |
|---|---|---|
| 三元 | `bool ok = x > 0 ? true : false;` | 跟 JS 一样 |
| 空合并 | `string n = name ?? "匿名";` | null 取默认 |
| 空条件 | `obj?.Method()` | null 安全调用 |
| 字符串插值 | `$"{p.Id}-{p.Value:F2}"` | 模板字符串(必学) |
| 范围符 | `arr[1..3]` (.NET 8+) | 取子数组(类似 slice) |
| 元组 | `(int Id, string Name) = (1, "x");` | 多返回值 |
| 解构 | `var (id, name) = device;` | 元组/record 拆解 |
| `out` 参数 | `int.TryParse(s, out int v)` | 多返回(老式) |
| `ref` 参数 | `void Inc(ref int x)` | 传引用(可改原值) |
| `in` 参数 | `void Show(in BigStruct s)` | 传引用只读(避免 struct 拷贝) |
| `params` | `int Sum(params int[] nums)` | 可变参数 |
| 扩展方法 | `arr.MyExtension()` | 给别人的类加方法(LINQ 就是) |

> 🎤 **面试加分点**:`is/as/强转` 三者区别是面试高频题。答:"**is 是判断返回 bool,as 是转换返回 null,强转是转换抛异常**。日常用模式匹配 `is Type x` 最优雅,一步到位。"

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ 属性 vs 字段 | 面试常问。字段是变量，属性是带存取逻辑的"方法伪装"，WPF 绑定必须用属性 |
| ⭐ 只读属性 | `{ get; }` + 构造赋值，防止外部乱改 Id |
| 🔥 static 不能访问实例成员 | static 方法里不能直接用 `this.xxx`（没有实例） |
| 🔥 静态字段是全局共享 | 所有实例共用一份，慎用（多线程下要加锁，见 Day 7） |

### 🎭 拟人秒懂:裸字段 vs 西装属性(WPF 绑定记忆锚点)

> 把字段想象成"穿睡衣的家伙",把属性想象成"穿西装的接待员"。

- **裸字段(field)**:直接暴露变量,任何外部代码都能改
  - `public int Count;` ← 穿睡衣的家伙站门口,谁都能拍他肩膀改他的值,你管不着
- **属性(property)**:带 getter/setter 的"接待员",控制谁能改、改成什么
  - `public int Count { get; set; }` ← 穿西装的接待员,有人要改 → 接待员检查"OK 这值合理"再传给后台
  - `public int Count { get; }` ← 只读属性:接待员只允许看,不允许改
  - `public int Count { get; protected set; }` ← 只有子类能改:接待员只听家族成员的
- **完整属性(带逻辑)**:像带 X 光机的接待员
  ```csharp
  public double Temperature
  {
      get => _t;
      set
      {
          if (value < -273) throw new ArgumentException("温度不能低于绝对零度");
          _t = value;
      }
  }
  ```

**WPF 绑定为什么只认属性不认字段**:WPF 数据绑定引擎靠**属性变化通知**(`INotifyPropertyChanged`)工作。属性可以触发"我变了"事件,字段不会通知。**所以你的领域模型 99% 用属性暴露,字段全 private**。

**前端类比**:
- 字段 = 普通对象属性 `obj.x = 1`(直接赋值,无校验)
- 属性 = ES6 getter/setter `get x() {...} set x(v) {...}`(可加校验)
- Vue 的 `data` / React 的 `state` 本质都是"属性" — 改它们会触发渲染,而不是改一个普通变量

**面试加分**:"工业软件的设定值属性会加边界校验 — 比如温度设定不能低于 -273℃ 或高于 1000℃,在 setter 里抛异常比 UI 层校验更可靠,因为多个 UI 入口共享同一份校验逻辑。"

### 🟢 基础题
写 `class Thermometer`：只读 `Id`、可读写 `Temperature`、构造传 Id。

### 🟡 进阶题
加方法 `bool IsOverheat(double limit) => Temperature > limit;`。

### 🔴 挑战题
给 `Thermometer` 的 `Temperature` 属性加逻辑：赋值超过 100 时自动 clamp 到 100（防止传感器溢出值污染系统）。

**✅ 答案**
```csharp
public class Thermometer
{
    public int Id { get; }
    public double Temperature { get; set; }
    public Thermometer(int id) => Id = id;
    public bool IsOverheat(double limit) => Temperature > limit;
    // 挑战
    private double _t;
    public double TemperatureClamped
    {
        get => _t;
        set => _t = value > 100 ? 100 : value;
    }
}
```

> 📦 **前置准备**:需要 `DaqMonitor.Core/Models/` 已有 `DeviceState` 枚举(Day 2 项目任务)。**还没建 Core 工程?** 跳到 [Day 2 开头「📦 立项目骨架」](#-立项目骨架本-day-项目任务的前提必做)。

**🏗️ 项目任务**：在 Core 抽象 `Devices/DeviceBase.cs` 基类（工程里真实存在）：含 `Id/Name/State` 属性（State 用 Day 2 的 `DeviceState` 枚举，`protected set` 只允许子类改）与 `Connect()/Disconnect()` 方法占位。这是 Day 5「IDevice 统一接口」的地基。

**🎓 工控导师说**：新手最爱把所有字段写成 `public`，结果别人在 UI 里把设备 `State` 直接改成 Online 骗自己。封装不是教条——**把"只有子类能改"的标 `protected set`，把"只读"的标 `get`**，系统才诚实。

**💼 职业建议**：封装 + 属性是面向对象基础题，答时务必联系 WPF："WPF 绑定只认属性，所以领域模型全用属性暴露"——这点位很加分。

**✅ 打卡[ ]**

---

## Day 5 — 继承 / 接口 / 泛型 🟡

**📌 技术来源**：🟦 继承（`:`）/ 接口（`interface`）/ 泛型（`<T>`）

### 一句话讲清楚
接口 = "契约"（规定能做什么，不管怎么做），继承 = "复用父类"，泛型 = "写一次适配任意类型"。上位机靠**面向接口**让 UI 不关心设备是串口还是 PLC——这是 15K 面试必讲的架构分水岭。

### 前端类比秒懂
| C# | 前端 (TS) | 说明 |
|---|---|---|
| `interface IDevice {}` | `interface IDevice {}` | TS 也有，几乎一样 |
| `class A : Base, IX` | `class A extends Base implements IX` | 单继承 + 多接口 |
| `abstract class` | `abstract class` | 不能直接 new |
| `<T>` 泛型 | `<T>` 泛型 | 概念一致 |
| `virtual` / `override` | 方法重写 | C# 要显式标 `virtual`/`override` |

### 分点精讲
**① 接口 —— 上位机架构核心**：🟦
```csharp
public interface IDevice
{
    int Id { get; }
    DeviceState State { get; }
    void Connect();
    void Disconnect();
    double Read(int addr);            // 读寄存器
    void Write(int addr, double v);   // 写寄存器
}
```
> 📂 `DaqMonitor.Core/Devices/IDevice.cs` · namespace `DaqMonitor.Core.Devices`
> 🔧 无 NuGet(纯 C# 接口)
> 💡 用到 [`DeviceState`](#day-2--类型系统值类型--引用类型--可空--struct--enum-)(Day 2 已建)。如果编译报"找不到 DeviceState"→ 回 Day 2 项目任务把 `Models/DeviceState.cs` 建好。

> UI 只依赖 `IDevice`，明天接串口、后天接 PLC，UI 代码一行不用改——这就是解耦的价值（工程里 `DaqMonitor.Core/Devices/IDevice.cs` 已是这个样子）。

**② 抽象基类 + 继承 + 多态**：🟦

> 📂 `DaqMonitor.Core/Devices/DeviceBase.cs` · namespace `DaqMonitor.Core.Devices`
> 🔧 无 NuGet
> 💡 用到 `IDevice`(本节①刚定义) + `DeviceState`(Day 2)

```csharp
public abstract class DeviceBase : IDevice
{
    public int Id { get; }
    public DeviceState State { get; protected set; } = DeviceState.Offline;
    protected DeviceBase(int id) => Id = id;
    public abstract void Connect();      // 子类必须实现
    public abstract void Disconnect();
    public abstract double Read(int addr);
    public abstract void Write(int addr, double v);
}
```

**③ 泛型 —— 写一次适配任意类型**：🟦
```csharp
public class RingBuffer<T>       // 环形缓冲，后面存历史数据用
{
    private readonly Queue<T> _q = new();
    private readonly int _max;
    public RingBuffer(int max) => _max = max;
    public void Add(T item)
    {
        _q.Enqueue(item);
        if (_q.Count > _max) _q.Dequeue();   // 超量丢最旧
    }
    public IEnumerable<T> Items => _q;
}
```

### 🔧 泛型约束 + 常用接口家族速查

> Day 5 学了继承/接口/泛型的概念,**这里补"日常写库时 99% 会遇到的接口和约束"**。前端类比都用 TS。

**① 泛型约束 `where T : ...`**(类似 TS 的 `<T extends SomeType>`):

| 约束 | 含义 | 用途 |
|---|---|---|
| `where T : class` | T 必须是引用类型 | 不让传值类型 |
| `where T : class?` | 引用类型,可为 null | 兼容可空 |
| `where T : struct` | T 必须是值类型 | 限制数值/struct |
| `where T : new()` | T 必须有无参构造 | `new T()` 创建实例 |
| `where T : BaseClass` | T 必须继承某类 | 限定子类家族 |
| `where T : ISomeInterface` | T 必须实现某接口 | 强制能力 |
| `where T : notnull` | T 不可空(Nullable 引用上下文) | 防空 |
| `where T : U` | T 必须派生自 U | 罕见,泛型方法内 |

```csharp
// 上位机仓储模式:必须有 Id + 无参构造 + 引用类型
public class Repository<T> where T : class, IHasId, new()
{
    public T Create() => new T();
}
// 多约束逗号串起来,顺序:可访问性 > class/struct > 基类 > 接口 > new()
```

**② 常用 BCL 接口家族**(写库天天见):

| 接口 | 前端类比 | 用途 | 典型实现 |
|---|---|---|---|
| `IEquatable<T>` | 自己写 equals | 类型安全的 `Equals(T other)` | 值类型/record 必实现 |
| `IComparable<T>` | 自己写 sort 比较 | 排序:`CompareTo(T) → -1/0/1` | 数值、字符串 |
| `IComparer<T>` | 比较函数 | 外部比较器(多字段排序) | `Comparer<T>.Default` |
| `IEqualityComparer<T>` | — | 相等比较器(Dictionary 用) | `StringComparer.OrdinalIgnoreCase` |
| `IEnumerable<T>` | iterable | **可遍历(LINQ 的基础)** | 所有集合都实现 |
| `IEnumerator<T>` | iterator | foreach 实际调用的"游标" | 编译器自动生成 |
| `ICollection<T>` | — | 集合基础(Count/Add/Remove) | List 实现的接口 |
| `IList<T>` | — | 可索引集合 | List/数组 |
| `IDictionary<K,V>` | `Map` | 键值集合 | Dictionary |
| `IReadOnlyList<T>` | readonly array | 只读视图 | `list.AsReadOnly()` |
| `IDisposable` | finally+close | 资源释放(Day 4 详) | FileStream/SerialPort |
| `ICloneable` | structuredClone | 克隆(微软已不推荐,新代码别用) | 老代码里才有 |
| `INotifyPropertyChanged` | Vue reactive | WPF 数据绑定**必备**(M8 重点) | ViewModel 全实现 |
| `IFormattable` | toString(format) | 自定义格式化 | DateTime/数值 |
| `IConvertible` | 类型转换 | Convert.ToXXX 的支持 | 基础类型 |

```csharp
// 让点可排序(按 Value 排序)
// 注意:这里改名 ComparablePoint,避免和项目里 SensorPoint(struct)冲突
public class ComparablePoint : IComparable<ComparablePoint>
{
    public double Value { get; }
    public ComparablePoint(double value) => Value = value;
    public int CompareTo(ComparablePoint? other) =>
        other is null ? 1 : Value.CompareTo(other.Value);
}
// 用法
var pts = new List<ComparablePoint> { new(10), new(5), new(20) };
pts.Sort();   // 自动用 CompareTo 排序 → [5, 10, 20]
```

**③ 协变(out)和逆变(in)**(高级,面试加分):

| 关键字 | 含义 | 类比 TS | 例子 |
|---|---|---|---|
| `out T` | 协变(只能返回) | — | `IEnumerable<out T>` → 可用 `IEnumerable<父>` 接 `IEnumerable<子>` |
| `in T` | 逆变(只能传入) | — | `Action<in T>` → 可用 `Action<子>` 接 `Action<父>` |

```csharp
// 协变:子类枚举可赋给父类枚举
IEnumerable<SerialDevice> serials = new List<SerialDevice>();
IEnumerable<IDevice> devices = serials;   // ✅ out 协变允许

// 逆变:父处理器可处理子类型
Action<IDevice> handler = d => Console.WriteLine(d.Name);
Action<SerialDevice> serialHandler = handler;   // ✅ in 逆变允许
```

**④ 接口默认实现(C# 8+,前端 `default method`)**:
```csharp
public interface ILogger
{
    void Log(string msg);
    void LogError(string msg) => Log($"[ERR] {msg}");   // 默认实现,实现类可重写也可不重
}
```

**⑤ .NET 常用接口速记表**(面试前过一遍):

| 领域 | 接口 | 一句话 |
|---|---|---|
| 集合 | `IEnumerable<T>` | 可遍历 — LINQ 的根 |
| 集合 | `ICollection<T>` / `IList<T>` | 可增删/可索引 |
| 集合 | `IDictionary<K,V>` | 键值对 |
| 比较 | `IEquatable<T>` / `IComparable<T>` | 值相等/排序 |
| 比较 | `IComparer<T>` / `IEqualityComparer<T>` | 外部比较器 |
| 资源 | `IDisposable` | 释放句柄 |
| WPF | `INotifyPropertyChanged` | 属性变化通知 |
| WPF | `INotifyCollectionChanged` | 集合变化通知(`ObservableCollection`) |
| 异步 | `IAsyncEnumerable<T>` | 异步流(Day 7 末尾) |

> 🎤 **面试加分点**:"接口和抽象类怎么选"是高频题。标准答:"**接口定义能力(can-do),抽象类共享实现(is-a)** — 比如 `IDevice` 是接口(任何设备都能 Connect),`DeviceBase` 是抽象类(共享默认实现)。一个类只能继承一个抽象类,但可实现多个接口,所以**优先用接口解耦**。"

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ 接口 vs 抽象类 | 面试高频！**接口=能做什么(可多实现)**，**抽象类=是什么(单继承+可含实现)**。设备用接口解耦 |
| ⭐ 多态 | 用 `IDevice d` 引用不同子类，调 `d.Read()` 自动走各自实现 |
| 🔥 virtual/override 要配对 | 父类方法不标 `virtual`，子类不能 `override` |
| 🔥 接口不含字段 | 只能有属性/方法（C# 8+ 可有默认实现，但别滥用） |

### 🎭 拟人秒懂:合同 vs 师徒(接口 vs 抽象类记忆锚点)

> 把接口想象成"劳动合同",把抽象类想象成"师徒传承"。

- **接口(Interface)= 劳动合同**:
  - 只规定"要做什么"(Connect / Disconnect / Read / Write),不管你怎么做
  - 一个工人可以签多份合同(多实现):`class ModbusDevice : IDevice, IDisposable, INotifyPropertyChanged`
  - 合同不带工具(`interface` 不能有字段,只有方法/属性签名)
  - **谁都可以签合同**(任何类都能实现):正式工、临时工、外包都能签
- **抽象类(Abstract Class)= 师徒传承**:
  - 师傅教徒弟"是什么"(身份 + 部分技能):`DeviceBase` 提供 Id/Name/State 公共字段和 RaiseData 实现方法
  - 徒弟**只能拜一个师傅**(单继承):`class ModbusDevice : DeviceBase` 不能再继承别的类
  - 师傅可以留"待徒弟补全的招式"(`abstract` 方法):`public abstract void Connect();` 徒弟必须实现
  - **不拜师傅不能入门**(抽象类不能 `new`):`new DeviceBase()` 编译报错,必须 `new ModbusDevice()`

**为什么 DAQMonitor 两个都用**:
- `IDevice`(接口):UI 只依赖它,**换设备 UI 零改动**
- `DeviceBase`(抽象类):所有设备共用 `Id/Name/State/RaiseData` 等基础能力,**避免重复代码**

**记忆口诀**:**"接口是合同(多签),抽象类是师徒(单拜)"**。
**面试加分话术**:"我们 DAQMonitor 同时用 IDevice(接口,UI 依赖它做解耦和 Mock 测试)和 DeviceBase(抽象类,提供 RaiseData/State 等公共实现),分工明确。"

**泛型 = 万能模具**:
- 写一份"模具"适配任意类型:`RingBuffer<T>`、`Max<T>(IEnumerable<T>)`
- 编译期类型安全(不像 JS `any` 后门):`RingBuffer<int>` 装不进 string
- **前端类比**:跟 TS `<T>` 一模一样,只是 C# 在运行时也保留类型信息(TS 编译后擦除)

### 🎓 一句话记住
**"接口=合同多签,抽象类=师徒单拜。两个一起用:接口做解耦,抽象类做复用。"**

### 🟢 基础题
定义 `interface IReadable { double Read(int addr); }`，让 Day 4 的 `Thermometer` 实现它。

### 🟡 进阶题
写一个 `SerialDevice : DeviceBase`，`Read` 暂时返回 0（M1 填真实逻辑），验证多态：用 `IDevice d = new SerialDevice(1);` 调用。

### 🔴 挑战题
写泛型方法 `T Max<T>(IEnumerable<T> items) where T : IComparable<T>`，返回集合最大值，体会泛型约束。

**✅ 答案**
```csharp
public interface IReadable { double Read(int addr); }
public class Thermometer : IReadable
{
    public int Id { get; }
    public double Temperature { get; set; }
    public Thermometer(int id) => Id = id;
    public double Read(int addr) => Temperature;
}
// 挑战
public static T Max<T>(IEnumerable<T> items) where T : IComparable<T>
{
    T max = items.First();
    foreach (var i in items) if (i.CompareTo(max) > 0) max = i;
    return max;
}
```

> 📦 **前置准备**:需要 `DaqMonitor.Core/Devices/DeviceBase.cs` 已存在(Day 4 项目任务)。**还没建 Core 工程?** 跳到 [Day 2 开头「📦 立项目骨架」](#-立项目骨架本-day-项目任务的前提必做)。

**🏗️ 项目任务**：在 Core 正式定义 `IDevice` 接口，让 `DeviceBase` 实现它（`DaqMonitor.Core/Devices/`）；建 `Devices/` 目录放接口与基类。DAQ Monitor 从"Day1 按钮"进化成"可插拔多设备框架"。

**🎓 工控导师说**：有个学员面试被问"为什么用接口而不是具体类"，他答"解耦"。面试官追问"解耦带来什么具体好处"，他卡壳了。标准答法：**"UI 只依赖 IDevice，换设备（串口→Modbus→PLC）UI 零改动，且能用 Mock 设备写单元测试——这是我们 DAQ Monitor 能 28 个测试全绿的根本原因。"**

**💼 职业建议**：接口/多态/依赖反转是 13K→15K 的核心考点。准备一个你项目里的真实例子（我们就是 DAQ Monitor 的 IDevice），比背定义强十倍。

**✅ 打卡[ ]**

---

## Day 6 — 委托 / 事件 / Lambda 🟡

**📌 技术来源**：🟦 委托 `delegate` / 事件 `event` / Lambda + 🟩 BCL（`Action`/`Func`/`EventHandler<T>`）

### 一句话讲清楚
委托 = "把方法当变量传"（≈ 回调函数），事件 = "基于委托的发布订阅"（≈ `addEventListener`）。上位机用它实现"后台采到数据 → 自动通知 UI 刷新"——这是响应式 UI 的底层机制。

### 前端类比秒懂
| C# | 前端 | 说明 |
|---|---|---|
| `Action` | `() => void` | 无返回的委托 |
| `Action<int>` | `(x:number) => void` | 带参无返回 |
| `Func<int,int>` | `(x:number) => number` | 带返回 |
| `event` | `EventEmitter` / `addEventListener` | 发布订阅 |
| `d.X += handler` | `emitter.on('x', handler)` | 订阅 |
| `d.X -= handler` | `emitter.off('x', handler)` | 退订（重要！） |
| Lambda `p => p.V > 1` | 箭头函数 `p => p.v > 1` | 几乎一样 |

### 分点精讲
**① Action / Func —— 内置委托**：🟩
```csharp
Action sayHi = () => Console.WriteLine("hi");
Action<string> log = msg => Console.WriteLine(msg);
Func<int, int, int> add = (a, b) => a + b;
sayHi(); log("采集启动"); int s = add(1, 2);   // s=3
```

**② 事件 —— 设备层→UI 层单向通知**：🟦🟩
```csharp
// 这里用自定义 DataEventArgs 演示"标准 EventArgs 模式"(教学版)
// 注意:实际项目 DaqMonitor.sln 里 IDevice.DataReceived 直接用 SensorPoint 做事件载荷
// (参 [前置类型定义](前置类型定义_学员粘贴版.md) 的 IDevice 定义:
//    event EventHandler<SensorPoint>? DataReceived;)
// 二者都正确,自定义 DataEventArgs 适合"事件需要带多个上下文字段"的复杂场景
public class DataEventArgs : EventArgs
{
    public int PointId { get; init; }
    public double Value { get; init; }
}
public abstract class DeviceBase
{
    public event EventHandler<DataEventArgs>? DataReceived;
    protected void RaiseData(int pointId, double value)
        => DataReceived?.Invoke(this, new DataEventArgs { PointId = pointId, Value = value });
}
// 订阅方（UI）
device.DataReceived += OnData;
void OnData(object? s, DataEventArgs e) => Console.WriteLine($"点{e.PointId}={e.Value}");
// 销毁时：device.DataReceived -= OnData;   // 退订，防内存泄漏
```

### 🔧 委托家族 + Lambda 速查

> Day 6 介绍了委托/事件概念,**这里补"日常写代码 99% 用内置委托就够,不用自己声明"**。前端类比都用 JS 回调。

**① 内置委托家族(背下来,告别自定义 delegate)**:

| 委托 | 签名 | 前端类比 | 用途 |
|---|---|---|---|
| `Action` | `void()` | `() => {}` | 无参无返回 |
| `Action<T>` | `void(T x)` | `x => {}` | 1 参无返回 |
| `Action<T1,T2>` | `void(T1,T2)` | `(a,b) => {}` | 2 参无返回(最多 16 参) |
| `Func<TResult>` | `TResult()` | `() => value` | 无参有返回 |
| `Func<T,TResult>` | `TResult(T x)` | `x => value` | **LINQ 标准**(1→1) |
| `Func<T1,T2,TResult>` | `TResult(T1,T2)` | `(a,b) => value` | 最多 16 参 |
| `Predicate<T>` | `bool(T x)` | `x => boolean` | 判断(LINQ 内部用 Func<T,bool>) |
| `Comparison<T>` | `int(T,T)` | `(a,b) => number` | List.Sort 的比较函数 |
| `Converter<TInput,TOutput>` | `TOutput(TInput)` | `.map` 回调 | List.ConvertAll |
| `EventHandler` | `void(object, EventArgs)` | DOM `addEventListener` | **标准事件签名** |
| `EventHandler<TArgs>` | `void(object, TArgs)` | 自定义事件 | 现代 WPF/WinForms 标准 |

```csharp
// 老式自定义(不推荐,代码冗余)
public delegate void DataReceivedHandler(SensorPoint p);
public event DataReceivedHandler OnData;

// 新式内置委托(推荐)
public event Action<SensorPoint>? OnData;
// 或标准事件签名
public event EventHandler<SensorPoint>? OnData;
```

**② Action / Func / Predicate 实战速写**:
```csharp
// Action:工具方法回调
Action<string> logger = msg => Console.WriteLine($"[{DateTime.Now:HH:mm:ss}] {msg}");
logger("设备已连接");

// Func:LINQ 投影/转换
Func<SensorPoint, string> toCsv = p => $"{p.Id},{p.Value:F2}";
var csv = points.Select(toCsv).ToList();

// Predicate:判断(老代码常见,List.FindAll)
Predicate<SensorPoint> isAlarm = p => p.Value > 100;
var alarms = points.FindAll(isAlarm);

// Comparison:排序
Comparison<SensorPoint> byValue = (a, b) => a.Value.CompareTo(b.Value);
points.Sort(byValue);
```

**③ EventHandler 标准 .NET 事件模式**(WPF/WinForms 都遵循):
```csharp
// 1. 自定义事件参数(继承 EventArgs)
public class AlarmEventArgs : EventArgs
{
    public int PointId { get; init; }
    public double Value { get; init; }
    public string Level { get; init; }
}

// 2. 发布者:声明 event
public class AlarmEngine
{
    public event EventHandler<AlarmEventArgs>? AlarmRaised;

    public void Evaluate(SensorPoint p)
    {
        if (p.Value > 100)
            AlarmRaised?.Invoke(this, new AlarmEventArgs {   // ?. 防空引用
                PointId = p.Id, Value = p.Value, Level = "Critical"
            });
    }
}

// 3. 订阅者:hook 上去
engine.AlarmRaised += (sender, e) => Console.WriteLine($"报警!点 {e.PointId} = {e.Value}");
// 解订阅
engine.AlarmRaised -= handler;   // 必须 + 创建的同一实例
```

> ⭐ **事件三件套**:`EventArgs` 子类 + `event` 关键字 + `?.Invoke(this, args)`。这套是 .NET 标准,WPF 路由事件、Prism EventAggregator 全是这套的演化。

**④ event vs 普通 delegate 关键区别**(面试常考):

| 维度 | `Action<T> field` | `event Action<T>` |
|---|---|---|
| 外部 +=/-= | ✅ 可以 | ✅ 可以 |
| 外部直接 Invoke | ✅ 可以 | ❌ **禁止** |
| 外部 = 赋值 | ✅ 可以(覆盖其他订阅者!) | ❌ **禁止** |
| 派生类调用 | ✅ | ❌ 只有声明类可触发 |

> 💡 **event = 受控的委托字段**。让外部能订阅/退订,**但只有类自己能触发**。这是封装的标准做法。

**⑤ Lambda 进阶**(高频用法):

| 用法 | 示例 | 说明 |
|---|---|---|
| 表达式体 | `(int x) => x * 2` | 一行表达式 |
| 语句体 | `(int x) => { return x * 2; }` | 多行 |
| 类型推断 | `(x, y) => x + y` | 编译器推 |
| 丢弃符 | `(_, e) => Handle(e)` | 不用的参数用 `_` |
| 闭包 | `int base = 10; Func<int,int> f = x => x + base;` | 捕获外部变量(注意:by reference) |
| 方法组 | `points.ForEach(Console.WriteLine)` | 直接传方法名 |
| 局部函数 | `int Add(int a, int b) => a+b;` | 方法内定义函数 |

> 🔥 **闭包陷阱**:`for` 循环里 `tasks.Add(Task.Run(() => Console.WriteLine(i)));` — 所有任务打印的是**最后的 i**,因为闭包捕获变量引用。修法:`for` 内 `int local = i;` 拷一份。

**⑥ 常用事件订阅姿势**:
```csharp
// C# 7+ 丢弃符(不用的 sender)
button.Click += (_, _) => MessageBox.Show("点了");

// 异步事件(注意 async void 仅限事件处理器)
button.Click += async (_, _) => {
    button.IsEnabled = false;
    try { await LongRunAsync(); }
    finally { button.IsEnabled = true; }
};
```

> 🎤 **面试加分点**:被问"event 和普通 delegate 区别",标准答:"**event 是受封装的委托** — 外部只能 +=/-= 订阅,不能直接 Invoke 也不能用 = 覆盖。这是 .NET 事件的标准模式,防止外部误触发或清空订阅者。"

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ 事件为什么解耦 | 设备不认识 UI，只管"喊一声"(`Invoke`)，谁订阅谁收到 |
| 🔥 忘了 -= 退订 | 对象销毁前不 `-=`，会导致**内存泄漏**（企业级必查项） |
| 🔥 `?.Invoke` 判空 | 没人订阅时事件为 null，直接 `Invoke` 会崩 |
| 🔥 跨线程触发事件改 UI | 后台线程触发的事件里**不能直接改 UI**，要 `Dispatcher.Invoke`（见 Day 7） |

### 🟢 基础题
给 `Thermometer` 加 `event EventHandler<DataEventArgs> ValueChanged`，在 `Temperature` 的 set 里触发。

### 🟡 进阶题
写两个订阅者 A、B 都订阅同一事件，验证"一发多收"（发布订阅本质）。

### 🔴 挑战题
写一个 `EventAggregator`（事件总线）：设备发事件 → 总线 → 多个消费者（UI + 报警引擎 + 日志）各自响应，体现"一对多解耦"（M6 报警引擎就靠这个）。

**✅ 答案**
```csharp
public class Thermometer
{
    private double _t;
    public event EventHandler<DataEventArgs>? ValueChanged;
    public int Id { get; }
    public Thermometer(int id) => Id = id;
    public double Temperature
    {
        get => _t;
        set { _t = value; ValueChanged?.Invoke(this, new DataEventArgs { PointId = Id, Value = value }); }
    }
}
```

> 📦 **前置准备**:需要 `IDevice` 接口 + `DeviceBase` 类已存在(Day 5 项目任务)。**还没建 Core 工程?** 跳到 [Day 2 开头「📦 立项目骨架」](#-立项目骨架本-day-项目任务的前提必做)。

**🏗️ 项目任务**：在 Core 的 `IDevice`/`DeviceBase` 加 `event EventHandler<DataEventArgs> DataReceived` + `RaiseData`（工程里已真实存在）。让"采集→通知 UI"的架构闭环，这是 Day 7 + 后续实时刷新的地基。

**🎓 工控导师说**：现场一个经典泄漏——窗口关了但 `device.DataReceived += Win_OnData` 没 `-=`，窗口对象被事件引用着回收不掉，跑一周内存涨到爆。记住：**订阅和退订成对出现，像开灯关灯**。

**💼 职业建议**：事件/委托是 WPF/MVVM 的命根子。面试能讲清"事件 vs 委托区别 + 内存泄漏怎么避免"，直接证明你写过真实项目。

**✅ 打卡[ ]**

---

## Day 7 — 多线程 / async-await / 锁 🔴（重头戏 ⭐⭐⭐）

**📌 技术来源**：🟦 `async`/`await`/`lock`/`Task`/`Thread` 语法 + 🟩 BCL（`Task`/`Thread`/`ConcurrentQueue`/`SemaphoreSlim`）

### 一句话讲清楚
多线程 = 多个工人同时干活，锁 = 抢厕所时排队（同一时刻只放一个人进去改数据）。**前端 JS 是单线程没这问题，C# 是真多线程——两个线程同时改一个变量，数据就乱了，这就是锁存在的意义。**

### 前端类比秒懂
| C# 概念 | 前端类比 | 说明 |
|---|---|---|
| `Thread` | `Web Worker` | 最底层的新线程 |
| `Task` | `Promise` | 更高级的异步，**推荐用这个** |
| `async/await` | `async/await` | 几乎一样，但 C# 有坑（见下） |
| `lock` | 前端没有！ | 保护共享数据不被同时改 |
| `ConcurrentQueue` | 前端没有！ | 自带锁的线程安全队列 |
| `SemaphoreSlim` | 前端没有！ | 更高级的限流锁 |

> ⚠️ **核心区别**：前端单线程，异步只是"排队等结果"，不会真并行改数据；C# 是真并行，多个线程能同时执行、同时读写内存，所以需要锁。

### 分点精讲
**① Thread —— 最基础的多线程**：🟩
```csharp
Thread t = new Thread(() =>
{
    Console.WriteLine($"后台线程 Id={Thread.CurrentThread.ManagedThreadId}");
});
t.IsBackground = true;   // 后台线程：主程序退出它也退出
t.Start();
// ⚠️ 输出顺序不固定！
```

**② Task —— 现代 C# 推荐（≈ Promise）**：🟩
```csharp
Task<int> calc = Task.Run(() => { Thread.Sleep(500); return 42; });
int result = await calc;   // ✅ 用 await 拿结果
// int r2 = calc.Result;   // ❌ .Result 会阻塞线程，UI 里用会死锁！
```

**③ async/await —— 最像前端的写法**：🟦
```csharp
public async Task<string> GetDataAsync()
{
    string data = await Task.Run(() => { Thread.Sleep(2000); return "数据已就绪"; });
    return data;
}
private async void BtnStart_Click(object sender, RoutedEventArgs e)
{
    StatusText.Text = "加载中...";
    string data = await GetDataAsync();  // UI 不卡顿！
    StatusText.Text = data;
}
```
> ⚠️ `async void` **只能用在事件处理器**。其他任何地方一律 `async Task`，否则异常会被吞掉、程序静默崩溃。

**④ ⚠️ 为什么需要锁 —— 经典竞态 Bug**：🟦
```csharp
private int _count = 0;
for (int i = 0; i < 10000; i++)
    Task.Run(() => _count++);   // ❌ 结果可能是 8234、9567... 随机
```
**为什么？** `_count++` 分三步：①读值→②+1→③写回。两个线程同时读到 5，各自 +1 都写回 6，就丢了一次 +1。
```csharp
private int _count = 0;
private readonly object _lock = new object();
for (int i = 0; i < 10000; i++)
    Task.Run(() => { lock (_lock) { _count++; } });  // ✅ 一定是 10000
```

**⑤ 生产者-消费者（上位机最高频场景）**：🟩
> 串口每秒收 100 条（生产者），UI 慢慢刷新（消费者），中间用**线程安全队列**缓冲。
```csharp
private ConcurrentQueue<SensorPoint> _queue = new();
private void OnDataReceived(SensorPoint p) => _queue.Enqueue(p);   // 线程安全
private void UiTimer_Tick(object? s, EventArgs e)
{
    while (_queue.TryDequeue(out var p))
    {
        DataGrid.Items.Add(p);
        if (DataGrid.Items.Count > 200) DataGrid.Items.RemoveAt(0);  // 防内存爆
    }
}
```

### 🔧 异步与并发工具箱速查(高频)

> Day 7 介绍了 Thread/Task/async/lock 概念,**这里把"上位机天天用的并发 API 全列出来"**。前端类比都用 Promise/Worker。

**① Task 方法家族(`System.Threading.Tasks`,异步核心)**:

| 方法 | 前端类比 | 用途 | 示例 |
|---|---|---|---|
| `Task.Run(fn)` | `new Worker` | **后台线程跑任务** | `await Task.Run(() => Heavy());` |
| `Task.Delay(ms)` | `await sleep(ms)` | **异步等待**(不阻塞线程) | `await Task.Delay(1000);` |
| `Task.WhenAll(tasks)` | `Promise.all` | **等所有完成**(并发 N 个) | `await Task.WhenAll(list);` |
| `Task.WhenAny(tasks)` | `Promise.race` | **等任一完成**(超时赛跑) | `var win = await Task.WhenAny(t1, t2);` |
| `Task.FromResult(x)` | `Promise.resolve` | 同步值包装成 Task | `return Task.FromResult(42);` |
| `Task.FromException(ex)` | `Promise.reject` | 异常包装 | 老接口兼容 |
| `Task.CompletedTask` | `Promise.resolve()` | 已完成的 Task(占位) | `return Task.CompletedTask;` |
| `Task.Yield()` | `await null`(让出) | 让出调度(罕见) | 主动让 UI 线程喘气 |
| `task.Wait()` / `task.Result` | — | **同步阻塞**(UI 死锁!) | 仅 Console 程序用 |

```csharp
// 经典:并发请求 10 台设备,全部完成才返回(不用 10 个 await 串行)
var tasks = devices.Select(d => d.PollAsync());
var results = await Task.WhenAll(tasks);   // 10 台并行,总耗时 ≈ 最慢那台

// 经典:超时赛跑
var winTask = await Task.WhenAny(
    device.ConnectAsync(),
    Task.Delay(5000)
);
if (winTask != connectTask) throw new TimeoutException("连接超时 5s");
```

**② CancellationToken 4 件套(取消异步任务必备)**:

| 角色 | 类/方法 | 用途 |
|---|---|---|
| **源** | `CancellationTokenSource cts = new();` | 创建令牌源(谁想取消谁持有) |
| **令牌** | `CancellationToken ct = cts.Token;` | 传递给被取消方法(到处传) |
| **触发** | `cts.Cancel();` | 通知所有 Token 持有者取消 |
| **检查** | `ct.ThrowIfCancellationRequested();` | 在循环里检查,抛 `OperationCanceledException` |
| **定时取消** | `cts.CancelAfter(5000);` | 5 秒后自动取消(超时神器) |
| **联动** | `CancellationTokenSource.CreateLinkedTokenSource(a, b);` | 任一源取消则联动取消 |
| **回调** | `ct.Register(() => cleanup());` | 取消时执行回调(释放资源) |

```csharp
public async Task PollAsync(CancellationToken ct)
{
    while (!ct.IsCancellationRequested)
    {
        var data = await device.ReadAsync(ct);   // 把 token 往下传
        Process(data);
        await Task.Delay(1000, ct);              // 延迟也可取消
        ct.ThrowIfCancellationRequested();       // 主动检查点(长循环必加)
    }
}

// 调用方
using var cts = new CancellationTokenSource();
cts.CancelAfter(30000);   // 30 秒兜底
try { await engine.PollAsync(cts.Token); }
catch (OperationCanceledException) { Console.WriteLine("已取消"); }
```

> ⭐ **现代 C# 规则:所有异步方法签名最后必须带 `CancellationToken ct = default`** — 这是企业代码的标准。EF Core、HttpClient 全部遵循。

**③ 同步原语(锁,保护共享数据)**:

| 工具 | 用途 | 示例 |
|---|---|---|
| `lock(obj)` | 互斥锁(最常用) | `lock(_lock){ _count++; }` |
| `Monitor.TryEnter(obj, ms)` | 超时版 lock | 防止死锁(返回 bool) |
| `SemaphoreSlim(initial, max)` | **信号量限流** | 限制并发数(数据库连接池) |
| `Semaphore` | 跨进程信号量 | 罕见 |
| `Mutex` | 跨进程互斥 | 单实例程序检测 |
| `Interlocked.Increment(ref x)` | **原子自增**(无锁) | 替代 `lock(x){x++}` |
| `Interlocked.Exchange(ref a, b)` | 原子赋值 | 无锁交换 |
| `Interlocked.CompareExchange(ref a, n, c)` | CAS 原子操作 | 无锁队列底层 |
| `ReaderWriterLockSlim` | 读写锁 | 读多写少(配置缓存) |
| `volatile` 关键字 | 禁止编译器缓存字段 | 字段级内存可见性 |

```csharp
// lock 标准模式(私有只读锁对象)
private readonly object _lock = new();
public void Add(SensorPoint p)
{
    lock (_lock)   // 进门拿钥匙,出门还
    {
        _points.Add(p);
    }
}

// SemaphoreSlim 异步限流(数据库并发不超过 10)
private readonly SemaphoreSlim _gate = new(10, 10);
public async Task SaveAsync(SensorPoint p)
{
    await _gate.WaitAsync();    // 异步等钥匙(不阻塞线程)
    try { await db.AddAsync(p); }
    finally { _gate.Release(); }   // 必须还!
}

// Interlocked 无锁(高性能场景)
private int _counter = 0;
public void Hit() => Interlocked.Increment(ref _counter);   // 比 lock 快 10x
```

**④ 线程安全集合(`System.Collections.Concurrent`)**:

| 类 | 前端类比 | 用途 |
|---|---|---|
| `ConcurrentDictionary<K,V>` | — | 线程安全 Dictionary(配置缓存) |
| `ConcurrentQueue<T>` | — | 线程安全 FIFO(无锁) |
| `ConcurrentStack<T>` | — | 线程安全 LIFO |
| `ConcurrentBag<T>` | — | 无序并发集合 |
| `BlockingCollection<T>` | — | **阻塞**版本(生产-消费者,旧式) |

```csharp
// ConcurrentDictionary 原子操作
var stats = new ConcurrentDictionary<string, int>();
stats.AddOrUpdate("hit", 1, (k, v) => v + 1);   // 不存在加 1,存在则 +1
stats.GetOrAdd("init", _ => 0);                 // 不存在初始化
```

> ⭐ **现代 C# 推荐用 `Channel<T>`**(Day 7 末尾详)替代 `BlockingCollection` — 异步、零分配、性能更好。DAQMonitor 就是这么做的。

**⑤ WPF Dispatcher(跨线程改 UI 必备)**:

| 方法 | 用途 | 示例 |
|---|---|---|
| `Application.Current.Dispatcher.CheckAccess()` | 当前是否 UI 线程 | 防御检查 |
| `Dispatcher.Invoke(action)` | **同步**切到 UI 线程(阻塞调用) | `Dispatcher.Invoke(() => txt.Text = "x");` |
| `Dispatcher.BeginInvoke(action)` | **异步**切到 UI 线程(立即返回) | 后台循环刷新 UI |
| `Dispatcher.InvokeAsync(action)` | 异步(可 await) | 现代推荐 |

```csharp
// 后台线程安全更新 UI 的标准姿势
private void OnDataReceived(SensorPoint p)
{
    if (!Application.Current.Dispatcher.CheckAccess())
    {
        Application.Current.Dispatcher.BeginInvoke(() => OnDataReceived(p));
        return;
    }
    // 这里保证是 UI 线程
    StatusText.Text = $"最新值:{p.Value:F2}";
}
```

**⑥ Thread 类基础(老代码常见,新代码用 Task)**:

| 属性/方法 | 用途 |
|---|---|
| `Thread.CurrentThread.ManagedThreadId` | 当前线程 ID(调试用) |
| `Thread.Sleep(ms)` | **同步**休眠(阻塞线程,别在 UI 用) |
| `Thread.Yield()` | 让出 CPU 时间片 |
| `Thread t = new(fn); t.IsBackground = true; t.Start();` | 启动后台线程 |
| `Thread.VolatileRead(ref x)` / `Volatile.Write` | 内存可见性 |

> 💡 **Thread vs Task 怎么选**:99% 用 Task(线程池调度、async 友好);只有"需要长生命周期线程"或"设置 COM 单线程单元(STA)"才用 Thread(WPF UI 线程本身是 STA Thread)。

**⑦ 上位机并发常用模式速查**:

| 场景 | 标准做法 |
|---|---|
| 后台采集不卡 UI | `Task.Run` + `Channel<T>` + Dispatcher.BeginInvoke 刷新 |
| 限制并发数据库写入 | `SemaphoreSlim(10)` 异步限流 |
| 全局计数(请求数/报警数) | `Interlocked.Increment` 无锁 |
| 共享配置缓存 | `ConcurrentDictionary` 或 `lock` + 普通 Dictionary |
| 取消长任务 | `CancellationTokenSource` + `ct.ThrowIfCancellationRequested` |
| 超时控制 | `Task.WhenAny(task, Task.Delay(ms))` 或 `cts.CancelAfter` |
| 批量并发请求 | `Task.WhenAll(tasks)` |
| 跨线程更新 UI | `Dispatcher.BeginInvoke` 或 `IProgress<T>` |
| 进度上报 | `IProgress<T>` + `Progress<T>`(自动切回 UI 线程) |

```csharp
// IProgress<T> 进度上报(自动 Dispatcher)
public async Task ProcessAsync(IProgress<int> progress, CancellationToken ct)
{
    for (int i = 0; i < 100; i++)
    {
        ct.ThrowIfCancellationRequested();
        await Task.Delay(50, ct);
        progress.Report(i);   // 调用方 new Progress<int>(v => ProgressBar.Value = v)
    }
}
```

> 🎤 **面试加分点**:被问"为什么用 Channel 不用 ConcurrentQueue",标准答:"**Channel 是 async-native** — `await reader.ReadAsync()` 在没数据时不占用线程,而 ConcurrentQueue 只能 `TryDequeue` 轮询(浪费 CPU)或 `Sleep` 阻塞(浪费线程)。在高频采集场景,Channel 把单核 CPU 占用从 60% 降到 5% — 这是 13K vs 8K 的真实差距。"

### ⚠️ 前端转 C# 多线程四大坑
| 坑 | 说明 | 解决 |
|---|---|---|
| 跨线程改 UI | 后台线程直接改 `TextBlock.Text` 会抛异常 | WPF：`Dispatcher.Invoke(() => txt.Text = "x")` |
| 死锁 | 两线程互等对方的锁 / `await` 里用 `.Result` | 锁顺序一致；UI 里永远 `await` |
| `async void` 吞异常 | 异常静默丢失、程序莫名崩 | 只在事件处理器用 `async void`，其他用 `async Task` |
| 竞态条件 | 多线程同时读写同一变量 | `lock` 或用 `ConcurrentQueue`/`Interlocked` |

### 🎭 拟人秒懂:Dispatcher + .Result 死锁 + Channel 队列(多线程记忆锚点)

> **Dispatcher = UI 大门的保安**(WPF/WinForms 都有)

- UI 控件(TextBlock/DataGrid)只能被**有 UI 线程身份证的人**改 — 这是 WPF 强制规定
- 后台线程要改 UI?**到大门找保安**:`Dispatcher.Invoke(() => txt.Text = "x")` — 保安替你进去改
- **前端类比**:跟 React 必须用 `setState` 改 state 一个道理 — 你不能直接 `state.x = 1`,要走官方通道

> **`.Result` 死锁 = 你和保安互等**

- 场景:UI 线程上调 `var x = task.Result` — **阻塞 UI 线程等结果**
- task 内部 `await` 后,想回到 UI 线程继续执行(因为 await 后默认回到原 SynchronizationContext)
- 但 UI 线程被 `.Result` 卡死了 → task 等不到 UI 线程 → 结果出不来 → `.Result` 永远等
- **两边互等 = 死锁**
- **正确做法**:UI 线程**永远 `await`,不用 `.Result` / `.Wait()`**
- **后台线程可以 `.Result`**:因为后台线程没 SynchronizationContext,task 完成后任意线程跑都行,不互等

> **Channel<T> = 高效的取号餐厅**

- 老版 ConcurrentQueue:消费者要 `while (true) { if (q.TryDequeue(...)) {...} else Thread.Sleep(10); }` — 像服务员每 10ms 跑到厨房问"菜好没"
- Channel<T>:消费者 `await foreach (var p in reader.ReadAllAsync())` — 没菜时**自动挂起**,菜来了立刻唤醒
- **CPU 占用从 30% 降到 2%**,延迟从 50ms 降到 5ms
- **前端类比**:跟 `for await (const x of stream)` 一样 — 异步迭代器,无数据时不占 CPU

**DAQMonitor 实战话术**:
> "我用 `System.Threading.Channels` 做采集到落库的异步管线:设备的 DataReceived 事件 → `_channel.Writer.TryWrite(p)`(生产者),后台 consumer `await foreach` 消费 + 200ms 批量触发 BatchReady 事件。无锁、自动背压、CPU 0 浪费。重构后 CPU 从 30% 降到 2%,延迟从 50ms 降到 5ms。"

**多线程三件套口诀**:
- **简单计数** → `Interlocked.Increment(ref x)`
- **共享状态** → `lock (_gate)`
- **生产消费** → `Channel<T>`(异步)或 `ConcurrentQueue`(同步轮询)

### 🟢 基础题
写一个 `Task.Run(() => Console.WriteLine("后台"))`，主线程等它完成（`await`）。

### 🟡 进阶题
用 `lock` 保护一个计数器，1000 个并行 `++` 后验证结果等于 1000。

### 🔴 挑战题
写一个**数据采集模拟器**（WPF）：后台线程每 100ms 生成一条（温度+压力+时间戳）→ `ConcurrentQueue` 缓冲 → `DispatcherTimer` 每 500ms 取数显示到 `DataGrid` → 数据超 200 条自动删最旧 → 实时显示队列积压条数。要求必须用 `Task.Run` + `ConcurrentQueue` + `Dispatcher.Invoke` 更新 UI。

**✅ 答案（挑战题核心骨架）**

> 📂 `DaqMonitor.UI/MainWindow.xaml.cs` · namespace `DaqMonitor.UI`(默认)
> 🔧 无 NuGet(WPF 自带 DispatcherTimer / ConcurrentQueue)
> 💡 完整可跑版本,含 XAML(下一节) + 后台代码(本节)
> ⚠️ **不要漏 XAML**:WPF 是"前台 XAML + 后台 .cs"双文件,光有 .cs 编译过但跑不起来(找不到 DataGrid/按钮)。完整 XAML 在下方。

**后台代码** `MainWindow.xaml.cs`:
```csharp
using System;
using System.Collections.Concurrent;
using System.Threading;
using System.Threading.Tasks;
using System.Windows;
using System.Windows.Controls;
using System.Windows.Threading;

namespace DaqMonitor.UI;

public partial class MainWindow : Window
{
    private readonly ConcurrentQueue<Reading> _queue = new();
    private readonly DispatcherTimer _uiTimer = new() { Interval = TimeSpan.FromMilliseconds(500) };
    private CancellationTokenSource? _cts;

    public record Reading(double Temp, double Pressure, DateTime Time);

    public MainWindow()
    {
        InitializeComponent();
        _uiTimer.Tick += UiTimer_Tick;
    }

    private void BtnStart_Click(object sender, RoutedEventArgs e)
    {
        _cts = new CancellationTokenSource();
        var token = _cts.Token;
        Task.Run(async () =>
        {
            var rnd = new Random();
            while (!token.IsCancellationRequested)
            {
                _queue.Enqueue(new Reading(20 + rnd.NextDouble() * 10, 100 + rnd.NextDouble() * 5, DateTime.Now));
                await Task.Delay(100, token);
            }
        }, token);
        _uiTimer.Start();
    }
    private void UiTimer_Tick(object? s, EventArgs e)
    {
        while (_queue.TryDequeue(out var r))
        {
            DataGrid.Items.Add(r);
            if (DataGrid.Items.Count > 200) DataGrid.Items.RemoveAt(0);
        }
        BacklogText.Text = $"积压：{_queue.Count} 条";
    }
    private void BtnStop_Click(object sender, RoutedEventArgs e) { _cts?.Cancel(); _uiTimer.Stop(); }
}
```

**前台 XAML** `MainWindow.xaml`(必须有,不然后台代码找不到 `DataGrid` / `BtnStart` / `BtnStop` / `BacklogText`):
```xml
<Window x:Class="DaqMonitor.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DAQ Monitor · Day 7 挑战题" Height="450" Width="800">
    <DockPanel Margin="8">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Name="BtnStart" Content="启动采集" Click="BtnStart_Click" Padding="12,4" Margin="0,0,8,0"/>
            <Button Name="BtnStop"  Content="停止"     Click="BtnStop_Click"  Padding="12,4" Margin="0,0,8,0"/>
            <TextBlock Name="BacklogText" VerticalAlignment="Center" Text="积压：0 条"/>
        </StackPanel>
        <DataGrid Name="DataGrid" AutoGenerateColumns="True" IsReadOnly="True"/>
    </DockPanel>
</Window>
```

**自测**:跑 `dotnet run --project src/DaqMonitor.UI`,点"启动采集",应看到表格每 500ms 跳出新行,积压数实时变化。点"停止"应停止采集。

### 🟢 知识点 N：System.Threading.Channels（现代化生产者-消费者，DAQMonitor 就用它）

**1. 一句话讲清楚**
> `Channel<T>` 是 .NET Core 2.1+ 的"高性能异步队列"，**前端类比：像 RxJS Subject，但更轻量；或像 Node.js Stream**。生产者 `Write`、消费者 `ReadAllAsync`，自动异步、自动背压、无锁。

**2. 前端类比秒懂表**

| `Channel<T>` | 前端类比 | 说明 |
|---|---|---|
| `Channel.Writer.WriteAsync(x)` | `subject.next(x)` / `writable.write(x)` | 生产 |
| `Channel.Reader.ReadAllAsync()` | `for await (const x of stream)` | 消费（异步流） |
| `Channel.CreateBounded(1000)` | 背压 / 高水位 | 容量上限，满了 `WriteAsync` 阻塞 |
| `Channel.CreateUnbounded()` | 无界队列 | 永不阻塞，但 OOM 风险 |
| `Writer.Complete()` | `writable.end()` | 标记生产结束 |

**3. 为什么要用 Channel 而不是 ConcurrentQueue**
- `ConcurrentQueue`：`TryDequeue` 是**同步**的，消费者要轮询 `while (true)` + `Thread.Sleep` —— 浪费 CPU
- `Channel`：`ReadAllAsync` 是异步的，没数据时**自动挂起**（不耗 CPU），数据来了立刻唤醒
- 性能：Channel 内部用 `ValueTask`、无锁 SPSC 队列，比 `ConcurrentQueue` 快 3-5 倍

**4. 最小可用代码（可读可跑）**
```csharp
using System.Threading.Channels;

var channel = Channel.CreateBounded<SensorPoint>(1000);

// 生产者（采集线程）
async Task ProduceAsync(CancellationToken ct)
{
    while (!ct.IsCancellationRequested)
    {
        var p = ReadFromDevice();
        await channel.Writer.WriteAsync(p, ct);   // 队列满时自动等待（背压）
    }
}

// 消费者（处理/落库线程）
async Task ConsumeAsync(CancellationToken ct)
{
    await foreach (var p in channel.Reader.ReadAllAsync(ct))
    {
        SaveToDatabase(p);
    }
}

// 启动
var cts = new CancellationTokenSource();
var producer = Task.Run(() => ProduceAsync(cts.Token));
var consumer = Task.Run(() => ConsumeAsync(cts.Token));

// 关闭时
// cts.Cancel();
// channel.Writer.Complete();   // 告诉消费者没数据了
```

**5. 🧪 运行结果 / 实际怎么用**
说明在 DAQMonitor 项目 `AcquisitionPipeline.cs` 里：
- 用了 `Channel.CreateUnbounded<SensorPoint>()`（无界，因为采集速率可控）
- 生产者：每个设备的 `DataReceived` 事件 → `_channel.Writer.TryWrite(p)`
- 消费者：后台任务 `_consumer = Task.Run(async () => await foreach (var p in _channel.Reader.ReadAllAsync()) ...`
- 200ms 定时器触发 `BatchReady` 事件，批量消费（比逐条省 IO）
- 详见 [M9 工程素养](M9_工程素养_测试DI容错_深度版.md) 和 `DAQMonitor/src/DaqMonitor.Core/AcquisitionPipeline.cs`

**6. ⚠️ 常见坑点预警表**
| 坑 | 说明 |
|---|---|
| 无界 Channel 内存炸 | 生产远快于消费时，`_channel` 内部 List 无限增长 → OOM。**生产环境推荐有界** + `BoundedChannelFullMode.DropOldest` 或 `Wait` |
| 忘 `Writer.Complete()` | 消费者 `ReadAllAsync` 永远不结束，程序关不掉 |
| 忘 `CancellationToken` | 关闭时 `WriteAsync` 卡住，程序挂 |
| `async void` 事件订阅 | 不能在 event handler 里直接 `await channel.Writer.WriteAsync()`，见 [C# 陷阱讲义 陷阱 4](C#_陷阱_前端转上位机必看_深度版.md) |
| 单读单写最快 | Channel 单生产者单消费者（SPSC）性能最高，多生产者多消费者（MPMC）也能用但稍慢 |

**7. 🧪 三档练习**
- 🟢 **基础**：用 Channel 写一个 "1 生产者 1 消费者"，生产 0-9 数字，消费打印平方。
  **✅ 答案**：见上方代码，生产 `Enumerable.Range(0,10)`，消费 `Console.WriteLine(p*p)`。
- 🟡 **进阶**：改成有界（容量 3），观察生产者会等待（背压）。
- 🔴 **挑战**：DAQMonitor `AcquisitionPipeline` 的 `BatchReady` 机制是怎么用 Channel 实现的？为什么不用 ConcurrentQueue？
  **✅ 答案**：Channel 消费者 `await foreach` 没数据时挂起不耗 CPU；200ms 定时器触达时把攒的批量打包触发 `BatchReady` 事件；用 `ConcurrentQueue` 的话消费者要 `while(true){ if(q.TryDequeue(out var p)) {...} else Thread.Sleep(10); }` 浪费 CPU 且延迟高。

**8. 💡 工控导师说（真实战例）**
> 我重构过一个用 `ConcurrentQueue` + `Thread.Sleep` 轮询的采集系统，改 `Channel<T>` 后 **CPU 从 30% 降到 2%**（消费者不再空转），延迟从 50ms 降到 5ms（数据来立刻唤醒）。前端转上位机，**Channel 是必学**，DAQMonitor 项目就是真用了，面试时讲"我用 `System.Threading.Channels` 做采集到落库的异步管线，无锁、自动背压、CPU 0 浪费"，面试官秒信你不是写玩具的。

**9. 📚 延伸阅读**
- Microsoft 官方 Channel 文档：https://learn.microsoft.com/dotnet/api/system.threading.channels
- Stephen Toub《An Introduction to System.Threading.Channels》：https://devblogs.microsoft.com/dotnet/an-introduction-to-system-threading-channels/

---

📌 **跨模块联动**：本节 `Channel<T>` 是 [M9 工程素养 AcquisitionPipeline](M9_工程素养_测试DI容错_深度版.md) 的核心，DAQMonitor 项目实际用的就是它；[C# 陷阱讲义 陷阱 2 多线程](C#_陷阱_前端转上位机必看_深度版.md) 把 Channel vs lock vs ConcurrentQueue 三件套做了对比。

---

> 📦 **前置准备**:需要 `IDevice`/`DeviceBase`/`DataReceived` 事件已就位(Day 5/6 项目任务)。**还没建 Core 工程?** 跳到 [Day 2 开头「📦 立项目骨架」](#-立项目骨架本-day-项目任务的前提必做)。

**🏗️ 项目任务**：给 DAQ Monitor 加"模拟采集"后台线程 + `ConcurrentQueue` 缓冲 + `DispatcherTimer` 刷新（工程里 `AcquisitionPipeline` 已是这套架构的生产版）。把上面骨架接到项目里，形成真实的"后台采集→前台展示"并发闭环——这是 M6 实时监控的核心地基。

**🎓 工控导师说**：我见过最惨的多线程 bug——界面数字偶尔跳回旧值。查了三天发现：两个线程一个在 `Task.Run` 里直接改 UI 的 `TextBlock`，另一个在 UI 线程也改，两边打架。铁律：**后台线程改 UI 一律 `Dispatcher.Invoke`**。

**💼 职业建议**：多线程是 13K+ 必考，且是前端转岗最大鸿沟。面试准备三句话：①"C# 真并行，前端单线程"；②"共享状态用 lock 或 ConcurrentQueue"；③"UI 线程改界面用 Dispatcher"。这三点讲清，面试官就信你真写过并发程序。

**✅ 打卡[ ]**

---

## Day 8 — 周复习 + 立起 WPF 工程 🟡

**📌 技术来源**：🟦 前 7 天综合 + 🟩 **WPF**（`<UseWPF>true</UseWPF>`，随 .NET，非 NuGet）

### 复习地图（一条主线串起来）
变量/类型(Day1-2) → 集合/LINQ 处理数据(Day3) → 类/属性封装设备(Day4) → 接口/泛型架构(Day5) → 事件解耦通知(Day6) → 多线程/异步/锁并发(Day7)。这条线后面贯穿 M1~M16。

### ⭐ 自测小测（答案见各 Day）
1. `int?` 和 `int` 区别？
2. LINQ 延迟执行是什么意思？为什么有时要 `.ToList()`？
3. 接口 vs 抽象类怎么选？
4. 事件订阅为什么要 `-=`？
5. `_count++` 多线程下为什么会丢数据？怎么修？
6. 后台线程能直接改 WPF 界面吗？不能的话怎么办？
7. WPF 用 NuGet 装吗？（答：不，`<UseWPF>true</UseWPF>`，属 🟩 生态）

### 立起真实工程
```bash
dotnet new sln -n DaqMonitor
dotnet new classlib -n DaqMonitor.Core
dotnet new wpf -n DaqMonitor.UI
dotnet sln add src/DaqMonitor.Core src/DaqMonitor.UI
dotnet add src/DaqMonitor.UI reference src/DaqMonitor.Core
```
- MainWindow 放"启动采集"按钮 + `DataGrid` + 状态栏；
- 把 Day2-7 写的 Core 类型（`SensorPoint`/`DeviceState`/`Alarm`/`IDevice`/`DeviceBase`/`PointStore`/事件）搬进 `DaqMonitor.Core`；
- 接入 Day 7 的"模拟采集+队列+定时刷新"，点按钮就能看到数据实时滚动。
- 工程骨架即成型，`git commit` 一次，作品集第一版诞生。

### 🟢 基础题
按上面命令建好工程，跑 `dotnet build` 确认 0 错误。

### 🟡 进阶题
把 Day 3 的 `PointStore` 接到工程，让模拟采集每产生一个点就 `AddOrUpdate`，`DataGrid` 显示最新 200 条。

### 🔴 挑战题
用 Day 5 的 `IDevice` 写一个 `SimulatedDevice : DeviceBase`（工程里 `DaqMonitor.Core/Devices/SimulatedDevice.cs` 已是真实实现），在 `Connect()` 后启动后台线程每 100ms `RaiseData`，验证"接口多态 + 事件通知 + 并发"三件事在真实项目里跑通。

**🏗️ 项目任务**：完成上面三步，`DaqMonitor` 工程骨架即成型。后续 M1~M16 往里加"串口/Modbus/PLC/存储/图表/报警/上云"，最终 = 能拿去面试的工业数据采集监控系统。

**🎓 工控导师说**：很多学员 Day 8 想一步到位把整个系统写完，结果卡在 WPF 绑定上三天没动。记住：**先把"按钮+DataGrid+模拟数据滚动"这个最小闭环跑起来**，后面每加一层都能立刻看到效果——这是项目驱动学习的节奏感。

**💼 职业建议**：面试官问"你的项目怎么搭的"，你要能一句话讲清分层："Core 领域层（IDevice/PointStore/并发管道）+ UI（WPF/MVVM）+ Tests（28 测试全绿），换设备 UI 零改动"。这段话背熟，是 13-15K 的入场券。

**✅ 打卡[ ]**

---

## 附录：M0 交付物 & 埋下的 5 个企业级地基
- **本地工程**：`DAQMonitor/src` → `DaqMonitor.Core`（领域模型+设备接口+点位存储）+ `DaqMonitor.UI`（WPF）。
- 从 Day 8 起它就是**真实项目**，后续模块往里加"串口/Modbus/PLC/存储/图表"，最终 = 能拿去面试的工业数据采集监控系统（13K~15K 作品集）。
- 本模块已提前埋下 **5 个企业级地基**：
  1. **分层**（Core/UI 分离）
  2. **接口解耦**（IDevice，UI 不认识具体设备）
  3. **事件通知**（DataReceived，采集→UI 单向推送）
  4. **双索引存储**（PointStore：List 遍历 + Dictionary 快查）
  5. **并发模型**（后台采集 + ConcurrentQueue + DispatcherTimer，真实工业架构）

## 🧩 完整代码组装（M0 全模块串联）
学完 M0，你家 `DaqMonitor.Core` 里应有这些真实文件（与工程对齐）：
```
DaqMonitor.Core/
├── Models/SensorPoint.cs        （Day2 struct + DeviceState 枚举）
├── Devices/IDevice.cs            （Day5 接口）
├── Devices/DeviceBase.cs        （Day4/5/6 基类：属性 + 事件 + RaiseData）
├── Store/PointStore.cs          （Day3 双索引存储 + LINQ 报警筛选）
└── Acquisition/AcquisitionPipeline.cs  （Day7 并发采集生产版）
```
> 这些在 `F:\00_project\上位机学习\DAQMonitor\src` 真实存在、可编译、28 测试守护。M0 不是"看完"，是"跑起一个会动的项目"。

## 🔗 明日预告
**M1 串口通信**：用上面立好的 `IDevice`/`DeviceBase`，实现 `SerialDevice`，让设备从"模拟"变"真实串口收发"，并彻底搞懂 CRC 校验（M2 会手算）。

## 📚 延伸阅读（卡点时点开）
- C# 官方文档总入口：https://learn.microsoft.com/dotnet/csharp/
- 异步编程 async/await 官方指南：https://learn.microsoft.com/dotnet/csharp/async —— **必看**
- WPF 官方文档：https://learn.microsoft.com/dotnet/desktop/wpf/
- `Dispatcher` / 线程模型：https://learn.microsoft.com/dotnet/api/system.windows.threading.dispatcher
- 设计模式（接口/策略/观察者）：https://learn.microsoft.com/dotnet/standard/modern-web-apps-azure/architectural-principles
- 全部模块外链汇总见 `外部链接索引.md`
