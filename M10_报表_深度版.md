# M10 — 报表（把 M4+M5+M6 收口成企业交付物）
> **优先级定位**：🔴 必学 · 报表 Excel/PDF（调研 P0 缺口，闭环）

> **给简历加的能力**：历史聚合报表 + 可视化 + Excel 导出 —— 车间主任天天要"昨天的班报""本月能耗"，光有实时曲线和 CSV 原始文件等于把活甩给别人。这是之前岗位调研里发现的**最大缺口**，补齐它，"独立做完整项目"才闭环。
>
> **🟦C#语法(不装包) · 🟩.NET类库BCL(默认不装包) · 🟧第三方NuGet(必须 `dotnet add package`)**
>
> **知识关联（事半功倍的关键）**：
> - M4 把点位存进 `PointStore`/历史库 → **本模块按时间窗聚合**
> - M5 画实时曲线 → **本模块用同样的图表库画"历史趋势报表"**
> - M6 报警引擎 → **报警记录可并入报表统计**
> 也就是说，M10 不是新学一堆东西，而是把前面已学的"收口成一张能交差的表"。

---

> ⏱️ **阅读路径**(按时间预算选入口)
> - **3 分钟**:看「知识关联」— 知道报表 = M4 历史 + M5 图表 + M6 报警 收口成企业交付物
> - **30 分钟**:加看 Day 1 LINQ GroupBy 聚合(按时间/点位维度)
> - **3 小时**:全文精读 + Day 2 ClosedXML 导 Excel + Day 3 报表模板
> - 🎯 **面试高频**:报表维度设计(班次/小时/天)/ **ClosedXML vs EPPlus 选型(License)** / 聚合 SQL vs 内存计算
> - 🔁 **配套复习**:[间隔重复表](记忆与复习机制_间隔重复版.md)

> 📚 **前置语法**(M10 用到的,陌生请查 [C# 语法速查 — 前端视角](CSharp语法速查_前端视角.md))
> - `_points.Where(p => p.Timestamp >= t0 && p.Timestamp < t1).GroupBy(p => p.Id).Select(g => new { Id = g.Key, Max = g.Max(p => p.Value) })` — LINQ 多级聚合,速查 §15
> - `var report = new { Date = today, Lines = new List<ReportLine>() }` — 匿名对象
> - `class ReportLine { public string Tag { get; init; } public double Max { get; init; } public double Avg { get; init; } }` — init-only 属性,速查 §4
> - `using var wb = new XLWorkbook()` — ClosedXML 资源释放,速查 §13
> - `await db.SensorPoints.Where(...).GroupBy(...).Select(...).ToListAsync()` — EF Core 异步聚合,速查 §8/§15

> 📦 **前置类型**(本模块示例代码用到的核心自定义类型)
> M10 示例引用 `SensorPoint` / `SensorRecord` / **`PointStat`**(本模块核心聚合结果)等类型 — 这些在 [📦 前置类型定义 · 学员粘贴版](前置类型定义_学员粘贴版.md) **集中定义**(`PointStat` 在"五、报表统计类型")。**遇到"找不到类型 XXX"报错,先去那份文档复制对应类型**,在项目里建 `_PredefinedTypes.cs` 粘进去就能跑。本模块还会**新建** `ReportService` 和 `ReportExporter`(ClosedXML 导出)。

## Day 1 — 历史聚合查询（LINQ GroupBy）

## Day 1 — 历史聚合查询（LINQ GroupBy）

企业报表的本质：**把海量原始记录，按"维度+时间窗"聚合成少量统计行**。

| 前端/JS 类比 | C# 对应 |
|---|---|
| `SELECT point, MIN(v),MAX(v),AVG(v) FROM t GROUP BY point` | `GroupBy(p => p.Id)` + `Min/Max/Average` |
| `arr.reduce((acc,cur)=>..., {})` | LINQ 链式 `Where → GroupBy → Select` |
| 时间戳过滤 `t >= from && t <= to` | `Where(p => p.Timestamp >= from && p.Timestamp <= to)` |

**① 聚合服务**（🟦 LINQ + 🟩 DateTime，纯计算、可单测）
```csharp
// DaqMonitor.Core/Reporting/ReportService.cs
public sealed class ReportService
{
    public IReadOnlyList<PointStat> Aggregate(IEnumerable<SensorPoint> points, DateTime from, DateTime to)
    {
        return points
            .Where(p => p.Timestamp >= from && p.Timestamp <= to)  // 时间窗过滤
            .GroupBy(p => p.Id)                                    // 按点位分组
            .Select(g => new PointStat(                            // 注意:PointStat record 的字段顺序
                PointId: g.Key,                                    // 参 [前置类型定义](前置类型定义_学员粘贴版.md)
                Count:  g.Count(),
                Min:    g.Min(p => p.Value),
                Max:    g.Max(p => p.Value),
                Avg:    g.Average(p => p.Value),
                First:  g.Min(p => p.Timestamp),
                Last:   g.Max(p => p.Timestamp)))
            .OrderBy(s => s.PointId)
            .ToList();
    }
}
```
> 真实可行性：这段代码已在 `DaqMonitor.Tests/ReportServiceTests.cs` 里用 6 条样例点验证（含窗外点被正确过滤），`dotnet test` 全绿。

### ⭐ 重点 & 🔥 坑
| 项 | 说明 |
|---|---|
| ⭐ 聚合放在 Core 而非 UI | 纯逻辑、无 IO，才能单测；UI 只负责"展示/导出" |
| ⭐ 时间窗边界 | `>= from && <= to` 含端点；跨时区存 `DateTime` 注意 `Kind` |
| 🔥 大数据量 | 真历史库要用 SQL `GROUP BY`（EF `IQueryable`），别把全表拉内存再 LINQ；本演示数据量小，内存聚合 OK |
| 🔥 空窗返回空集合 | 不是 null，UI 绑定 `ItemsSource` 才不崩 |

### ✏️ 练习
1. 给 `ReportService` 加一个 `AggregateByHour(points, from, to)`，按"小时"分桶（提示：`p.Timestamp.Date.AddHours(p.Timestamp.Hour)` 当 key）。
2. 加一条"报警次数"统计：传入 `IEnumerable<AlarmEvent>`，按点位统计触发次数。

### ✅ 答案
```csharp
// 1) 按小时
.GroupBy(p => p.Timestamp.Date.AddHours(p.Timestamp.Hour))
// 2) 报警次数(AlarmEvent 在前置类型定义里)
alarms.GroupBy(a => a.PointId)
      .Select(g => new { PointId = g.Key, Triggers = g.Count() });
```

### 🏗️ 项目任务
把 `ReportService` 接入 DAQ Monitor：在 `Bootstrapper` 注册，主界面"导出报表"时先用它对 `PointStore.GetAll()` 聚合。

---

## Day 2 — 报表可视化（表格 + 历史趋势）

报表不是只导出 Excel，界面里也要能看。复用 M5 的图表库。

| 前端类比 | C# 对应 |
|---|---|
| `<table>` / Ant Design Table | WPF `DataGrid` 绑定 `ObservableCollection<PointStat>` |
| ECharts 折线 | LiveCharts `LineSeries` 绑历史点 |

**② 表格绑定**（🟩 WPF DataGrid，关联 M5）
```xml
<DataGrid ItemsSource="{Binding ReportRows}" AutoGenerateColumns="False">
  <DataGrid.Columns>
    <DataGridTextColumn Header="点位"   Binding="{Binding PointId}"/>
    <DataGridTextColumn Header="样本数" Binding="{Binding Count}"/>
    <DataGridTextColumn Header="最小"   Binding="{Binding Min, StringFormat=F2}"/>
    <DataGridTextColumn Header="最大"   Binding="{Binding Max, StringFormat=F2}"/>
    <DataGridTextColumn Header="平均"   Binding="{Binding Avg, StringFormat=F2}"/>
  </DataGrid.Columns>
</DataGrid>
```
> 历史趋势图：把 `PointStat` 或原始 `SensorPoint` 按时间轴喂给 LiveCharts（同 M5），区别只是数据源从"实时批量"换成"历史查询"。

### ⭐ 重点 & 🔥 坑
| 项 | 说明 |
|---|---|
| ⭐ 报表用"快照数据" | 历史查询结果是固定集合，不用实时 `Dispatcher` 刷新那套 |
| 🔥 精度格式 | `StringFormat=F2` 统一小数位，报表才整齐 |
| 🔥 大数据表格卡顿 | 用 `VirtualizingStackPanel`（WPF 默认开），别一次绑十万行 |

---

## Day 3 — 导出 Excel（ClosedXML）/ PDF（QuestPDF）

企业最终要的是**文件**：班报发邮件、日报交主管、月报归档。

| 前端类比 | C# 对应 |
|---|---|
| SheetJS (`xlsx`) 前端导出 | **ClosedXML** 后端生成 `.xlsx`（无需装 Office） |
| jsPDF | **QuestPDF** 生成 `.pdf`（Fluently 式 API） |

**③ 导出 Excel**（🟧 ClosedXML，`dotnet add package ClosedXML`）
```csharp
// DaqMonitor.UI/Reporting/ReportExporter.cs
public static void ExportToExcel(IEnumerable<PointStat> stats, string path)
{
    using var wb = new XLWorkbook();
    var ws = wb.Worksheets.Add("报表");
    ws.Cell(1,1).Value = "点位"; ws.Cell(1,2).Value = "样本数"; /* …表头… */
    int r = 2;
    foreach (var s in stats)
    {
        ws.Cell(r,1).Value = s.PointId; ws.Cell(r,2).Value = s.Count;
        ws.Cell(r,3).Value = s.Min;      ws.Cell(r,4).Value = s.Max;
        ws.Cell(r,5).Value = s.Avg;      ws.Cell(r,6).Value = s.First;
        ws.Cell(r,7).Value = s.Last;     r++;
    }
    ws.Columns().AdjustToContents();   // 自动列宽
    wb.SaveAs(path);
}
```
> 真实可行性：DAQ Monitor 的 `MainWindow` 已加"导出报表"按钮 + 起止时间选择器，命令里 `new ReportService().Aggregate(...)` → `ReportExporter.ExportToExcel(...)`，UI 构建 **0 错误 0 警告**，点按钮即生成真实 `.xlsx`。

**PDF（🟧 QuestPDF，可选）**：`Document.Create(cfg => cfg.Page(p => p.Content().Table(...)))`，适合要盖章/打印的场景。

### ⭐ 重点 & 🔥 坑
| 项 | 说明 |
|---|---|
| ⭐ ClosedXML 不用装 Office | 纯托管代码生成 xlsx，部署机干净 |
| ⭐ 用 `using` 包 `XLWorkbook` | 不释放会锁文件 |
| 🔥 路径权限 | 导出到用户选的路径（`SaveFileDialog`），别写 `C:\Program Files` |
| 🔥 大数据导出 | 流式写、分批，避免一次性占内存 |

### ✏️ 练习
1. 在 DAQ Monitor 里加一个"导出 PDF"按钮，用 QuestPDF 把同一份 `PointStat` 写成 PDF 报表。
2. 给 Excel 加一行"合计/平均汇总"，放在数据行之后。

### 🏗️ 项目任务
DAQ Monitor 现已具备：启动采集 → 实时表+报警 → 选时间窗 → 一键导出 Excel 报表。**这就是一个完整可交付的企业上位机雏形**。把这句写进你的简历项目描述。

---

## 📌 温故知新（跨模块联动）
- **本模块 = M4(存储) + M5(可视化) + M6(报警) 的收口**：没有前面三块，报表就是无米之炊。
- **聚合逻辑放 Core（M9 工程素养）**：所以它能单测、能被 UI 和测试共用——这正是 M9 讲"分层+DI"的意义。
- **导出是 IO 边界（M0 Day7 并发）**：文件写入放 UI 命令里、用 `SaveFileDialog` 拿到路径，不阻塞采集线程。

## 📄 本模块应产出的文档
- **报表设计文档**：指标定义（点位/维度/时间窗）、字段含义、导出格式（xlsx 列结构）。
- **操作说明**：用户如何选时间窗、点导出、文件存哪。

## 打卡
- [ ] Day 1 历史聚合查询（LINQ GroupBy）
- [ ] Day 2 报表可视化（表格 + 历史趋势）
- [ ] Day 3 导出 Excel（ClosedXML）/ PDF（QuestPDF）
- [ ] 🏗️ DAQ Monitor 导出报表跑通

## 📚 延伸阅读（卡点时点开）
- LINQ GroupBy 官方：https://learn.microsoft.com/dotnet/csharp/linq/ —— 聚合查询
- ClosedXML（Excel）：https://github.com/ClosedXML/ClosedXML · QuestPDF（PDF）：https://github.com/QuestPDF/QuestPDF
- WPF DataGrid 绑定：https://learn.microsoft.com/dotnet/desktop/wpf/controls/datagrid/
- 全部模块外链汇总见 `外部链接索引.md`
