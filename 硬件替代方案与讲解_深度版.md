# 硬件替代方案与实战讲解（深度版）

> **你最关心的两件事，这一篇一次讲清**：
> 1. 上位机到底在跟什么"硬件"打交道？那些 RS232/485、4-20mA、PLC、寄存器到底是啥？（硬件科普）
> 2. 我没有 PLC、没有仪表、没有串口线，**怎么练、怎么证明自己会**？（替代方案）
>
> 学完这篇，你**不需要买任何硬件**就能把 M1–M3、M7 全部练通；真想"真刀真枪"时，也知道花几十块钱买什么。

---

## 一、先搞懂：上位机在跟什么硬件打交道

### 1.1 上位机 / 下位机（一句话 + 类比）

| 词 | 是什么 | 前端类比 |
|---|---|---|
| **上位机** | 你写的 **PC 软件**（DAQ Monitor 这种） | 你写的**前端 + Node 后端**（跑在电脑上，有界面） |
| **下位机** | 现场真正干活的**硬件**：PLC、仪表、传感器、控制器 | 后端连的**数据库 / 第三方 API / 硬件设备** |
| **通信** | 上位机向下位机"读数据 / 下指令" | 前端调后端接口 `fetch('/api/points')` |

> 🔑 **核心心智模型**：上位机 = 一个**会画图、会报警、会存数据库、有按钮**的"高级客户端"；它自己**不产生数据**，数据全靠往下位机要。你 90% 的活儿是：**把下位机的数据搬上来、画出来、存起来、超阈值报警**。

### 1.2 一张表：每个模块对应的"物理硬件"是啥

| 模块 | 物理硬件 | 物理接口 / 信号 | 你实际连的是什么 |
|---|---|---|---|
| M1 串口 | 仪表、传感器、老设备、单片机 | RS232 / RS485（串口线） | 一根 COM 口线，或 USB 转串口 |
| M2 Modbus | 智能电表、温控仪、变频器、PLC（当从站） | RS485（RTU）或 网线（TCP） | 串口线 / 网线 |
| M3 PLC | 西门子 / 欧姆龙 / 三菱 等 PLC | 网线（S7 协议）或 RS485（Modbus） | 网线 / 串口线 |
| M4 数据库 | 本机 SQLite / 企业 SQL Server | 文件 / 网线 | 不用硬件 |
| M5 可视化 | 显示器（你电脑屏幕） | 无 | 不用硬件 |
| M6 报警 | 同上 | 无 | 不用硬件 |
| M7 上云 | OPC UA 服务器 / MQTT Broker | 网线 | 本地装个服务即可 |
| M10 报表 | 打印机 / Excel | 无 | 不用硬件 |

> ⚠️ **重点**：**只有 M1/M2/M3/M7 沾硬件**，而且 M7 的"硬件"只是个**网络服务**（本地就能装）。所以"没硬件"完全不是借口——下面给你四层替代方案，从零成本到真机全覆盖。

---

## 二、硬件基础科普（不懂硬件也能懂，全用类比）

### 2.1 串口：RS232 vs RS485（最容易混的两个）

🟦 C# 语法天生物 · 🟩 .NET 类库（System.IO.Ports）· 🟧 第三方（无，串口是系统自带）

> 串口 = 计算机和外设之间**一根线一根线传字节**的古老但极可靠的通信方式。工业现场 90% 的简单设备都靠它。

| 维度 | **RS232** | **RS485** |
|---|---|---|
| 连接拓扑 | **点对点**（1 对 1，像一根 USB 直连线） | **总线**（1 根线挂 N 个设备，像一根网线接多个设备） |
| 信号方式 | 单端（对地电压 ±12V） | **差分**（A、B 两线电压差，抗干扰强） |
| 距离 | 短（< 15 米） | 远（**1200 米**） |
| 节点数 | 1 对 1 | **32+ 个**设备挂同一条总线 |
| 前端类比 | `localhost` 直连（只有你和我） | 公司内网（一个交换机挂一堆电脑） |

**为什么工业爱 RS485？** 工厂里传感器离电脑几十米、电机干扰大，RS232 早被干扰搞死；RS485 用"差分信号"（A 比 B 高就是 1，低就是 0），电磁噪声同时打到 A 和 B 上会被抵消，**天生抗干扰**。你的 USB 转 485 线（¥20）插电脑上，电脑就多出一个 COM 口，当 RS485 用。

**串口"三件套"参数（双方必须完全一致，否则乱码/连不上）：**
| 参数 | 常见值 | 类比 |
|---|---|---|
| 波特率 (Baud) | 9600 / 19200 / 115200 | **网速**（双方必须同速，否则"鸡同鸭讲"） |
| 数据位 | 通常 8 | 每个"字节"几位 |
| 停止位 | 通常 1 | 帧之间的间隔位 |
| 校验 (Parity) | None / Odd / Even | 简单的"校验和"，防传错 |

> 🔥 **坑**：下位机手册写"9600,8,N,1"就是 波特率9600、8数据位、无校验、1停止位。**你设错一个就全乱码**——这是现场第一高频故障。

### 2.2 传感器信号：设备在"用什么语言"告诉你数值

🟦 概念科普 · 🟩 无需代码 · 🟧 无

真实世界的物理量（温度、压力）要变成上位机能读的数字，中间有"变送"过程。常见信号：

| 信号类型 | 含义 | 前端类比 | 为啥这么设计 |
|---|---|---|---|
| **4–20mA** | 电流环：4mA=量程最低，20mA=最高 | 用**电流大小**当数值 | **抗干扰最强**；断线时电流=0mA，立刻知道"线断了"（不像电压会被干扰） |
| **0–10V** | 电压：0V=最低，10V=最高 | 用**电压高低**当数值 | 简单便宜，但线长易受干扰 |
| **PT100** | 热电阻：0°C 时电阻正好 100Ω，温度变电阻变 | 一个"会随温度变阻值的电阻" | 测温度经典方案，需变送器转成 4–20mA |
| **热电偶** | 两种金属接合产生毫伏级电压（K 型等） | 温差→微小电压 | 测高温（上千度），需冷端补偿 |
| **数字量 DI/DO** | 开关量：24V=通(1)，0V=断(0) | `true` / `false` | 按钮、限位开关、继电器 |

> 🔑 **你作为上位机工程师要知道的**：绝大多数仪表把"物理量"先变成 **4–20mA 或 0–10V**，再用 **RS485+Modbus** 把数字传给你的软件。所以你**只管读 Modbus 寄存器里的数字**，不用自己处理电流电压——那是仪表厂的事。你只要知道"这个寄存器的值 × 量程系数 = 真实温度"就行。

### 2.3 PLC 是什么（你最大的陌生点，掰开讲）

🟦 概念科普 · 🟩 无需代码 · 🟧 S7.Net（M3）

> **PLC = 现场的一台专用小电脑**，专门用来控制机器（比你的笔记本皮实，能在电柜里 7×24 跑十年）。

| PLC 概念 | 是啥 | 前端类比 |
|---|---|---|
| **DB 块（数据块）** | PLC 里一块**命名的内存区**，存变量 | 一个 `const config = {...}` 对象 / 一块共享内存 |
| **地址**（如 `DB1.DBD0`） | 变量在内存里的**具体位置** | 对象属性路径 `config.temp` |
| **I / Q / M** | 输入(I) / 输出(Q) / 中间标志(M) 区 | 读传感器 / 控制执行器 / 临时变量 |
| **S7 通信** | 西门子私有的"读 DB 块"协议 | 一个专用的 `readDb(block, offset)` API |
| **TIA Portal** | 西门子官方编程软件（写 PLC 程序用） | PLC 的"IDE" |

> 🔑 **你写上位机，不是写 PLC 程序**。你的工作是：用 S7.Net（或 Modbus）**去读 PLC 的 DB 块里已经算好的数值**。PLC 程序员负责把温度传感器的值算好放进 DB1 的第 0 个浮点数——你只管 `ReadFloat("DB1", 0)` 拿走。这就是"上位机对接 PLC"的全部日常。

### 2.4 接线与抗干扰（现场最容易翻车的地方）

🟦 经验科普 · 🟩 无需代码 · 🟧 无

| 问题 | 现象 | 解决 |
|---|---|---|
| **A/B 接反**（RS485） | 完全不通 / 疯狂乱码 | 把 A、B 两根线对调 |
| **没共地**（两端电源不同） | 时通时断、偶发乱码 | 把两端 **GND 地线连起来**（参考电位） |
| **终端电阻缺失** | 远距离 / 高波特率时乱码 | 总线**两端**各并一个 **120Ω** 电阻（防信号反射） |
| **强电干扰** | 电机一启动就掉线 | 信号线和动力线**分开走线**，用屏蔽双绞线，屏蔽层单端接地 |

> 🔥 **真实现场口诀**："连不上先查线，乱码先查地（共地）和终端电阻，偶发掉线查干扰和电源。"——这比任何协议都重要，因为 **80% 的"通信故障"是物理层，不是代码**。

---

## 三、没有硬件怎么办？四层替代方案（重点）

> **核心结论**：从零成本到真机，四层任选。M0–M3、M7 全部能练，**不用买任何东西也能学完**。

### 🎯 你有什么 → 用哪层（决策表）

| 你的条件 | 推荐方案 | 成本 | 真实度 |
|---|---|---|---|
| 啥都没有，只想学架构/代码 | **T1 纯软件**（SimulatedDevice / Moq / 回放） | ¥0 | 中（验证逻辑够） |
| 想练"串口程序"但不想买线 | **T2 虚拟串口对**（com0com） | ¥0 | 高（和真串口代码一模一样） |
| 想练 Modbus 主站 | **T2 Modbus 从站模拟器** | ¥0 | 高 |
| 想练 PLC 通信 | **T2 PLCSIM + NetToPLCSim** | ¥0（需 TIA） | 高（真 PLC 协议） |
| 想练上云 | **T2 本地 MQTT / OPC UA 模拟服务器** | ¥0 | 高 |
| 想要"真设备"手感又省钱 | **T3 智能电表 / USB-485 / 开发板从站** | ¥20–100 | 很高 |
| 想冲简历"真 PLC 经验" | **T3 二手 S7-200 SMART / S7-1200** | ¥300–800 | 真机 |
| 实习/工作现场 | **T4 企业真机** | 公司出 | 真机 |

---

### 🟩 T1 纯软件方案（零硬件，随时可练）

**① SimulatedDevice（项目里已经有了！）**
- 它实现 `IDevice`，后台线程周期产点，**就是个"假数据源"**。
- 你之前跑 `dotnet run` 看到的跳动点位，就是它产的。
- **用途**：练通"采集→管道→存储→报警→界面"全链路，完全不需要硬件。

**② Moq 虚拟设备（M9 教过）**
```csharp
var device = new Mock<IDevice>();
device.Setup(d => d.Connect()).Raises(d => d.DataReceived += null,
    new DataEventArgs { PointId = 1, Value = 36.5 });
```
- 单元测试里用，隔离硬件，测你的逻辑。

**③ 数据回放（最像现场）**
- 录一段真实数据到 CSV/JSON，回放器逐行 `RaiseData`。
- 好处：能反复复现"那天凌晨 3 点报警"的 bug，不用等真机再出一次。

> 💡 **企业真实做法**：很多上位机团队**先无硬件开发**（用 SimulatedDevice 把界面、报警、报表全做完），等真机到了只换一个 `Device` 实现，一天接入。你现在的 DAQMonitor 就是这路数——M1–M7 只需把 `SimulatedDevice` 换掉。

---

### 🟩 T2 虚拟接口方案（代码和真机一模一样，只是对端是软件）

**① 虚拟串口对（M1 练手必备）** 🟧 com0com / VSPD
- **是什么**：装一个驱动，虚拟出 `COM3 ↔ COM4` 一对"背靠背"的串口。你的 DAQ Monitor 开 COM3，另一个小程序（或串口调试助手）开 COM4 发数据，**数据从 COM4 流进 COM3**，完全模拟"仪表连着电脑"。
- **怎么用**：
  1. 装 `com0com`（Win10/11 需测试模式签名，见外链）→ 建 `COM3<->COM4`。
  2. DAQ Monitor 把 `SerialDevice` 注册到 COM3。
  3. 用"串口调试助手"或 10 行 Python 往 COM4 发一帧 `AA 55 ...`。
  4. 看 MainWindow 是否出现对应点位。
- **真实度**：⭐⭐⭐⭐ 你的串口代码和连真仪表**一行都不用改**。

**② Modbus 从站模拟器（M2 练手）** 🟧 QModMaster / Modbus Slave / diagslave
- **是什么**：一个跑在你电脑上的"假仪表"，监听某个串口或 TCP 端口，响应你主站的读请求。
- **怎么用**：
  1. 开 `diagslave -m tcp -p 502`（TCP 从站）或 QModMaster 当从站。
  2. DAQ Monitor 用 NModbus 主站连 `localhost:502`，读保持寄存器。
  3. 你在模拟器里改个值，软件里立刻变——和真电表一模一样。

**③ PLCSIM + NetToPLCSim（M3 练手）** 🟧
- **是什么**：西门子官方 `PLCSIM` 在电脑里仿真一台 PLC；`NetToPLCSim` 把它的通信"透传"到网络端口，这样 **S7.Net 能像连真 PLC 一样连它**。
- **怎么用**：TIA 里写个简单 DB 块 → 下载进 PLCSIM → 开 NetToPLCSim 映射 → DAQ Monitor 用 S7.Net 连 `127.0.0.1` 读 DB。
- **限制**：需有 TIA Portal + PLCSIM（学生版/试用版可）。

**④ OPC UA 模拟服务器 / 本地 MQTT（M7 练手）** 🟧
- OPC UA：`Prosys OPC UA Simulation Server` 本地跑，提供随机变化的节点，你的 UA 客户端去订阅。
- MQTT：本地装 `Mosquitto` broker，`mosquitto_pub` 发消息，你的 `MQTTnet` 客户端订阅——和真上云代码一致。

---

### 🟧 T3 低成本真硬件（花小钱，真实感拉满）

| 买啥 | 价格 | 能干啥 | 说明 |
|---|---|---|---|
| **USB↔RS485 转换器**（CH340/FT232） | ¥20–50 | 电脑多出 RS485 口 | 接任何 485 设备；FT232 驱动稳 |
| **智能电表 / 温控仪**（带 Modbus RTU） | ¥50–120 | **真能读到电流/温度数据** | 最便宜的"真设备"，Modbus 协议，接上就能练 M2 |
| **Arduino/STM32 + RS485 模块** | ¥50–100 | 自己写个 Modbus **从站**固件 | 比模拟器更真：你控制它回什么值；理解"从站怎么想" |
| **二手 S7-200 SMART / S7-1200** | ¥300–800 | **真 PLC** | 简历写"对接西门子 S7-1200"的底气；需配网线+电源 |

> 💡 **性价比之王**：买个 **¥60 的 Modbus 智能电表 + ¥30 的 USB-485 线**，你立刻拥有一个"真下位机"——DAQ Monitor 改 `SerialDevice` 为 `ModbusRtuDevice` 注册进去，就能看到**真实电流在跳**。这比任何模拟器都长经验。

---

### 🟧 T4 企业真机（实习 / 工作才有）
真实产线 PLC、SCADA、DCS——这时候你已经有 T1–T3 的全部能力，上手真机只是"换根线 + 问清楚地址表"。**别怂，你练过的虚拟/低成本方案，和真机代码 95% 一样。**

---

## 四、连不上的万能排查清单（真实现场经验，背下来）

> 无论虚拟还是真机，**按这个顺序查，能解决 95% 的"通信失败"**：

```
1. 物理层（先查线！）
   □ 串口：COM 口号对吗？（设备管理器看）
   □ 485：A/B 接反了吗？（对调试一下）
   □ 共地接了吗？（两端 GND 连起来）
   □ 远距离/高波特率：总线两端 120Ω 终端电阻有吗？
2. 参数层（双方必须一致）
   □ 波特率 / 数据位 / 停止位 / 校验 对吗？
   □ 流控 DTR/RTS 要不要拉高？（部分设备必须）
3. 网络层（TCP/OPC/MQTT 类）
   □ IP / 端口对吗？防火墙放了吗？
   □ PLC：DB 号/偏移和 TIA 一致吗？该 DB "优化访问"关了吗？
   □ PLC 在 RUN 吗？允许 PUT/GET 通信吗？
4. 协议层
   □ Modbus：寄存器地址 ±1 偏移对吗？（手册 40001 vs 代码 0）
   □ 从站地址（站号）对吗？
5. 软件层
   □ 你的 Device 注册进 Bootstrapper 了吗？
   □ 异常信息是啥？（看 Serilog 日志，别瞎猜）
```

> 🔥 **血的教训**：新手 80% 时间花在"代码没错但线没接对/参数没对齐"。**先看物理层和参数层，再看代码**。

---

## 五、🏗️ 动手练习（从零搭虚拟环境跑通，零硬件）

### 练习 1：用虚拟串口让 DAQ Monitor 收到"假仪表"数据
**目标**：不买任何东西，让 M1 的串口代码真正跑起来。
1. 装 `com0com`，建虚拟对 `COM3<->COM4`。
2. 在 `DaqMonitor.Core/Devices/` 写 `SerialDevice : DeviceBase`，`Connect()` 里 `new SerialPort("COM3", 9600, Parity.None, 8, StopBits.One)` 并 `Open()`，`DataReceived` 里把字节喂给帧解析器，`RaiseData` 抛出点位。
3. 注册进 `Bootstrapper` 替换 `SimulatedDevice`。
4. 用任意"串口调试助手"开 COM4，发一帧 `AA 55 02 00 1E 3C 9E`（示意帧）。
5. 运行 DAQ Monitor，`dotnet run`，看 MainWindow 是否出现点位。

**✅ 答案要点**：
- `SerialPort` 来自 🟩 `System.IO.Ports`（.NET 8 需 `dotnet add package System.IO.Ports`）。
- 必须用 `DataReceived` 事件 + 字节缓冲解决**半包/粘包**（见 M1 帧解析器），不能"收到就当一帧"。
- 收不到先查：COM 号对不对、`DataReceived` 在某些机器不触发 → 改用 `BaseStream.ReadAsync` 循环读。
- 跑通 = 你**已经会写工业串口程序了**，和连真仪表代码一致。

### 练习 2：用 Modbus 从站模拟器读"假电表"
**目标**：练 M2 主站。
1. 下载 `diagslave`，命令行 `diagslave -m tcp -p 502 -a 1` 起一个 TCP 从站（站号 1）。
2. DAQ Monitor 用 NModbus `TcpClient` 连 `127.0.0.1:502`，读保持寄存器 0 起 5 个值。
3. 在 diagslave 里改寄存器值，看软件是否同步变化。

**✅ 答案要点**：注意地址偏移（手册 40001 → 代码 0）；NModbus 是 🟧 第三方包；这就是真实智能电表的读法。

### 练习 3：画出"你家的通信拓扑"
画一张图：电脑 →（USB-485）→ 智能电表 →（RS485 总线）→ PLC →（网线）→ 你的 DAQ Monitor。标注每个环节的接口、协议、参数。**这是面试常考的"你懂不懂现场"题。**

---

## 六、📌 温故知新 / 跨模块联动

- **关联 M0**：M0 教的"后台线程 + 事件 + 并发"在这里直接用到——`SerialPort.DataReceived` 是**后台线程回调**，你处理字节时绝不能跨线程碰 UI（复习 M0 Day7 的 `Dispatcher`/批量原则）。
- **关联 M1→M2→M3**：三者是**同一件事的不同对端**——都是"上位机往下位机要数据"。串口是"传输方式"，Modbus 是"对话语言"，PLC 是"对端设备类型"。学会一个，另外两个只是换 `Device` 实现。
- **关联 M9（DI）**：为什么我们抽象 `IDevice`？因为这样**硬件可替换**——今天 `SimulatedDevice`，明天 `SerialDevice`，后天 `ModbusDevice`，UI 和采集层一行都不用改。这就是"面向接口"的真实价值，也是你能在"没硬件"时照样开发的原因。
- **关联 M7（上云）**：OPC UA / MQTT 的"对端"也可以本地模拟（Prosys / Mosquitto），思路和 T2 虚拟串口一模一样。

---

## 七、📚 延伸阅读（外链，解决"还不懂"的地方）

- **串口基础 + com0com 虚拟串口**：https://sourceforge.net/projects/com0com/ —— M1 零硬件闭环，Win10/11 需测试模式签名，教程见站内搜索"com0com windows 11"
- **VSPD 虚拟串口（商业可试用）**：https://www.eltima.com/products/vspd/ —— 比 com0com 安装简单
- **Modbus 官方协议**：https://modbus.org/specs.php —— 想啃原始帧格式看这个
- **diagslave Modbus 从站模拟器**：http://www.modbusdriver.com/diagslave.html —— M2 练手
- **QModMaster（开源主/从站工具）**：https://sourceforge.net/projects/qmodmaster/ —— 调试 Modbus 神器
- **NetToPLCSim（西门子仿真透传）**：https://github.com/mesta1/NetToPLCSim —— M3 无真机练手
- **Prosys OPC UA Simulation Server**：https://www.prosysopc.com/products/opc-ua-simulation-server/ —— M7 本地 OPC UA 服务器
- **Eclipse Mosquitto（MQTT Broker）**：https://mosquitto.org/ —— M7 本地 MQTT
- **西门子 S7-1200 文档中心**：https://support.industry.siemens.com/cs/ —— 真 PLC 资料
- **RS485 应用笔记（终端电阻/布线）**：搜索"TI RS485 设计指南"或"ADI RS485 故障排查" —— 硬件抗干扰权威

---

## 打卡
- [ ] 看懂"上位机/下位机/串口/Modbus/PLC"的关系图
- [ ] 能向别人讲清 RS232 vs RS485、4–20mA 为什么抗干扰
- [ ] T1：用 SimulatedDevice 跑通 DAQ Monitor 全链路
- [ ] T2：虚拟串口 / Modbus 从站 / PLCSIM 至少选一个跑通
- [ ] 背下"连不上的排查清单"
- [ ] 🏗️ 练习 1/2/3 至少完成 1 个并跑绿
