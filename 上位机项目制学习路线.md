# 上位机转行 13K · 项目制学习路线（DAQ Monitor）

> **目标**：从中高级前端工程师（0 上位机基础）→ 达到企业 **13K** 上位机开发岗位要求
> **方式**：**项目驱动**——选定一个真实工业项目，边做边学；每个模块 = **先学本次知识点 → 做 1 道练习 → 直接写进项目**
> **你提的 4 个硬要求（已贯彻全文）**：① 知识点**串联**成线 ② 讲解**通俗易懂**、**对比前端 / 举例** ③ 确保达到**企业级**要求 ④ 内容进**腾讯文档**方便随时更新
> **节奏**：8 大模块（≈56 天），每周 6 天学 + 1 天复盘，可加速/跳过已会
> **进度可见**：每个学习日行尾有 `打卡[ ]`，在配套 HTML 看板勾选，进度条实时涨

---

## 〇、看文档前先认这 3 类东西（不然会云里雾里）

C# 世界里「写出来能跑的东西」分三类来源。本路线每个模块都会标注 🟦/🟩/🟧，请对照看：

- 🟦 **【C# 语法】** = 语言自带的关键字/写法。装好 .NET 就有，**不用装任何包**。
  例：`using`、`class`、`int`、`var`、`=>`
  （对照 TS：`let/const` → `var`；`: type` → 类型写在前面；`function` → `方法`）
- 🟩 **【.NET 类库 / BCL】** = 微软随 .NET 提供的"标准零件库"，`using` 对应命名空间即可用，**默认不用额外装包**。
  少数（如 `SerialPort`）需加一行 `dotnet add package System.IO.Ports`——但它仍是**微软官方库，不是第三方**。
  例：`Console`、`List<T>`、`Dictionary`、`Math`、`Task`、`HttpClient`、`System.Text.Json`、`LINQ`
- 🟧 **【第三方 NuGet 包】** = 别人写好的库，**必须 `dotnet add package 包名`** 才能用。
  例：`NModbus`、`S7.Net`、`LiveCharts`、`Serilog`、`MQTTnet`、`EF Core`
  → 看到 `using Xxx;` 报错"找不到类型"，十有八九是要装 NuGet 包了。

> 💡 口诀：**语法天生物、BCL 随 .NET、第三方要装包。**

---

## 一、为什么改成「项目驱动」（你的策略，我加了 3 道保险）

你已经有中高级前端工程能力，**真正的短板只是上位机专属领域知识**（串口/Modbus/PLC/工业协议），这些只有写真实程序才学得会。所以「先学后练再应用」是最优解。我补 3 个保险防翻车：

1. **精准预习**：每个模块先给 10–20 分钟只学"本次用到的点 + 1 道练习"，再写代码 → 不盲目试错。
2. **知识点串联地图**（见第三节）：把 `设备 → 串口/网口 → 协议 → 字节解析 → 业务对象 → 数据库 → 图表/报警` 看成一条**数据流水线**，学一个就知道它接在链的哪一环。
3. **企业级工程化**（见第五节）：日志、配置化、断线重连、异常处理、MVVM 分层、测试意识——这些正好是 13K 面试必问的"工程化"分，功能是基础，工程化才是溢价。

**怎么用这份文档**：每模块按顺序走 ① 前置知识点（带 🟦🟩🟧 + 对比前端）② 练习 ③ 项目任务（落到 DAQ Monitor）。

---

## 二、选定项目：工业数据采集监控系统（DAQ Monitor）

**为什么是它**：这是工厂里上位机每天干的典型活——从 PLC/仪表采数据、解析、存库、画面实时展示、超限报警。一个项目覆盖 13K 全部要求，面试拿来讲最有说服力。

**它长什么样（功能清单）**：
- 连接串口/网口设备，按 Modbus / S7 协议周期读取温度、压力、产量等数据点
- 实时曲线 + 数字面板展示，超限弹报警
- 数据落 SQLite 历史库，可查历史趋势
- 断线自动重连、运行日志记录
- 一键打包成可安装程序

**技术栈 × 13K 要求对照**：

| 13K 要求 | 本项目里怎么体现 |
|---|---|
| C# / .NET 熟练 | 全程 C#，MVVM 分层 |
| WPF 熟练 | 监控主界面、实时曲线、报警弹窗 |
| 串口 + Modbus | M1/M2 串口调试助手 + Modbus 读写 |
| PLC（西门子 S7） | M4 用 S7.Net 读 DB 块 |
| 数据库 + EF Core | M5 SQLite 历史库 |
| TCP/UDP | M3 Socket 多设备轮询 |
| 多线程/异步 | 采集线程不卡 UI |
| OPC UA / MQTT | M7 可选扩展 |
| 工程化 | 日志/配置/重连/异常/测试（贯穿） |

**架构（数据流水线）**：
```
[PLC/仪表]──串口/网口──▶[采集层]──Modbus/S7──▶[解析层]──▶[业务对象]
                                                                 │
                                          ┌──────────────┼──────────────┐
                                       [存储层]        [展示层]        [报警层]
                                      SQLite/EF      WPF+LiveCharts   阈值判断+弹窗
```

---

## 三、知识点串联地图（数据流水线）

把整条链路记熟，考点就串起来了：

1. **设备层**：PLC、仪表、传感器——它们只认"寄存器/地址 + 数值"。
2. **传输层**：串口（RS485，SerialPort 🟩）或网口（TCP Socket 🟩）。→ *对应 M1/M3*
3. **协议层**：Modbus RTU/TCP（🟩手搓 + 🟧NModbus）、西门子 S7（🟧S7.Net）。协议 = "怎么把'读地址100的温度'打包成字节发给设备，再把回的字节解出来"。→ *对应 M2/M4*
4. **解析层**：字节序（大小端）、BitConverter 🟩、类型转换——把原始字节变成 `float temperature`。→ *贯穿 M2–M4*
5. **业务层**：变成 `SensorData` 等业务对象，用 `List<T>`/LINQ 🟩 处理。→ *M2 起*
6. **存储层**：SQLite + EF Core 🟧 落历史库。→ *M5*
7. **展示层**：WPF Binding/MVVM 🟩 + LiveCharts 🟧 实时曲线（类比前端：Binding≈`v-model`，LiveCharts≈`echarts`）。→ *M6*
8. **报警层**：阈值判断 + 弹窗 + Serilog 🟧 日志。→ *M6/M7*

> 考试常问"Modbus 一帧怎么组成的""大小端怎么处理""为什么要用 MVVM"——答案都在这条链上，串起来就不会慌。

---

## 四、模块路线（M0–M8：学 → 练 → 用）

### ▶ M0 脚手架 + C#/.NET 热身（你的主场，可加速）· 对应原 Day1-7

- **目标/产出**：建好 DAQ Monitor 的 WPF 工程骨架，跑出第一个窗口。
- 📌 **本周技术来源**：🟦 C# 语法 + 🟩 BCL（`Console`/`List<T>`/`LINQ`/`Task`）+ 🟩 **WPF**（`<UseWPF>true</UseWPF>`，不是 NuGet）。
- **前置知识点（对比前端）**：
  - `var x = 1;` ≈ TS `let x = 1;`；类型在**前面**：`int x = 1;`
  - `class`/`方法` ≈ JS 的 `class`/`function`，但 C# 方法要写返回类型：`int Add(int a,int b)`
  - `=>` 既是 Lambda（≈ JS 箭头函数）也是属性简写：`public int Sum => A+B;`
  - `List<T>` ≈ JS 数组但**类型固定**；`LINQ` 的 `Where/Select` ≈ JS 的 `filter/map`
- **练习**：用 `struct Point{public int X,Y;}` 表示一个坐标，写个方法算两点距离。· 打卡[ ]
- **项目任务**：用 `dotnet new wpf` 建工程，MainWindow 放一个"启动采集"按钮 + 文本框，点按钮在文本框打印"DAQ Monitor 启动"。· 打卡[ ]
- **Day 1** C# 环境+首程序+对比TS · 3-4h · 打卡[ ]
- **Day 2** 类型系统 var/可空/struct · 3h · 打卡[ ]
- **Day 3** 集合与 LINQ · 3h · 打卡[ ]
- **Day 4** 类/属性/封装/static · 3h · 打卡[ ]
- **Day 5** 继承/接口/泛型 · 3h · 打卡[ ]
- **Day 6** 委托/事件/Lambda · 3h · 打卡[ ]
- **Day 7** 周复习+小测 · 2h · 打卡[ ]

### ▶ M1 串口通信（⚠️ 新领域，重点突破）· 对应原 Day15-17

- **目标/产出**：做出"串口调试助手"子模块，能收发字符串。
- 📌 **本周技术来源**：🟦 位运算 + 🟩 **SerialPort**（`System.IO.Ports`，需 `dotnet add package System.IO.Ports`，但仍官方）+ 🟩 `System.Text.Encoding`。
- **前置知识点（举例）**：串口像"一根只能按顺序说句子的电话线"——双方约定波特率（语速）、数据位、停止位、校验（防口误）。`SerialPort` 就是这根线的 .NET 封装。
- **练习**：用虚拟串口（com0com）把本机两个虚拟口连起来，A 发"B"，B 收到打印。· 打卡[ ]
- **项目任务**：在 DAQ Monitor 加"串口设置"面板（端口/波特率下拉）+ 收发日志框；连虚拟串口能自发自收。· 打卡[ ]
- **Day 8** 通信概念(比特/字节/进制/ASCII) · 2h · 打卡[ ]
- **Day 9** 串口参数+SerialPort类 · 3h · 打卡[ ]
- **Day 10** 串口实战收发+虚拟串口 · 4h · 打卡[ ]
- **Day 11** 异常/文件IO/JSON序列化 · 3h · 打卡[ ]

### ▶ M2 Modbus 协议（⚠️ 核心壁垒）· 对应原 Day22-28

- **目标/产出**：能读保持寄存器，把值显示到界面。
- 📌 **本周技术来源**：🟦 字节数组/struct 手搓 + 🟩 `System.Net.Sockets`(Modbus TCP) + 🟧 **NModbus**（Day25 起）。先纯 BCL 手搓再引包。
- **前置知识点（通俗）**：Modbus 是"设备间的普通话"。功能码=动词（03=读保持寄存器），寄存器地址=名词。一帧 ≈ `[从站号][功能码][起始地址][数量][CRC校验]`。
- **练习**：手工组一帧"读从站1的寄存器0开始2个字"的 RTU 报文，并算 CRC。· 打卡[ ]
- **项目任务**：DAQ Monitor 接入一个 Modbus 从站（用仿真软件），周期读温度/压力寄存器，显示数值。· 打卡[ ]
- **Day 12** Modbus概念/主从/寄存器 · 2h · 打卡[ ]
- **Day 13** Modbus RTU帧结构+手搓组包 · 4h · 打卡[ ]
- **Day 14** Modbus TCP+Socket实现 · 4h · 打卡[ ]
- **Day 15** NModbus库实战读写 · 4h · 打卡[ ]
- **Day 16** 字节序大小端+BitConverter · 3h · 打卡[ ]
- **Day 17** 数据解析(寄存器→工程量缩放) · 3h · 打卡[ ]

### ▶ M3 TCP/Socket 多设备（⚠️ 重点）· 对应原 Day19-21

- **目标/产出**：支持网口设备 + 多设备轮询架构。
- 📌 **本周技术来源**：🟦 异步 + 🟩 `System.Net.Sockets`(`TcpClient`/`TcpListener`/`UdpClient`) + 🟩 `Task`/多线程。
- **前置知识点（对比前端）**：`Socket` ≈ 浏览器底层的"网络连接"；`async/await` 在 C# 里和 JS 的 `Promise/async` 几乎同构，但 C# 有 `CancellationToken`（≈ `AbortController`）。
- **练习**：写 TCP 客户端连本地回显服务，发消息收回来。· 打卡[ ]
- **项目任务**：DAQ Monitor 抽象出 `IDevice` 接口，串口设备和网口设备都实现它；主循环轮询多个设备。· 打卡[ ]
- **Day 18** TCP客户端编程 · 3h · 打卡[ ]
- **Day 19** TCP服务端+多线程多客户端 · 4h · 打卡[ ]
- **Day 20** UDP+自定义帧+校验和 · 3h · 打卡[ ]

### ▶ M4 PLC 通信（西门子 S7，⚠️ 重点突破）· 对应原 Day29-35

- **目标/产出**：读真实/模拟 PLC 的 DB 块数据点。
- 📌 **本周技术来源**：🟦 语法 + 🟧 **S7.Net**（读 DB 块）+ 🟧 **OPC UA 客户端库**（可选）。
- **前置知识点（举例）**：PLC 的 DB 块 ≈ "设备给你的一块带地址的结构体变量"；S7.Net 就是帮你读这块结构体的工具，不用自己组 S7 报文。
- **练习**：用 S7.Net 连 PLC 仿真，读一个 `Real` 温度值打印。· 打卡[ ]
- **项目任务**：DAQ Monitor 接入 PLC 数据源，把 DB 块里的温度/产量映射到业务对象。· 打卡[ ]
- **Day 21** PLC与上位机关系+S7概述 · 2h · 打卡[ ]
- **Day 22** S7.Net连接+读DB块 · 4h · 打卡[ ]
- **Day 23** 读写Bool/Int/Real/字符串 · 4h · 打卡[ ]
- **Day 24** OPC UA概念+客户端 · 3h · 打卡[ ]
- **Day 25** 多设备轮询+断线重连机制 · 3h · 打卡[ ]

### ▶ M5 数据存储（SQLite + EF Core）· 对应原 Day36-42

- **目标/产出**：采集数据落历史库，能查趋势。
- 📌 **本周技术来源**：🟦 语法 + 🟧 **EF Core**(`Microsoft.EntityFrameworkCore`+`Sqlite` 包) + 🟧 **SQLite**(`Microsoft.Data.Sqlite`) + 🟩 多线程队列。
- **前置知识点（对比前端）**：EF Core ≈ 前端的 ORM（如 Prisma/TypeORM）——用 C# 类描述表，LINQ 当 SQL 写。
- **练习**：用 EF Core Code First 建 `History` 表（时间/点名/值），插入并查询昨天数据。· 打卡[ ]
- **项目任务**：DAQ Monitor 把每次采集值写入历史库；提供"查某点某时段"的方法。· 打卡[ ]
- **Day 26** SQL基础+SQLite · 2h · 打卡[ ]
- **Day 27** EF Core入门CodeFirst · 3h · 打卡[ ]
- **Day 28** 多线程Thread/Task/Parallel · 3h · 打卡[ ]
- **Day 29** 生产者-消费者队列缓冲 · 4h · 打卡[ ]
- **Day 30** 实时存储策略(历史/缓存) · 3h · 打卡[ ]

### ▶ M6 实时可视化 + 报警（你的 UI 主场）· 对应原 Day43-47

- **目标/产出**：监控主界面——实时曲线 + 数字面板 + 超限报警弹窗。
- 📌 **本周技术来源**：🟦🟩 前面全用上 + 🟧 **LiveCharts2**(实时曲线) + 🟩 WPF Binding/MVVM + 🟧 **Serilog**(日志)。
- **前置知识点（对比前端）**：WPF 的 `Binding` ≈ Vue 的 `v-model`/`{{}}`；`INotifyPropertyChanged` ≈ 响应式 `ref`；LiveCharts ≈ ECharts/Chart.js；MVVM ≈ 前端"数据/视图分离"。
- **练习**：用 LiveCharts 画一条随定时器更新的正弦曲线。· 打卡[ ]
- **项目任务**：DAQ Monitor 主界面绑定采集数据，实时曲线滚动；某点超阈值弹报警+写 Serilog 日志。· 打卡[ ]
- **Day 31** UI搭建 WPF+LiveCharts · 4h · 打卡[ ]
- **Day 32** 通信层抽象(Modbus+PLC统一接口) · 4h · 打卡[ ]
- **Day 33** 数据存储+报警逻辑 · 4h · 打卡[ ]
- **Day 34** 完善+打包(ClickOnce/安装包) · 3h · 打卡[ ]

### ▶ M7 企业级工程化（溢价项，13K 必问）· 对应原 Day41/48

- **目标/产出**：把 DAQ Monitor 打磨成"能拿出手的作品"，补工程化。
- 📌 **本周技术来源**：🟦🟩🟧 全复习 + 🟧 Serilog/配置/单元测试。
- **企业级清单（做到位）**：
  1. **日志**：Serilog 写文件+分级（Info/Error），代替 `Console.WriteLine`。
  2. **配置化**：端口/轮询周期/报警阈值放 `appsettings.json`，不写死代码。
  3. **断线重连**：采集层捕获异常→指数退避重连→日志。
  4. **异常处理**：设备通信必须用 `try/catch`，单点故障不崩整个程序。
  5. **MVVM 分层**：View / ViewModel / Model / Service 清晰分层，不在后台代码写业务逻辑。
  6. **测试意识**：对"字节解析/缩放公式"写几个单元测试（用 xUnit 🟧）。
- **练习**：给 M2 的"寄存器→工程量"缩放函数写 3 个 xUnit 用例。· 打卡[ ]
- **项目任务**：把上述 6 条逐项落到 DAQ Monitor；写 README 说明架构与跑法。· 打卡[ ]
- **Day 35** 日志Serilog+配置化 · 3h · 打卡[ ]
- **Day 36** 断线重连+异常处理完善 · 3h · 打卡[ ]
- **Day 37** 单元测试xUnit(解析/缩放) · 3h · 打卡[ ]
- **Day 38** 项目复盘+GitHub提交+README · 3h · 打卡[ ]

### ▶ M8 求职冲刺（拿 Offer）· 对应原 Day49-56

- **目标/产出**：简历 + 项目讲解 + 笔试面试通关。
- 📌 **本周技术来源**：🟦🟩🟧 全复习，**无新技术**。
- **练习**：用 STAR 法则写 3 段 DAQ Monitor 的项目经历。· 打卡[ ]
- **项目任务**：把作品集（DAQ Monitor 源码+README+演示视频）挂 GitHub；按 Boss/猎聘/智联投递。· 打卡[ ]
- **Day 39** 简历撰写(突出转型/UI/工程化) · 3h · 打卡[ ]
- **Day 40** 上位机面试题(C#/WPF/通信) · 4h · 打卡[ ]
- **Day 41** 项目讲解STAR准备 · 2h · 打卡[ ]
- **Day 42** 笔试题协议/字节处理刷题 · 3h · 打卡[ ]
- **Day 43** 投递策略+渠道 · 2h · 打卡[ ]
- **Day 44** 模拟面试+查漏 · 3h · 打卡[ ]
- **Day 45** 持续投递+复盘 · 2h · 打卡[ ]
- **Day 46** 学习网站进度同步(所学做成课程) · 3h · 打卡[ ]

---

## 五、企业级工程化清单（13K 面试必问，贯穿全程）

| 工程化能力 | 在哪练 | 面试常问 |
|---|---|---|
| 日志 Serilog | M6/M7 | "你们怎么排查线上问题？" |
| 配置化 appsettings | M7 | "参数写死还是配置？为什么？" |
| 断线重连 | M4/M7 | "设备掉线程序怎么办？" |
| 异常处理 | M3/M7 | "通信超时怎么处理？" |
| MVVM 分层 | M0/M6 | "为什么用 MVVM 不用事件驱动？" |
| 单元测试 xUnit | M7 | "核心算法有没有测试？" |
| 打包部署 | M6 | "怎么交付给客户？" |

---

## 六、进度打卡总表（按模块/天，随时勾）

| 模块 | 主题 | 天数 | 状态 |
|---|---|---|---|
| M0 | 脚手架+C#热身 | Day1-7 | 打卡[ ] |
| M1 | 串口通信 | Day8-11 | 打卡[ ] |
| M2 | Modbus | Day12-17 | 打卡[ ] |
| M3 | TCP/Socket多设备 | Day18-20 | 打卡[ ] |
| M4 | PLC(S7) | Day21-25 | 打卡[ ] |
| M5 | 数据存储 | Day26-30 | 打卡[ ] |
| M6 | 实时可视化+报警 | Day31-34 | 打卡[ ] |
| M7 | 企业级工程化 | Day35-38 | 打卡[ ] |
| M8 | 求职冲刺 | Day39-46 | 打卡[ ] |

---

## 七、资源清单

- **官方**：Microsoft Learn《C# 文档》《WPF》《.NET 网络通信》
- **协议**：Modbus 协议规范（只读功能码/帧格式即可）；西门子 S7 通信手册
- **NuGet**：NModbus / S7.Net / LiveCharts / Serilog / Microsoft.EntityFrameworkCore.Sqlite / xUnit
- **工具**：com0com（虚拟串口）、Modbus 从站仿真、PLC 仿真（或真实 S7-1200/200 SMART）、Visual Studio 2022、.NET 8 SDK
- **对比学习**：把你熟悉的 TS/JS 概念列在旁边（本路线已尽量标注）

---

## 八、怎么用这份文档（必读）

1. **顺序走模块 M0→M8**，每模块先读"前置知识点+练习"，做对练习再写"项目任务"。
2. 遇到 🟦/🟩/🟧 不确定属哪类，回看**第〇节**口诀。
3. 卡住就说「**帮我讲 Mx 的 xx**」或「**Day N 练习答案**」，我展开讲。
4. 学完一个模块就把 DAQ Monitor 对应功能提交一次 Git，作品集逐步成形。
5. 进度在配套 HTML 看板勾 `打卡[ ]`，看到进度条涨 = 你在进步。
6. 这份文档在**腾讯在线文档**同步更新；本地 `上位机项目制学习路线.md` 为源文件。
