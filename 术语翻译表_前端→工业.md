# 📖 术语翻译表 · 前端 → 工业(读文档卡词就查这张表)

> **用法**:读讲义/R 篇/MC 篇遇到任何黑词 → 先查这张表 → 查不到直接问 Cline「X 是什么」,永远先用前端语言解释。
> **心法**:左列吓人,右列全是老朋友。真正前端完全没有的新概念,整个领域不到 10 个。

---

## 一、通讯类(最大的一座"假山")

| 工业黑话 | 人话(一句话) | 你的前端等价物 |
|---|---|---|
| 回环 Loopback | 字节写进去,原样"假装是设备回的"弹回来 | msw / axios-mock-adapter 拦截请求返回假响应 |
| 模拟通道 SimulatedChannel | 接口的假实现,专供测试 | mock 实现 / fake service |
| 主站 Master | 主动发问的一方(=你的上位机) | 发请求的浏览器/client |
| 从站 Slave | 被问话的设备端(一问一答) | 被调用的 server |
| 进程内从站 | 测试里在内存起一个假设备服务器 | Vitest mock fetch / in-memory server |
| 真串口 RealSerialChannel | 包一层真实硬件的实现 | 生产环境的真 axios(生产实现) |
| 轮询 polling | 每 N ms 主动读一次设备 | setInterval + fetch / SWR refreshInterval |
| 订阅推送 | 设备有数据主动上报 | WebSocket / SSE 推送 |
| 粘包 | 两条消息的字节连在一起到达 | WebSocket 攒 buffer 切消息 |
| 半包 | 一条消息只到了一半 | 流式 chunk 读取,没攒够不处理 |
| 拆帧/组帧 | 从字节流切出一条完整消息 / 拼出一条消息 | message framing / 解析 buffer 协议 |
| 帧 Frame | 一条消息的固定格式(头+长度+数据+校验) | 自定义消息协议 schema |
| 帧头 AA55 | 每条消息开头的暗号字节 | 消息 magic number / 协议标识 |
| 心跳 heartbeat | 定期互发"还活着吗" | WebSocket ping/pong |
| 静默判掉线 | N 秒没消息就认为断了 | WebSocket onclose / timeout |
| 指数退避 | 失败后 1s/2s/4s/8s 重试 | axios-retry / react-query retry |
| 抖动 jitter | 退避时间加随机量,防同时重试 | 同左,业界通用套路 |
| 重同步 resync | 流错位后丢垃圾字节重新找帧头 | 流解析器 reset / 重新对齐 |
| RTU / TCP | Modbus 的串口版 / 网口版 | 同一 API 的两种 transport |
| 功能码 | "读寄存器/写线圈"等操作编号 | REST 的 method+path |
| 寄存器 Register | 设备里按地址读写的 16 位格子 | 远程对象按 key 读属性 |
| 线圈 Coil | 1 个开关量(1 bit) | 远程 boolean |
| 地址偏移 ±1 | 文档写 40001、实际要读 0 的坑 | API 文档和实际路径差一位 |
| 异常码 | 设备拒绝你时的错误编号 | HTTP 4xx 状态码 |
| 字节序 大端/小端 | 字节排列顺序(42C80000 vs 0000C8 42) | DataView 的 BE/LE 参数 / Buffer.readUInt16BE |
| 字交换 CDAB | 2 个寄存器谁当高字的顺序问题 | (两层字节序的第二层,见 ByteOrderLab) |
| CRC 校验 | 数据的防伪指纹,任何一位变了就对不上 | ≈一个 hash 校验值(1 个新概念) |
| 位运算 \| & ^ << >> | 在 bit 层面拼/拆字节 | (JS 也有,只是很少用;见陷阱讲义) |

## 二、架构/工程类

| 工业黑话 | 人话 | 前端等价物 |
|---|---|---|
| 上位机 | 跑在电脑上、控制/监视设备的软件 | 你开发的那整个 web app |
| 下位机 | 设备侧(PLC/单片机/控制卡) | 后端 / 嵌入式 |
| 组态软件 | 配置式搭监控画面的成品软件 | 低代码平台 |
| 双写 | 内存一份(实时)+数据库一份(历史) | state + redux-persist/storage |
| 组合根 CompositionRoot | DI 统一装配的入口文件 | main.tsx 里的 Provider 栈 |
| 可测试性设计 | 依赖接口不依赖实现,才好 mock | 依赖注入/控制反转 |
| 行为测试 | 按验收标准组织测试,不按方法 | Cypress 按用户故事写用例 |
| 冒烟测试 | 主流程走一遍不崩就行 | Playwright 冒烟链路 |
| 回归测试 | 改完代码重跑,确认没改坏 | CI 里重跑全量测试 |
| 黑盒测试 | 只调公共 API,不窥内部 | 组件测试只看渲染输出 |
| 仿真节拍 tickMs | 模拟器"每帧步长",测试可快进 | rAF 的 dt / cy.clock() 时间快进 |
| 7×24 长跑 | 连续运行数周不崩不漏 | 没有刷新按钮的 SPA(内存泄漏无处可逃) |
| 内存泄漏排查 | 长跑吃内存的定位 | Chrome Memory 面板同款技能 |

## 三、运控类(MC 系列)

| 工业黑话 | 人话 | 前端等价物 |
|---|---|---|
| 使能 Enable | 给电机上电解锁,没使能动不了 | 先拿权限才能操作 |
| 点动 Jog | 按住动、松开停 | 长按按钮 drag |
| 绝对定位 | 走到坐标 X mm | 目标值动画 |
| 回零 Home | 回到坐标 0 点 | reset state |
| 软限位 | 软件层的行程边界 ±1000mm | 表单 min/max 校验 |
| 硬限位 | 行程开关(物理挡块) | 物理约束,软件拦不住的 |
| 急停 E-Stop | 立刻冻结一切运动 | AbortController + 全局熔断 |
| 令牌/取消令牌 | 每个运动一把 CancellationToken,新指令取消旧的 | AbortController 的 signal |
| 报警锁轴 AlarmActive | 有报警该轴拒绝一切运动 | 错误态禁用按钮 |
| 插补 Interpolation | 多轴按比例同步走,轨迹是直线 | transform 动画多属性同步 |
| 节拍 | 仿真循环每次推进的时间片 | 动画帧 dt |
| G01 | 真卡的直线插补指令 | (行业词汇,知道即可) |

## 四、报警/采集类

| 工业黑话 | 人话 | 前端等价物 |
|---|---|---|
| 报警回滞 Hysteresis | 阈值附近抖动不反复报警(80 触发,75 才恢复) | debounce / 施密特触发器 |
| 边沿触发 | 只在"未报警→报警"那一刻报 | 事件只在状态翻转时 emit |
| 上升沿/下降沿 | 状态从 0→1 / 1→0 的瞬间 | onChange 里 prev!==next |
| 点位 Point | 一个被采集的传感器值 | 一条订阅的数据流 |
| 点表 | 点位清单(地址↔物理量映射) | 字段映射配置表 |
| 工程量转换 | 原始值→物理量(250→25.0℃) | 单位换算/线性映射 y=kx+b |
| 标定 | 定 k 和 b 的过程 | 建立换算公式 |
| 双写一致性 | 内存和库最终一致 | state 与 storage 同步策略 |
| Channel 缓冲 | 生产者/消费者的缓冲队列 | RxJS buffer / 异步批处理队列 |

## 五、真正的新概念清单(前端完全没有,需要真学)

> 只有这些是真·新知识,总共 8 个,每个都不难:

1. **CRC** —— 防伪指纹,15 行代码,记住"低字节在前"即可
2. **字节序两层结构** —— 寄存器内大端 + 跨寄存器字序(ByteOrderLab 已亲手玩过)
3. **主从问答协议** —— 一问一答的时序(比 REST 还简单,没有回调)
4. **报警回滞** —— 施密特触发器思想(理解后 1 分钟讲清)
5. **PLC / DB 块** —— 设备侧的数据存储概念(知道是什么即可)
6. **现场抗干扰** —— 接地/屏蔽/隔离,硬件话题(面试口头了解)
7. **RS232/485/4-20mA** —— 物理层硬件知识(科普级了解)
8. **使能/软限位/急停** —— 运控安全语义(概念简单,行业约定)

---

## 六、使用协议

1. 读任何文档卡词 → 先查本表
2. 查不到 → 问 Cline「X 是什么」→ 永远先给前端类比
3. 每弄懂一个新词 → 自己在表里补一行"我自己的话"
4. 面试前 → 把第五节 8 个新概念过一遍

**记住:你 6 年前也听不懂 CORS 和 Hydration。术语密度 ≠ 难度,只是暴露时长问题。**
