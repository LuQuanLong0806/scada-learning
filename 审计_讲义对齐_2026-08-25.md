# 🔍 审计 · 逐行导读 × 模块讲义对齐(2026-08-25)

> **背景**:《[项目逐行导读 · DaqMonitor 从零到吃透](项目逐行导读_DaqMonitor_从零到吃透.md)》每站贴了「📚 对应讲义」链接。链接成立的前提 = 讲义真能撑起"回讲义重学"。本次以**导读各站的知识点清单为标尺**,并行深读 M0/M4/M6/M8/M9 五份主线讲义,查三件事:**知识点漏没漏 / 讲得透不透 / 练习和答案对不对得上**。
> **结论**:整体支撑度约 70%;发现 **13 处硬伤已当场修复**(讲义已改),**5 节「工程对齐补丁」已补进讲义**,其余偏浅项列入下方清单供后续迭代。

---

## 一、修复总账(已落进讲义,直接生效)

### M0(4 处硬伤)
| # | 问题 | 修复 |
|---|---|---|
| 1 | 「类型依赖总览」表 7 处与工程不符:SensorPoint 误标 `readonly struct`;DeviceState/AlarmLevel 标成独立文件(实际与 SensorPoint 同文件);DeviceBase 标 `DeviceBase.cs`(实际与 IDevice 同文件);Pipeline 文件夹误标 `Pipeline/`(实际 `Acquisition/`);SensorRecord 误标 `Persistence/`(实际 `Store/`);AlarmEvent 误标 `Models/`(实际 `Alarms/`) | 全部改正,并加"同文件"提示 |
| 2 | Day 7 挑战题**题目自相矛盾**:前句要求 DispatcherTimer、末句要求"必须用 Dispatcher.Invoke",答案(DispatcherTimer 版)答非所问 | 题目改为与答案一致的 DispatcherTimer 路线,并新增「DispatcherTimer vs Dispatcher.Invoke 是两回事」辨析框 + 想练 Invoke 的改造指引 |
| 3 | Day 6 答案用 `DataEventArgs` 未提示 using;Day 5/Day 6 两份答案都定义 `Thermometer`,先后粘贴会重名冲突 | 补「两个防撞墙提示」 |
| 4 | 「完整代码组装」节残留旧机器绝对路径 `F:\00_project\...`;DeviceBase 又标独立文件;"28 测试"过时 | 改为相对路径 + 同文件说明 + 85 测试 |

### M4 / M6 / M8 / M9(各补一节「🔧 工程对齐补丁」)
| 讲义 | 审计发现的缺口(❌=讲义零覆盖) | 补丁内容 |
|---|---|---|
| **M4** | ❌Fluent API 全套(工程用 Fluent API,讲义只教特性版) ❌Channel 串行写泵(工程 PointStore 灵魂) | 补丁① Fluent API 全套+复合索引为什么;补丁② 写泵三件套+database is locked;补丁③ EnsureCreated/Migrations/断电取舍/构造差异四个一句话 |
| **M6** | ❌AlarmCleared 双事件 ❌HashSet.Add/Remove 返回值语义 ❌ToList 快照 ⚠️边沿触发只有一句话 | 补丁① 工程版 Evaluate 全文(53 行核心);补丁② 四个精华逐个点名+面试问法;补丁③ Serilog 与工程真实关系(工程未接,别装接了) |
| **M8** | ❌手写 RelayCommand(讲义只教 Toolkit 源生成器,工程是手写) ❌ObservableCollection 局限/PointView ❌struct 不能绑 UI ❌IValueConverter/TemplateColumn/双条件 | 补丁① 手写 RelayCommand 29 行+CanExecuteChanged 转接原理;补丁② INPC 模板+PointView 存在意义+OC 局限;补丁③ Converter/TemplateColumn/双条件速记;开头点明"Toolkit 帮你生成了手写那套" |
| **M9** | ❌GetServices 多实例 ❌Func 工厂注入 ❌容器 Dispose 级联 ⚠️GetRequiredService vs GetService 无对比 | 补丁①②③④:多实例/工厂注入(工程 TcpDevice 真例)/单复数解析死法对比/Dispose 总闸(挂在主窗 Closed) |

### M0 也补了一节
| 缺口 | 补丁 |
|---|---|
| ❌登录拦截(ShowDialog/bool?/Shutdown+return) ❌async void 全包 try-catch ❌Start 幂等守卫/Stop Wait(500) ❌SingleReader 选项 ❌10% 越界剧本 | 「工程对齐补丁」三小节:启动三件套/Start-Stop 对称防御/SingleReader+剧本设计 |

---

## 二、抽查结论(没改的,给你交底)

- **M5 ✅ / M14 ✅**:LiveCharts2 接入、自定义控件自绘与工程 ChartView/GaugeControl 完全对得上,支撑充分。
- **练习答案抽查**(M0×4、M4×2、M6×2、M8/M9×3):除已修复的 M0 Day7/Day6 两处,其余答案与题面匹配、类型签名与工程/前置类型定义一致,照敲可编译。
- **M6 进阶题(回滞序列)⚠️ 留观**:教学版 Evaluate 本身不含回滞实现,答案只有注释级判定——现已由「补丁① 工程版全文」补齐,做该题时直接对照工程版。

## 三、遗留偏浅清单(不阻塞学习,后续迭代)

| 讲义 | 点 | 现状 |
|---|---|---|
| M0 | IEC 60073 报警分色含义 | 导读第 2 站已讲,讲义一句话带过 |
| M0 | params/` : base()` 构造链示例 | 速查有,讲义正文无完整示例 |
| M0 | 攒批"锁内换引用"手法 | 导读第 5 站逐行讲了,讲义正文一句带过 |
| M4 | 双写内存索引(List+Dictionary)展开 | 导读第 6 站逐行讲了 |
| M9 | 魔数集中组合根的管理思想 | 导读第 9 站讲了 |
| M8 | Dispatcher"包整批"原理 | 在 M9 Day3(已存在),非缺失,只是跨模块 |

> 偏浅项的共同解法 = 导读对应站(这正是导读存在的意义:讲义管教学动线,导读管工程逐行)。

## 四、对学习路径的影响(一句话)

**现在"导读卡壳 → 点 📚 讲义 → 回来继续"这条链路是通的**:硬伤已修,缺口已补,链接指向的每一处都有真货。剩余偏浅项不影响主线,遇到再深挖。
