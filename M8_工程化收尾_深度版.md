# M8 — 工程化收尾（MVVM + 安装包 + 简历）🏁
> **优先级定位**：🔴 必学 · 工程化收尾（配置化/MVVM/打包/简历，把项目收口成作品）

> **技术来源**：🟧 `CommunityToolkit.Mvvm`（MVVM）、🟧 安装包（MSIX / ClickOnce）、🟦 `IConfiguration` 配置化。
> **给简历加的能力**：把"能跑的项目"打磨成"能交付的作品" —— 架构清晰、可配置、能安装、讲得清。
> **前置**：M0–M7（DAQ Monitor 已具备采集/可视化/存储/报警/上云）。

> ⏱️ **阅读路径**(按时间预算选入口)
> - **3 分钟**:看「模块目标」— 知道这是把项目收口成"作品"的模块
> - **30 分钟**:加看 Day 1 MVVM 重构 + Day 2 appsettings.json 配置化
> - **3 小时**:全文精读 + Day 3 全局异常 + Day 4 安装包(MSIX/ClickOnce) + Day 5 README/简历话术
> - 🎯 **面试高频**:MVVM 解耦(VM 可单测)/ 配置 vs 硬编码 / **全局异常处理 DispatcherUnhandledException**
> - 🔁 **配套复习**:[速记卡 Q2-Q3](面试高频知识点_速记卡.md) · [代码肌肉 B9 RelayCommand 手写](代码肌肉训练手册_30天刷题版.md) · [间隔重复表](记忆与复习机制_间隔重复版.md)

## 模块目标
① 用 MVVM 重构 UI（解耦界面与逻辑）；② 配置化（设备/报警/上云参数进 `appsettings.json`）；③ 全局异常处理；④ 打包成可安装程序；⑤ 写 README + 简历描述。

## Day 1 — MVVM 重构 + 配置化

### 一句话讲清楚
MVVM = 把 UI(XAML) 和逻辑(ViewModel) 用"绑定"解耦：ViewModel 只管数据和命令，界面自动跟着变。配置化 = 把易变的参数挪到外部文件，改配置不改代码。

### 前端类比秒懂
| MVVM | 前端 |
|---|---|
| View (XAML) | 模板 / JSX |
| ViewModel | 组件逻辑 / store |
| `INotifyPropertyChanged` | 响应式 `ref` / `reactive` |
| `[RelayCommand]` | 事件处理函数 |
| `appsettings.json` | `.env` 配置 |

### 分点精讲
**① ViewModel（CommunityToolkit.Mvvm）**（🟧🟦）
```csharp
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;

public partial class MainViewModel : ObservableObject
{
    [ObservableProperty] private string _status = "就绪";
    [ObservableProperty] private double _currentValue;

    [RelayCommand]
    private async Task StartAsync()
    {
        Status = "采集中...";
        // 调设备/引擎，结果自动反映到界面
    }
}
```

**② XAML 绑定**（🟩 WPF）
```xml
<TextBlock Text="{Binding Status}" />
<Button Command="{Binding StartCommand}" Content="启动" />
```

**③ 配置化**（🟦 `IConfiguration` + 🟧 `Microsoft.Extensions.Configuration.Json`）
```json
// appsettings.json
{
  "Devices": [
    { "Type": "Modbus", "Ip": "192.168.1.10", "Port": 502, "Points": [0,1,2] },
    { "Type": "Plc", "Ip": "192.168.0.1", "Rack": 0, "Slot": 1 }
  ],
  "Alarms": [ { "PointId": 3, "Threshold": 100, "Level": "Critical" } ],
  "Mqtt": { "Broker": "broker.emqx.io", "Port": 1883, "TopicPrefix": "factory/line1" }
}
```
```csharp
var cfg = new ConfigurationBuilder().AddJsonFile("appsettings.json").Build();
var devices = cfg.GetSection("Devices").Get<List<DeviceConfig>>();
```

### 🔬 掰开揉碎：`[ObservableProperty]` 到底生成了什么（源生成器 Source Generator）
手写 `INotifyPropertyChanged` 又臭又长（每个属性要 `OnPropertyChanged`）。`CommunityToolkit.Mvvm` 用 **Roslyn 源生成器**在**编译期**帮你生成样板代码：
- 你写：`[ObservableProperty] private double _currentValue;`
- 编译后自动生成：`public double CurrentValue { get => _currentValue; set { _currentValue = value; OnPropertyChanged(); } }` + 实现 `INotifyPropertyChanged`。
- `[RelayCommand]` 同理生成 `StartCommand` 属性和 `ICommand` 实现。
- **为什么比手写好**：零样板、不会漏写 `OnPropertyChanged`、编译期就检查类型。**进阶阅读**：源生成器原理 https://learn.microsoft.com/dotnet/csharp/roslyn-sdk/

### 🔬 掰开揉碎：DI 生命周期怎么选（WPF + EF Core 场景）
`AddSingleton / AddScoped / AddTransient` 选错会出诡异 bug：
- **Singleton（全局唯一）**：`PointStore`、`AlarmEngine`、`AcquisitionPipeline`——整个程序就一份，所有窗口/线程共享。
- **Scoped（一次作用域一份）**：**`DbContext` 必须用 Scoped**（或每次 `new`）。原因见 M4「变更追踪缓存」——一个 DbContext 用太久缓存膨胀、并发冲突。在 WPF 里常手动开 `using var scope = provider.CreateScope()` 包住一次数据库操作。
- **Transient（每次都新建）**：轻量无状态工具类用，别给有状态的服务用（会各自一份、不共享）。
- **循环依赖**：A 构造函数要 B、B 又要 A → 启动即炸，拆出公共依赖到第三个类。

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ MVVM 不是必须但加分 | 13K+ 岗位普遍要求，面试常问 |
| ⭐ 配置不写死 | IP/阈值进文件，现场改配置不动代码 |
| 🔥 命令异步 | `[RelayCommand]` 方法用 `async Task`，别 `async void` |
| 🔥 配置缺失 | 读不到配置要给默认值 + 日志告警 |

### ✏️ 练习
把 DAQ Monitor 主窗口的"启动"按钮逻辑抽成 `MainViewModel` 的 `[RelayCommand]`，状态用 `[ObservableProperty]`。

**✅ 答案**（见上方 ①）。

**🏗️ 项目任务**：DAQ Monitor 全 UI 迁移到 MVVM；设备/报警/MQTT 配置读 `appsettings.json`。工程素养达标。

**✅ 打卡[ ]**

## Day 2 — 异常处理 + 安装包 + README + 简历

### 一句话讲清楚
企业级 = "出问题不崩、能装、讲得清"。全局异常兜底、打包成 exe/msix、写好 README 和简历描述，作品才算完整。

### 分点精讲
**① 全局异常兜底**（🟦，呼应 M6）
```csharp
AppDomain.CurrentDomain.UnhandledException += (s, e) =>
    Log.Fatal((Exception)e.ExceptionObject, "未处理异常");
System.Windows.Application.Current.DispatcherUnhandledException += (s, e) =>
    { Log.Error(e.Exception, "UI异常"); e.Handled = true; };
```

**② 打包（MSIX / ClickOnce）**（🟧）
- VS 右键项目 → 发布 → 创建安装包(MSIX)；或 DotNet 用 `Microsoft.NET.Sdk.WindowsDesktop` 的打包工程。
- 目标：双击安装、有开始菜单图标、能卸载。

**③ README 模板**（🟦文档）
```markdown
# DAQ Monitor
工业数据采集监控系统（C#/.NET 8 + WPF）
## 功能
- 多设备接入：串口 / Modbus / PLC(S7)
- 实时曲线 + 仪表盘
- 历史库(SQLite) + 查询/导出
- 报警引擎 + Serilog 日志
- MQTT 上云
## 运行
dotnet restore && dotnet run --project src/DaqMonitor.UI
```

**④ 简历描述（15K 写法）**
> 独立开发工业数据采集监控系统：基于 .NET 8 + WPF(MVVM)，对接串口/Modbus/西门子PLC 多源设备，采用后台采集+ConcurrentQueue 缓冲+Dispatcher 刷新避免卡顿；实现实时曲线/仪表盘、SQLite 历史库与 CSV 导出、阈值报警引擎与 Serilog 日志、MQTT 上云；分层架构(领域/设备/持久化/UI) + 面向接口设计，支持配置化与一键安装。

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ 简历写"架构决策" | 写"为什么用接口/事件/锁"，不是罗列技术名 |
| ⭐ 能演示 | 准备虚拟串口/模拟 PLC 的演示链路，面试现场跑 |
| 🔥 安装包签名 | 企业分发常要证书签名，练习可先免签 |

### ✏️ 练习
写一段你的简历"项目描述"（按上方 ④ 改造成你自己的话）。

**✅ 答案**（见上方 ④，按个人经历改写）。

**🏗️ 项目任务**：DAQ Monitor 加全局异常处理；用 VS 发布 MSIX 安装包；补全 README；把简历描述沉淀到项目根 `RESUME.md`。🏁 全部模块达标！

**✅ 打卡[ ]**

## Day 3 — 配置实操 + 自动更新 + 第三方 UI 库（企业标配）

> 前面 Day1 讲了"配置化思想 + appsettings.json 读取"，但没动手写文件。本节补齐 **Ini / XML / JSON 三种格式的读写实操**，再加 **自动更新/部署** 与 **HandyControl 第三方控件库**——这三项 JD 都点名。

### ① 配置格式三选一（动手）

**JSON（推荐，🟧 `System.Text.Json` / `Microsoft.Extensions.Configuration.Json`）**
```csharp
// 读
var json = File.ReadAllText("appsettings.json");
var cfg = JsonSerializer.Deserialize<AppConfig>(json);
// 写（改了配置落盘）
File.WriteAllText("appsettings.json",
    JsonSerializer.Serialize(cfg, new JsonSerializerOptions { WriteIndented = true }));
```

**INI（老设备软件最爱，🟦 手写解析或 🟧 `ini-parser`）**
```csharp
// 简单 INI 结构：[Section] Key=Value
// 推荐用 NuGet `ini-parser`：
var ini = new IniFileParser.IniDataParser();
var data = ini.ReadFile("config.ini");
string ip = data["Device"]["Ip"];
data["Device"]["Ip"] = "192.168.1.20";
ini.WriteFile("config.ini", data);
```

**XML（工控老系统常见，🟩 `System.Xml`）**
```csharp
var doc = new XmlDocument();
doc.Load("config.xml");
var ip = doc.SelectSingleNode("//Device/Ip")?.InnerText;
```

### ② 自动更新 / 部署（🟧 ClickOnce / Squirrel）
- **ClickOnce**：VS 发布时勾选"自动更新"，用户下次启动自动拉新版本，企业内网分发最省心。
- **Squirrel.Windows**：把应用打包成 `Setup.exe`，启动时检查远程 RELEASES 文件，有新版静默下载替换。
- **文件比对更新**：简单方案——启动时比对本地 vs 服务器 `version.txt`，不一致则下载增量包。
- 简历可写："实现自动更新（Squirrel/ClickOnce），降低现场运维成本"。

### ③ 第三方 UI 库 HandyControl（🟧，国内最流行）
```xml
<PackageReference Include="HandyControl" />
```
- 80+ 现成控件：仪表盘 `Gauge`、轮播、Loading、MessageBox、主题切换。
- 一行换肤；告别 WinForms 时代丑界面。
- 对比：LiveCharts(M5 图表) 管"数据可视化"，HandyControl 管"整体控件/美观 HMI"，两者互补。
- 简历可写："使用 HandyControl 构建现代化工业 HMI 界面"。

### ⭐ 重点 / 🔥 坑
| | |
|---|---|
| ⭐ 配置三格式都会 | JSON 通用、INI 老设备、XML 工控老系统 |
| ⭐ 自动更新是企业标配 | 别只交 exe，要能"升级" |
| 🔥 配置写回注意编码/权限 | 程序目录可能无写权限，配置放 `AppData` |
| 🔥 HandyControl 仅 WPF | WinForm 用其它库(DotNetBar 等) |

### ✏️ 练习 + 🏗️
- ✏️ 用 `System.Text.Json` 把设备配置读出来改 IP 再写回。
- 🏗️ 项目任务：DAQ Monitor 加"设置"页，JSON 存配置到 `AppData`；评估接入 HandyControl 换肤。打卡[ ]

## 📌 温故知新（跨模块联动）
- **M0 接口/事件 → 这里 MVVM 是它们的「UI 版」**：ViewModel 用接口/命令解耦界面，思想同源。
- **M9 DI 容器 → 这里 `Bootstrapper` 是组合根**：M8 的配置 + M9 的注入合起来，才是「可配置、可测试、可交付」的完整工程素养。
- **全模块收口**：M1–M7 的能力，经 M8(MVVM/配置/异常/打包) + M9(测试/DI/容错) + M10(报表) 打磨，才成为一个「能演示、讲得清、有测试兜底」的简历作品。

## 📚 延伸阅读（卡点时点开）
- CommunityToolkit.Mvvm 文档（源生成器）：https://learn.microsoft.com/dotnet/communitytoolkit/mvvm/
- Microsoft DI 官方：https://learn.microsoft.com/dotnet/core/extensions/dependency-injection
- MSIX 打包：https://learn.microsoft.com/windows/msix/
- 全部模块外链汇总见 `外部链接索引.md`

## 模块交付清单（M8）
- [ ] MVVM 重构（CommunityToolkit.Mvvm）
- [ ] 配置化（appsettings.json）
- [ ] 全局异常处理 + 日志
- [ ] MSIX / ClickOnce 安装包
- [ ] README + 简历描述（RESUME.md）
