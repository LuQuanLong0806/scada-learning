# M18 — 配方管理（工艺参数版本化）

> **优先级定位**：🟡 加分项 · 配方管理（13-15K JD 偶尔点名，注塑机/包装线/食品配方场景刚需）
> **技术来源**：🟦 `System.Text.Json`（参数 JSON 列）+ 🟧 `Microsoft.EntityFrameworkCore.Sqlite`（EF Core，沿用 M4）+ 🟧 `ClosedXML`（Excel 导入导出，沿用 M10）
> **给简历加的能力**：让上位机支持「切换产品配方」——一键把工艺参数（温度设定 / 压力阈值 / 时间序列）从一组切换到另一组，并带版本化 + 审计。这是真实工厂里最常见的一个「软需求」，能讲出来就比只做采集的同行多一项能打的牌。
> **前置**：M4（EF Core / DbContext / 迁移）、M9（DI / 单测 / 工程分层）、M10（ClosedXML 导出）。

> ⏱️ **阅读路径**(按时间预算选入口)
> - **3 分钟**:看「模块目标」— 知道配方 = 一组工艺参数的命名版本
> - **30 分钟**:加看 Day 1 配方 CRUD + JSON 序列化
> - **3 小时**:全文精读 + Day 2 **版本化 + 回滚事务(写入+回读+失败回滚)** + Day 3 Excel 导入导出
> - 🎯 **面试高频**:**JSON 列 vs 关联表权衡** / 配方切换事务思想 / 版本 diff 实现 / 审计谁改了啥
> - 🔁 **配套复习**:[代码肌肉 B23 Recipe 配方管理](代码肌肉训练手册_30天刷题版.md) · [间隔重复表](记忆与复习机制_间隔重复版.md)

> 📚 **前置语法**(M18 用到的,陌生请查 [C# 语法速查 — 前端视角](CSharp语法速查_前端视角.md))
> - `class Recipe { public int Id { get; set; } public string Name { get; set; } = ""; public List<RecipeParameter> Parameters { get; set; } = new(); }` — class + 默认值
> - `string json = JsonSerializer.Serialize(recipe)` — System.Text.Json 序列化
> - `var recipe = JsonSerializer.Deserialize<Recipe>(json) ?? throw new JsonException()` — 反序列化 + null 检查,速查 §5
> - `using var tx = await db.Database.BeginTransactionAsync(ct)` — EF Core 事务,速查 §13/§8
> - `await tx.CommitAsync(ct)` / `await tx.RollbackAsync(ct)` — 提交/回滚
> - `public record RecipeParameter(string Key, double Value, string Unit)` — 不可变参数项,速查 §12

## 模块目标
**配方 = 一组工艺参数（温度设定 / 压力阈值 / 时间序列）的命名版本**。本模块让上位机：① 编辑 / 保存配方；② 切换产品时一键下发到设备；③ 版本化（可回滚到任意历史版本）；④ 审计（谁什么时候改了啥、激活了哪个版本）。学完你能讲清：「JSON 列 vs 关联表」的权衡、配方切换的「写入 + 回读 + 失败回滚」事务思想、版本 diff 的实现——这三点是「做过配方系统」和「只听过这个词」的分界线。

---

## Day 1 — 配方数据模型 + 增删改查

### 一句话讲清楚
配方 = 命名的参数集合 + 版本号 + 创建时间 + 创建人，**前端类比：Git 的 tag + `package.json` 的 version**——同一个 `Name`（项目名）下可以有多个 `Version`（tag），任何时候只有一个「当前生效」（`main` / `HEAD`）。

### 前端类比秒懂（每个概念都对上）
| 配方管理概念 | 前端类比 | 说明 |
|---|---|---|
| 配方 `Recipe` | `package.json` / `docker-compose.yml` | 一份命名 + 版本化的参数集合 |
| 版本号 `v1.2.3`（SemVer） | SemVer | 大.中.小，可字符串比较 |
| 当前生效配方 `IsActive=true` | Active branch（`main` / `HEAD`） | 同名下只允许一个生效 |
| 切换配方（`ActivateAsync`） | `git checkout <tag>` | 切换前要落库审计 |
| 配方历史列表 | `git log --oneline` | 按创建时间倒序 |
| 配方 diff（Day 3） | `git diff v1..v2` | 增 / 删 / 改三类变更 |

### 分点精讲

**① 数据模型 `Recipe` + `RecipeParameter`**（🟦）
配方主体放关系列（`Name`/`Version`/`IsActive`/`CreatedBy`/`CreatedAt`），**参数本身放 JSON 列**（`ParametersJson`）。这是一个**刻意权衡**：参数 schema 经常变（今天加一个新温度点，明天加一个保压时间），用 JSON 列不用改表结构；但用于查询 / 唯一约束的字段仍然在关系列，索引能用。

```csharp
// DaqMonitor.Core/Recipes/Recipe.cs
public class Recipe
{
    public int Id { get; set; }
    public string Name { get; set; } = "";            // 如 "饮料瓶_500ml"
    public string Version { get; set; } = "1.0.0";    // SemVer 字符串
    public bool IsActive { get; set; }                // 当前生效（同名仅一个 true）
    public bool IsDeleted { get; set; }               // 软删除标记（防误删历史）
    public string CreatedBy { get; set; } = "";       // 创建人（工号 / 用户名）
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
    public string ParametersJson { get; set; } = "{}"; // 灵活参数（JSON 列）
}

// 配方里的一个参数（仅作为 JSON 反序列化的 DTO，不建表）
public class RecipeParameter
{
    public string Key { get; set; } = "";        // "temperature_setpoint"
    public double Value { get; set; }            // 85.5
    public string Unit { get; set; } = "";       // "℃"
    public string? Description { get; set; }     // 可选注释
}
```

**逐行白话**：
- `Name + Version`：配方的「身份证」。同名下不同版本，像 `docker-compose.yml` 的 `v1` / `v2`。
- `IsActive`：同 `Name` 下**只允许一个 `true`**（Day 1 末尾的坑点）。前端类比 React Router 的「当前激活路由」——同时只能有一个。
- `IsDeleted`：**软删除**——不真删行，只标记。理由：配方是工艺历史，删了就回滚不了（Day 3 的 diff / 回滚都靠历史还在）。
- `ParametersJson`：参数列表序列化成 JSON 字符串存进去。SQLite 自带 JSON 函数（`json_extract`），需要按参数查也能查（见坑点表）。

**② EF Core 配置：唯一约束 + 索引**（🟧，关联 M4）

```csharp
// DaqMonitor.Core/Persistence/AppDb.cs（在 M4 已有的 AppDb 上加）
public class AppDb : DbContext
{
    public DbSet<Recipe> Recipes => Set<Recipe>();
    public DbSet<AuditEntry> AuditLog => Set<AuditEntry>();

    protected override void OnModelCreating(ModelBuilder b)
    {
        // 唯一约束:Name + Version 不能重复(且只针对未软删的)
        b.Entity<Recipe>()
            .HasIndex(r => new { r.Name, r.Version })
            .IsUnique()
            .HasFilter("[IsDeleted] = 0");   // SQLite 的 partial index,软删的版本可重建

        b.Entity<Recipe>().Property(r => r.ParametersJson).IsRequired();
        b.Entity<AuditEntry>().HasIndex(a => a.At);
    }
}
```

**逐行白话**：
- `HasIndex(...).IsUnique()`：让数据库帮我们拦「同一 `Name` 下重复 `Version`」。前端类比：唯一邮箱约束，重复注册直接拒。
- `.HasFilter("[IsDeleted] = 0")`：partial index——软删的版本不算重复，可以重建同版本号（小写 SQLite 语法，迁移到 SQL Server 要改 `WHERE` 大小写）。

**③ CRUD + 激活服务 `RecipeService`**（🟦+🟧）

```csharp
// DaqMonitor.Core/Recipes/RecipeService.cs
public class RecipeService
{
    private readonly IDbContextFactory<AppDb> _factory;     // M4 / M9 的 DI 注入
    private readonly IAuditLogger _audit;

    public RecipeService(IDbContextFactory<AppDb> factory, IAuditLogger audit)
    { _factory = factory; _audit = audit; }

    public async Task<Recipe> CreateAsync(string name, string version,
                                          RecipeParameter[] parameters, string user)
    {
        await using var db = _factory.CreateDbContext();   // M4:短生命周期,用完即弃
        var recipe = new Recipe
        {
            Name = name,
            Version = version,
            CreatedBy = user,
            ParametersJson = JsonSerializer.Serialize(parameters),
        };
        db.Recipes.Add(recipe);
        await db.SaveChangesAsync();                       // 唯一约束会在这里拦重复
        await _audit.LogAsync(user, "CreateRecipe", $"{name}@{version}", null, null);
        return recipe;
    }

    public async Task ActivateAsync(int recipeId, string user)
    {
        await using var db = _factory.CreateDbContext();
        var target = await db.Recipes.FindAsync(recipeId)
                     ?? throw new InvalidOperationException("配方不存在");

        // 同名配方只允许一个 IsActive:true
        var siblings = db.Recipes.Where(r => r.Name == target.Name && !r.IsDeleted);
        foreach (var s in siblings) s.IsActive = (s.Id == recipeId);

        await _audit.LogAsync(user, "ActivateRecipe", $"{target.Name}@{target.Version}", null, null);
        await db.SaveChangesAsync();
    }

    // 软删除(保留历史,不真删行)
    public async Task DeleteAsync(int recipeId, string user)
    {
        await using var db = _factory.CreateDbContext();
        var r = await db.Recipes.FindAsync(recipeId)
                ?? throw new InvalidOperationException("配方不存在");
        if (r.IsActive) throw new InvalidOperationException("不能删除当前生效配方,先切换到其他版本");
        r.IsDeleted = true;
        await _audit.LogAsync(user, "DeleteRecipe", $"{r.Name}@{r.Version}", null, null);
        await db.SaveChangesAsync();
    }

    public async Task<Recipe?> GetActiveAsync(string name)
    {
        await using var db = _factory.CreateDbContext();
        return await db.Recipes
            .Where(r => r.Name == name && r.IsActive && !r.IsDeleted)
            .FirstOrDefaultAsync();
    }
}
```

**逐行白话**：
- `CreateAsync`：唯一约束兜底，重复的 `Name+Version` 直接抛异常，不用自己加锁。
- `ActivateAsync`：先把同名所有配方的 `IsActive` 算出来「只有目标是 true」，再一次性 `SaveChanges`——**EF Core 在一个事务里把所有兄弟置 false、目标置 true**，避免「先 A 置 false 再 B 置 true」之间被人读到「没有任何生效」的中间态。
- `DeleteAsync`：**禁止删当前生效配方**——否则设备就找不到下个版本可切，必须先切到别的再删。这条规则面试讲出来直接证明你做过。

**④ 审计入口 `IAuditLogger` + `AuditEntry`**（🟦）
审计是「谁什么时候改了啥」的黑匣子。定义成接口便于 Mock 测试（关联 M9 的 DI 思想）：

```csharp
// DaqMonitor.Core/Audit/AuditEntry.cs
public class AuditEntry
{
    public int Id { get; set; }
    public string User { get; set; } = "";
    public string Action { get; set; } = "";     // "ActivateRecipe"
    public string Target { get; set; } = "";     // "饮料瓶_500ml@1.2.0"
    public string? Detail { get; set; }          // 可选 JSON(改了哪些字段)
    public DateTime At { get; set; } = DateTime.UtcNow;
}

public interface IAuditLogger
{
    Task LogAsync(string user, string action, string target, string? before, string? after);
}
```

> **为什么审计独立成服务**：① 单测能 Mock（M9 思想）；② 以后想接 Serilog（M6）或上报 MES（M7）只换实现，不动业务代码。

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ JSON 列查询性能 | 按参数过滤（`WHERE json_extract(ParametersJson,'$.temp')>80`）走不了普通索引，高频查要建 **生成列 + 索引** 或把热字段提到关系列 |
| ⭐ 并发激活（同名 IsActive 唯一） | 用 EF Core 事务把「兄弟置 false + 目标置 true」一次性提交，避免中间态被人读到「没有任何生效」 |
| ⭐ 软删除而不是硬删 | 配方是工艺历史，硬删就回滚不了；partial index 让软删的版本号可重建 |
| 🔥 禁止删除生效配方 | 删了设备找不到下个版本，必须先切到别的再删——这条规则是面试加分点 |
| 🔥 参数验证放 Service 层 | `temperature_setpoint` 超过物理上限（如 > 500℃）应在 `CreateAsync` 拦下，不是写库后才报错 |
| 🔥 审计追溯 | 创建 / 激活 / 删除 / 应用全部走 `IAuditLogger`，无审计的配方系统过不了 GMP / ISO 审计 |
| 🔥 权限控制 | 编辑 / 激活 / 删除必须分角色（操作员只能切，工艺工程师才能改），见 Day 3 末尾 |

### 🧪 今日三档练习
- 🟢 **基础题**：建 `Recipe` + `AppDb`（含 `Recipes` DbSet），用 `RecipeService.CreateAsync` 写一份「`饮料瓶_500ml@1.0.0`」配方（含 `temperature_setpoint=185℃`、`pressure_setpoint=120bar`），并 `GetActiveAsync("饮料瓶_500ml")` 验证 `null`（因为还没激活）。
  **✅ 答案**：调用 `CreateAsync("饮料瓶_500ml","1.0.0", new[]{ new RecipeParameter{Key="temperature_setpoint",Value=185,Unit="℃"} }, "u1")` 后查 `GetActiveAsync` 返回 `null`（IsActive 默认 false）。再调 `ActivateAsync(id,"u1")` 后 `GetActiveAsync` 返回该对象。
- 🟡 **进阶题**：同一 `Name` 下插两个相同 `Version`，验证第二次抛 `DbUpdateException`（唯一约束生效）；并写一个 xUnit 测试用 Moq 替身 `IAuditLogger`，验证激活时 `LogAsync` 被调用一次、`action == "ActivateRecipe"`。
- 🔴 **挑战题**：给 `RecipeService` 加一个 `ValidateParameters` 私有方法，按一份硬编码的「参数白名单 + 范围表」（如 `temperature_setpoint ∈ [0, 500]`）校验，超出范围抛 `ArgumentException`；写测试覆盖「正常 / 超上限 / 未知 key」三种用例。

### 💡 工控导师说
> 我在某注塑厂做过一个项目：操作员误删了正在用的配方，机器停了 4 小时找不回参数，损失几万块。从那以后我们做了两条铁律：① 配方**只软删不硬删**；② **当前生效配方不允许删除**（先切换才能删）。这两条是面试时讲「配方管理」最有说服力的故事。
> 另一条：JSON 列很灵活，但**别滥用**。如果某个参数（如 `temperature_setpoint`）几乎所有报表都要按它过滤，提到关系列建索引；JSON 列只放「偶尔回读、几乎不查」的参数。

### 🎓 职业建议
13-15K 的 JD 里，配方管理偶尔出现，但**一旦出现你做过就是差异化**。面试时能讲出：「我用 EF Core 的 partial index 做了软删 + 唯一约束」「激活用事务保证同名只有一个生效」「审计走独立接口便于 Mock 测试」——这三条直接证明你做过生产级配方系统，不是「写个 CRUD 就完事」。

### 📅 明日预告
Day 2 讲**配方下发到设备 + 实时切换**——把 JSON 参数转成 PLC 地址写入，关键点是「写入 + 回读校验 + 失败回滚」的事务思想，对应工厂最怕的「切到一半配方」。

---

## Day 2 — 配方下发到设备 + 实时切换

### 一句话讲清楚
配方切换 = 把 JSON 参数按「地址映射表」转成一组 `PlcDevice.Write(addr, value)`，**前端类比：切换主题（切换前先卸载旧主题，逐项加载新参数，加载失败回滚到旧主题）**。

### 前端类比秒懂
| 配方切换 | 前端类比 |
|---|---|
| 地址映射表 `_addressMap` | i18n 资源文件的「key → 文本」映射 |
| 暂停采集 → 写入 → 恢复 | `loading=true → fetch → loading=false` |
| 回读校验 | 写完 input 再 `expect(getValue()).toBe(setValue)` |
| 失败回滚 | 切主题失败,回滚到原主题的 `<link href>` |
| `RecipeApplyException` | 自定义 Error 子类,带上下文（哪步炸的） |

### 分点精讲

**① 地址映射表：参数 key → PLC 地址**（🟦）
配方里存的是「逻辑参数名」（`temperature_setpoint`），但 PLC 只认「DB 块地址」（`DB1.DBD100`）。映射表是两者的桥梁——和前端的 i18n 字典一样，集中在一处维护，不散在代码里。

```csharp
// DaqMonitor.Core/Recipes/RecipeApplier.cs
public class RecipeApplier
{
    private readonly PlcDevice _plc;             // M3 的 PLC 设备抽象
    private readonly RecipeService _recipes;
    private readonly IAuditLogger _audit;

    // 地址映射:参数 key → PLC DB 偏移(DB1 内的偏移,DWORD=4 字节)
    private readonly Dictionary<string, int> _addressMap = new()
    {
        ["temperature_setpoint"] = 100,   // DB1.DBD100
        ["pressure_setpoint"]    = 104,   // DB1.DBD104
        ["cycle_time"]           = 108,   // DB1.DBD108
        ["holding_time"]         = 112,   // DB1.DBD112
    };

    public RecipeApplier(PlcDevice plc, RecipeService recipes, IAuditLogger audit)
    { _plc = plc; _recipes = recipes; _audit = audit; }
}
```

**逐行白话**：
- `_addressMap`：硬编码在工程里，**也可以从配置文件读**（`appsettings.json` 的 `RecipeMaps` 节）。前端类比：从硬编码常量改成 i18n 资源文件加载。
- `PlcDevice` 是 M3 讲过的 PLC 抽象（S7 / Modbus 都包成一个 `PlcDevice`），这里假设它有 `Read(addr)` / `Write(addr, value)`。

**② 切换流程：暂停 → 写入 → 回读校验 → 恢复（含失败回滚）**（🟦）

```csharp
    public async Task ApplyAsync(int recipeId, string user, CancellationToken ct)
    {
        // 1. 取配方 + 解析参数
        var recipe = await _recipes.GetAsync(recipeId)
                     ?? throw new InvalidOperationException("配方不存在");
        var parameters = JsonSerializer.Deserialize<RecipeParameter[]>(recipe.ParametersJson)
                         ?? throw new InvalidOperationException("配方参数无效");

        // 2. 切换前快照旧值(用于失败回滚)
        var snapshot = new Dictionary<string, double>();
        foreach (var p in parameters)
        {
            if (!_addressMap.TryGetValue(p.Key, out var addr))
                throw new InvalidOperationException($"参数 {p.Key} 没有地址映射,无法下发");
            snapshot[p.Key] = _plc.Read(addr);
        }

        // 3. 暂停采集(避免写入过程被读出"中间态"值)
        _plc.SuspendAcquisition();
        try
        {
            // 4. 逐项写入 + 回读校验
            foreach (var p in parameters)
            {
                ct.ThrowIfCancellationRequested();          // 用户取消立刻停
                int addr = _addressMap[p.Key];
                _plc.Write(addr, p.Value);

                // 回读校验:写入 50.0,读出来应在 49.9-50.1 之间(1% 容差)
                double readBack = _plc.Read(addr);
                double tolerance = Math.Max(Math.Abs(p.Value) * 0.01, 0.1);
                if (Math.Abs(readBack - p.Value) > tolerance)
                    throw new InvalidOperationException(
                        $"写入 {p.Key}={p.Value},回读 {readBack},超出 1% 容差,校验失败");
            }

            // 5. 全部成功:激活 + 审计
            await _recipes.ActivateAsync(recipeId, user);
            await _audit.LogAsync(user, "ApplyRecipe",
                $"{recipe.Name}@{recipe.Version}", null, recipe.ParametersJson);
        }
        catch (Exception ex)
        {
            // 6. 失败回滚:把所有参数写回旧值
            foreach (var kv in snapshot)
                _plc.Write(_addressMap[kv.Key], kv.Value);
            await _audit.LogAsync(user, "ApplyRecipeFailed",
                $"{recipe.Name}@{recipe.Version}", null, ex.Message);
            throw new RecipeApplyException($"配方 {recipe.Name}@{recipe.Version} 应用失败,已回滚", ex);
        }
        finally
        {
            _plc.ResumeAcquisition();   // 无论成败都恢复采集
        }
    }
```

```csharp
// 自定义异常,带上下文(对应前端的 class AppError extends Error)
public class RecipeApplyException : Exception
{
    public RecipeApplyException(string msg, Exception inner) : base(msg, inner) { }
}
```

**逐行白话**：
- **快照 `snapshot`**：切换前先把 PLC 当前值读出来备份。万一中途失败，按快照逐项写回，**回到切换前的状态**而不是「半个新配方 + 半个旧配方」。这是工厂最怕的「配方写到一半炸了」的兜底。
- `SuspendAcquisition` / `ResumeAcquisition`：写入期间暂停后台轮询（M2 / M3 讲过的 `PollLoop`），避免 UI 读到「温度已切到 200 但压力还是旧值」的中间态。
- **回读校验**：写完立刻读回来比，差超过 1% 就抛异常。理由：PLC 通信可能丢包 / 写入失败但没报错（M3 讲过的「写完不回读」是现场最隐蔽的坑），**必须显式校验**。
- **`finally` 恢复采集**：无论成功失败，最后都要 `ResumeAcquisition`。`finally` 块保证即使异常抛出也不漏。前端类比：`try { fetch() } finally { setLoading(false) }`。
- **`CancellationToken`**：用户在切换中途按「取消」能立即停下，不卡 UI。关联 M0 Day7 / M9 讲的优雅退出。

**③ UI 实时刷新（事件通知）**

```csharp
// RecipeApplier 加一个事件,UI 订阅(关联 M0 Day6 观察者模式)
public event EventHandler<RecipeAppliedEvent>? RecipeApplied;
public record RecipeAppliedEvent(string Name, string Version, bool Success, string? Error);

// UI 端(MainActivityViewModel)
_applier.RecipeApplied += (s, e) =>
{
    Application.Current.Dispatcher.Invoke(() =>   // 跨线程更新 UI(M5)
    {
        StatusText = e.Success
            ? $"已切换到 {e.Name}@{e.Version}"
            : $"切换失败:{e.Error}(已回滚)";
    });
};
```

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ 快照 + 回滚 | 切换前读旧值备份,失败时按快照写回。**没有回滚的配方切换是定时炸弹** |
| ⭐ 回读校验 | 写完立刻读回来比,1% 容差内才算成功。PLC 写入可能「不报错但没写进去」 |
| ⭐ 暂停 / 恢复采集 | 写入期间停后台轮询,避免 UI 读到中间态 |
| 🔥 写入中途异常 | 必须 `try/catch/finally`,`finally` 里恢复采集,否则线程泄漏 + 采集永远停 |
| 🔥 大配方写入慢 | 几十个参数逐项写 + 回读可能要几秒,UI 要给进度条 / 可取消(`CancellationToken`) |
| 🔥 并发切换 | 同一设备不能两个操作员同时切配方,`ApplyAsync` 入口加 `SemaphoreSlim(1,1)` 串行化 |
| 🔥 设备掉线 | 写入前先检查 `_plc.IsConnected`,掉线直接抛 `RecipeApplyException`,别等到回读失败 |

### 🧪 今日三档练习
- 🟢 **基础题**：用 Moq 造一个 `PlcDevice`，`Setup` 它的 `Read/Write`，调 `ApplyAsync(recipeId,"u1",default)`，验证 `Write` 被调用的次数 = 参数数量，且最后 `ActivateAsync` 被调用了。
  **✅ 答案**：`mock.Verify(p => p.Write(It.IsAny<int>(), It.IsAny<double>()), Times.Exactly(parameters.Length))`；`recipeServiceMock.Verify(s => s.ActivateAsync(recipeId,"u1",default), Times.Once)`。
- 🟡 **进阶题**：让 Moq 的 `Read` 在第二个参数（`pressure_setpoint`）回读时返回一个偏离写入值 5% 的值，验证 `ApplyAsync` 抛 `RecipeApplyException`，且**所有参数被写回旧值**（用 `Verify` 检查 `Write` 被调了「参数数 × 2」次：N 次新值 + N 次回滚）。
- 🔴 **挑战题**：给 `RecipeApplier` 加 `SemaphoreSlim(1,1)` 串行化入口，写两个并发测试：① 两个 `ApplyAsync` 串行执行（第二个等第一个完成）；② 第二个在 `CancellationToken` 触发后立即抛 `OperationCanceledException` 而不是死等。

### 💡 工控导师说
> 切配方是工厂里**最容易出事故**的操作——温度设定一下从 180℃ 切到 220℃，注塑件可能直接烧糊、模具受损。我做过一个项目，配方切换写了一半 PLC 通信断了，机器停在「温度 220 但保压时间还是旧的 5 秒」，废了一批料。从那以后我们：① 强制回读校验；② 强制失败回滚；③ 切换期间屏幕大红字「正在切换配方，禁止开模」，操作员一秒不能马虎。这三条进简历就是「做过生产级配方切换」。
> 另一条：**别在切换里搞「写完一项立刻校验」 vs 「全部写完再校验」的犹豫**。前者更早发现失败、回滚的数据少，**永远是正确选择**。

### 🎓 职业建议
面试时被问「你怎么保证配方切换的可靠性」，能讲出「快照 + 回读校验 + 失败回滚 + 暂停采集 + finally 恢复」五点的人，比只说「调 PLC.Write 写下去」的人薪资能多 2K。这五点对应「事务」思想——SQL 数据库的事务你早懂了，把这套思想搬到 PLC 写入就是配方切换的工程化。

### 📅 明日预告
Day 3 讲**配方导入导出 + 版本对比**——用 ClosedXML 把配方导出 Excel 给工艺工程师离线改、JSON 导入做校验，再做两个版本的 key-value diff（像 Git diff），这是配方系统最后一个能打的牌。

---

## Day 3 — 配方导入导出 + 版本对比

### 一句话讲清楚
配方支持 Excel / JSON 导入导出，版本对比像 Git diff，**前端类比：导出 JSON 配置 + 在线 diff 工具**——工艺工程师在自己电脑上离线改 Excel，传回上位机导入校验，diff 一眼看出改了啥。

### 前端类比秒懂
| 配方导入导出 / diff | 前端类比 |
|---|---|
| Excel 导出（ClosedXML） | SheetJS (`xlsx`) 前端导出 |
| Excel 导入 + schema 校验 | 表单提交前 `zod` / ` Yup ` 校验 |
| JSON 导入 | `JSON.parse` + schema 验证 |
| 版本 diff（增 / 删 / 改） | Git diff / `diff-match-patch` |
| 配方权限（只读 / 编辑 / 激活） | RBAC 中间件 |

### 分点精讲

**① Excel 导出（ClosedXML，沿用 M10）**（🟧）

```csharp
// DaqMonitor.Core/Recipes/RecipeExporter.cs
using ClosedXML.Excel;

public static class RecipeExporter
{
    public static void ExportToExcel(Recipe recipe, string path)
    {
        using var wb = new XLWorkbook();
        var ws = wb.Worksheets.Add("配方");
        // 头部:配方信息
        ws.Cell(1,1).Value = "配方名"; ws.Cell(1,2).Value = recipe.Name;
        ws.Cell(2,1).Value = "版本";   ws.Cell(2,2).Value = recipe.Version;
        ws.Cell(3,1).Value = "创建人"; ws.Cell(3,2).Value = recipe.CreatedBy;
        ws.Cell(4,1).Value = "创建时间"; ws.Cell(4,2).Value = recipe.CreatedAt;

        // 参数表(从 JSON 反序列化)
        var parameters = JsonSerializer.Deserialize<RecipeParameter[]>(recipe.ParametersJson)
                         ?? Array.Empty<RecipeParameter>();
        ws.Cell(6,1).Value = "Key"; ws.Cell(6,2).Value = "Value";
        ws.Cell(6,3).Value = "Unit"; ws.Cell(6,4).Value = "Description";
        int row = 7;
        foreach (var p in parameters)
        {
            ws.Cell(row,1).Value = p.Key;
            ws.Cell(row,2).Value = p.Value;
            ws.Cell(row,3).Value = p.Unit;
            ws.Cell(row,4).Value = p.Description ?? "";
            row++;
        }
        ws.Columns().AdjustToContents();
        wb.SaveAs(path);
    }
}
```

**逐行白话**：和 M10 的报表导出同款套路，只是数据源换成「配方的 JSON 参数」。`using` 包 `XLWorkbook` 保证文件锁释放（M10 已强调）。工艺工程师拿到这个 Excel 离线改完，用下面的导入。

**② Excel / JSON 导入 + schema 校验**（🟦+🟧）

```csharp
// DaqMonitor.Core/Recipes/RecipeImporter.cs
public class RecipeImporter
{
    private readonly RecipeService _service;

    // 参数白名单 + 范围(真实项目从 appsettings.json 读)
    private readonly Dictionary<string, (double Min, double Max)> _ranges = new()
    {
        ["temperature_setpoint"] = (0, 500),
        ["pressure_setpoint"]    = (0, 300),
        ["cycle_time"]           = (0, 600),
        ["holding_time"]         = (0, 3600),
    };

    public RecipeImporter(RecipeService service) => _service = service;

    public async Task<Recipe> ImportFromJsonAsync(string json, string name,
                                                  string version, string user)
    {
        // 1. schema 校验:能不能反序列化
        var parameters = JsonSerializer.Deserialize<RecipeParameter[]>(json)
                         ?? throw new ArgumentException("JSON 不符合 RecipeParameter[] 格式");

        // 2. 业务校验:key 白名单 + value 范围
        foreach (var p in parameters)
        {
            if (!_ranges.TryGetValue(p.Key, out var range))
                throw new ArgumentException($"未知参数 key:{p.Key}");
            if (p.Value < range.Min || p.Value > range.Max)
                throw new ArgumentException(
                    $"参数 {p.Key}={p.Value} 超出允许范围 [{range.Min}, {range.Max}]");
        }

        // 3. 校验通过,交给 Service 入库
        return await _service.CreateAsync(name, version, parameters, user);
    }
}
```

**逐行白话**：
- **两层校验**：① schema 校验（JSON 能不能解析成 `RecipeParameter[]`）；② 业务校验（key 在白名单、值在范围内）。前端类比：先 `zod.parse()` 验类型，再 `if (value > MAX)` 验业务。
- 校验失败抛 `ArgumentException`，调用方决定 UI 怎么提示（红字 / 弹窗）。**永远不要把不合法的配方写库**——回去重新改。

**③ 版本 diff：两个配方的 key-value 对比**（🟦）

```csharp
// DaqMonitor.Core/Recipes/RecipeDiff.cs
public record RecipeDiff(
    IReadOnlyList<string> Added,                                      // b 有,a 没有
    IReadOnlyList<string> Removed,                                    // a 有,b 没有
    IReadOnlyDictionary<string, (double Old, double New)> Changed);   // 都有但值变

public class RecipeDiffer
{
    public RecipeDiff Compare(Recipe a, Recipe b)
    {
        var pa = JsonSerializer.Deserialize<Dictionary<string, double>>(a.ParametersJson)
                 ?? new Dictionary<string, double>();
        var pb = JsonSerializer.Deserialize<Dictionary<string, double>>(b.ParametersJson)
                 ?? new Dictionary<string, double>();

        return new RecipeDiff(
            Added:   pb.Keys.Except(pa.Keys).ToList(),
            Removed: pa.Keys.Except(pb.Keys).ToList(),
            Changed: pa.Keys.Intersect(pb.Keys)
                .Where(k => Math.Abs(pa[k] - pb[k]) > 0.001)         // 浮点比较用容差
                .ToDictionary(k => k, k => (pa[k], pb[k]))
        );
    }

    // 人可读的 diff 文本(像 git diff)
    public string FormatDiff(Recipe a, Recipe b, RecipeDiff diff)
    {
        var sb = new StringBuilder();
        sb.AppendLine($"diff {a.Name}@{a.Version} → {b.Name}@{b.Version}");
        foreach (var k in diff.Added)   sb.AppendLine($"+ {k}");                       // 新增
        foreach (var k in diff.Removed) sb.AppendLine($"- {k}");                       // 删除
        foreach (var (k, v) in diff.Changed)
            sb.AppendLine($"~ {k}: {v.Old} → {v.New}");                                // 修改
        return sb.ToString();
    }
}
```

**逐行白话**：
- `Keys.Except` / `Intersect`：LINQ 集合运算，前端类比 JS 的 `arr1.filter(x => !arr2.includes(x))`。
- 浮点比较用 `> 0.001` 容差，不用 `==`——浮点 `0.1 + 0.2 != 0.3` 是经典坑（M2 讲过 IEEE754）。
- `FormatDiff` 输出像 `git diff` 的格式：`+ 新增 / - 删除 / ~ 修改`。UI 绑定到文本框直接展示。

**④ 配方权限（RBAC，基于角色）**（🟦）
真实工厂里：操作员只能「切换」，工艺工程师能「编辑 + 切换」，管理员才能「删除」。基于角色拦截：

```csharp
// DaqMonitor.Core/Recipes/RecipePermissions.cs
public enum RecipePermission { Activate, Edit, Delete }

public static class RecipePermissions
{
    // 角色 → 允许的操作
    private static readonly Dictionary<string, HashSet<RecipePermission>> _roleMap = new()
    {
        ["Operator"]     = new() { RecipePermission.Activate },
        ["Engineer"]     = new() { RecipePermission.Activate, RecipePermission.Edit },
        ["Administrator"]= new() { RecipePermission.Activate, RecipePermission.Edit, RecipePermission.Delete },
    };

    public static bool Can(string role, RecipePermission p)
        => _roleMap.TryGetValue(role, out var perms) && perms.Contains(p);
}

// Service 入口加守卫
public async Task DeleteAsync(int recipeId, string user, string role)
{
    if (!RecipePermissions.Can(role, RecipePermission.Delete))
        throw new UnauthorizedAccessException($"角色 {role} 无删除权限");
    // ... 原有删除逻辑
}
```

> **前端类比**：和前端路由守卫 `if (!hasRole('admin')) redirect('/login')` 一模一样，只是这里拦的是「业务操作」而不是「页面跳转」。

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ 导入两层校验 | schema(JSON 能不能解析) + 业务(key 白名单 / 值范围),永远别把不合法配方写库 |
| ⭐ Excel 模板下载 | 导入前先让用户下载「标准模板」,避免他自己乱排版 |
| ⭐ diff 用容差比较浮点 | `Math.Abs(a-b) > 0.001`,别用 `==`,IEEE754 经典坑 |
| 🔥 Excel 单元格类型 | ClosedXML 的 `Cell.Value = double` 才是数值;赋 string `"3.14"` 会变文本,后续公式失效 |
| 🔥 导入并发覆盖 | 两人同时改一份 Excel 上传,后写的覆盖先写的;加乐观锁(`RowVersion` 列) |
| 🔥 权限拦截放 Service | 别只在 UI 隐藏按钮,Service 入口必须二次校验,API 直调绕不过 |
| 🔥 导入大文件 | 几千参数的配方 Excel 读取慢,用 `IXLWorksheet.RowsUsed()` 别遍历整张表 |

### 🧪 今日三档练习
- 🟢 **基础题**：用 `RecipeExporter.ExportToExcel` 导出一份配方，用 Excel 打开确认表头和参数都对。再用 `RecipeImporter.ImportFromJsonAsync` 把同一份 JSON 字符串导入并校验通过。
- 🟡 **进阶题**：写一份「`temperature_setpoint=9999`」（超 500 上限）的 JSON，调用 `ImportFromJsonAsync`，验证抛 `ArgumentException` 且消息包含「超出允许范围」；写测试覆盖「正常 / 超上限 / 未知 key」三种用例。
- 🔴 **挑战题**：给 `RecipeDiffer.Compare` 写 xUnit 测试：构造两份配方（a 有 `temp=180`、`pressure=120`；b 有 `temp=185`、`cycle_time=30`），断言 `Added=["cycle_time"]`、`Removed=["pressure"]`、`Changed={"temp":(180,185)}`。再用 `FormatDiff` 输出文本，肉眼看像 `git diff`。

### 💡 工控导师说
> 我在某食品厂，工艺工程师不在上位机改配方（嫌界面慢），喜欢在自己电脑 Excel 里改完发邮件。我们做了「Excel 模板下载 + 导入校验」之后，他改完上传，系统自动 diff 给他看「你改了 3 个参数：temp 180→185、pressure 120→125、新增 cycle_time=30」，他确认才入库。**这套流程是工厂最欢迎的**——尊重用户习惯 + 强制校验兜底。
> 权限那条更关键：没权限控制的配方系统，操作员一不小心删了工艺工程师调好的配方，事故就来了。RBAC 是「企业级」和「玩具」的分水岭。

### 🎓 职业建议
能讲出「Excel 模板下载 + 导入校验 + diff 展示 + RBAC」四点的候选人，面试官会直接把你定位为「做过企业级上位机」——这是 15K 岗位的差异化。配方管理不是「写个 CRUD」就完事，**真正值钱的是这套围绕配方的工程化流程**。

---

## 📌 温故知新（跨模块联动）
- **M4 EF Core → 这里 DbContext + 唯一约束**：`Recipe` 实体 / `IDbContextFactory` 短生命周期 / 迁移 partial index，全部沿用 M4 那套，不重复学。
- **M9 工程素养 → 这里分层 + 测试**：`RecipeService` 放 Core，UI 只调不写业务；`IAuditLogger` 抽接口便于 Moq；测试覆盖「重复 Version / 超范围参数 / 回滚」三类用例，正是 M9 讲的「测行为不测实现」。
- **M10 报表 → 这里 Excel 导出**：ClosedXML 同款用法，只是数据源换成「配方参数」，关联 M10 的 `ReportExporter`。
- **M2/M3 PLC → 这里地址映射 + 写入**：`PlcDevice.Write(addr, value)` 是 M2 / M3 的设备抽象；写入 + 回读校验正是 M3 讲过的「PLC 写完不回读」的工程化。
- **M6 日志 → 这里审计**：`IAuditLogger` 实现可桥接到 Serilog（M6），统一进一个日志管道。
- **前瞻（未来模块）**：MES / SCADA 上报——配方变更通过 M7 的 MQTT 发给车间系统；电子签名（FDA 21 CFR Part 11）——审计 + 权限的合规化升级。

## 📚 延伸阅读（卡点时点开）
- **ISA-88 批次控制标准**：国际通用的「配方管理 + 批次生产」标准，化工 / 制药行业必读，<https://www.isa.org/standards-and-publications/isa-standards/isa-standards-committees/isa88>
- **S88 配方管理模型**： procedural / formula / equipment 三层模型，面试讲「我参考 ISA-88 设计」直接加分，<https://en.wikipedia.org/wiki/S88>
- **FDA 21 CFR Part 11**：电子记录 + 电子签名法规（制药 / 医疗器械），配方审计的法理依据，<https://www.fda.gov/regulatory-information/search-fda-guidance-documents>
- EF Core 唯一索引 + partial index：<https://learn.microsoft.com/ef/core/modeling/indexes>
- ClosedXML（Excel）：<https://github.com/ClosedXML/ClosedXML>
- 全部模块外链汇总见 `外部链接索引.md`

## 🏗️ 项目任务（边学边做，落到 DAQMonitor）
1. 在 `src/DaqMonitor.Core/Recipes/` 加 `Recipe.cs` / `RecipeParameter.cs` / `RecipeService.cs` / `RecipeApplier.cs` / `RecipeDiffer.cs` / `RecipeImporter.cs` / `RecipeExporter.cs` / `RecipePermissions.cs`。
2. 在 `src/DaqMonitor.Core/Audit/` 加 `AuditEntry.cs` + `IAuditLogger`（实现可写 Serilog，关联 M6）。
3. 在 `src/DaqMonitor.Core/Persistence/AppDb.cs`（M4 已有）加 `Recipes` + `AuditLog` DbSet，配 partial index 唯一约束。
4. 在 `Bootstrapper`（M9 DI）注册 `IDbContextFactory<AppDb>` / `RecipeService` / `RecipeApplier` / `IAuditLogger`。
5. UI 加「配方管理」页：列表 + 新建 / 编辑 / 删除 / 激活 / Excel 导入导出 / diff 对比 + 一个「RBAC 角色选择下拉框」演示权限。
6. 配套 xUnit 测试（`DaqMonitor.Tests`）：重复 Version 拦截、超范围参数、激活原子性、diff 输出、回滚行为——目标 6+ 个测试全绿。

## 模块交付清单（M18）
- [ ] `Recipe` + `RecipeParameter` 数据模型（含软删 + JSON 参数列）
- [ ] EF Core 唯一约束（`Name + Version`）+ partial index（仅未软删）
- [ ] `RecipeService` CRUD + `ActivateAsync`（事务保证同名唯一生效）
- [ ] `IAuditLogger` + `AuditEntry` 审计管道（创建 / 激活 / 删除 / 应用全记录）
- [ ] `RecipeApplier` 含快照 + 回读校验 + 失败回滚 + 暂停 / 恢复采集
- [ ] `RecipeExporter`（ClosedXML）/ `RecipeImporter`（两层校验）
- [ ] `RecipeDiffer` 版本对比（增 / 删 / 改三类）
- [ ] `RecipePermissions` RBAC（Operator / Engineer / Administrator）
- [ ] DAQMonitor 配方管理 UI 页 + 6+ 个 xUnit 测试全绿

## ✅ 打卡
- [ ] Day 1 配方数据模型 + 增删改查 + 审计入口
- [ ] Day 2 配方下发到设备（快照 + 回读校验 + 失败回滚）
- [ ] Day 3 导入导出 + 版本 diff + RBAC 权限
- [ ] 🏗️ DAQMonitor 配方管理页跑通（含测试）
