# 📴 内网学习完全手册 · 量身定做版

> **写给谁**:白天在公司内网、无外网、想完整完成「学习+练习+理解」闭环的你。
> **核心结论先行**:这套资料的讲义全部自包含(代码经沙盒验证、无 CDN 依赖),项目自带零硬件测试(回环/模拟/进程内从站)——**内网不是降级环境,而是恰好匹配这套资料设计哲学的环境**。缺的只是 NuGet 包和 SDK,本文教你一次备齐。

---

## 〇、离线包使用时间线:什么时候用、怎么用(先看这节)

> **一句话定义**:离线学习包 = 家里电脑的一次「快照 + 预装弹药」。**在家学习永远直接用 `scada-learning` 仓库,不碰离线包;离线包只在两个时刻登场——带去内网之前(准备)、在内网机器上(使用)。**

### 生命周期三阶段

| 阶段 | 什么时候做 | 做什么 | 用时 |
|---|---|---|---|
| ① 首次准备 | 开始内网学习前,在家做**一次** | 跑 `在家准备离线包.ps1`(拉齐 NuGet)+ 可选下载 SDK zip → 整个文件夹拷进 U 盘 | 15-30 分钟 |
| ② 内网初始化 | 内网机器**第一天** | U 盘拷到内网机 →(🥇档)解压 SDK + 放 NuGet.config → 验证 restore/test 全绿 | 10 分钟 |
| ③ 日常循环 | **每个工作日** | 白天:内网用 site 学习 + 改代码;晚上:回家 3 分钟同步(代码合并 + 问题清单批改) | 每晚 3 分钟 |

### 组件速查:每个东西什么时候用

| 包里的东西 | 什么时候用 | 怎么用 |
|---|---|---|
| `起步说明.md` | 内网第一次打开包 | 读一遍,知道有什么 |
| `内网学习完全手册.md` | 随时,有疑问先翻它 | 本文,完整方案 |
| `site\` | **每天学习主入口** | 双击 `site\index.html` |
| `DAQMonitor\` | 教练计划🅑读档断点 / 🅐敲码 | 🥈档:直接跑 bin 下 exe;🥇档:VS 打开 sln,F5 |
| `ByteOrderLab\` | L0 课 / 字节序忘了 | 双击 bin 下 exe 重放实验 |
| `白天问题清单.txt` | 内网卡壳超 30 分钟时 | 按格式记一条;晚上回家逐条问 Cline |
| `在家准备离线包.ps1` | **只在家里跑,内网永远不用** | 家机执行,拉齐并拷贝 NuGet 缓存 |
| `NuGet.config.template` | 🥇档第一次在内网 build/test 前 | **拷进 `DAQMonitor\` 文件夹,改名为 `NuGet.config`**,内容零修改 |
| `nuget-packages\` | **只有🥇全开发档用**(见下方三问三答) | 不直接打开它——被 NuGet.config 引用,见下方三问三答 |
| `tools\sdk\`(自备) | 只有🥇档且内网没装 SDK 时 | 解压 zip,用 `<sdk路径>\dotnet.exe` 跑命令 |

### 📦 nuget-packages 三问三答(它到底是什么、什么时候才需要)

**它是什么**:225 个 NuGet 包的本地仓库(1.75GB)。作用 = 把 nuget.org 网站"装进 U 盘"。
你**永远不会直接打开或运行它**——它只在 `dotnet restore/build/add package` 时被自动读取。

**① 什么时候用?**——只有 🥇 全开发档的这 3 种时刻:
- 在内网机**第一次**跑 `dotnet restore` / `dotnet build` / `dotnet test` 之前(配一次,永久生效)
- 在内网机想 `dotnet add package 某新包` 时(从本地解析,如 R4 调库版装 FluentModbus)
- 换了台内网机器 / 全局缓存被清空时(重新配一次)

**什么时候不用?**——🥉纯看讲义(用法A)、🥈跑现成 exe(用法B)完全不需要它;
**在家里永远不用**(家里有网,dotnet 直接走 nuget.org,速度更快)。

**② 在哪用?**——位置固定两处,别挪:
```
离线学习包\nuget-packages\          ← 包仓库本体(只读,别动)
离线学习包\DAQMonitor\NuGet.config  ← 引用文件(从模板拷来,指向上面的路径)
```

**③ 怎么用?**——内网机上一共 3 步,配完就不用再管:
```powershell
# 1. 把包根目录的 NuGet.config.template 拷进 DAQMonitor\ 并改名:
Copy-Item "D:\offline\NuGet.config.template" "D:\offline\DAQMonitor\NuGet.config"
#    (保留离线学习包目录结构 → 相对路径 ..\nuget-packages 自动对上,内容零修改)

# 2. 进工程目录,验证(应见 "已还原"/restore 成功,全程无联网请求):
cd D:\offline\DAQMonitor
dotnet restore DaqMonitor.sln

# 3. 之后一切照常:build / test / add package 全部自动走本地包
dotnet test    # 85/85 绿 = 配置成功
```

> 原理一句话:模板里 `<clear/>` 清空了 nuget.org 等所有在线源 → dotnet 只认 `..\nuget-packages` 这个本地文件夹。
> ✅ 已金标准验证:空全局缓存+仅此离线源,net8.0 解决方案 restore/build 全通过(163 包实际从本地解析)。

### 三条铁律(防坑)

1. **家 = 仓库,内网 = 离线包,不要交叉改**:家里改代码永远改 `scada-learning\DAQMonitor`;内网改的是包里的副本——回家用 git/文件拷贝合并,别两边各自长出没同步的版本。
2. **site 以家为准,里程碑后重拷**:内网完成教练计划某课 / 家里站点有更新 → 回家 `python build_site.py` → 重拷 `site\` 进 U 盘 → 内网覆盖。
3. **离线包不是备份**:它不含 git 历史。内网写的代码怕丢,当天就拷回 U 盘(或推到内网 git)。

### 决策树:「我现在想做 X → 去哪」

```
我想看讲义/背面试题   → site\index.html
我想跑项目看数据流    → DAQMonitor\...\DaqMonitor.UI.exe(或 VS F5)
我想改代码/跑测试     → 🥇档:SDK + NuGet.config + dotnet build/test
我卡住了            → 术语表 → 知识点弹窗 → 记白天问题清单(晚上问)
我要下班了          → 拷当天改动 + 问题清单 → 回家合并 + 问问题
家里站点更新了       → 重拷 site\ 到 U 盘 → 内网覆盖
```

---

## 一、内网能做什么/不能做什么(诚实清单)

| 能(✅完全无网) | 不能(❌需要外网) |
|---|---|
| 读全部 77 页讲义/项目实践/面试题 | 点开讲义里的外部链接(延伸阅读) |
| 打卡、知识点弹窗、术语表 | `dotnet add package` 默认从 nuget.org 拉包 |
| 跑已编译的 DaqMonitor.UI.exe / ByteOrderLab | 安装 VS 在线组件 |
| 改代码、编译、跑 56 个测试(配好本地包源后) | 问 Cline(用"问题清单"替代) |
| 白纸手写代码肌肉题、对答案 | WebView2 大屏的 npm install(可在家预装) |
| 断点调试走数据流(教练计划 🅑 档核心动作) | — |

**一句话**:唯一的真障碍是"包怎么进来"——下面第三节解决。

## 二、环境三档(按内网机器权限选,先看你能到哪档)

| 档 | 前提 | 你获得的能力 |
|---|---|---|
| 🥉 纯阅读 | 无要求 | 双击 `site\index.html` 学完全部理论+背诵 |
| 🥈 跑程序 | 装了 .NET 桌面运行时 8/9/10 任一(RollForward 兜底) | + 运行 UI 看数据流 + 断点调试 |
| 🥇 全开发 | .NET SDK(便携 zip 版免安装) + 本地包源(第五节) | + 敲调库版/改功能/跑全部测试 = 完整闭环 |

> SDK 便携版获取:在家从 `https://dotnet.microsoft.com/download` 下载 **.NET SDK x64 Binaries(zip)**,解压即用,不需要管理员权限。U 盘约 300MB。

## 三、离线包全家福(讲义 × 包对照表)

### A. 工程实际引用(csproj 已锁版本,跟工程走)

| 包 | 用在哪 | 家里缓存 | 备注 |
|---|---|---|---|
| Microsoft.EntityFrameworkCore.Sqlite 8.0.10 | M4/R6 双写 | ✅ | 含 sqlite 原生库 |
| MQTTnet 4.3.7 | M7 上云 | ✅ | |
| System.IO.Ports 8.0 | M1/R4 真串口 | ✅ | |
| BCrypt.Net-Next 4.0.3 | M17 认证 | ✅ | |
| Microsoft.Extensions.DependencyInjection 8.0.1 | R7 组合根 | ✅ | |
| ClosedXML 0.102.3 | M10 Excel | ✅ | UI 侧 |
| LiveChartsCore.SkiaSharpView.WPF 2.0-rc4.5 | M5/R8 曲线 | ✅ | 含 SkiaSharp 全家 |
| xunit 2.9.2 / Moq 4.20.72 / TestSdk 17.11.1 | 全部测试 | ✅ | |

### B. 讲义练习用到、家里缓存还没有(必须在家预拉!)

| 包 | 哪篇讲义 | 用途 | 内网必要性 |
|---|---|---|---|
| **FluentModbus** | **R4 调库版(你的主路线)** | Modbus TCP 主从 | 🔴 必拉 |
| NModbus4 | M2 调库路线 | Modbus 老牌库 | 🟡 选拉(走手搓+FluentModbus 可不拉) |
| S7netplus | M3 PLC | S7 直连 | 🟡 M3 练习用 |
| Serilog + Sinks.File | M6 日志 | 结构化日志 | 🟡 M6 练习用 |
| Microsoft.EntityFrameworkCore.SqlServer / Pomelo...MySql / Dapper / Microsoft.Data.SqlClient | M12 多数据库 | 企业库 | 🟢 选做 M12 才要 |
| HslCommunication | M13 国产库 | 多品牌 PLC | 🟢 选做 M13 才要 |
| Microsoft.Extensions.Http.Polly + Polly.Extensions.Http | M17 MES 对接 | 重试熔断 | 🟢 选做 M17 才要 |
| HidLibrary | M16 USB-HID | HID 真实库 | ⚪ 可跳过(M16 用模拟通道) |
| Prism.DryIoc | M8.5 | 企业 MVVM | ⚪ M8.5 未进主线 |
| BenchmarkDotNet | M9.5 | 性能压测 | ⚪ 选做 |
| QuestPDF | M10 PDF | PDF 导出 | ⚪ 选做 |

### C. 工具软件(可选,非必需)

| 工具 | 讲义哪里用 | 内网必要吗 |
|---|---|---|
| Modbus Poll / Modbus Slave | M2/调库指南抓帧验证 | ❌ 讲义用**进程内 ModbusTcpServer** 测试,零硬件零工具 |
| Mosquitto + MQTTX | M7 本地 broker | 🟡 M7 练习想真连 broker 才要(可在家装完整个目录拷走,Mosquitto 绿色版) |
| com0com 虚拟串口 | M1/硬件替代 | ❌ R4 用 LoopbackSerialChannel,不需要 |
| Wireshark | M19 抓包 | ⚪ 选做 |
| Python + markdown 库 | 重建 site | ❌ site 已是成品;确要改 md 重建,在家重建再拷 site |

> **设计哲学点破**:这套资料的"零硬件"不只是教学妥协——回环通道、进程内从站、模拟卡,就是工业界正经的可测试性设计。内网学它,恰好是原汁原味。

## 四、在家准备流程(一次性,15 分钟)

1. 在家电脑跑 **`在家准备离线包.ps1`**(U 盘/移动硬盘接到家机):它会 ①还原两个工程拉准 A 表所有包 → ②建临时工程把 B 表全部包拉进缓存 → ③把 NuGet 缓存(~1.5GB)拷进离线学习包;
2. 手动下载(可选):.NET SDK 便携 zip(MSDN 官网)→ 放进 `离线学习包\tools\`;
3. 整个 `离线学习包` 文件夹拷进 U 盘 → 完成。

## 五、内网机器初始化(第一次,10 分钟)

```powershell
# 1️⃣ 把 U 盘内容拷到内网机任意盘,如 D:\offline\
# 2️⃣ (🥇档)解压 SDK zip 到 D:\offline\sdk,然后用它跑命令:
D:\offline\sdk\dotnet.exe --info

# 3️⃣ 关键一步:给工程配"只走本地"的包源
#    包根目录已备好 NuGet.config.template(相对路径版):
#    把它拷进 DAQMonitor 文件夹,改名为 NuGet.config 即可(保留离线学习包目录结构时零修改)。
#    目录结构不同才需要改里面的 value 路径。

# 4️⃣ 验证:断网状态下
D:\offline\sdk\dotnet.exe restore DaqMonitor.sln     # 应全部成功
D:\offline\sdk\dotnet.exe build                      # 0 错误
D:\offline\sdk\dotnet.exe test                       # 85/85 绿
```

> `NuGet.config` 的 `<clear/>` 是灵魂:清空 nuget.org,restore/add 全部只认本地文件夹——**这就是"内网搞到包"的完整答案**。以后 `dotnet add package FluentModbus` 也会从本地解析。
> ✅ **已在 2026-08-24 金标准验证**:空全局缓存 + 仅离线源,net8.0 解决方案 restore/构建全部成功(163 个包实际从离线文件夹解析),全部包按 .NET 8 兼容拉取。

## 六、内网每日学习 SOP(对应教练计划三档吃法)

```
① 打开 site\index.html → 教练计划 → 看今天该学哪课
② 🅑读档:VS/vscode 打开 DAQMonitor → 断点 → F5 走数据流 → 合上文档自己讲一遍(出声)
③ 🅐敲码:调库版/改功能 → dotnet build/test 全绿 → git commit(内网仓库或本地提交,回家 push)
④ 卡词 → 术语表;卡概念 → 📖 知识点弹窗;卡壳超 30 分钟 → 记「白天问题清单.txt」→ 降级🅒跳过
⑤ 白纸手写 1 道代码肌肉题(手册有答案,写完自己对)
⑥ 下班拷贝:当天改的代码 + 问题清单.txt 回家
```

**没有教练的自助三层**:术语表(60 词)→ KPOINTS 弹窗(30+ 概念)→ 问题清单(晚上批改)。90% 的卡点在前两层就能解——**讲义本来就把"为什么"写在正文里,内网读和联网读没有区别**。

## 七、练习在内网怎么做(零妥协方案)

| 练习类型 | 内网做法 | 完整度 |
|---|---|---|
| 讲义 L1/L2/L3 练习 | 照做(纯代码,无网依赖) | 100% |
| 代码肌肉白板题 | 白纸写 → 手册对答案 → 错题记清单 | 100% |
| 项目测试 | dotnet test(进程内从站/回环,零硬件) | 100% |
| Modbus 抓帧实验 | 用测试里的 ModbusTcpServer 当"设备" | 95%(等价替代) |
| WebView2 大屏 | **在家** npm install 完整个 node_modules 拷进 U 盘 | 100%(目录拷贝即用) |
| 间隔重复自测 | 讲义内嵌题目+白纸默写 | 100% |

## 八、回家同步(3 分钟仪式)

1. `git bundle` 或直接拷文件:内网改动 → 家里仓库 commit;
2. 问题清单 → 逐条问 Cline → 高频问题升级成站点新页;
3. site 有更新 → 重拷 `site\` 目录进 U 盘 → 明天内网就是新版。

## 九、合规提醒

带个人学习资料入内网前,确认公司制度允许(自学材料一般无碍;**涉密内网严禁私带存储介质**,先问 IT/主管一句话,别踩红线)。

---

**✅ 打卡[ ]**
