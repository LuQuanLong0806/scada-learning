# 上位机转行 13K · 学习计划总纲（手把手版）

> **目标**：从中高级前端工程师（0 上位机基础）→ 达到企业 **13K** 上位机开发岗位要求
> **方式**：用你已有的编程底子快速迁移，重点突破通信/PLC 短板，边学边做作品集
> **节奏**：8 周 56 天，每周 6 天学习 + 1 天灵活/复盘。你可按情况**加速或跳过已会部分**
> **进度可见**：每天学完在文末「打卡表」打勾，看到勾越来越多 = 你在进步

---

## 〇、看文档前先认这 3 类东西（不然会云里雾里）

C# 世界里「写出来能跑的东西」分三类来源。本讲义每个 Day 开头都会先列一张「今天用到的技术从哪来」，请对照看——**分清"天生物 / 类库 / 要装包"，你就不会迷糊了**。

- 🟦 **【C# 语法】** = 语言自带的关键字/写法。装好 .NET 就有，**不用装任何包**。
  例：`using`、`class`、`int`、`string`、`var`、`=>`
  （对照 TS：`let/const` → `var`；`: type` → 类型写在前面；`function` → `方法`）
- 🟩 **【.NET 类库 / BCL】** = 微软随 .NET 提供的"标准零件库"，`using` 对应命名空间即可用，**默认不用额外装包**。
  少数（如 `SerialPort`）需要加一行 `dotnet add package System.IO.Ports` 才能用——但它仍是**微软官方库，不是第三方**，只是被拆成了独立包。
  例：`Console`、`List<T>`、`Dictionary`、`Math`、`Task`、`HttpClient`、`System.Text.Json`、`LINQ`（`using System.Linq;`）
- 🟧 **【第三方 NuGet 包】** = 别人（或微软但独立发布）写好的库，**必须 `dotnet add package 包名` 安装**后才能 `using` 使用，不在 .NET 默认里。
  例：`NModbus`、`S7.Net`、`LiveCharts`、`Serilog`、`MQTTnet`、`EF Core`（`Microsoft.EntityFrameworkCore`）
  → 看到 `using Xxx;` 但报错"找不到类型/命名空间"，十有八九是要装 NuGet 包了。

> 💡 记忆口诀：**语法天生物、BCL 随 .NET、第三方要装包。**

---

## 一、13K 上位机岗位到底要什么（照着这个练，不浪费时间）

把招聘 JD 拆成「一票否决项」和「加分项」：

**必会（不会直接刷掉）：**
1. **C# / .NET 熟练** —— 你有编程底子，语言关最快
2. **WPF 或 WinForms 熟练** —— 你的 UI 经验直接复用（你的最大优势）
3. **串口通信（SerialPort）+ Modbus** —— 你的核心短板，投入最大精力
4. **至少一种 PLC 通信（西门子 S7 最常见）** —— 重点突破

**加分（有就更好，不强制）：**
- 数据库（SQL Server / MySQL / SQLite + EF Core）
- TCP/UDP 网络编程
- 多线程 / 异步（你已经会）
- OPC UA / MQTT
- 工业组态 / SCADA 概念

**你的差异化卖点（简历必须写）：**
- 现代 UI 开发能力（上位机界面普遍丑，这是真痛点 → 你降维打击）
- 完整软件工程经验（Git / 测试 / 架构 / CI）
- 跨界学习能力（前端 → 上位机就是最好证明）

---

## 二、为什么你能加速（别按零基础来）

| 你已会的（前端） | 直接迁移到 | 省下的时间 |
|---|---|---|
| TS 类型 / 接口 / 泛型 | C# 类型系统 / interface / 泛型 | 大半周 |
| React/Vue 组件化 | WPF 控件 + MVVM | 约 1 周 |
| 数据绑定 / 状态管理 | WPF Binding / ViewModel | 数天 |
| async/await / Promise | C# Task / async | 几乎不用学 |
| 数组 map/filter/reduce | LINQ（Select/Where） | 几乎不用学 |
| fetch / Axios | HttpClient | 几乎不用学 |
| JSON 处理 | System.Text.Json | 几乎不用学 |

→ **结论**：阶段 1-2（C# + WPF）你 2-3 周就能过，把省下的时间砸在阶段 3-5（通信 / PLC / 数据）。

---

## 三、8 周 56 天总览

| 周次 | 主题 | 核心目标 | 周末产出 |
|---|---|---|---|
| W1 | C# 语言基础 | 写控制台程序，理解类型/集合/类/委托 | 多个小程序 |
| W2 | C# 进阶 + WPF | 写桌面程序，MVVM 雏形 | JSON 小工具 |
| W3 | 通信基础 | 串口 / TCP / UDP 收发数据 | 通信测试工具 |
| W4 | Modbus 协议 | 读写寄存器、解析数据 | Modbus 客户端 |
| W5 | PLC 通信 | 连西门子 PLC 读写 DB | PLC 客户端 |
| W6 | 数据库 + 多线程 | 存采集数据、并发安全 | 数据服务模块 |
| W7 | 综合项目 | 数据采集监控系统 | **作品集核心** |
| W8 | 求职冲刺 | 简历 + 面试 + 投递 | **Offer** |

---

## 四、每日详细计划（56 天）

> 格式：**[主题] · 重点 → 练习 · 时长 · 打卡[ ]**
> 已会的内容直接跳过打卡即可；标注「重点突破」的别跳。

### ▶ 第 1 周：C# 语言基础（快速迁移，详见第五节讲义）
> 📌 **本周技术来源**：🟦 C# 语法（`class`/`var`/`using`/`=>`/`LINQ` 查询语法）+ 🟩 BCL（`Console`/`List<T>`/`Dictionary`/`System.Linq`/`Task`）。**零第三方包**，装好 .NET 8 SDK 即可跑。

- **Day 1** 环境 + 第一个程序 + C# 与 TS 对比 · 重点：.NET 8 SDK、VS、Hello World、输入输出 → 练习：两数求和程序 · 3-4h · 打卡[ ]
- **Day 2** 类型系统 · 重点：值类型/引用类型、var、可空、命名规范 → 练习：用 struct 表示一个「点」· 3h · 打卡[ ]
- **Day 3** 集合与 LINQ · 重点：List/Dictionary，Select/Where/GroupBy 对照 map/filter · 3h · 打卡[ ]
- **Day 4** 类与对象 · 重点：class、属性、构造、封装、static · 3h · 打卡[ ]
- **Day 5** 继承 / 接口 / 多态 / 泛型 · 对照 TS interface/泛型 · 3h · 打卡[ ]
- **Day 6** 委托 / 事件 / Lambda · 对照 JS 回调/事件 → 练习：做一个简单事件通知 · 3h · 打卡[ ]
- **Day 7** 周复习 + 小测 · 综合练习 5 题 · 2h · 打卡[ ]

### ▶ 第 2 周：C# 进阶 + WPF（你的主场，可加速）
> 📌 **本周技术来源**：🟦 C# 语法（`async`/`await`/`try-catch`/泛型）+ 🟩 BCL（`System.IO.File`、`System.Text.Json`）+ 🟩 **WPF**（微软官方桌面框架，csproj 加 `<UseWPF>true</UseWPF>`，**不是 NuGet 包**）。JSON 用 🟩 自带的 `System.Text.Json`，别误装 Newtonsoft。

- **Day 8** 异常 / 文件 IO / JSON 序列化 · try-catch、File、JsonSerializer → 练习：读 json 转对象 · 3h · 打卡[ ]
- **Day 9** 异步编程 async/await/Task · 你已会，深化取消令牌/CancellationToken · 2h · 打卡[ ]
- **Day 10** WPF 项目结构 + XAML 基础 · 对照 HTML 标签 · 2h · 打卡[ ]
- **Day 11** 布局 + 常用控件 · Grid/StackPanel，Button/TextBox 对照 div/input · 3h · 打卡[ ]
- **Day 12** 数据绑定 Binding + MVVM 入门 · 对照 v-model / 响应式 · 3h · 打卡[ ]
- **Day 13** 命令 Command + 依赖属性 · 3h · 打卡[ ]
- **Day 14** 综合：做一个 WPF 小工具（如 JSON 格式化器）· 4h · 打卡[ ]

### ▶ 第 3 周：通信基础（⚠️ 重点突破，新领域）
> 📌 **本周技术来源**：🟦 语法（位运算/`unchecked`）+ 🟩 BCL（`System.IO.Ports` 串口——**这是 🟩 官方库，但需加一行 `dotnet add package System.IO.Ports`**、`System.Net.Sockets` 的 TCP/UDP、`BitConverter`、`System.Text.Encoding`）。**零第三方包**，Socket/串口全是官方库。

- **Day 15** 通信概念通俗讲 · 比特/字节/进制/ASCII/编码 → 练习：十进制转十六进制 · 2h · 打卡[ ]
- **Day 16** 串口基础 · 波特率/数据位/停止位/校验 + SerialPort 类 · 3h · 打卡[ ]
- **Day 17** 串口实战：收发字符串 + 虚拟串口(com0com) · 4h · 打卡[ ]
- **Day 18** 二进制与字节序（大小端）+ BitConverter · 3h · 打卡[ ]
- **Day 19** TCP Socket 客户端编程 · 3h · 打卡[ ]
- **Day 20** TCP Socket 服务端 + 多线程处理多客户端 · 4h · 打卡[ ]
- **Day 21** UDP + 自定义帧格式 + 校验和 · 3h · 打卡[ ]

### ▶ 第 4 周：Modbus 协议（⚠️ 核心壁垒，重中之重）
> 📌 **本周技术来源**：🟦 语法（字节数组/`struct` 手动组包）+ 🟩 BCL（`System.Net.Sockets` 实现 Modbus TCP）+ 🟧 **NModbus**（真正第三方 NuGet 包，`dotnet add package NModbus`，Day25 起用）。Day23-24 先**纯 BCL 手搓**，Day25 才引入 🟧 NModbus——体会"能不能不装包"。

- **Day 22** Modbus 是什么 · 主从/寄存器/功能码 通俗讲 · 2h · 打卡[ ]
- **Day 23** Modbus RTU（串口）帧结构 + 手工组包/解包 · 4h · 打卡[ ]
- **Day 24** Modbus TCP 帧结构 + 用 Socket 实现 · 4h · 打卡[ ]
- **Day 25** 用 NModbus 库实战（读写线圈/保持寄存器）· 4h · 打卡[ ]
- **Day 26** 数据解析：寄存器值 → 工程量（缩放/类型转换）· 3h · 打卡[ ]
- **Day 27** 虚拟从站模拟 + 完整读写流程 · 4h · 打卡[ ]
- **Day 28** Modbus 综合练习 + 常见坑总结 · 3h · 打卡[ ]

### ▶ 第 5 周：PLC 通信（⚠️ 重点突破，西门子为主）
> 📌 **本周技术来源**：🟦 语法 + 🟧 **S7.Net**（西门子 PLC 通信，`dotnet add package S7.Net`）+ 🟧 **OPC UA 客户端库**（如 `Opc.Ua.Client`，`dotnet add package`）。三菱/欧姆龙只了解概念，不装包。

- **Day 29** PLC 与上位机关系 + 西门子 S7 协议概述 · 2h · 打卡[ ]
- **Day 30** S7.Net 库：连接 PLC + 读 DB 块 · 4h · 打卡[ ]
- **Day 31** 读写数据类型：Bool/Int/Real/字符串 · 4h · 打卡[ ]
- **Day 32** 三菱 MC / 欧姆龙 FINS 概念了解（不深究）· 2h · 打卡[ ]
- **Day 33** OPC UA 概念 + 客户端连接 · 3h · 打卡[ ]
- **Day 34** 多设备轮询架构 + 断线重连机制 · 3h · 打卡[ ]
- **Day 35** PLC 通信综合练习 · 3h · 打卡[ ]

### ▶ 第 6 周：数据库 + 多线程 + 实时数据
> 📌 **本周技术来源**：🟦 语法 + 🟩 BCL（`System.Threading`/`Task` 多线程是 🟩）+ 🟧 **EF Core**（`Microsoft.EntityFrameworkCore` + `Microsoft.EntityFrameworkCore.Sqlite`，`dotnet add package`）+ 🟧 **Serilog**（`dotnet add package Serilog` 日志）+ 🟧 **SQLite** 用 `Microsoft.Data.Sqlite` 包。本周 🟧 包最多。

- **Day 36** SQL 基础 + SQLite · 2h · 打卡[ ]
- **Day 37** EF Core 入门（Code First）· 3h · 打卡[ ]
- **Day 38** 多线程 Thread/Task/Parallel + 线程安全 · 3h · 打卡[ ]
- **Day 39** 生产者-消费者模式 + 队列缓冲采集数据 · 4h · 打卡[ ]
- **Day 40** 实时数据存储策略（历史库/缓存）· 3h · 打卡[ ]
- **Day 41** 日志 Serilog · 2h · 打卡[ ]
- **Day 42** 综合：把采集数据落库 · 4h · 打卡[ ]

### ▶ 第 7 周：综合项目（作品集核心，必须做）
> 📌 **本周技术来源**：🟦🟩 前面全用上 + 🟧 **LiveCharts2**（实时曲线，`dotnet add package LiveCharts`）+ 沿用 🟧 NModbus/S7.Net/EF Core/Serilog。把前几周的 🟧 包综合起来做成一个作品。

- **Day 43** 项目设计：工业数据采集监控系统架构 · 3h · 打卡[ ]
- **Day 44** UI 搭建（WPF + LiveCharts 实时曲线）· 4h · 打卡[ ]
- **Day 45** 通信层抽象（Modbus + PLC 统一接口）· 4h · 打卡[ ]
- **Day 46** 数据存储 + 报警逻辑 · 4h · 打卡[ ]
- **Day 47** 完善 + 打包（ClickOnce / 安装包）· 3h · 打卡[ ]
- **Day 48** 项目复盘 + GitHub 提交 + 写 README · 3h · 打卡[ ]

### ▶ 第 8 周：求职冲刺（拿 Offer）
> 📌 **本周技术来源**：🟦🟩🟧 全复习，**无新技术**。这周是简历/面试/投递，不写新代码，只巩固前面学的。

- **Day 49** 简历撰写（突出转型优势/UI 能力/工程化）· 3h · 打卡[ ]
- **Day 50** 上位机常见面试题（C#/WPF/通信）· 4h · 打卡[ ]
- **Day 51** 项目讲解准备（STAR 法则）· 2h · 打卡[ ]
- **Day 52** 笔试题：协议/字节处理刷题 · 3h · 打卡[ ]
- **Day 53** 投递策略 + 渠道（Boss/猎聘/智联）· 2h · 打卡[ ]
- **Day 54** 模拟面试 + 查漏补缺 · 3h · 打卡[ ]
- **Day 55** 持续投递 + 复盘 · 2h · 打卡[ ]
- **Day 56** 学习网站进度同步（把所学做成课程内容）· 3h · 打卡[ ]

---

## 五、第 1 周详细讲义（通俗讲解 + 练习 + 答案）

> 格式示范：后面每周都可照这个格式展开。卡住就说「帮我讲 Day X」。

### Day 1 — 环境 + 第一个程序 + C# 与 TS 对比

**🎯 目标**：装好环境，跑出第一个 C# 程序，建立「C# = 强类型 TS」的直觉。

**📌 今天用到的技术从哪来**
| 写法 | 来源 | 对照 / 说明 |
|---|---|---|
| `using System;` | 🟦语法 + 命名空间 | 引入系统库，类比 TS 的 `import` |
| `class` / `Program.cs` | 🟦语法 | 类与程序入口文件 |
| `int` / `string` / `double` | 🟦语法 | 基础类型关键字（类型写在前面） |
| `Console.WriteLine/Write/ReadLine` | 🟩.NET 类库 | 控制台输入输出，BCL 自带 |
| `int.Parse(...)` | 🟩.NET 类库 | 字符串转数字，BCL 自带 |
| `$"..."` 字符串插值 | 🟦语法 | 类比 JS 模板字符串 `` `...${}` `` |
| `dotnet new / dotnet run` | ⚙️.NET SDK 命令 | **不是 C# 代码**，是终端命令（类比 `npm init` / `npm start`） |

**📖 通俗讲解**
- C# 是微软的编程语言，运行在 .NET 平台上。你可以把它想成「**加了严格类型的 TypeScript**」。
- TypeScript 里你写 `let x: number = 1;`，C# 里写 `int x = 1;`（类型写在前面）。
- TypeScript 用 Node/npm 跑，C# 用 `.NET` 跑：`dotnet new console` 相当于 `npm init`，`dotnet run` 相当于 `npm start`。
- `Program.cs` 是程序入口，`Console.WriteLine("文字")` 就是 `console.log("文字")`。

**💡 关键代码**
```csharp
// Program.cs
using System;

Console.WriteLine("Hello 上位机！");

// 输入两个数，输出和
Console.Write("输入第一个数：");
int a = int.Parse(Console.ReadLine()!);
Console.Write("输入第二个数：");
int b = int.Parse(Console.ReadLine()!);
Console.WriteLine($"和是：{a + b}");
```

**✏️ 练习**
1. 安装 .NET 8 SDK + Visual Studio 2022 社区版（免费）。
2. 新建控制台项目，把输出改成 `Hello，我是{你的名字}`。
3. 写一个程序：让用户输入两个数，输出它们的和。
4. （挑战）写一个程序：输入半径，输出圆面积（3.14 × r × r）。

**✅ 答案**
- 练习 2：`Console.WriteLine("Hello，我是张三");`
- 练习 3：见上方关键代码。
- 练习 4：
```csharp
Console.Write("输入半径：");
double r = double.Parse(Console.ReadLine()!);
double area = 3.14 * r * r;
Console.WriteLine($"圆面积：{area}");
```

---

### Day 2 — 类型系统（值类型 / 引用类型）

**🎯 目标**：搞懂 C# 里「值」和「引用」的区别，这是和 JS/TS 最大不同之一。

**📌 今天用到的技术从哪来**
| 写法 | 来源 | 说明 |
|---|---|---|
| `struct` / `class` | 🟦语法 | 值类型 / 引用类型关键字 |
| `int` / `bool` / `string` | 🟦语法 | 基础类型 |
| `var x = 10;` | 🟦语法 | 类型推断（写起来像 JS，仍是强类型） |
| `int?` 可空类型 | 🟦语法 | 类比 TS 的 `number \| null` |
| `new Point { ... }` 对象初始化器 | 🟦语法 | 创建对象并赋值的简写 |
| `.HasValue` / `.Value` | 🟩.NET 类库 | 可空类型的成员（BCL 自带） |

**📖 通俗讲解**
- **值类型**（int、double、bool、struct）：像两张纸上各写一个数，改一张不影响另一张。赋值 = 复制一份。
- **引用类型**（class、string、数组）：像两张便利贴写同一个抽屉的编号，改抽屉里的内容，两张便利贴看到的都变。
- `var` 让编译器自动推断类型：`var x = 10;` 等价于 `int x = 10;`（写起来像 JS，但仍是强类型）。
- 可空类型：`int? age = null;` 表示「可能没有值」，对应 TS 的 `number | null`。

**💡 关键代码**
```csharp
struct Point { public int X; public int Y; } // 值类型

Point p1 = new Point { X = 1, Y = 2 };
Point p2 = p1;      // 复制了一份
p2.X = 99;
Console.WriteLine(p1.X); // 输出 1（p1 没变）

class Person { public string Name = ""; } // 引用类型
Person a = new Person { Name = "张三" };
Person b = a;       // 同一个对象
b.Name = "李四";
Console.WriteLine(a.Name); // 输出 李四（a 也变了）
```

**✏️ 练习**
1. 用 `struct` 定义一个 `Point`（含 X、Y），演示值类型赋值互不影响。
2. 用 `class` 定义一个 `Student`（含 Name），演示引用类型赋值共享。
3. 声明一个可空 int：`int? score = null;`，判断它是否有值并打印。

**✅ 答案**
```csharp
struct Point { public int X; public int Y; }
class Student { public string Name = ""; }

// 3.
int? score = null;
if (score.HasValue) Console.WriteLine(score.Value);
else Console.WriteLine("没有分数");
```

---

### Day 3 — 集合与 LINQ

**🎯 目标**：用 List/Dictionary，并用 LINQ 把前端数组方法「翻译」成 C#。

**📌 今天用到的技术从哪来**
| 写法 | 来源 | 对照 |
|---|---|---|
| `List<T>` / `Dictionary<K,V>` | 🟩.NET 类库 | 集合类型，BCL 自带（类比 JS 数组 / 对象） |
| `using System.Linq;` | 🟩.NET 类库（命名空间） | LINQ 在这里 |
| `.Select / .Where / .Sum / .First` | 🟩.NET 类库 + LINQ | 类比 `map / filter / reduce / find` |
| `=>` Lambda | 🟦语法 | 类比 JS 箭头函数 |
| `.ToList() / .Average()` | 🟩.NET 类库 + LINQ | 集合扩展方法 |

**📖 通俗讲解**
| 前端（JS） | C#（LINQ） | 作用 |
|---|---|---|
| `arr.map(x=>x*2)` | `list.Select(x=>x*2)` | 转换 |
| `arr.filter(x=>x>3)` | `list.Where(x=>x>3)` | 过滤 |
| `arr.reduce(...)` | `list.Sum()/Aggregate()` | 聚合 |
| `arr.find(...)` | `list.First(x=>...)` | 查找 |

LINQ 是「**对集合做查询的语言集成查询**」，写起来像写 SQL，但作用在内存里的集合上。

**💡 关键代码**
```csharp
using System.Linq;
var nums = new List<int> { 1, 2, 3, 4, 5 };
var doubled = nums.Select(x => x * 2).ToList();        // [2,4,6,8,10]
var big = nums.Where(x => x > 2).ToList();             // [3,4,5]
var sum = nums.Sum();                                   // 15
var dict = new Dictionary<string, int> { ["张三"] = 90, ["李四"] = 80 };
Console.WriteLine(dict["张三"]); // 90
```

**✏️ 练习**
1. 有一个 `List<int>{10,20,30,40}`，用 LINQ 算出所有大于 15 的数的和。
2. 用 Dictionary 存 3 个学生的成绩，打印平均分。
3. （挑战）把一个字符串列表里长度 > 2 的挑出来并转大写。

**✅ 答案**
```csharp
var nums = new List<int> { 10, 20, 30, 40 };
int s = nums.Where(x => x > 15).Sum(); // 90

var scores = new Dictionary<string, int> { ["A"] = 90, ["B"] = 80, ["C"] = 70 };
double avg = scores.Values.Average();  // 80

var names = new List<string> { "ab", "cat", "go" };
var r = names.Where(n => n.Length > 2).Select(n => n.ToUpper()).ToList(); // ["CAT"]
```

---

### Day 4 — 类与对象（class）

**🎯 目标**：用 class 封装数据和行为，对照 TS 的 class。

**📌 今天用到的技术从哪来**
| 写法 | 来源 | 对照 |
|---|---|---|
| `class` / `public` / `void` | 🟦语法 | 类、访问修饰符、无返回值 |
| `{ get; set; }` 属性 | 🟦语法 | 类比 TS 的 `get/set` 或 public 字段 |
| `public Car(int speed)` 构造函数 | 🟦语法 | 类比 TS `constructor` |
| `static` | 🟦语法 | 属于类本身（如 `Math.PI`、`Console`） |
| `=>` 表达式体方法 | 🟦语法 | 方法简写，类比箭头函数 |
| `new Car(120) { Brand = ... }` | 🟦语法 | 构造 + 对象初始化器 |

**📖 通俗讲解**
- `class` 是「模板」，对象是「按模板造出来的具体东西」。比如 `class 汽车` 是模板，`我的车`是对象。
- **属性** `public int Speed { get; set; }` 对照 TS 的 `get/set` 或 public 字段，但更规范。
- **构造函数** `public Car(int speed)` 是「造对象时必做的初始化」，对照 TS 的 `constructor`。
- **static** 属于「类本身」而不是某个对象，比如 `Math.PI`、`Console.WriteLine`。

**💡 关键代码**
```csharp
class Car
{
    public int Speed { get; set; }      // 属性
    public string Brand { get; set; } = "";
    public Car(int speed) { Speed = speed; } // 构造函数
    public void Show() => Console.WriteLine($"{Brand} 速度 {Speed}");
    public static int Wheels = 4;       // static 属于类
}

var c = new Car(120) { Brand = "比亚迪" };
c.Show();                   // 比亚迪 速度 120
Console.WriteLine(Car.Wheels); // 4
```

**✏️ 练习**
1. 定义一个 `Student` 类，含 `Name`、`Age` 属性和一个 `SayHi()` 方法打印问候。
2. 写构造函数，创建对象时直接赋值 Name。
3. 加一个 `static` 字段 `School`，所有学生共享。

**✅ 答案**
```csharp
class Student
{
    public string Name { get; set; } = "";
    public int Age { get; set; }
    public static string School = "北大青鸟";
    public Student(string name) { Name = name; }
    public void SayHi() => Console.WriteLine($"你好，我是{Name}，{Age}岁");
}
var s = new Student("小明") { Age = 18 };
s.SayHi();
Console.WriteLine(Student.School);
```

---

### Day 5 — 继承 / 接口 / 多态 / 泛型

**🎯 目标**：理解「面向接口编程」，这是上位机驱动层设计的核心思想。

**📌 今天用到的技术从哪来**
| 写法 | 来源 | 说明 |
|---|---|---|
| `interface` / `: 父类` / `: 接口` | 🟦语法 | 接口与继承关键字 |
| `void Connect()` | 🟦语法 | 方法签名 |
| 多态（父类/接口引用子类对象） | 🟦语法 | OOP 特性 |
| `where T : IComparable<T>` | 🟦语法 | 泛型约束 |
| `T Max<T>(...)` 泛型方法 | 🟦语法 | 类比 TS `Array<T>` |

**📖 通俗讲解**
- **继承**：`class 卡车 : 汽车` 表示卡车「是一种」汽车，复用汽车的能力。
- **接口（interface）**：像「插座标准」。不管什么厂家的设备，只要插口（接口）一样，上位机就能连。上位机里常用 `IDevice` 接口统一不同 PLC/仪表。
- **泛型**：`List<T>` 的 `T` 是「占位类型」，对照 TS 的 `Array<T>` / `List<T>`。

**💡 关键代码**
```csharp
interface IDevice { void Connect(); }      // 统一标准
class ModbusDevice : IDevice { public void Connect() => Console.WriteLine("连Modbus"); }
class PlcDevice : IDevice { public void Connect() => Console.WriteLine("连PLC"); }

void Use(IDevice d) => d.Connect();         // 多态：不管哪种设备都能用
Use(new ModbusDevice());
Use(new PlcDevice());

// 泛型
T Max<T>(T a, T b) where T : IComparable<T> => a.CompareTo(b) > 0 ? a : b;
```

**✏️ 练习**
1. 定义接口 `IAnimal` 含 `Speak()`，让 `Dog` 和 `Cat` 实现。
2. 写一个方法接收 `IAnimal` 并调用 `Speak()`（多态）。
3. 用泛型写 `Swap<T>(ref T a, ref T b)` 交换两个变量。

**✅ 答案**
```csharp
interface IAnimal { void Speak(); }
class Dog : IAnimal { public void Speak() => Console.WriteLine("汪"); }
class Cat : IAnimal { public void Speak() => Console.WriteLine("喵"); }
void Speak(IAnimal a) => a.Speak();
Speak(new Dog()); Speak(new Cat());

void Swap<T>(ref T a, ref T b) { T t = a; a = b; b = t; }
```

---

### Day 6 — 委托 / 事件 / Lambda

**🎯 目标**：理解「把方法当变量传」和「事件驱动」，对照 JS 的回调和 addEventListener。

**📌 今天用到的技术从哪来**
| 写法 | 来源 | 对照 |
|---|---|---|
| `delegate void LogHandler(...)` | 🟦语法 | "方法的类型"，类比函数类型 |
| `event Action<T>?` | 🟦语法 | 事件，类比 `addEventListener` |
| `LogHandler log = (m) => ...` | 🟦语法 | 委托赋 Lambda |
| `+=` 订阅事件 | 🟦语法 | 类比 `addEventListener` |
| `?.Invoke(...)` 安全调用 | 🟦语法 | 空值判断后调用（类比 `?.`） |
| `Action<T>` | 🟩.NET 类库 | 内置委托类型（BCL，类比 JS 回调类型） |

**📖 通俗讲解**
- **委托（delegate）**：可以理解为「方法的类型」。就像 `int` 是数字的类型，委托是「函数」的类型。
- **事件（event）**：和前端 `button.addEventListener('click', fn)` 一模一样 —— 设备「数据到了」就通知订阅者。
- **Lambda**：`x => x + 1` 就是 JS 的箭头函数，前面 Day 3 已经用过。

**💡 关键代码**
```csharp
// 委托：方法的类型
delegate void LogHandler(string msg);
LogHandler log = (m) => Console.WriteLine($"[日志]{m}");
log("系统启动");

// 事件：像 addEventListener
class Sensor
{
    public event Action<double>? DataReceived;
    public void Read() => DataReceived?.Invoke(36.5); // 通知订阅者
}
var s = new Sensor();
s.DataReceived += (v) => Console.WriteLine($"收到温度：{v}");
s.Read();
```

**✏️ 练习**
1. 定义一个 `event Action<string> MessageArrived`，订阅后打印消息。
2. 用 Lambda 订阅该事件，触发时输出「收到：XXX」。
3. （挑战）做一个「按钮点击计数器」：每次触发事件计数 +1。

**✅ 答案**
```csharp
class Messenger
{
    public event Action<string>? MessageArrived;
    public void Send(string m) => MessageArrived?.Invoke(m);
}
var m = new Messenger();
m.MessageArrived += (msg) => Console.WriteLine($"收到：{msg}");
m.Send("Hello");

// 挑战
int count = 0;
m.MessageArrived += (_) => { count++; Console.WriteLine($"第{count}次"); };
```

---

### Day 7 — 周复习 + 小测

**🎯 目标**：巩固第 1 周，能独立写出综合小程序。

**📌 本周技术来源汇总（很重要）**
第 1 周你实际只用了 **🟦【C# 语法】** 和 **🟩【.NET 类库】** 两类——**没有任何第三方包**，全是"天生物 / BCL"，不用 `dotnet add package` 装任何东西。
→ 这周是"语言关"，你已经带着前端底子飞快过了。从 **第 2 周 WPF / EF Core** 开始，才会碰到需要 `dotnet add package` 的库——到时候每个都会先标明 🟧 并告诉你装哪个包。

**✏️ 综合练习（5 题）**
1. 用 List 存 5 个商品名，用 LINQ 找出包含「机」字的。
2. 定义 `Product` 类（Name, Price），用 Dictionary 存 3 个商品，算总价。
3. 用接口 `ICalculate` 定义 `Calc(int a, int b)`，实现「加」和「减」两个类。
4. 用事件模拟「温度报警」：温度 > 80 触发报警。
5. 把上面学到的任意两个知识点组合，写一个你自己的小程序（如学生成绩管理）。

**✅ 答案要点**
- 1：`products.Where(p => p.Contains("机")).ToList();`
- 2：`products.Values.Sum(p => p.Price);`
- 3：接口 + 两个实现类（加/减），用多态调用。
- 4：`if(temp>80) Alarm?.Invoke("温度过高");`
- 5：自由发挥，能跑通即过关。

---

## 六、每日进度打卡总表（复制此表，每天填）

| Day | 主题 | 计划时长 | 实际时长 | 完成✅ | 卡点 / 备注 |
|---|---|---|---|---|---|
| 1 | C# 环境+首程序 | 3-4h | | | |
| 2 | 类型系统 | 3h | | | |
| 3 | 集合与 LINQ | 3h | | | |
| 4 | 类与对象 | 3h | | | |
| 5 | 继承/接口/泛型 | 3h | | | |
| 6 | 委托/事件 | 3h | | | |
| 7 | 周复习小测 | 2h | | | |
| 8 | 异常/IO/JSON | 3h | | | |
| 9 | 异步 Task | 2h | | | |
| 10 | WPF+XAML | 2h | | | |
| 11 | 布局+控件 | 3h | | | |
| 12 | Binding+MVVM | 3h | | | |
| 13 | Command | 3h | | | |
| 14 | WPF 小工具 | 4h | | | |
| 15 | 通信概念 | 2h | | | |
| 16 | 串口基础 | 3h | | | |
| 17 | 串口实战 | 4h | | | |
| 18 | 字节序 | 3h | | | |
| 19 | TCP 客户端 | 3h | | | |
| 20 | TCP 服务端 | 4h | | | |
| 21 | UDP+帧 | 3h | | | |
| 22 | Modbus 概念 | 2h | | | |
| 23 | Modbus RTU | 4h | | | |
| 24 | Modbus TCP | 4h | | | |
| 25 | NModbus 库 | 4h | | | |
| 26 | 数据解析 | 3h | | | |
| 27 | 虚拟从站 | 4h | | | |
| 28 | Modbus 综合 | 3h | | | |
| 29 | PLC 概念 | 2h | | | |
| 30 | S7.Net 连接 | 4h | | | |
| 31 | S7 数据类型 | 4h | | | |
| 32 | 三菱/欧姆龙 | 2h | | | |
| 33 | OPC UA | 3h | | | |
| 34 | 轮询+重连 | 3h | | | |
| 35 | PLC 综合 | 3h | | | |
| 36 | SQL+SQLite | 2h | | | |
| 37 | EF Core | 3h | | | |
| 38 | 多线程 | 3h | | | |
| 39 | 生产者消费者 | 4h | | | |
| 40 | 存储策略 | 3h | | | |
| 41 | Serilog | 2h | | | |
| 42 | 落库综合 | 4h | | | |
| 43 | 项目设计 | 3h | | | |
| 44 | UI+曲线 | 4h | | | |
| 45 | 通信层抽象 | 4h | | | |
| 46 | 存储+报警 | 4h | | | |
| 47 | 打包 | 3h | | | |
| 48 | 复盘+GitHub | 3h | | | |
| 49 | 简历 | 3h | | | |
| 50 | 面试题 | 4h | | | |
| 51 | 项目讲解 | 2h | | | |
| 52 | 笔试刷题 | 3h | | | |
| 53 | 投递策略 | 2h | | | |
| 54 | 模拟面试 | 3h | | | |
| 55 | 持续投递 | 2h | | | |
| 56 | 网站同步 | 3h | | | |

---

## 七、资源清单

- **开发环境**：Visual Studio 2022 社区版（免费）/ VS Code + C# 插件 / .NET 8 SDK
- **虚拟串口**：com0com（Windows）
- **关键库**：NModbus（Modbus）、S7.Net（西门子）、LiveCharts（图表）、Serilog（日志）、EF Core（ORM）、MQTTnet（MQTT）
- **学习资料**：微软官方 C# 教程（learn.microsoft.com）、B 站「上位机」系列视频
- **本项目**：学习网站（Blazor + .NET + Docker）同步开发中，学以致用

---

## 八、怎么用这份文档（必读）

1. 每天打开，看「当日」那一行，照着学。
2. 学完在「打卡表」打勾，写下卡点 —— **进度可见就是你坚持的动力**。
3. 已会的内容（比如异步、LINQ）直接跳过，把时间留给通信/PLC。
4. 卡住就叫我：「帮我讲 Day X」或「Day X 练习不会做」。
5. 想让我把某周也展开成「通俗讲解 + 答案」版，直接说「生成第 X 周详细讲义」。
6. 每周日看总进度，节奏太快就放慢，太慢就加速——**以拿到 13K Offer 为唯一目标**。

> 记住：你不是从零开始，你是带着前端功力去「跨界收割」上位机岗位。通信和 PLC 是唯一的硬骨头，啃下来，13K 就在眼前。
