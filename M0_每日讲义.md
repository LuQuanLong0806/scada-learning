# DAQ Monitor · M0 每日讲义：脚手架 + C#/.NET 热身

> **模块定位**：这是你的主场。你已是中高级前端，C# 语法能极快迁移。M0 只做两件事：① 把 DAQ Monitor 工程骨架立起来；② 把 C# 核心语法热身到「能写业务代码」。后面串口 / Modbus / PLC 的难点不在语言，所以本模块可加速。
> **全程标注**：🟦C# 语法（不装包）/ 🟩.NET 类库 BCL（默认不装包）/ 🟧第三方 NuGet（要 `dotnet add package`）。口诀：**语法天生物·BCL 随 .NET·第三方要装包**。
> **对比前端**：你熟悉的 TS/JS 概念直接对标，降低负担。本模块每个点都给你「前端类比」。

## 怎么用这份讲义
1. 每天按顺序：**看讲解 → 做练习（先自己写，再对照答案）→ 做项目任务 → 打卡**。
2. 遇 🟦🟩🟧 拿不准，回看总纲第〇节。
3. 卡住就说「**讲 Day N 的 XX**」或「**Day N 练习答案**」，我展开讲。
4. 每完成一天，把 DAQ Monitor 对应代码提交一次 Git，作品集逐步成形。

> 📚 **前置语法**(M0 是 C# 入门,但若完全没接触过 C#,先扫 [C# 语法速查 — 前端视角](CSharp语法速查_前端视角.md) 的 §1/§2/§4/§10/§8 — 各 5 分钟,有了视觉记忆再读 M0,效率高很多)

> ⭐ = 考试 / 面试高频重点；🔥 = 新手易踩坑。

---

## Day 1 — 环境 + 第一个 C# 程序（对标 TS）
**📌 技术来源**：🟦 C# 语法（`using` / `class` / `static void Main` / `var` / 字符串插值） + 🟩 BCL（`Console`）
**🎯 今日目标**：装好 .NET 8 SDK + Visual Studio 2022；跑出第一个程序；建立「C# ≈ 带强类型的 TS」直觉。
**📖 讲解**
- ⭐ .NET = 运行时 + 巨类库（BCL）；C# = 语言。类比：**Node.js runtime ≈ .NET runtime**，**npm 包 ≈ NuGet 包**，**VS Code/TS ≈ C# / Visual Studio**。
- 程序入口：🟦 `static void Main()`（类比 JS 的入口模块）。.NET 6+ 支持「顶级语句」，可省略 Main 直接写代码。
- 变量：🟦 `var x = 1;` ≈ TS `let x = 1;`（类型推断）。🟦 类型写在前：`int x = 1;` ≈ TS `let x: number = 1;`
- 输出：🟩 `Console.WriteLine("hi")` ≈ `console.log("hi")`
- 字符串插值：🟦 `$"Hello {name}"` ≈ JS `` `Hello ${name}` ``
- 编译运行：`dotnet run`
**⭐ 重点**：C# 一切皆类型，变量默认不能为 `null`（要可空得写 `int?`）；没有 `undefined`，只有 `null`——这比 JS 安全，也是 13K 面试「C# 优势」常考点。
**✏️ 练习**：新建 console 项目，输出「DAQ Monitor 启动」，并用 `var name="DAQ"` 拼接输出一句含变量的话。
**✅ 答案**：
```csharp
Console.WriteLine("DAQ Monitor 启动");
var name = "DAQ";
Console.WriteLine($"{name} Monitor 启动，版本 1.0");
```
**🏗️ 项目任务**：确认本机 `dotnet --version` 输出 `8.x`；在 `DAQMonitor/src` 下建好后续要用的两个工程目录（Core / UI）的占位（真正建工程在 Day 7）。今天重点是**环境通**。
**✅ 打卡[ ]**

---

## Day 2 — 类型系统：值类型 / 引用类型 / 可空 / struct / enum
**📌 技术来源**：🟦 C# 类型系统（`int` / `double` / `bool` / `string` / `struct` / `enum` / 可空 `?`）
**🎯 目标**：搞懂值类型 vs 引用类型（面试高频），会用 `struct` 组织数据（工业点位的天然载体）。
**📖 讲解**
- ⭐ 值类型（`int` / `double` / `bool` / `struct` / `enum`）存「值本身」，赋值 = 拷贝；引用类型（`string` / 类 / 数组 / `List<T>`）存「地址」，赋值 = 共享。类比 JS 的 primitive vs object（但 C# 的 `int` 是真值类型，不是包装对象）。
- `string` 虽是引用类型但**不可变**（≈ JS 字符串），拼接产生新对象。
- 可空：🟦 `int? age = null;` ≈ TS `number | null`。非可空 `int age;` 必须赋值，否则编译报错——这是 C# 比 JS 安全的地方。
- 🟦 `struct`：轻量值类型，适合「坐标 / 点位 / 寄存器」这类小数据：
```csharp
public struct SensorPoint { public int Id; public double Value; }
```
- 🟦 `enum`：命名常量，适合「设备状态 / 功能码」：
```csharp
enum DeviceState { Offline, Connecting, Online }
```
**⭐ 重点**：上位机里「一个传感器读数」天然就是 struct（Id + Value + Quality + Timestamp）；别用一堆散变量。
**🔥 易踩坑**：`struct` 是值类型，当作方法参数传递会被**拷贝**，想改原值要传 `ref` 或用 `class`。
**✏️ 练习**：定义 `enum AlarmLevel { Normal, Warning, Critical }` 和 `struct Alarm { public int PointId; public AlarmLevel Level; public double Value; }`。
**✅ 答案**：
```csharp
public enum AlarmLevel { Normal, Warning, Critical }
public struct Alarm { public int PointId; public AlarmLevel Level; public double Value; }
```
**🏗️ 项目任务**：在 `DaqMonitor.Core` 里建 `Models/` 目录，写下未来要用的核心类型骨架：`SensorPoint`、`DeviceState`（上面两个），先留好位置，后面模块往里填。
**✅ 打卡[ ]**

---

## Day 3 — 集合与 LINQ（对标数组 / filter / map）
**📌 技术来源**：🟦 集合语法 + 🟩 BCL（`List<T>` / `Dictionary<K,V>` / `LINQ`：`Where` / `Select` / `OrderBy` / `GroupBy`）
**🎯 目标**：用集合管理多设备 / 多点位；用 LINQ 像写 SQL 一样处理数据（上位机每秒几百个点，LINQ 是日常）。
**📖 讲解**
- 🟩 `List<SensorPoint> points = new();` ≈ TS `let points: SensorPoint[] = []`（但类型固定、性能更高）。
- 🟩 `Dictionary<int, SensorPoint>` ≈ JS `Map`，按键查设备。
- ⭐ LINQ 是 C# 杀手锏：🟩 `points.Where(p => p.Value > 100)` ≈ JS `filter`；`.Select(p => p.Value)` ≈ `map`；`.OrderBy(...)` / `GroupBy(...)` 更是 JS 没有的「数据库级」能力。
- LINQ 两种写法：方法链 `points.Where(...).Select(...)` 或 查询语法 `from p in points where p.Value>100 select p`。推荐方法链（和前端链式调用直觉一致）。
**🔥 易踩坑（延迟执行）**：LINQ 默认 **lazy**——只有 `.ToList()` / `.Count()` 等「落地」时才真算。遍历时改集合会报错，先 `.ToList()` 再改。
**✏️ 练习**：给 5 个 `SensorPoint`（值 10 / 50 / 120 / 200 / 30），用 LINQ 找出值 >100 的，投影成只含 Id 的列表。
**✅ 答案**：
```csharp
var hot = points.Where(p => p.Value > 100)
                .Select(p => p.Id)
                .ToList();
// 结果: [3, 4]
```
**🏗️ 项目任务**：在 Core 写 `PointStore` 类，内部用 `List<SensorPoint>` + `Dictionary<int,SensorPoint>` 双索引，提供 `Add/Update/GetAll`，并用 LINQ 提供 `GetAlarms(threshold)` 返回超阈值的点（Day 6 报警直接复用）。
**✅ 打卡[ ]**

---

## Day 4 — 类 / 属性 / 封装 / static
**📌 技术来源**：🟦 `class` / 属性（property）/ 访问修饰符 / `static`
**🎯 目标**：用类封装设备逻辑；理解属性（不是字段）是 C# 一等公民；`static` 单例 / 工具方法。
**📖 讲解**
- ⭐ 属性 `public double Value { get; set; }` ≈ TS 的 getter/setter，但语法更简洁；可加逻辑：`set { if(value<0) throw...; _value=value; }`。前端常把数据塞字段，C# 规范是**用属性暴露状态**。
- 访问修饰符：`public` / `private` / `internal`（同程序集）。类比 JS 没有的「编译期访问控制」——这是企业级封装基础。
- 🟦 `static`：属于「类」而非「实例」。`Math.Sqrt()` ≈ 你用过的 `Math.sqrt()`。`static` 类可做工具集（如后面算校验的 `Crc16`）。
- 构造函数：`public Device(int id) { Id = id; }` ≈ JS `constructor(id){this.id=id}`。
**⭐ 重点**：上位机里 `Device` / `Channel` / `Driver` 都是类；把「配置」做成属性（可绑定到 UI），把「动作」做成方法。
**✏️ 练习**：写 `class Thermometer { public int Id {get;} public double Temperature {get;set;} public Thermometer(int id){Id=id;} }`，加方法 `bool IsOverheat(double limit) => Temperature > limit;`
**✅ 答案**：见上，方法用 🟦 表达式体 `=>`。
**🏗️ 项目任务**：在 Core 抽象出 `DeviceBase` 基类，含 `Id/Name/State` 属性与 `Connect()` / `Disconnect()` 虚方法占位（具体实现留到 M1 / M3）。这步是 M3「IDevice 统一接口」的地基。
**✅ 打卡[ ]**

---

## Day 5 — 继承 / 接口 / 泛型
**📌 技术来源**：🟦 继承（`:`）/ 接口（`interface`）/ 泛型（`<T>`）
**🎯 目标**：为多设备架构打基础——不同设备（串口 / 网口 / PLC）共用一套接口，这正是 M3 的核心。
**📖 讲解**
- ⭐ 接口 `interface IDevice { void Connect(); double Read(int addr); }` ≈ TS `interface IDevice {...}`（C# 接口默认不含实现，C# 8+ 可带默认实现）。上位机里**面向接口编程**是关键：UI 不关心设备是串口还是 PLC，只认 `IDevice`。
- 继承：`class SerialDevice : DeviceBase, IDevice`——一个基类 + 一个接口，复用 + 多态。
- 🟦 泛型 `<T>`：`class Buffer<T> { }` ≈ TS 泛型，写一次适配任意类型。后面「数据缓冲队列」会用到 `Buffer<SensorPoint>`。
**⭐ 重点（面试常问）**：接口 vs 抽象类？——接口是「能做什么」（多实现），抽象类是「是什么」（单继承）。上位机设备用接口解耦。
**✏️ 练习**：定义 `interface IReadable { double Read(int addr); }`，让 Day4 的 `Thermometer` 实现它。
**✅ 答案**：
```csharp
public interface IReadable { double Read(int addr); }
public class Thermometer : IReadable {
    public int Id {get;} public double Temperature {get;set;}
    public Thermometer(int id){Id=id;}
    public double Read(int addr) => Temperature; // 简化
}
```
**🏗️ 项目任务**：在 Core 正式定义 `IDevice` 接口（`Connect/Disconnect/Read/Write` + `State` 属性），并让 `DeviceBase` 实现它；后面 M1 / M3 的串口 / 网口设备都继承它。这一步让 DAQ Monitor 从「Day1 按钮」进化成「可插拔多设备框架」。
**✅ 打卡[ ]**

---

## Day 6 — 委托 / 事件 / Lambda（对标回调 / 事件总线）
**📌 技术来源**：🟦 委托（delegate）/ 事件（event）/ Lambda + 🟩 BCL（`Action` / `Func` / `EventHandler<T>`）
**🎯 目标**：理解 C# 的事件机制——这是上位机「设备数据到了 → 通知 UI 刷新」的解耦核心（类比前端 `EventEmitter` / `addEventListener`）。
**📖 讲解**
- ⭐ 事件 ≈ 前端 `element.addEventListener('click', fn)`。C# 里：`public event EventHandler<DataEventArgs> DataReceived;`，外部 `device.DataReceived += OnData;`。
- 🟦 `Action` = 无返回委托 ≈ `() => void`；`Func<int,int,int>` = 有返回 ≈ `(a,b)=>int`。`Action` / `Func` 是 .NET 内置泛型委托，99% 场景不用自己写 delegate。
- Lambda `p => p.Value > 100` 你 Day3 已用过（LINQ），今天明确它本质是「匿名函数赋值给委托」。
- ⭐ 事件用于「设备层 → UI 层」单向通知，避免 UI 直接轮询（性能 + 解耦）。
**🔥 易踩坑**：订阅了事件要在对象销毁时 `-=` 取消订阅，否则**内存泄漏**（企业级必查项）。
**✏️ 练习**：定义 `class DataEventArgs : EventArgs { public double Value; }`，在 `Thermometer` 加 `event EventHandler<DataEventArgs> ValueChanged;`，温度 set 时触发。
**✅ 答案**：
```csharp
public class DataEventArgs : EventArgs { public double Value; }
public class Thermometer {
    private double _t;
    public event EventHandler<DataEventArgs> ValueChanged;
    public double Temperature {
        get => _t;
        set { _t = value; ValueChanged?.Invoke(this, new DataEventArgs{Value=value}); }
    }
}
```
**🏗️ 项目任务**：在 Core 的 `IDevice` 增加 `event EventHandler<DataEventArgs> DataReceived;`，`DeviceBase` 提供受保护的触发方法。这一步让「采集线程拿到数据 → 自动推给 UI」的架构闭环，是 M6 实时刷新的地基。
**✅ 打卡[ ]**

---

## Day 7 — 周复习 + 小测 + 立起 WPF 工程
**📌 技术来源**：🟦 前 6 天综合 + 🟩 **WPF**（`dotnet new wpf`，`<UseWPF>true</UseWPF>`，非 NuGet）
**🎯 目标**：把前 6 天串成「能写业务代码」的能力；真正建出 DAQ Monitor 的 WPF 工程骨架，点按钮打印「DAQ Monitor 启动」。
**📖 复习地图**：变量 / 类型（Day1-2）→ 集合 / LINQ 处理数据（Day3）→ 类 / 属性封装设备（Day4）→ 接口 / 泛型架构（Day5）→ 事件解耦通知（Day6）。这条线后面贯穿 M1~M8。
**⭐ 小测（自测，答案见各 Day）**：
1. `int?` 和 `int` 区别？2. LINQ 延迟执行是什么意思？3. 接口和抽象类怎么选？4. 事件订阅为什么要 `-=`？5. WPF 用 NuGet 装吗？（答：不，`<UseWPF>true</UseWPF>`，属 🟩 BCL 生态）
**🏗️ 项目任务（今日落地工程）**：
- `dotnet new sln -n DaqMonitor`
- `dotnet new classlib -n DaqMonitor.Core` 和 `dotnet new wpf -n DaqMonitor.UI`
- 引用：`DaqMonitor.UI` 引用 `DaqMonitor.Core`
- MainWindow 放「启动采集」按钮 + 文本框；点按钮在文本框追加「DAQ Monitor 启动」。
- 把 Day1-6 写的 Core 类型（`SensorPoint` / `DeviceBase` / `IDevice` / 事件）搬进 `DaqMonitor.Core`，工程骨架即成型。
**✅ 打卡[ ]**

---

## 附录：M0 交付物清单
- 本地工程：`DAQMonitor/src` → `DaqMonitor.Core`（领域模型 + 设备接口 + 点位存储）+ `DaqMonitor.UI`（WPF 主窗口）。
- 从 Day7 起它就是**真实项目**，后续模块往里加「串口 / Modbus / PLC / 存储 / 图表」，最终 = 一个能拿去面试的工业数据采集监控系统（13K~15K 作品集）。
- 本模块你已提前埋下 4 个企业级地基：① 分层（Core / UI 分离）② 接口解耦（IDevice）③ 事件通知（DataReceived）④ 双索引存储（PointStore）——后面模块只是「填空」。
