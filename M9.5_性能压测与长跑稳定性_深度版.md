# M9.5 — 性能压测与长跑稳定性（BenchmarkDotNet / dotnet counters / dotMemory）⚡

> **优先级定位**：🟧 必学（冲 15K 加分项）· 性能压测 + 长跑稳定性（7×24 不崩）
> **技术来源**：🟧 `BenchmarkDotNet`（官方压测）、🟧 `dotnet counters` / `dotnet trace` / `dotnet dump`（CLI 监控三件套）、🟧 JetBrains `dotMemory`（内存快照，免费试用）/ 🟦 自定义 `EventCounter`（业务指标）、🟦 `System.Threading.Channels`（限流背压）。
> **前端类比总纲**：前端有 Jsben.ch / Vitest bench 比两段代码谁快、Chrome DevTools Memory 抓堆快照、Performance 看火焰图、Service Worker/PWA 做离线降级——本模块把这套"性能工程 + 长跑韧性"在 C# 上位机里完整复刻。**电脑永远不眠、产线永远不停**，你的程序也得稳得起。
> **给简历加的能力**：能在简历写"用 BenchmarkDotNet 验证 1000Hz 采集单帧 < 1ms；用 `dotnet counters` 实时监控 GC/线程/业务 EventCounter；用 dotMemory 抓快照排查并修复事件订阅泄漏；长跑策略 Channel 限流 + 重试熔断 + 心跳自愈"——这四句一摆，15K 岗位稳过简历关。
> **前置**：M0（并发/分层）、M9（统一管道 + Retry + 心跳）。本模块是给前 14 个模块"做体检 + 上保险"。

> ⏱️ **阅读路径**(按时间预算选入口)
> - **3 分钟**:看「模块目标」— 知道这是 15K 加分项(7×24 不崩)
> - **30 分钟**:加看 Day 1 BenchmarkDotNet + dotnet counters 监控 GC/线程
> - **3 小时**:全文精读 + Day 2 **dotMemory 抓内存泄漏(事件订阅泄漏)** + Day 3 Channel 限流 + Day 4 长跑策略
> - 🎯 **面试高频**:**BenchmarkDotNet 报告怎么读** / **dotnet counters vs dotnet dump** / **4 种内存泄漏(event/Cached delegate/Static/Timer)** / Channel 限流背压 DropOldest
> - 🔁 **配套复习**:[代码肌肉 B25 PerformanceCounter / B26 IDisposable 完整模式](代码肌肉训练手册_30天刷题版.md) · [间隔重复表](记忆与复习机制_间隔重复版.md)

## 模块目标
让上位机 **7×24 稳跑**：① 用 BenchmarkDotNet 量化关键路径性能（CRC / 解析 / Span vs byte[]）；② 用 `dotnet counters` 实时看 GC/堆/线程 + 自定义业务 EventCounter；③ 用 dotMemory 抓快照找"只增不减"的内存泄漏（4 种典型）；④ 长跑稳定性策略——限流 / 降级 / 熔断 / 心跳 / 自愈。**做完这套，你的程序才算"工业级"**。

---

## Day 1 — BenchmarkDotNet 量化性能 🟡

### 一句话讲清楚
BenchmarkDotNet 是 .NET 官方压测工具，告诉你"哪个写法快多少 μs、分配了多少字节"——**前端类比：Jsben.ch / Vitest `bench` 但更专业**：自动预热、跑多轮、统计 Mean/Median/StdDev、还能反汇编看 JIT 出的机器码。

### 前端类比秒懂
| BenchmarkDotNet | 前端类比 |
|---|---|
| `[Benchmark]` 方法 | `benchmark.js` 的 `suite.add('name', fn)` / `vitest.bench` |
| `[Params(8,64,256)]` 多档输入 | bench 不同 size 的输入数组 |
| Mean / Median / StdDev | Chrome Performance 的多轮采样统计 |
| `[MemoryDiagnoser]`（Allocated 列） | Chrome DevTools Memory heap snapshot 对比 |
| `[DisassemblerDiagnoser]` | 看 JIT 编译出的汇编（更底层 than Source Map） |
| Baseline 比例（`Ratio` 列） | "比基线快 3 倍"的相对值 |

### 分点精讲

**① 装 BenchmarkDotNet + 第一个压测项目**（🟧）
新建一个独立的 Console 项目（**别和主工程混在一起**，压测项目 Release 跑、主工程 Debug 跑）：
```bash
dotnet new console -o src/DaqMonitor.Bench
cd src/DaqMonitor.Bench
dotnet add package BenchmarkDotNet
dotnet add reference ../DaqMonitor.Core   # 引用被压测工程
```
入口三行（注意要 `BenchmarkRunner.Run`，不是直接调方法）：
```csharp
// Program.cs
BenchmarkRunner.Run<CrcBench>();   // 跑完自动在 Results/ 生成 Markdown 报告
```

**② 真实场景：CRC16 两套实现 PK（按位 vs 查表）**（🟧🟦）
Modbus CRC16 有两种经典实现——按位（M2 写过）和查表法。哪个快？嘴说不算，bench 一下：
```csharp
using BenchmarkDotNet.Attributes;
using BenchmarkDotNet.Running;

[MemoryDiagnoser]                          // 显示 Allocated 字节/Op
public class CrcBench
{
    private byte[] _data = null!;

    [Params(8, 64, 256, 1024)]             // 多档输入：短帧/中帧/长帧
    public int Size { get; set; }

    [GlobalSetup]
    public void Setup() => _data = new byte[Size];

    [Benchmark(Baseline = true)]           // 基线：M2 的按位实现
    public ushort ModbusCrc16_ByBit()
    {
        ushort crc = 0xFFFF;
        foreach (var b in _data)
        {
            crc ^= b;
            for (int i = 0; i < 8; i++)
                crc = (crc & 1) != 0 ? (ushort)((crc >> 1) ^ 0xA001)
                                     : (ushort)(crc >> 1);
        }
        return crc;
    }

    private static readonly ushort[] _crcTable = BuildTable();
    [Benchmark]
    public ushort ModbusCrc16_Table()      // 查表法：空间换时间
    {
        ushort crc = 0xFFFF;
        foreach (var b in _data)
            crc = (ushort)((crc >> 8) ^ _crcTable[(crc ^ b) & 0xFF]);
        return crc;
    }

    private static ushort[] BuildTable()
    {
        var t = new ushort[256];
        for (int i = 0; i < 256; i++)
        {
            ushort crc = (ushort)i;
            for (int k = 0; k < 8; k++)
                crc = (crc & 1) != 0 ? (ushort)((crc >> 1) ^ 0xA001)
                                     : (ushort)(crc >> 1);
            t[i] = crc;
        }
        return t;
    }
}
```
**逐行白话**：
- `[Params(8,64,256,1024)]`：跑 4 档——Modbus 一帧典型 8 字节、长一点 64、大块批量 256/1024。这样能看到"小包谁快、大包谁快"。
- `[GlobalSetup]`：每档参数切之前跑一次，准备数据，**不算进测量**。
- `[Benchmark(Baseline = true)]`：标基线，输出里 `Ratio` 列会显示别的实现相对它几倍。
- `[MemoryDiagnoser]`：开内存诊断，输出多一列 `Allocated Bytes/Op`——按位法 0 字节（栈上算），查表法也是 0（表是 static 一次建好）。

**跑一下**（**必须 Release**，Debug 会乱算）：
```bash
cd src/DaqMonitor.Bench
dotnet run -c Release
```
典型输出（节选）：
```
| Method               | Size | Mean      | Ratio | Allocated |
|--------------------- |----- |----------:|------:|----------:|
| ModbusCrc16_ByBit    | 8    |  45.6 ns  |  1.00 |         - |
| ModbusCrc16_Table    | 8    |  18.2 ns  |  0.40 |         - |
| ModbusCrc16_ByBit    | 1024 | 5,180 ns  |  1.00 |         - |
| ModbusCrc16_Table    | 1024 | 2,050 ns  |  0.40 |         - |
```
**怎么读**：查表法比按位快 2.5 倍（`Ratio 0.40`），而且**大小包都稳**——这就是 1000Hz 采集该选的实现。

**③ MemoryDiagnoser 看分配：struct vs class、Span vs byte[]、Channel vs ConcurrentQueue**（🟦）
这三个对比是面试高频，bench 一次你终身记得：
```csharp
[MemoryDiagnoser]
public class AllocBench
{
    [Benchmark(Baseline = true)]
    public SensorPoint_Class ReturnClass() => new() { Id = 1, Value = 42.0 };

    [Benchmark]
    public SensorPoint_Struct ReturnStruct() => new() { Id = 1, Value = 42.0 };

    // Span<T> 切片 vs byte[] + MemoryStream
    [Benchmark]
    public int SpanSlice()
    {
        var buf = new byte[1024];
        var slice = buf.AsSpan(64, 128);    // 0 字节分配
        return slice.Length;
    }

    [Benchmark]
    public int MemoryStreamCopy()
    {
        var buf = new byte[1024];
        using var ms = new MemoryStream(buf, 64, 128);   // 多一个对象
        return (int)ms.Length;
    }
}
```
**输出会告诉你**：struct 走栈、`Allocated = -`；class 上堆、`Allocated = 24 B`（每 Op 多 24 字节）。1000Hz 下你一秒就多 24KB 垃圾，GC 立刻被催。**这就是为什么 M5/M9 推 `readonly struct SensorPoint`**。

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ **必须 Release 跑** | Debug 编译没优化，结果差 10 倍都是常事；CI 里加 `-c Release` |
| ⭐ 自动预热不要手动循环 | 别自己写 `for i=0..1e6` 跑计时——JIT 第一遍冷、第二遍热，BND 自动预热 |
| 🔥 **Dead code elimination** | 单纯 `var x = Compute()` 不用 x，JIT 会把整段删掉→测出"0ns"，要用 `_ = Compute()` 或 `[Benchmark]` 返回值 |
| 🔥 异常路径贵 100x | `try/catch` 包热路径、且不抛还好；一旦抛异常，单 Op 多 1μs 起 |
| 🔥 GC 噪声会让 StdDev 飙 | 单次跑波动大，看 `Median` 比 `Mean` 稳；StdDev 大于 Mean 的 10% 说明干扰大，关其他进程重跑 |
| 🔥 JIT 分层编译 | 加 `[MethodImpl(MethodImplOptions.NoOptimization)]` 会反优化；别在 bench 方法上加奇怪 Attribute |

### 🧪 三档练习
**🟢 基础题**：新建 `DaqMonitor.Bench` 项目，复制上面 CRC 的代码，`dotnet run -c Release` 跑出表格，把截图贴到笔记里。

**🟡 进阶题**：给 `FrameParser.Feed`（M2 粘包拆分）写一个 bench——`[Params(64, 256, 1024)]` 不同 buffer 大小，看哪种实现切包最快。要求加 `[MemoryDiagnoser]` 看 Allocated。

**🔴 挑战题**：对比 `Channel<SensorPoint>` vs `ConcurrentQueue<SensorPoint>` 在「单生产单消费、1000 点/秒」下的吞吐。提示：写一个 `[GlobalSetup]` 起两个 Task 一发一收，`[Benchmark]` 测"塞 1 万点全部消费完"的耗时。**结论会让你记住：高并发下 Channel 完胜**。

**✅ 答案（基础题）**
跑出来的报告路径：`src/DaqMonitor.Bench/BenchmarkDotNet.Artifacts/results/CrcBench-report.md`。复制 Markdown 表到 README，面试讲"我 bench 过 CRC 两种实现，选查表法省 60% 时间"——立刻拉开档次。

**💡 工控导师说**：性能优化的第一条原则——**别瞎猜，先 bench**。我见过太多学员"我觉得 Span 快"然后改一通发现根本没瓶颈（瓶颈在串口 I/O）。**bench 完知道热点在哪，再决定值不值得改**。

**🎓 职业建议**：简历写"用 BenchmarkDotNet 量化采集路径性能，单帧 < 1ms"远比"精通性能优化"有力。面试官问"你怎么知道是 1ms"，你掏出 bench 报告——这就叫**数据驱动**，15K 岗位爱这个。

**📚 延伸阅读**
- BenchmarkDotNet 官方：https://benchmarkdotnet.org/
- 选择正确的 diagnoser：https://benchmarkdotnet.org/articles/configs/diagnosers.html
- 性能优化的 6 个 R：https://github.com/dotnet/coreclr/blob/master/Documentation/project-docs/performance-guidelines.md

**🏗️ 项目任务**：给 DAQ Monitor 加 `src/DaqMonitor.Bench` 项目，至少 bench 三件事——CRC 两种实现、struct vs class、FrameParser 不同 size。把报告 md 提交进仓库。

**✅ 打卡[ ]**

---

## Day 2 — 实时监控 + 内存泄漏排查 🔴

### 一句话讲清楚
`dotnet counters` 是命令行实时仪表盘（GC/堆/线程/自定义业务指标），`dotnet trace` 抓火焰图，`dotMemory` 抓堆快照找"只增不减"的泄漏——**前端类比：Chrome Task Manager + Performance 火焰图 + Memory Profiler，全套搬到 .NET**。

### 前端类比秒懂
| .NET 工具 | 前端类比 | 用途 |
|---|---|---|
| `dotnet counters monitor` | Chrome Task Manager | 实时看 CPU/GC 堆/线程数 |
| `dotnet trace` | Performance 火焰图 | CPU 采样找热点 |
| `dotnet dump` + SOS | Heap snapshot CLI | 抓堆、找对象引用链 |
| JetBrains dotMemory | Memory Profiler（GUI） | 双快照对比找泄漏 |
| PerfView | 高级 Lighthouse | 免费但学习曲线陡 |
| 自定义 `EventCounter` | `performance.mark()` 上报业务指标 | 上报队列长度、批次大小 |

### 分点精讲

**① `dotnet counters` 实时看 GC / 堆 / 线程**（🟧 全平台命令行）
先全局装一次：
```bash
dotnet tool install -g dotnet-counters
dotnet tool install -g dotnet-trace       # 顺便装上
dotnet tool install -g dotnet-dump        # 顺便装上
```
启动你的 DAQ Monitor，另开终端：
```bash
# 找到你的进程
dotnet counters ps

# 实时监控（-n 是进程名，--counters 指定要看的指标类别）
dotnet counters monitor -n DaqMonitor.UI --counters System.Runtime
```
**屏幕会实时滚动**（每秒刷新）：
```
System.Runtime
    CPU Usage (%)                                 12
    Working Set (MB)                             184
    GC Heap Size (MB)                              42.3
    Gen 0 GC / 1 sec                               3.2     ← 关注！每秒 3 次 Gen0
    Gen 1 GC / 1 sec                               0.1
    Gen 2 GC / 1 sec                               0       ← Gen2 必须接近 0
    Time in GC (%)                                 4.8
    ThreadPool Thread Count                       12
    Monitor Lock Contention / sec                  2.1     ← 锁竞争高？警惕
```
**怎么看**：
- `GC Heap Size` 一直涨不回落 → **内存泄漏**（看 Day 2 后半段）。
- `Gen 2 GC / 1 sec` > 0 → 大对象频繁进老年代，**最致命**——多半是 `new byte[大]` 没复用。
- `Monitor Lock Contention` 高 → 多线程在抢同一个 `lock`，M0 并发那块要回顾。
- `Time in GC (%)` > 10% → GC 占用过多 CPU，分配太频繁，回去加 `struct` / 池化。

**② 自定义 EventCounter：上报业务指标（队列长度、批次大小）**（🟦）
系统指标不够用——你想知道"采集管道里堆了多少点""每批多大"。这就要 `EventCounter`：
```csharp
// src/DaqMonitor.Core/Metrics/PipelineMetrics.cs
using System.Diagnostics.Tracing;

[EventSource(Name = "DaqMonitor-Pipeline")]
public sealed class PipelineMetrics : EventSource
{
    private readonly EventCounter _queueLength;
    private readonly PollingCounter? _batchSizeAvg;     // 平均批量

    public static readonly PipelineMetrics Instance = new();

    public PipelineMetrics()
    {
        _queueLength = new EventCounter("QueueLength", this)
        {
            DisplayName = "管道队列长度",
            DisplayUnits = "个"
        };
    }

    [Event(1)]
    public void RecordQueueLength(int len) => _queueLength.WriteMetric(len);

    public void ReportBatch(int size)
    {
        _queueLength.WriteMetric(size);   // 每次 Flush 后报当前批量
    }
}
```
**管道里调用**（呼应 M9 Day 3）：
```csharp
private void Flush()
{
    List<SensorPoint>? batch = null;
    lock (_gate) { /* ... */ }
    if (batch is not null)
    {
        PipelineMetrics.Instance.ReportBatch(batch.Count);   // 上报业务指标
        BatchReady?.Invoke(this, batch);
    }
}
```
监控它：
```bash
dotnet counters monitor -n DaqMonitor.UI --counters DaqMonitor-Pipeline
```
**前端类比**：这就是 `performance.mark('flush')` + `measure`，但能从命令行/容器里实时看，不依赖 UI。

**③ dotMemory 抓双快照对比，找"只增不减"的对象**（🟧 GUI）
工业程序跑 7 天内存涨到 OOM，99% 是泄漏。排查流程：
1. 启动程序，连上设备，**等初始化结束**（5 分钟，让"一次性分配"过去）。
2. dotMemory Attach 到进程，**抓快照 1**（baseline）。
3. 让程序跑它的典型工作负载 30 分钟（采集 + 报警 + 上云）。
4. **抓快照 2**。
5. 选 "Compare Snapshots" → 看 **Surviving Objects**（快照 2 比 1 多出来的）。

典型报告会告诉你：
- `EventHandler` 多了 5000 个 → **事件忘退订**（最常见）。
- `List<SensorPoint>` 持续涨 → **static 容器没清理**。
- `Timer` 多了 200 个 → **每次重连 `new Timer` 没释放旧的**。

**④ 4 种典型内存泄漏（必背）**

**泄漏 1：事件订阅忘退订**（呼应 M3）
```csharp
// ❌ 错的：每次重连都 +=，旧 handler 持有旧 device 引用
public void Reconnect()
{
    _device.DataReceived += OnData;     // 第 10 次重连 = 10 个 handler 持有 10 个旧 device
}

// ✅ 对的：先退订再订阅
public void Reconnect()
{
    if (_device is not null) _device.DataReceived -= OnData;
    _device = new ModbusDevice();
    _device.DataReceived += OnData;
}
```

**泄漏 2：static 字段持 List 无限增长**
```csharp
// ❌ 错的：永远只 Add，从不 Trim
public static class GlobalBuffer
{
    public static List<SensorPoint> All = new();   // 跑 7 天涨到 GB 级
}

// ✅ 对的：限长 + 旧数据落盘后清掉
public static class GlobalBuffer
{
    private static readonly LinkedList<SensorPoint> _buf = new();
    public static void Add(SensorPoint p)
    {
        _buf.AddLast(p);
        while (_buf.Count > 10_000) _buf.RemoveFirst();   // 滑动窗口
    }
}
```

**泄漏 3：字典 key 元数据（Tuple / string）没释放**
```csharp
// ❌ 错的：key 用 $"{deviceId}-{DateTime.Now.Ticks}"，每次都不一样，字典只增不减
_cache[$"{id}-{DateTime.Now.Ticks}"] = value;

// ✅ 对的：用 MemoryCache 设过期，或自己定时清理
_cache.Set(id, value, TimeSpan.FromMinutes(5));
```

**泄漏 4：Timer 回调闭包持有引用**
```csharp
// ❌ 错的：闭包持有 this，Timer 不停 → this 永不释放
public class BadWorker
{
    public void Start()
    {
        var timer = new Timer(_ => DoWork(), null, 1000, 1000);
        // 没 Dispose timer，它持有 this 引用
    }
}

// ✅ 对的：实现 IDisposable，Dispose 时关 Timer
public sealed class GoodWorker : IDisposable
{
    private readonly Timer _timer;
    public GoodWorker() => _timer = new Timer(_ => DoWork(), null, 1000, 1000);
    public void Dispose() => _timer.Dispose();
}
```

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ Finalizer 隐藏引用 | C# 析构函数 `~Class()` 会让对象多活一代，频繁写 finalizer 会卡 GC 线程 |
| 🔥 异步 lambda 捕获 | `device.Connect += async () => await X()` 闭包持有引用，事件不退订永远活着 |
| 🔥 `WeakReference` 不是免死金牌 | 用错（如订阅 WeakEvent 系统却持有强引用）照样泄漏 |
| 🔥 static 容器是泄漏大户 | 任何 static `List/Dict` 都要限长或定期清，否则撑到 OOM |
| 🔥 `Timer` 不 Dispose | `new Timer(callback, ...)` 不 Dispose 就一直持引用；IDisposable 模式别省 |
| ⭐ dotMemory 看 Retention Path | 不只看"对象多"，要看"谁持有它"——Retention Path 是定位泄漏的关键 |

### 🧪 三档练习
**🟢 基础题**：启动 DAQ Monitor，`dotnet counters monitor` 观察 GC Heap Size 跑 5 分钟的曲线，截图。要求 Gen 2 GC 一直是 0。

**🟡 进阶题**：给 `AcquisitionPipeline` 加 `PipelineMetrics`（队列长度 EventCounter），从 `dotnet counters monitor` 实时看到队列长度变化。

**🔴 挑战题**：故意制造一个泄漏版本（在 `MainViewModel` 里订阅一个 `static event` 不退订），用 dotMemory 抓两个快照对比，定位到具体是哪个对象、哪条 Retention Path。然后改成正确写法，再抓一次快照验证泄漏消失。

**✅ 答案（基础题）**
正常曲线：GC Heap Size 在某个范围波动（20-50MB），不单调上升。Gen 2 GC 始终 0。如果一直涨——**就是泄漏，回 Day 2 ④ 四个模板对一遍**。

**💡 工控导师说**：我审过一段代码——程序跑 3 天内存涨到 2GB，工厂被迫每周重启一次。后来用 dotMemory 抓两个快照一对比，是 `static event` 每次重连都订阅一次没退订。**改一行 `-=` 修好，省下每次重启的 30 分钟停机**。这就是性能工程的真金白银。

**🎓 职业建议**："你怎么排查内存泄漏？"是中高级岗面试必问。答"用 dotMemory 抓两个快照，看 Surviving Objects + Retention Path；常见 4 类：事件忘退订、static 容器、字典 key 没清、Timer 不 Dispose"——这一套术语一出口，面试官直接把你归到"真上过生产"的档。

**📚 延伸阅读**
- `dotnet counters` 文档：https://learn.microsoft.com/dotnet/core/diagnostics/dotnet-counters
- EventCounter 自定义：https://learn.microsoft.com/dotnet/core/diagnostics/event-counters
- dotMemory 教程（JetBrains）：https://www.jetbrains.com/dotmemory/
- 排查内存泄漏（官方）：https://learn.microsoft.com/dotnet/core/diagnostics/debug-memory-leak

**🏗️ 项目任务**：DAQ Monitor 加 `PipelineMetrics` EventCounter；启动后用 `dotnet counters monitor` 至少能看到 3 个业务指标（队列长度、批次大小、报警触发数）。

**✅ 打卡[ ]**

---

## Day 3 — 长跑稳定性策略（限流 / 降级 / 熔断 / 自愈）🔴

### 一句话讲清楚
工业系统跑 7×24 不崩 = **限流（防压垮）+ 降级（备路兜底）+ 熔断（防雪崩）+ 心跳（主动探活）+ 自愈（自动重连）+ 监控（事后追溯）**——**前端类比：Service Worker 离线缓存 / PWA 降级页面 / 请求限流 / Sentry 上报**。

### 前端类比秒懂
| 上位机长跑策略 | 前端类比 |
|---|---|
| Channel 有界 + DropOldest | axios 拦截器丢弃过期请求 |
| DB 挂 → 内存兜底 | Service Worker 缓存兜底 |
| 连续失败 5 次 → 停 30s | axios-retry + 熔断器（Polly 风格） |
| 每 5s 心跳探活 | WebSocket ping/pong |
| 设备掉线自动重连 | 断网自动 reconnect |
| Serilog 滚动日志 | Sentry / LogRocket |

### 分点精讲

**① 限流：Channel 有界 + DropOldest（采集永远不停）**（🟦，呼应 M0/M9）
无界 Channel 是"只要生产够快，内存管够"——一旦消费端卡顿（DB 慢查询），内存爆。**生产级必须有界**：
```csharp
var channel = Channel.CreateBounded<SensorPoint>(new BoundedChannelOptions(1000)
{
    FullMode = BoundedChannelFullMode.DropOldest,   // 队列满丢最旧，采集不停
    SingleReader = false,
    SingleWriter = false
});

// 写入端（采集线程）
public void OnDataReceived(SensorPoint p)
{
    // TryWrite 不阻塞：满就丢，采集线程绝不卡
    if (!channel.Writer.TryWrite(p))
        Interlocked.Increment(ref _droppedCount);   // 记丢了多少，监控用
}
```
**为什么 DropOldest 不是 Wait**：工业现场"实时值"最重要——宁可丢老的，也不能让采集线程被卡住漏掉新值。这和后端 Web 不同（后端是 Wait 排队），**采集系统永远要先保新数据**。

**② 降级：DB 挂了走内存兜底，设备掉线用模拟值占位**（🟦）
```csharp
// 持久化降级：DB 异常 → 内存队列暂存 + 后台重试
public async Task SaveAsync(IEnumerable<SensorPoint> batch)
{
    try
    {
        await using var db = _dbFactory.CreateDbContext();
        db.Points.AddRange(batch.Select(p => p.ToEntity()));
        await db.SaveChangesAsync();
    }
    catch (Exception ex)        // DB 挂了
    {
        _log.Warning(ex, "DB 不可用，转入内存兜底 {Count} 条", batch.Count());
        _inMemoryFallback.AddRange(batch);    // 暂存
        _dbAvailable = false;                  // 触发后台重试
    }
}

// 后台任务：每 30s 试着重连 DB
private async Task DbReconcileLoopAsync(CancellationToken ct)
{
    while (!ct.IsCancellationRequested)
    {
        await Task.Delay(30_000, ct);
        if (!_dbAvailable && _inMemoryFallback.Count > 0)
        {
            try
            {
                await using var db = _dbFactory.CreateDbContext();
                db.Points.AddRange(_inMemoryFallback.Select(p => p.ToEntity()));
                await db.SaveChangesAsync();
                _inMemoryFallback.Clear();
                _dbAvailable = true;
                _log.Information("DB 恢复，已重放 {Count} 条", _inMemoryFallback.Count);
            }
            catch { /* 还没好，下次再试 */ }
        }
    }
}
```

**③ 熔断：连续失败 5 次 → 停 30s 重连（防雪崩）**（🟦）
为什么需要熔断：设备挂了你每秒重试 1000 次，把现场总线打死、把 CPU 跑满。**熔断 = 失败到一定阈值就主动"歇一会"**：
```csharp
public sealed class CircuitBreaker
{
    private int _failures;
    private DateTime _openUntil = DateTime.MinValue;
    private readonly int _threshold;          // 失败阈值
    private readonly TimeSpan _openDuration;  // 熔断时长

    public CircuitBreaker(int threshold = 5, TimeSpan? openDuration = null)
    {
        _threshold = threshold;
        _openDuration = openDuration ?? TimeSpan.FromSeconds(30);
    }

    public bool IsClosed => DateTime.UtcNow >= _openUntil;   // 闭=可调用

    public void RecordSuccess()
    {
        Interlocked.Exchange(ref _failures, 0);
        _openUntil = DateTime.MinValue;
    }

    public void RecordFailure()
    {
        if (Interlocked.Increment(ref _failures) >= _threshold)
        {
            _openUntil = DateTime.UtcNow + _openDuration;
            // 触发回调：广播设备离线、记日志
        }
    }
}

// 使用：在 Retry 外面再包一层熔断
if (_breaker.IsClosed)
{
    try { await _device.ReadAsync(); _breaker.RecordSuccess(); }
    catch { _breaker.RecordFailure(); /* 连续 5 次后停 30s */ }
}
else
{
    _log.Warning("熔断中，跳过读取，{Sec} 秒后重试",
        (_openUntil - DateTime.UtcNow).TotalSeconds);
}
```

**④ 心跳：每 5s 发 0x00，3 次无响应判离线**（🟦，M9 Day 4⑤ 已落地）
M9 的 `DeviceHealthMonitor` 已经完整实现——主动探活，**别信 `IsConnected`**。这里复习关键参数：

| 参数 | 推荐值 | 说明 |
|---|---|---|
| 心跳间隔 | 3~10s | 太短压设备，太长发现掉线慢 |
| 判离线阈值 | 连续 2~3 次 | 防 1 次抖动误判 |
| 重连退避 | 1s→2s→4s...→封顶 30s | 指数退避别死循环 |

**⑤ 自愈：设备掉线 → 自动重连，Redis 挂 → 走内存**（🟦）
自愈 = 系统检测到故障后**自动恢复到可用状态**，不需要人工重启。组合上面 ① ~ ④：
```
心跳探活 → 检测掉线 → 熔断打开（停 30s）→ 退避重连 → 成功后熔断关闭 → 恢复采集
                          ↓（DB/Redis 挂）
                     降级走内存/模拟值
```
**关键：故障期间 UI 仍然能显示"上次值 + 离线标记"**，而不是白屏崩溃。这就是 7×24 的真正含义——**永远不挂，最多降级**。

**⑥ 监控报警：Serilog 滚动日志 + 关键指标推送**（🟧 呼应 M6）
```csharp
Log.Logger = new LoggerConfiguration()
    .MinimumLevel.Information()
    .WriteTo.File("logs/daq-{Date}.log",
        rollingInterval: RollingInterval.Day,     // 每天一个文件
        retainedFileCountLimit: 30,                // 只留 30 天
        fileSizeLimitBytes: 100_000_000)           // 单文件 100MB 上限
    .WriteTo.Console()
    .CreateLogger();

// 关键事件推送到监控系统（钉钉/企业微信/邮件）
_alarm.AlarmTriggered += async (s, e) =>
{
    Log.Warning("报警 点位{Id} 值{Value}", e.PointId, e.Value);
    if (e.Level == AlarmLevel.Critical)
        await _notifier.SendAsync($"[严重] 设备 {e.PointId} 值 {e.Value}");
};
```

### ⚠️ 重点 & 易踩坑
| 项 | 说明 |
|---|---|
| ⭐ 限流先于优化 | 别等性能不够才限流——先限流保命，再优化提性能 |
| ⭐ 降级要可见 | UI 上明示"DB 异常 / 数据缓存中"，别让操作员以为系统正常 |
| 🔥 熔断阈值要合理 | 太敏感（失败 1 次就熔断）= 频繁中断；太迟钝 = 不熔断等于没装 |
| 🔥 重连退避必须封顶 | `Math.Min(delay, 30s)`——不然指数涨到几分钟，现场等不起 |
| 🔥 日志文件别无限增长 | `retainedFileCountLimit` + `fileSizeLimitBytes` 必须设，否则一周塞满磁盘 |
| ⭐ 故障期间采集不停 | 数据可以丢（DropOldest），但采集线程绝不能因为下游故障而停摆 |

### 🧪 三档练习
**🟢 基础题**：把 M9 的 `AcquisitionPipeline` 从 `Channel.CreateUnbounded` 改成 `Channel.CreateBounded(1000, DropOldest)`，并加一个 `_droppedCount` 计数器，每次丢弃 +1。

**🟡 进阶题**：给 DAQ Monitor 的 DB 写入层加"降级走内存"——`SaveAsync` catch 异常后入 `_inMemoryFallback` 队列，后台 30s 重试。

**🔴 挑战题**：把 Day 3 ③ 的 `CircuitBreaker` 接进 M9 的 `DeviceHealthMonitor`——心跳连续失败 5 次后熔断 30s（期间不连不读），30s 后半开（试连一次），成功关闭熔断 / 失败再开 30s。这就是"**熔断器三态（Closed / Open / HalfOpen）**"，Polly 库的核心思想。

**✅ 答案（基础题）**
```csharp
private readonly Channel<SensorPoint> _channel =
    Channel.CreateBounded<SensorPoint>(new BoundedChannelOptions(1000)
    {
        FullMode = BoundedChannelFullMode.DropOldest
    });
private int _droppedCount;

public void Enqueue(SensorPoint p)
{
    if (!_channel.Writer.TryWrite(p))
        Interlocked.Increment(ref _droppedCount);
}
public int DroppedCount => _droppedCount;   // 通过 PipelineMetrics 上报
```

**💡 工控导师说**：稳定性这事没捷径，就是把"**限流 / 降级 / 熔断 / 心跳 / 自愈 / 监控**"六件事都做到位。我带过最稳的一个 SCADA 系统，**连续跑 2 年没重启过**——靠的就是这套。不是炫技，是每个边界条件都想到、每个失败路径都有兜底。

**🎓 职业建议**：面试问"你的程序怎么保证 7×24 不崩"，把上面六件事讲一遍——**限流（Channel 有界）、降级（DB 挂走内存）、熔断（连续失败停 30s）、心跳（5s 探活判离线）、自愈（指数退避重连）、监控（Serilog 滚动 + 关键指标推送）**。这套术语一出口，面试官直接给"高级工程师"档，15K 稳。

**📚 延伸阅读**
- `System.Threading.Channels` 限流：https://learn.microsoft.com/dotnet/api/system.threading.channels
- Polly 熔断器（业界标杆）：https://github.com/App-vNext/Polly
- Serilog 滚动日志：https://github.com/serilog/serilog-sinks-file
- Microsoft 限流模式：https://learn.microsoft.com/azure/architecture/patterns/

**🏗️ 项目任务**：给 DAQ Monitor 上完整长跑铠甲——Channel 改有界 + DropOldest、DB 写入加降级、关键设备加 CircuitBreaker、日志配滚动 + 大小上限。跑 24 小时验证 GC Heap Size 不单调上涨。

**✅ 打卡[ ]**

---

## 模块交付清单（M9.5）
- [ ] `src/DaqMonitor.Bench` 项目跑通，至少 3 组压测（CRC / struct vs class / FrameParser）
- [ ] `dotnet counters monitor` 能看到 GC/线程 + 至少 1 个自定义业务 EventCounter
- [ ] dotMemory 双快照对比报告：无"只增不减"对象
- [ ] Channel 改有界（1000 + DropOldest），带丢弃计数
- [ ] DB 写入层降级走内存兜底
- [ ] 关键设备接 CircuitBreaker（Closed/Open/HalfOpen 三态）
- [ ] 日志滚动 + 大小上限（30 天 × 100MB）
- [ ] 24 小时长跑测试：GC Heap Size 稳定、Gen 2 GC = 0

## 📌 温故知新 / 跨模块联动
- **← M0（并发 + 分层）**：本模块的"Channel 有界限流"正是 M0 Day7 的进阶版——无界是教学版，有界 + DropOldest 才是生产版。
- **← M2（CRC / FrameParser）**：Day 1 的 bench 直接量化 M2 写过的两段代码，让你看到"按位 vs 查表"的真实差距。
- **← M3（PLC 容错）**：M3 的 `IsConnected 不可信` → 本模块的心跳 + 熔断 + 自愈是完整解法。
- **← M4（持久化）**：DB 写入的"降级走内存兜底"呼应 M4 的失败重放——DB 不是永远可用，兜底必须做。
- **← M5（实时可视化）**：Day 1 的"struct vs class"bench 证明了 M5 用 `readonly struct SensorPoint` 的选择正确——每秒少 24KB 垃圾。
- **← M9（统一管道 + Retry + 心跳）**：本模块是 M9 的"性能体检 + 长跑保险"——M9 解决"工程化结构"，本模块解决"性能数据 + 跑死怎么办"。
- **↔ M19（问题排查与调试）**：M19 调试讲义的 4 种内存泄漏排查(`dotnet counters` / `dotnet gcdump` / `dotnet dump`)正是本模块 Day 2-3 的工具链。**M9.5 偏"压测找瓶颈",M19 偏"调试找根因",两者工具栈重叠 70%,建议合并学习**(参考 [30 天作战路线 Day 11 + Day 18](30天作战路线_转行冲刺版.md) 把两块串起来)。
- **→ 简历一句话**：「用 BenchmarkDotNet 量化采集路径性能 < 1ms；用 dotnet counters 监控 GC + 自定义业务 EventCounter；用 dotMemory 排查并修复事件订阅泄漏；长跑策略 Channel 限流 + 熔断 + 心跳自愈」——四句对齐 15K 加分项。

## 🧩 完整代码组装（M9.5 落地的关键代码，可直接抄 / 添加到工程）
```csharp
// src/DaqMonitor.Bench/CrcBench.cs           —— Day 1 CRC 压测
// src/DaqMonitor.Bench/AllocBench.cs         —— Day 1 struct vs class
// src/DaqMonitor.Core/Metrics/PipelineMetrics.cs   —— Day 2 EventCounter
// src/DaqMonitor.Core/Reliability/CircuitBreaker.cs —— Day 3 熔断器（三态）
// src/DaqMonitor.Core/Reliability/FallbackQueue.cs  —— Day 3 DB 降级内存兜底

// 限流：M9 的 AcquisitionPipeline 改成有界 + DropOldest
var channel = Channel.CreateBounded<SensorPoint>(new BoundedChannelOptions(1000)
{
    FullMode = BoundedChannelFullMode.DropOldest
});
```
> 做完这套，DAQ Monitor 就从"功能跑通"升级到"**性能可量化、长跑可信任**"——这才是 15K 工程师的产出标准。

## 🔗 下一步建议
本模块结束后，整个上位机学习路径（M0–M16 + M9.5）已完整覆盖：通信 / 持久化 / 可视化 / 报警 / 上云 / 工程 / 性能 / 长跑。建议下一步进入**真实项目实战**——把 BenchmarkDotNet 报告 + dotMemory 截图 + 24h 长跑日志整理成作品集，简历与面试直接拉开档次。

## 📚 延伸阅读（卡点时点开）
- BenchmarkDotNet 官方：https://benchmarkdotnet.org/
- `dotnet counters` / `dotnet trace` / `dotnet dump`：https://learn.microsoft.com/dotnet/core/diagnostics/
- JetBrains dotMemory：https://www.jetbrains.com/dotmemory/
- PerfView（免费高级分析）：https://github.com/microsoft/perfview
- Polly 弹性库：https://github.com/App-vNext/Polly
- 全部模块外链汇总见 `外部链接索引.md`
