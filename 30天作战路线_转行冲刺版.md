# 30-45 天作战路线 · 转行冲刺版(代码练习融入日常版)

> **北极星**:**30-45 天后**(看你节奏,赶进度可压到 30 天,内容多可放宽到 45 天),简历上有一个**你自己一行行手敲出来**的「DAQ Monitor 工业数据采集监控系统」,投 13-15K 上位机岗。
> **核心策略**:**讲义(理论)+ 练习(代码肌肉)+ 手敲进项目(实践)** 三位一体。每天 = 看讲义 → 做练习 → **把今天学的写进你的 DAQMonitor**。学完 = 项目手敲一遍。
> **节奏建议**:每天 5-6 小时(深度学习 2h + **手敲项目 2h** + 代码肌肉 1h + 打卡复盘 0.5h)。周末加量。**如果某周内容偏重(如 Week 1 通讯基础 + Week 3 上云),允许延长 3-5 天,不要为了赶进度跳过手敲**。
> **前置**:你是中高级前端,所以 **M0 大部分速过**(只精读"C# 陷阱专项")。
> **节奏调整原则**:**学会为标准,不是看时间为标准**。每个 Day 的"打卡"全部 ✅ 才能进下一天;某天没空就休息,不要"半懂就过"。

> 📕 **配套讲义**:[C# 陷阱](C#_陷阱_前端转上位机必看_深度版.md) · [代码肌肉训练手册(题库)](代码肌肉训练手册_30天刷题版.md) · [面试逐字稿 30 题](面试问答_逐字稿_30题.md) · [简历模板](简历模板_上位机_13-15K.md) · [JD 调研](JD调研_13-15K上位机岗位对照.md) · [整体串联审计](审计_整体串联复核_v2.md) · [C# 语法速查](CSharp语法速查_前端视角.md)

---

## 🛑 必读:理论和实践结合规则(反"看懂 ≠ 默写"陷阱)

> ⚠️ **这是 30 天路线最重要的一节。看完再继续。**

### 陷阱描述

```
你拿到的:
  ├── DAQMonitor/            ← 完整工业级代码(85 测试绿)— 这是「参考答案」
  ├── 22 个讲义.md            ← 解释每个模块
  └── 代码肌肉手册             ← 白板题库

陷阱:
  读 DAQMonitor 代码点头"懂了" → 关掉参考 → 一字写不出
  面试官:"现场写 ModbusDevice" → 你卡死
```

**5 年前端理解能力极强,但理解 ≠ 默写**。面试是默写场景,不是阅读理解。

### 解决方案:建立你的"手敲版"DAQMonitor

**Day 0.5(周日晚上,30 分钟)做这件事**(详见下文 Day 0.5):
```bash
# 在你的工作目录(不在 DAQMonitor 内)新建一个空项目
mkdir DAQMonitor-mine && cd DAQMonitor-mine
git init
dotnet new sln -n DaqMonitor
dotnet new classlib -o src/DaqMonitor.Core
dotnet new wpf      -o src/DaqMonitor.UI
dotnet new xunit    -o src/DaqMonitor.Tests
# 加 reference、初始化 git、首次 commit
```

**之后每个 Day**:
1. **学**:读对应讲义.md(理解原理)
2. **练**:代码肌肉手册对应题(白板默写片段)
3. **写进项目**:**关掉 DAQMonitor 参考版**,在你的 `DAQMonitor-mine/` 里写当天模块
4. **对照**:写完打开 DAQMonitor 参考版,对比差异,改进
5. **commit**:`feat(MXX-主题): 手敲实现 + 测试`

### 30 天后你有什么

| 仓库 | 是什么 | 用途 |
|------|--------|------|
| `DAQMonitor/` | **参考答案**(我已写好的) | 卡住时对照、看高级写法 |
| `DAQMonitor-mine/` | **你手敲的版本** | 简历项目、面试演示、git 历史证明 |

**面试时**:
- 简历链接 `DAQMonitor-mine/`(你的 GitHub)
- 面试官能看 git log,**30 天的 commit 历史** = 你的学习轨迹 = 亲手写的证据
- 问"这个模块怎么实现的",你说"看我的 commit,Day X 我手敲的",面试官立刻信

### 反理解陷阱自检(每天结束问自己)

1. 我能关掉参考,在白板上写出核心代码吗?
2. 我能解释每一行为什么这样写吗?
3. 我能告诉面试官"如果换一个协议怎么改"吗?
**任何一条做不到 → 当天没学透,周末补**。

### 节奏校准

原 5h/天 → 现 5-6h/天(项目实战环节由"改 DAQMonitor"变成"自己手敲进 DAQMonitor-mine")。**没有额外负担,只是项目环节换了姿势**。

---

## 0. 开始之前(必读 30 分钟)

### 心态校准
- **前端 vs 上位机的本质差**:前端是「快速迭代+用户体验」,上位机是「7×24 稳定+协议正确+现场排错」。**慢就是快,稳就是好**。
- **三大致命坑**(前端背景必踩):① struct 值类型拷贝 ② 字节序/CRC 位运算 ③ async void 吞异常。**Day 1 必读** [C#_陷阱_前端转上位机必看_深度版.md](C#_陷阱_前端转上位机必看_深度版.md)。

### 代码肌肉训练的纪律(🔑 转行核心)
- **白板**:不准用 IDE 自动补全,VS Code 关闭 IntelliSense 或用记事本写
- **限时**:超时也算完成,但标"未达标",二刷时降低时间
- **错题本**:错的题写到 `错题本.md`,每天复习
- **可讲**:写完必须能口头讲清思路(模拟面试官追问)
- **二刷**:错题 3 天后二刷,7 天后三刷,直到不假思索写出

### 环境清单
- .NET 8 SDK + Visual Studio 2022(勾「.NET 桌面开发」)
- VS Code(写 md / Python 脚本 / 白板刷题)
- Git + GitHub(每天 commit)
- 可选:VMware + Ubuntu(后续 OPC UA/MQTT broker 用)

---

## Week 1 · 主干打底(Day 1-7)

> **目标**:能独立用串口/Modbus 跟设备通信,理解"工业协议"是怎么回事。项目里多出 ModbusDevice / PlcDevice 两个真设备。
> **代码肌肉目标**:**5 分钟白板写完 Modbus CRC16 + 10 分钟白板写完 Channel 生产消费者**

### Day 0.5(周日晚上,30 分钟)· 建立 DAQMonitor-mine 空骨架(必做)
> **为什么加这一步**:你看 [实操入门](实操入门_从零搭建企业级工程.md) 是"读懂",但面试官问"你这项目怎么建的",你能脱口而出 `dotnet new sln + 3 个 csproj + reference` 才算你的。**只花 30 分钟,在你自己的工作目录建一个空骨架**——这就是你接下来 30 天每天往里加代码的项目。

#### 🚦 前置检查(2 分钟,不做后面全卡死)

| 检查项 | 命令 | 期望 | 卡死场景 |
|---|---|---|---|
| .NET 8+ SDK 装了吗 | `dotnet --version` | `8.0.xxx` 或 `10.x.x` | 没装 → `dotnet new` 命令找不到 |
| Git 装了吗 | `git --version` | `git version 2.x` | 没装 → 第 5 步推不上去 |
| GitHub 账号 + SSH key 配了吗 | `ssh -T git@github.com` | 看到 `Hi 你的用户名!` | 没配 → `git push` 报权限错 |
| 当前目录对吗 | `pwd` (Git Bash) | `f:\00_project\上位机学习` | 不对 → 后面路径全错 |
| DAQMonitor-mine 是否已存在 | `ls` | 不存在 | 已存在 → 换名 or 删了重建 |

#### 📂 关于 src/ 目录(新手必看)

下面的命令 `dotnet new classlib -o src/DaqMonitor.Core` 会**自动创建 src/ 父目录**,你**不用手动 `mkdir src`**。`dotnet new -o <路径>` 会创建路径上所有不存在的目录。

#### 动作清单(全部命令行,不用 VS)

> ⚠️ **每条命令单独执行,等上一条跑完再敲下一条**。一次性粘贴多条容易因为某条失败导致后面全废。

```bash
# 1. 在你的工作目录(不在 DAQMonitor 参考版内)新建空项目
mkdir DAQMonitor-mine && cd DAQMonitor-mine
git init

# 2. 建解决方案 + 3 个工程(对照参考版 DAQMonitor/src/ 的结构)
#    src/ 目录会自动建,不用手动 mkdir
dotnet new sln -n DaqMonitor
dotnet new classlib -o src/DaqMonitor.Core -n DaqMonitor.Core
dotnet new wpf      -o src/DaqMonitor.UI   -n DaqMonitor.UI
dotnet new xunit    -o src/DaqMonitor.Tests -n DaqMonitor.Tests

# 3. 工程引用关系(UI/Tests 都用 Core)
dotnet sln add src/DaqMonitor.Core/DaqMonitor.Core.csproj
dotnet sln add src/DaqMonitor.UI/DaqMonitor.UI.csproj
dotnet sln add src/DaqMonitor.Tests/DaqMonitor.Tests.csproj
dotnet add src/DaqMonitor.UI/DaqMonitor.UI.csproj reference src/DaqMonitor.Core/DaqMonitor.Core.csproj
dotnet add src/DaqMonitor.Tests/DaqMonitor.Tests.csproj reference src/DaqMonitor.Core/DaqMonitor.Core.csproj

# 4. 验证(每条都应成功,失败先看错误信息再继续)
dotnet build           # 必须 0 错
dotnet test            # xUnit 默认 1 个测试通过(看到 Passed: 1 即可)
dotnet run --project src/DaqMonitor.UI   # WPF 空窗口能开(看到空白窗口就成功,close 关掉)

# 5. 推到 GitHub(你的私人仓库,简历用)
#    先去 https://github.com/new 建一个名为 DAQMonitor-mine 的空仓库(不要勾 README)
git add -A
git commit -m "chore: init DAQMonitor-mine skeleton (Day 0.5)"
git remote add origin git@github.com:你的用户名/DAQMonitor-mine.git
git push -u origin main
```

#### ❓ 常见卡死

| 报错 | 原因 | 解法 |
|---|---|---|
| `dotnet new wpf` 失败 | 你在 Mac/Linux 上 | WPF 只能 Windows 跑,改用 WinForms 或换 Windows 机器 |
| `dotnet test` 报 "No test found" | xUnit 模板版本差异 | 跑 `dotnet new xunit -o src/DaqMonitor.Tests` 重建测试工程 |
| `dotnet run` 报找不到 MainWindow | WPF 模板没装 | 装 `.NET Desktop Runtime` + 桌面开发 workload |
| `git push` 报 Permission denied | SSH key 没配 | 看 https://docs.github.com/ssh 配 key,或改用 HTTPS |
| `dotnet build` 报 NETSDK109 | SDK 版本太低 | 装 .NET 8 SDK 或更高 |

**别删!这是你接下来 30 天的"主战场",每天 commit 都进这里。**

**打卡**:✅ 不查文档能背 `dotnet new sln/classlib/wpf/xunit` 4 个命令 ✅ `dotnet build` 0 错 ✅ WPF 空窗口能开 ✅ 已推到 GitHub

---

### Day 1(周一)· 立骨架 + C# 陷阱专项
**学习**(2h):
- 速过 [M0_每日讲义_深度版.md](M0_每日讲义_深度版.md) Day 1-6(前端背景,C# 语法/WPF 基础大部分能迁移)
- **精读** [C#_陷阱_前端转上位机必看_深度版.md](C#_陷阱_前端转上位机必看_深度版.md) — 重点 struct/字节序/async void 三节
- 速过 [实操入门_从零搭建企业级工程.md](实操入门_从零搭建企业级工程.md)
- 📚 顺手扫一遍 [C# 语法速查 — 前端视角](CSharp语法速查_前端视角.md) §1-§5

**写进 DAQMonitor-mine**(1.5h):
- **关掉 DAQMonitor 参考版**,在你的 `DAQMonitor-mine/src/DaqMonitor.Core/` 下手敲(**先打开 [前置类型定义_学员粘贴版.md](前置类型定义_学员粘贴版.md) 作为唯一权威源,字段/方法签名一字不差照抄**):
  - `Devices/IDevice.cs` 接口 — `Task ConnectAsync(CancellationToken ct = default)` / `Task DisconnectAsync(CancellationToken ct = default)` / `event EventHandler<SensorPoint>? DataReceived`。⚠️ **没有 `ReadAsync`** — 数据通过事件推送(reactive 模型),不是主动读
  - `Models/SensorPoint.cs` — **`readonly struct`,3 字段**:`int Id` / `double Value` / `DateTime Timestamp`,带构造函数 `SensorPoint(int id, double value, DateTime? timestamp = null)`。⚠️ **不要加 Name/Quality**,前置类型定义里没有,加了 Day 8 起所有代码会类型不匹配
  - `Devices/SimulatedDevice.cs` — 实现 `IDevice`,timer 每 500ms 调用 `RaiseData(new SensorPoint(...))` 触发事件(继承 `DeviceBase` 走 `RaiseData` 助手方法)
- 写完打开 DAQMonitor 参考版的同名文件对比,补上你漏掉的(比如 CancellationToken)
- 在 `DaqMonitor.Tests/` 写 1 个测试:验证 SimulatedDevice 触发事件
- commit:`feat(M0): IDevice 接口 + SimulatedDevice + 测试`

**代码肌肉**(1h):
- 翻译题 A1-A4([代码肌肉手册 Part A](代码肌肉训练手册_30天刷题版.md#part-a--前端--c-翻译题20-道))— Promise→Task / Promise.all→Task.WhenAll / Array.map→LINQ Select
- Debug 题 C1(async void 吞异常)— 5 分钟找 bug + 看答案

**打卡**:✅ DAQMonitor-mine 跑通 `dotnet test` ✅ 看懂 IDevice 接口(自己写的) ✅ 手算 1 道 CRC 题 ✅ A1-A4 + C1 限时完成

### Day 2-3(周二-周三)· 串口 + Modbus RTU 全套
**学习**(4h):
- [M1_串口通信_深度版.md](M1_串口通信_深度版.md) 全文(波特率/RS232/RS485/CRC/半包)
- [M2_Modbus_深度版.md](M2_Modbus_深度版.md) 全文 — **手算 CRC 必须过关**

**写进 DAQMonitor-mine**(3h):
- **关掉参考版**,在 `DAQMonitor-mine/src/DaqMonitor.Core/` 手敲(注意目录名跟参考版对齐):
  - `Devices/ISerialChannel.cs` — **抽串口接口**(参考版做了这层抽象,让串口可测试 — 依赖倒置原则的实战)
  - `Devices/LoopbackSerialChannel.cs` — 不依赖真串口的回环实现,测试用
  - `Devices/RealSerialChannel.cs` — 包 `System.IO.Ports.SerialPort`,实现 `ISerialChannel`
  - `Devices/SerialDevice.cs` — 依赖 `ISerialChannel`(不是直接 `SerialPort`),实现 IDevice,订阅 DataReceived
  - `Protocol/Crc16.cs` — 手写 CRC16 表(参考 [代码肌肉 B1](代码肌肉训练手册_30天刷题版.md))
  - `Protocol/ModbusFrameParser.cs` — Modbus RTU 帧组装 + 解析(状态机 + 半包处理)
  - `Devices/ModbusDevice.cs` — 组合 SerialDevice + ModbusFrameParser + Crc16,实现 IDevice(参考版把 RTU 主站逻辑直接合并进 ModbusDevice,没有独立的 ModbusRtuMaster 类)
- 在 `DaqMonitor.Tests/` 写测试:
  - Crc16 测试(给定字节数组验证 CRC 值)
  - ModbusFrameParser 帧组装测试(用 LoopbackSerialChannel,不依赖真串口)
- **对照** DAQMonitor 参考版,看差异,补漏
- commit Day 2:`feat(M1): ISerialChannel + SerialDevice 实现 + 测试`
- commit Day 3:`feat(M2): CRC16 + ModbusFrameParser + ModbusDevice`

**代码肌肉**(2h):
- Day 2: **手写题 B1(CRC16,5 分钟白板)** — 这是 13-15K 面试高频白板题
- Day 2: 翻译题 A5-A8(filter/reduce/event/useState 翻译)
- Day 3: Debug 题 C2(.Result 死锁) + C10(字节序搞反)
- Day 3: 翻译题 A9-A12(fetch/HttpClient/JSON 等)

**打卡**:✅ 手算 CRC 16 题 ✅ DAQMonitor-mine 里 SerialDevice + ModbusDevice 跑通(虚拟串口/虚拟从站) ✅ B1 5 分钟白板过关 ✅ C2/C10 5 分钟找 bug

### Day 4-5(周四-周五)· PLC(西门子 S7)
**学习**(3h):
- [M3_PLC_深度版.md](M3_PLC_深度版.md) 全文 — 重点 S7.Net 用法/DB 块/优化访问/错误模型
- 不用精通,会用 S7.Net 读 DB1.DBX0.0 / DB1.DBD4 这种地址就行

**写进 DAQMonitor-mine**(3h):
- **关掉参考版**,在 `DAQMonitor-mine/src/DaqMonitor.Core/Devices/` 手敲:
  - `IPlcClient.cs` — **先抽接口**!S7.Net 的 `Plc` 类是 sealed,Moq 无法直接 mock,必须自己包一层接口(`Task Open()` / `Task Close()` / `Task<object> Read(string variable)` / `Task Write(string variable, object value)`),`PlcDevice` 依赖 `IPlcClient` 而不是 `Plc`。这是 13-15K 面试官会追问的"你测试怎么做的"的标准答法 — **依赖倒置 → 可测试**
  - `S7PlcClient.cs` — 真正调 S7.Net 的 `Plc` 类,实现 `IPlcClient`
  - `PlcDevice.cs` — 依赖 `IPlcClient`,实现 IDevice,提供读 DB 块的方法
  - 没有真实 PLC 不实测没关系,代码必须写对(对照参考版验证)
- 在 `DaqMonitor.Tests/` 写测试:Mock `IPlcClient`(不是 S7.Net 的 `Plc`),验证 PlcDevice 在 Open 失败时正确重试 / Read 返回正确类型转换
- commit Day 4:`feat(M3): IPlcClient + S7PlcClient + PlcDevice`
- commit Day 5:`test(M3): PlcDevice 用 Mock IPlcClient 测试`

**代码肌肉**(2h):
- Day 4: **手写题 B2(Channel 生产消费者,10 分钟白板)** — 13-15K 必考
- Day 4: Debug 题 C3(多线程竞态丢数) + C4(event 没退订)
- Day 5: 翻译题 A13-A16(闭包/async/继承/泛型)
- Day 5: Debug 题 C5(DbContext 线程不安全)

**打卡**:✅ DAQMonitor-mine 里 PlcDevice 编译通过 + 测试绿 ✅ 能讲清 S7.Net 优势 vs Modbus ✅ B2 10 分钟白板过关

### Day 6(周六)· 多设备抽象 + 收口
**学习**(2h):
- 复习 M0 Day 7(并发/Channel/CancellationToken)
- 看 [硬件替代方案与讲解_深度版.md](硬件替代方案与讲解_深度版.md) — 没硬件怎么练

**写进 DAQMonitor-mine**(3h):
- **关掉参考版**,在 `DAQMonitor-mine/src/DaqMonitor.Core/Acquisition/` 手敲:
  - `AcquisitionPipeline.cs` — **统一采集管道**(参考版没有独立 `IAcquisitionPipeline` / `AcquisitionService` / `DeviceManager`,职责全部集中在 `AcquisitionPipeline` 一个类里:管理多设备注册、用 `Channel<SensorPoint>` 汇总、后台任务消费)。**别过度拆分**,先把功能跑通,Day 12 Prism 重构时再考虑分层
  - 字段:`List<IDevice> _devices` / `Channel<SensorPoint> _channel`
  - 方法:`RegisterDevice(IDevice)` / `StartAsync(CancellationToken)` / `StopAsync()` / `IAsyncEnumerable<SensorPoint> ReadAllAsync(CancellationToken)`
  - 多设备并发:每个设备的 `DataReceived` 事件 → `_channel.Writer.WriteAsync(point)`
- 写测试:多设备(SimulatedDevice × 3)并发采集 10 秒,验证 Channel 不丢数据(读到的点数 ≈ 设备数 × 采集频率 × 时长)
- commit:`feat(M0+M3): AcquisitionPipeline 多设备 Channel 采集管道`

**代码肌肉**(1h):
- **手写题 B3(IDevice 完整实现,15 分钟白板)** — 含 Connect/Read/Disconnect/异常处理
- 翻译题 A17-A20(模块化/枚举/接口/反射)

**打卡**:✅ DAQMonitor-mine 里 4 种设备同时跑 ✅ B3 15 分钟白板过关 ✅ A1-A20 翻译题全过

### Day 7(周日)· 调试入门 + 错题二刷
**学习**(2h):
- **精读** [M19_问题排查与调试_深度版.md](M19_问题排查与调试_深度版.md) Day 1(VS 调试器高级功能:条件断点 / 数据断点 / 调用栈 / Threads 窗口)
- 装 Wireshark + 串口监视器

**在你的 DAQMonitor-mine 上调试**(2h):
- 在 `DAQMonitor-mine/src/DaqMonitor.Core/Devices/ModbusDevice.cs` 的 `ReadAsync` 下条件断点(`pointId == 42 && value > 100`)
- 用 Wireshark 抓一次本机 Modbus TCP,看真实字节,跟你 Day 3 手敲的帧格式对比
- 故意制造一个粘包 bug,用调用栈追踪到源头
- commit:`docs(debug): 调试笔记 Day 7 - 条件断点 + Wireshark`

**代码肌肉**(2h):
- B1+B2+B3 三刷(限时 80%,即 CRC 4 分钟、Channel 8 分钟、IDevice 12 分钟)
- Debug C1+C2 二刷

**Week 1 检查点**(自测):
- [ ] 能背 Modbus RTU 帧格式 + 异常码 0x01-0x04 含义
- [ ] 能 5 分钟白板手算 `01 03 00 00 00 02` 的 CRC
- [ ] 能讲清 IDevice 抽象为什么这么设计
- [ ] 能讲清 struct vs class 在多线程的差异
- [ ] **DAQMonitor-mine Week 1 commit 数 ≥ 6**(Day 0.5 / 1 / 2 / 3 / 4 / 6 各一个)
- [ ] **代码肌肉**:CRC 5min / Channel 10min / IDevice 15min,不查文档能写
- [ ] **调试**:会用 VS 条件断点 + Wireshark 抓 Modbus 字节

---

## Week 2 · 数据落库 + 可视化 + Prism(Day 8-14)

> **目标**:数据真的进 SQLite,UI 上有曲线/仪表盘/报警,用 Prism 重构 MVVM。这周结束项目就达到"13K 简历可投"线。
> **代码肌肉目标**:**10 分钟白板写完完整 ModbusDevice + 5 分钟写完 RelayCommand**

> 📌 **本周所有"项目"环节 = 在你的 `DAQMonitor-mine/` 手敲**
> 本章每个 Day 的"项目"任务都假定你**关掉 DAQMonitor 参考版**,在自己的 `DAQMonitor-mine/` 仓库手敲当天模块,写完打开参考版对比、补漏,然后 git commit。**不要"读+改"参考版假装会**。Week 1 你已经手敲完 IDevice / Simulated / Serial / Modbus / Plc / DeviceManager,本周在此基础上加 Store / Chart / Alarm / Prism 重构。

### Day 8-9(周一-周二)· 数据持久化(EF Core + SQLite)
**学习**(3h):
- [M4_数据持久化_深度版.md](M4_数据持久化_深度版.md) 全文 — 重点 EF Core 变更追踪/迁移/DbContext 生命周期/异步查询
- 学习 SQLite 浏览工具(DB Browser for SQLite)

> ⚠️ **EF Core 4 大必考点(15K 面试官会追问,讲义缺口,在此补)**:
> 1. **为什么 DbContext 不能单例**:变更追踪字典(ChangeTracker)非线程安全,同一实体被多线程同时修改会脏读;`SaveChanges` 是串行的,多个线程并发调会报 "A second operation was started on the same context instance" 异常。**正确做法**:`AddDbContextFactory`(工厂,每次 `CreateDbContext()` 拿短命实例)或 `AddDbContext(Scoped)`(每个 HTTP 请求/工作单元一个)
> 2. **AsNoTracking 何时用**:批量只读查询(如历史数据导出)用 `_context.SensorRecords.AsNoTracking().Where(...).ToListAsync()`,**跳过变更追踪字典的写入,提升性能 2-3 倍**。写场景必须 tracking
> 3. **并发 Token(乐观锁)**:两人同时改一条配方 → 后写的会覆盖先写的。解决:在实体加 `[Timestamp] public byte[] RowVersion { get; set; }`,SaveChanges 时 EF 自动 `WHERE RowVersion = @original`,冲突抛 `DbUpdateConcurrencyException`,业务侧 catch 后让用户选择"重看 / 强制覆盖"
> 4. **生产环境迁移怎么应用**:❌ 启动时 `Database.Migrate()`(多实例并发会竞争,生产不建议);✅ `dotnet ef migrations script --idempotent -o migrate.sql` 生成幂等 SQL 让 DBA 执行;✅ EF Core 6+ 的 `Bundle` 生成独立可执行 migrate.exe

**项目**(3h):
- 读+理解 `DAQMonitor/src/DaqMonitor.Core/Store/PointStore.cs`(双写内存+异步 SQLite)
- 读+理解 `AppDbContext.cs` + `SensorRecord.cs`
- 写一个历史查询方法(按时间范围 + 点位 ID 查)

**代码肌肉**(2h):
- Day 8: **手写题 B11(PointStore 内存索引+SQLite 落库,15 分钟白板)** — 含 Channel 异步消费
- Day 8: 翻译题 A1-A10 二刷(限时 50%)
- Day 9: **手写题 B4(Retry 指数退避,10 分钟白板)**
- Day 9: Debug C5 二刷 + C6(struct 拷贝)

**打卡**:✅ 数据真的进 .db 文件 ✅ 用 DB Browser 能看到数据 ✅ B11 15 分钟白板过关

### Day 10(周三)· 实时可视化(LiveCharts2)
**学习**(1.5h):
- [M5_实时可视化_深度版.md](M5_实时可视化_深度版.md) — 重点实时曲线性能(高频滚动)、ObservableCollection 更新策略

**项目**(2.5h):
- 读+理解 `Views/ChartView.xaml`(LiveCharts2 实时曲线,60 秒滚动)
- 自定义仪表盘控件(已有 GaugeControl,改样式)
- 在 MainWindow 里加一个 Tab 显示 ChartView

**代码肌肉**(1h):
- 手写题 B9(RelayCommand 实现 ICommand,5 分钟白板)
- 手写题 B10(INotifyPropertyChanged,5 分钟白板,无 Prism 基类)

**打卡**:✅ 实时曲线显示 2 个点位 60 秒滚动 ✅ B9 + B10 各 5 分钟白板

### Day 11(周四)· 报警 + 日志 + 内存排查实操
**学习**(2h):
- [M6_报警引擎日志_深度版.md](M6_报警引擎日志_深度版.md) — 重点回滞机制 + Serilog 配置

**项目**(2h):
- 验证现有 AlarmEngine(回滞已实现,看代码理解原理)
- 加 Serilog 的 Rolling + 文件 + 控制台 sink
- **M19 实操**:用 `dotnet counters` 监控 DAQMonitor 跑 10 分钟,看 GC / ThreadPool / 内存曲线

**代码肌肉**(1h):
- **手写题 B5(AlarmRule 回滞,15 分钟白板)** — 这是 M6 的核心,面试高频

**打卡**:✅ Serilog 配置好 ✅ AlarmEngine 回滞能讲 ✅ dotnet counters 实操 ✅ B5 15 分钟白板

> 📌 **Day 11 末尾必做(30 分钟,Day 12 的前置)**:在 `DAQMonitor-mine/src/DaqMonitor.UI/` 加一个 `Bootstrapper.cs`,用 `Microsoft.Extensions.DependencyInjection` 装配 `IDevice` / `IPlcClient` / `PointStore` / `AlarmEngine` / `MainViewModel`,在 `App.xaml.cs` 调用它。**Day 12 你会把它改成 PrismBootstrapper,今天先有非 Prism 版才能"改造"**。Day 12 的"改造 Bootstrapper.cs"指的是这个文件,不是凭空冒出来的。

### Day 12-14(周五-周末)· 工程素养 + Prism 重构
**学习**(3h):
- [M9_工程素养_测试DI容错_深度版.md](M9_工程素养_测试DI容错_深度版.md) — 速过(项目已实现大部分)
- **精读** [M8.5_Prism企业级MVVM_深度版.md](M8.5_Prism企业级MVVM_深度版.md) — 重点模块化/导航/事件聚合器/Region

**项目**(5h):
- 安装 Prism.DryIoc(或 Prism.Unity)
- 改造 `Bootstrapper.cs` → 继承 PrismBootstrapper
- 改造 MainViewModel → 加 `[ObservableProperty]` / `[RelayCommand]`(或 BindableBase)
- 拆分 MainWindow 为多个 View(Header / DeviceList / Chart / Alarm),用 RegionManager 注入

**代码肌肉**(3h):
- Day 12: **手写题 B8(Bootstrapper DI 配置,10 分钟白板)** + B12(HttpClient Polly 重试,10 分钟)
- Day 13: **手写题 B17(EventAggregator 简化版,10 分钟)** + B18(ViewModelLocator 命名约定,10 分钟)
- Day 14: Week 1+2 错题二刷(限时 50%)+ 综合题(随机抽 5 题白板)

**Week 2 检查点**:
- [ ] 数据能从 SQLite 查历史
- [ ] UI 有曲线/仪表盘/报警/日志
- [ ] 用了 Prism + DI + 模块化
- [ ] **代码肌肉**:CRC/Channel/IDevice/PointStore/AlarmRule 五大件不假思索写完
- [ ] **调试**:`dotnet counters` + `dotnet gcdump` 能用
- [ ] **简历可以开始投 10-13K 岗位练手**

---

## Week 3 · TCP + 上云 + 报表(Day 15-21)

> **目标**:补齐 JD 必考点(TCP/MES/上云/报表),把项目拉到"15K 稳"线。
> **本周拆为「必做 3 + 选做 3」**:必做 = TcpDevice / MQTT 双向 / 报表 Excel;选做 = OPC UA / MES / Recipe(应急预案可砍)

> 📌 **本周所有"项目"环节 = 在你的 `DAQMonitor-mine/` 手敲**
> 选做的 OPC UA / MES / Recipe 也好,必做的 TCP / MQTT / 报表也好,**全部手敲进 DAQMonitor-mine**。这是 13-15K 面试时"我能从 0 写一个上位机"的硬证据。Week 4 你要写简历,简历上的项目链接就是 DAQMonitor-mine 的 GitHub 仓库,面试官能看到你 30 天的 commit 历史。

### Day 15-16(周一-周二)· TCP Socket 自定义协议【必做】
**学习**(3h):
- [M11_TCP_Socket_深度版.md](M11_TCP_Socket_深度版.md) — 重点粘包/拆包/帧头+长度+payload+CRC 结构

**项目**(4h):
- 读+理解 `DaqMonitor.Core/Devices/TcpDevice.cs`(长度前缀帧 + 心跳 + 重连)
- 读+理解 `Protocol/TcpFrameParser.cs`(状态机解析)
- 写一个测试用 TCP Server(Python 或 C#)模拟设备

**代码肌肉**(2h):
- Day 15: **手写题 B16(TcpDevice 粘包处理状态机,15 分钟白板)** — 这是 13-15K 面试高频白板题
- Day 15: Debug C7(TCP 缓冲区 MemoryStream 性能坑) + C8(闭包捕获循环变量)
- Day 16: 手写题 B6(帧解析 AA55 + 长度 + payload + CRC,10 分钟)
- Day 16: Debug C9(async lambda 没 await)二刷

**打卡**:✅ TcpDevice 真粘包处理 ✅ 模拟设备能通 ✅ B16 15 分钟白板

### Day 17(周三)· MQTT 上云双向【必做】 + 工程量转换
**学习**(2h):
- [M7_OPCUA_MQTT_深度版.md](M7_OPCUA_MQTT_深度版.md) — **重点:全程 async/await + MQTT 双向**
- [M12_工程量转换与多数据库_深度版.md](M12_工程量转换与多数据库_深度版.md) — 重点量程标定公式 + 字节序交换
- 装本地 Mosquitto(Windows 安装包,断网车间也跑得起来,**不依赖公网 broker**)

> ⚠️ **MQTTnet 三大必考点(15K 面试官会追问,讲义缺口,在此补)**:
> 1. **ManagedMqttClient vs MqttClient**:`MqttClient` 裸用要自己写重连/订阅恢复/消息队列;`ManagedMqttClient` 内置这三件事(投递排队、断线重连后自动恢复订阅),业务侧省心。**生产环境必用 Managed**
> 2. **Topic 通配符**:`+` 单层匹配(如 `daq/+/telemetry` 匹配 `daq/A/telemetry` 但不匹配 `daq/A/B/telemetry`),`#` 多层匹配(只能放最后,如 `daq/#` 匹配所有子路径)
> 3. **QoS 0/1/2**:0 = 至多一次(TCP,丢就丢,遥测数据用)、1 = 至少一次(PUBACK,可能重复,要业务去重)、2 = 恰好一次(PUBREC/PUBREL/PUBCOMP 四次握手,命令用,但开销大)。**遥测用 0,命令用 1**

**项目**(3h):
- 读+理解 `DaqMonitor.Core/Cloud/MqttPublisher.cs`(MQTTnet 4.x 双向 + Channel 缓冲)
- 读+理解 `DaqMonitor.Core/Engineering/EngineeringConverter.cs`(线性标定 + 字节序)
- **本地 Mosquitto + MQTTX** 模拟遥测(不依赖公网/不依赖真 PLC):
  - 起 Mosquitto(`mosquitto.exe -v` 或装服务)
  - MQTTX 订阅 `daq/+/telemetry`,看到上位机发的数据
  - **从 MQTTX 发命令到 `daq/{deviceId}/command`**:上位机订阅这个 topic 接收命令 → **在 `MqttPublisher` 里把命令转回 `IPlcClient.WriteAsync`** → 命令真的写进 SimulatedDevice(不是真 PLC,但闭环通)。这一步替代"PLC 真的写入",**断网车间也能跑**
- 用 Python 10 行脚本模拟订阅端(可选,练面试讲法)

**代码肌肉**(1h):
- **手写题 B13(MQTT Publish+Subscribe 双向,15 分钟白板)** — 这是修正版核心
- **手写题 B24(EngineeringConverter 量程标定,5 分钟白板)**

**打卡**:✅ MQTTX 看到上位机发的数据 ✅ 从 MQTTX 发命令,SimulatedDevice 真的改值(本地闭环,不依赖外网/真 PLC) ✅ B13 15 分钟白板 ✅ 能讲清 Managed vs MqttClient / QoS 0-1-2 / 通配符 +/#

### Day 18(周四)· OPC UA【选做 P2】 + 性能压测
**学习**(2h):
- [M7_OPCUA_MQTT_深度版.md](M7_OPCUA_MQTT_深度版.md) OPC UA 部分 — 全程 async(不再 .Result)
- [M9.5_性能压测与长跑稳定性_深度版.md](M9.5_性能压测与长跑稳定性_深度版.md) — BenchmarkDotNet / dotnet counters / dotMemory

**项目**(3h,选做):
- 新建 `DaqMonitor.Core/Cloud/OpcUaClient.cs`(用 OpcUaNet or MQTTnet 绑定,真异步)
- **M9.5 实操**:写一个 BenchmarkDotNet 项目,压 PointStore.QueryHistoryAsync 的性能
- 跑 1 小时长跑,记录内存曲线(应该平稳不涨)

**代码肌肉**(1h):
- 手写题 B14(OPC UA 异步客户端,15 分钟白板)
- Debug C1-C10 全二刷(限时 50%,即每题 2.5 分钟)

**打卡**(选做):✅ OpcUaClient 编译通过 ✅ BenchmarkDotNet 报告 ✅ 1 小时长跑内存平稳

### Day 19(周五)· 报表 + 安装包【必做】
**学习**(1.5h):
- [M10_报表_深度版.md](M10_报表_深度版.md) + [M8_工程化收尾_深度版.md](M8_工程化收尾_深度版.md)

**项目**(2.5h):
- 用 ClosedXML 导出 Excel(按时间范围 + 点位聚合)— 读+增强 `ReportService.cs` / `ReportExporter.cs`
- 用 Inno Setup 或 WiX 打安装包

**代码肌肉**(1h):
- 手写题 B19(IDialogService 标准对话框,10 分钟)
- 手写题 B20(Serilog + TraceId 日志中间件,10 分钟)

**打卡**:✅ Excel 报表能导 ✅ 安装包能装 ✅ B19+B20 白板过关

### Day 20(周六)· MES 对接【选做 P2】 + 工业安全
**学习**(2h):
- [M17_工业安全与MES对接_深度版.md](M17_工业安全与MES对接_深度版.md) — HttpClient + REST + Polly + JWT + IT/OT 分层

**项目**(3h,选做):
- 新建 `DaqMonitor.Core/Mes/MesApiClient.cs` — HttpClient + Polly + JWT
- 加白名单验证器(IpAllowedHosts)+ 审计日志

**代码肌肉**(1h):
- 手写题 B21(白名单验证器,10 分钟)
- 手写题 B22(AuditLogger,10 分钟)

**打卡**(选做):✅ MesApiClient 跑通本地 mock ✅ B21+B22 白板过关

### Day 21(周日)· 配方管理【选做 P2】 + M19 调试综合实操
**学习**(2h):
- [M18_配方管理_深度版.md](M18_配方管理_深度版.md) — 工艺参数版本 / ISA-88 / 审计
- **复习** [M19_问题排查与调试_深度版.md](M19_问题排查与调试_深度版.md) Day 2-5(异常/竞态/内存/性能/物理工具)

**项目**(3h,选做):
- 新建 `DaqMonitor-mine/src/DaqMonitor.Core/Recipes/RecipeService.cs` — JSON 配方 + 版本化 + 回滚(参考版用 `Recipes/Recipe.cs` + `RecipeParameter.cs` + `RecipeSnapshot.cs` + `RecipeService.cs` 4 件套,**手敲时先抄 4 个文件结构再填逻辑**)

**代码肌肉 + 调试综合实操**(2h):
- 手写题 B23(Recipe 配方管理,15 分钟)
- **M19 综合实操**:故意造一个 bug(event 没退订导致内存泄漏),用 `dotnet gcdump` 抓快照,找出泄漏对象 → 修复
- **M19 综合实操**:用 Wireshark 抓一次 Modbus RTU,从字节解释每个字段

**Week 3 检查点**:
- [ ] 项目有 5 种设备(Simulated/Serial/Modbus/PLC/TCP)
- [ ] MQTT 双向跑通(选做:OPC UA + MES + Recipe)
- [ ] 报表能导 Excel
- [ ] **代码肌肉**:TcpDevice 粘包 / MQTT 双向 / 报表导出,10-15 分钟白板
- [ ] **调试**:dotnet gcdump 抓泄漏 + Wireshark 抓协议
- [ ] **简历可以投 13-15K 岗位**

---

## Week 4 · 文档 + 简历 + 面试冲刺(Day 22-30)

> **目标**:敢演示、能讲透、面试不卡。这周不再写新代码,只补文档/练讲法/投简历/真面试。

> 📌 **本周所有文档/简历链接 = 你的 `DAQMonitor-mine/`**
> 本周写文档、改简历、面试时演示的项目都是 `DAQMonitor-mine/`(你手敲版),不是 `DAQMonitor/`(参考版)。简历项目链接指向 DAQMonitor-mine 的 GitHub 仓库,文档也写在 `DAQMonitor-mine/docs/`。30 天的 git commit 历史 + 完整文档 = 你能讲透、面试官挖不倒的硬证据。

### Day 22-23(周一-周二)· 交付文档 + M15 工程协作速过
**学习**(2h):
- [文档能力_设计协议操作测试文档_深度版.md](文档能力_设计协议操作测试文档_深度版.md)
- 速过 [M15_工程协作与联调_深度版.md](M15_工程协作与联调_深度版.md) Day 1(Git 协作流程 + 联调方法,30 分钟)

**项目**(4h):
- 写 `DAQMonitor/README.md`(项目讲法,带截图)
- 写 `DAQMonitor/docs/架构设计.md`(分层 + 时序图,用 PlantUML)
- 写 `DAQMonitor/docs/协议文档.md`(Modbus 寄存器表 + 自定义 TCP 协议帧格式)
- 写 `DAQMonitor/docs/操作手册.md`(怎么跑起来,操作员视角)
- 写 `DAQMonitor/docs/测试报告.md`(套 M9.5 BenchmarkDotNet 数据)

**代码肌肉**(1h):
- 手写题 B25(PerformanceCounter 业务指标上报,10 分钟)
- 手写题 B26(IDisposable 完整模式含 finalizer,10 分钟)

**打卡**:✅ 4 份文档完成 ✅ GitHub 仓库 docs/ 目录完整 ✅ B25+B26 白板过关

### Day 24-25(周三-周四)· 简历 + 面试材料
**学习**(2h):
- 速过 [面试高频知识点_速记卡.md](面试高频知识点_速记卡.md)
- **精读** [面试问答_逐字稿_30题.md](面试问答_逐字稿_30题.md)(30 题覆盖 C#/并发/Modbus/PLC/EF/Prism/TCP/报警/性能/调试 10 大主题)

**产出**(4h):
- 用 [简历模板_上位机_13-15K.md](简历模板_上位机_13-15K.md) 改出自己的简历(3 个版本:13K / 14-15K / 15K+)
- 把 DAQMonitor 项目讲法背 3 遍(STAR 法)
- 准备 5 个"项目难点+怎么解决"故事(对应 [JD 调研](JD调研_13-15K上位机岗位对照.md) 行业偏好)
- 对照 [JD 调研](JD调研_13-15K上位机岗位对照.md) 第 7 节"按 JD 调简历 5 建议",每投一家改一遍关键词

**代码肌肉**(2h):
- **毕业测试预演**:不查文档白板写 B1+B2+B5+B16+B13 五大件,目标 60 分钟(总限时 65 分钟)
- 错题三刷

**打卡**:✅ 简历 3 版本 ✅ 项目 STAR 讲法 3 遍 ✅ 30 题逐字稿背完 ✅ 毕业测试预演过关

### Day 26-27(周五-周六)· 差异化王牌:WebView2 + Vue3 工业大屏 ⭐ 两天加分包

> **为什么花两天做这个**:你 5 年前端经验是 95% 上位机候选人没有的王牌。这两天给 DAQMonitor 加一个 Vue3 大屏前端,简历多一句「**全栈:上位机 WPF + 前端 Vue3 大屏双栈**」,面试排名从 30% 跳到 10%。**新能源 / 半导体 / 3C 非标 / 智慧工厂**这 4 个行业大量要这种双栈组合。
> **完整方案**:[前端×上位机差异化冲刺_WebView2与工业大屏.md](前端×上位机差异化冲刺_WebView2与工业大屏.md)

**Day 26(周五,5h)· WebSocket 服务 + Vue 大屏骨架**
- **学习**(1.5h):精读 [前端×上位机差异化冲刺](前端×上位机差异化冲刺_WebView2与工业大屏.md) 第 1-2 节
- **项目**(3.5h):
  - 在 `DAQMonitor-mine/` 根目录新建 `DaqMonitor.Web` 项目:`dotnet new web -o src/DaqMonitor.Web -n DaqMonitor.Web`(`web` 模板是 ASP.NET Core 空模板,比 `webapi` 更轻,自带 Kestrel),加到 sln:`dotnet sln add src/DaqMonitor.Web/DaqMonitor.Web.csproj`,加 Core 引用:`dotnet add src/DaqMonitor.Web reference src/DaqMonitor.Core`
  - 用 ASP.NET Core Kestrel 起嵌入式 WebSocket 服务
  - 复制冲刺文档里的 `WebBroadcastService.cs`(已给完整 120 行代码)
  - Vue3 + Vite 起新项目,装 ECharts + Pinia,复制 `useWebSocket.ts`(含指数退避重连)
  - Vue 大屏接 WebSocket 实时点位跑通(先画一条曲线验证)

**Day 27(周六,5h)· WebView2 嵌入 + 4 大组件 + 演示视频**
- **项目**(4h):
  - 在 `MainWindow.xaml` 嵌 `WebView2` 控件,加载本地 Vue dev server(`http://localhost:5173`)
  - 复制 `WebViewBridge.cs` 实现 C# ↔ JS 双向通信(WebMessageReceived / PostMessage)
  - Vue 大屏加 4 个组件:`RealtimeChart.vue`(实时曲线)+ `DeviceCard.vue`(设备状态卡片)+ `AlarmTicker.vue`(报警滚动)+ `GaugePanel.vue`(仪表盘)— 冲刺文档已给完整代码
  - **截图 6 张 + 录屏 1 分钟**(简历附件,演示给面试官)

- **简历话术**(1h):按冲刺文档第 4 节改简历,**3 句话必须出现**:
  - 「全栈能力:上位机 WPF + 前端 Vue3 大屏双栈」
  - 「用 WebView2 在 WPF 中嵌入 Vue3 工业大屏,统一实时曲线/报警/设备状态展示」
  - 「用 ASP.NET Core Kestrel 嵌入式 WebSocket 把 C# 采集层和 Vue 大屏解耦,支持多客户端订阅」

**打卡**:✅ Vue 大屏跑起来看到实时曲线 ✅ WebView2 嵌入 WPF 成功 ✅ 6 张截图 + 1 分钟录屏 ✅ 简历 3 句话已加

> ⚠️ **进度紧可砍**:这两天的产出是 15K 加分包,如果 Week 1-3 主干还没稳,**优先保主干**,这 2 天改回原来的 Day 26-28 投递节奏。

### Day 27.5(可选,周日,8h)· 差异化王牌三:运动控制卡 demo(雷赛/固高模拟器)⭐ 一天加分包

> **为什么花一天做这个**:[JD 调研 P1 缺口 #2](JD调研_13-15K上位机岗位对照.md) — 设备控制类 JD 70% 写"运动控制卡经验优先",**非标自动化 / 3C 装配 / 半导体设备** 3 大行业几乎必考。你 DAQMonitor 已有 `IAxisController` 抽象(PLCopen 兼容,见 `src/DaqMonitor.Core/Motion/`),只需 8h 把它**手敲一遍**并**接一次模拟器**,简历多一句"运动控制卡模拟器实操",覆盖面从 60% JD → 80% JD。
> **核心策略:30 天只用模拟器,不买真硬件**。理由:① 真卡 + 步进 + 滑台至少 2000-3000 元,学习用一次性投入不划算;② 没机械配套真卡用不起来;③ 入职后公司有真机,1 个月真机经验 > 自学 6 个月。**模拟器 80% 加分效果,0% 成本**,简历同样能写"模拟器联调"。
> **可不可不做**:可以。13K 主流盘子(数据采集 / 监控 / MES 上云)70% 不要运控,投递时避开"非标自动化 / 3C 装配 / 半导体设备"3 个运控密集行业,完全可以不学运控拿 13K。**做 = 多覆盖 25% JD;不做 = 也能拿 13K**。看你时间预算。

#### 学习(2h):PLCopen 标准 + 轴状态机(讲义缺口补)
> **为什么补这一节**:[M19](M19_问题排查与调试_深度版.md) 讲义没讲运控,DAQMonitor 项目里直接实现了 `IAxisController` 等代码但没讲原理,这是讲义缺口。先补理论再手敲,避免"看代码点头、关掉写不出"。

- **PLCopen Motion**(IEC 61131-3 标准):`MC_MoveAbsolute` / `MC_MoveRelative` / `MC_Home` / `MC_Stop` 4 大动作 — **工业通用语言**,固高/雷赛/正运动/Beckhoff 都对齐这套命名,面试讲法标准化
- **轴状态机**(5 态):
  ```
  Disabled ──Enable──> Idle ──Move/Jog──> Moving ──完成──> Idle
                        Idle ──Home───> Homing ──完成──> Idle
                        任意态 ──E-Stop──> Idle(立即停,但工业现场规范要求 E-Stop 后需操作员确认 → Reset 才能再 Move,本模拟器简化版直接回 Idle,真机/严格规范需加 Stopping 中间态)
                        任意态 ──超限/超速/驱动故障──> Alarm(锁死,需人工 Reset)
  ```
  > **注**:严格 PLCopen 规范的 E-Stop 进 `Aborting`/`Stopping` 中间态、完成减速后才回 `Idle` 或 `ErrorStop`(若同时驱动故障);学习版状态机做简化,但**面试被追问时要会答**:"严格规范 E-Stop 不是瞬时回 Idle,要走 Stopping 减速曲线;真机版我会加 Stopping/ErrorStop 中间态,本模拟器为教学简化为直接回 Idle"
- **软限位 vs 硬限位**:软限位 = 软件检查 MinPosition/MaxPosition 拦截;硬限位 = 物理限位开关 + 继电器急停电路(独立于软件,软件挂了一样能停)
- **回零为什么必须**:编码器只能记相对位移,机械装好后真实位置未知,必须慢速移动碰到回零开关 → 反向脱离一小段 → 记录 0 点。**未回零时绝对定位无意义**
- **E-Stop vs Stop**:E-Stop 立即切断伺服输出(可能丢位置,但避免碰撞);Stop 走减速曲线(平稳,不丢位置)
- **CountsPerUnit**:脉冲 ↔ 用户单位转换系数(如 10000 脉冲/mm),真卡 API 用脉冲,业务用 mm,中间靠这个系数

#### 写进 DAQMonitor-mine(4h):手敲 PLCopen 抽象
- **关掉 DAQMonitor 参考**,在 `DAQMonitor-mine/src/DaqMonitor.Core/Motion/` 手敲:
  - `AxisState.cs` — 5 态枚举(Disabled/Idle/Moving/Homing/Alarm)
  - `AxisConfiguration.cs` — 行程(MinPosition/MaxPosition)、MaxVelocity、HomeVelocity、CountsPerUnit、IsReversed 配置(`init` only 不可变)
  - `IAxisController.cs` — PLCopen 兼容接口(Home/MoveAbsolute/MoveRelative/Jog/Stop/EmergencyStop/ResetAlarm + StateChanged/PositionChanged/AlarmRaised 3 事件)
  - `SimulatedAxisController.cs` — 关键实现要点:
    - `System.Timers.Timer` 10ms tick(工业典型 PLC 扫描周期 1-4ms,这里 10ms 平衡 CPU)
    - `lock` 保护状态(命令与 tick 不能同时改位置)
    - Tick 内部:`newPosition = _position + _velocity * dt.TotalSeconds`
    - 软限位检查:越界 → 设 Alarm,触发 AlarmRaised 事件
    - 到达目标判断:`_velocity > 0 ? newPosition >= _targetPosition : newPosition <= _targetPosition`
    - 回零完成:把 `_position` 重置为 0、`_isHomed = true`
    - **事件触发放 lock 外**(避免订阅者回调里再 lock 造成死锁)
- 在 `DaqMonitor.Tests/MotionControlTests.cs` 写测试:
  - 软限位越界 → Alarm 触发
  - 未回零时 MoveAbsolute → InvalidOperationException
  - Jog 撞软限位 → Alarm
  - E-Stop 立即清零 velocity、回 Idle
  - 回零完成 → IsHomed = true 且 Position = 0
- commit:`feat(M-motion): IAxisController + SimulatedAxisController + 软限位 + 测试`

#### 模拟器实操(2h,0 元,不要买真硬件)
> ⚠️ **30 天内绝对不要买真硬件**。理由:① 入门真卡 + 步进 + 滑台至少 2000-3000 元,学习用一次性投入不划算;② 没机械配套真卡也用不起来;③ 入职后公司有真机,边工作边学免费还快。**只用模拟器,简历同样能写"模拟器联调"**,80% 加分效果,0% 成本。

- 下载 **雷赛 DMC SDK**(官网免费,含 demo 控制卡模拟器 + C# API 文档):<https://www.leisai.com/>
- 模拟器与真卡的 C# API **完全一致**,只是不用接电机。学习阶段用模拟器=学真卡 API 95%,只差"真实编码器反馈/机械抖动/驱动器报警"5% 现场感(这 5% 留给入职后真机学)
- 5 大核心 API(雷赛命名,固高 GT_ 前缀、正运动 zm_ 前缀类似):
  | API | 作用 | 类比前端 |
  |---|---|---|
  | `dcm_set_axis(axis, mode)` | 配置轴模式(位置/速度/回零) | `el.style.display = 'flex'` |
  | `dcm_home(axis, mode)` | 启动回零序列 | `await fetch('/api/reset')` |
  | `dcm_pmove(axis, pos, vel)` | 点位运动到 pos | `element.scrollTo({left: pos})` |
  | `dcm_get_pos(axis)` | 读当前位置(脉冲) | `element.scrollLeft` |
  | `dcm_emg_stop()` | 全局急停 | `window.stop()` |
- 在 `DAQMonitor-mine/src/DaqMonitor.Core/Motion/LeiSai/LeiSaiAxisController.cs` 实现 `IAxisController`:
  - PLCopen 命令翻译成雷赛模拟器 API 调用
  - `CountsPerUnit` 单位转换(脉冲 ↔ mm)
  - 状态轮询线程(10ms tick 调 `dcm_get_pos`,推进状态机)
  - `EmergencyStopAsync` → `dcm_emg_stop()`
- 完整流程 demo 控制台程序:**使能 → 回零 → MoveAbsolute(100mm) → MoveRelative(-10mm) → E-Stop**
- 录屏 1 分钟,简历附件

> 📌 **真硬件什么时候碰**:**入职后第 1 个月**。公司有真机 + 老员工带 + 真机械场景,1 个月真机经验 > 自学 6 个月。简历的"运控真机经验"等入职 2-3 个月后再加这一句,30 天简历不要写。

#### 简历话术(0.5h)
按 [JD 调研](JD调研_13-15K上位机岗位对照.md) 第 7 节"按 JD 调简历"原则。**关键:话术必须诚实降级,避免诚信风险**——你只学过抽象层 + 模拟器,没碰过真机、没碰过插补算法,简历写"运动控制实操"投非标自动化岗,面试官一句"圆弧插补要哪几个参数"就拆穿。

- **基础版**(只手敲 Simulated,**推荐**):
  > 运动控制抽象层:基于 PLCopen (IEC 61131-3) 标准设计 `IAxisController` 接口(Home / MoveAbsolute / MoveRelative / Jog / Stop / EmergencyStop),`SimulatedAxisController` 用 10ms tick 推进位置 + 软限位 + 5 态状态机(Disabled/Idle/Moving/Homing/Alarm),业务代码零改动可接固高/雷赛/正运动卡
- **升级版**(接模拟器后加一句,**不写"实操"写"模拟器联调"**):
  > 雷赛 DMC 模拟器联调,完成"使能 → 回零 → 点位 → 急停"全流程,实现 CountsPerUnit 脉冲↔mm 双向单位转换

**❌ 千万别这么写**(15K 面试官 30 秒拆穿):
- ❌ "运动控制实操经验" / "运动控制系统开发" — 你没真机经验
- ❌ "精通运动控制" / "熟练掌握插补算法" — 你不会插补
- ❌ "做过 3 轴 X/Y/R 项目" — 你只做过单轴模拟

**面试讲法 STAR(诚实版)**:
- S(场景):"项目需要预留运动控制扩展位,未来可能接不同厂商的控制卡"
- T(任务):"设计一套运控抽象层,业务代码不因换卡而改"
- A(行动):"参考 PLCopen IEC 61131-3 标准,抽象 5 大动作 + 5 态状态机,用 SimulatedAxisController 验证逻辑,再实现 LeiSai 适配器接模拟器"
- R(结果):"业务代码 0 改动切换实现,测试覆盖软限位/E-Stop/回零 5 个场景"

**面试被追问插补/伺服/EtherCAT 时怎么回答**(诚实回避,不装懂):
- "插补算法(直线/圆弧)我学到的是概念,知道圆弧要圆心+半径+起止角,但没有真机实操过,可以入职后基于现有抽象层补充。"
- "伺服 vs 步进的核心差异是闭环 vs 开环,伺服有编码器反馈不丢步,步进会失步——这些是基础知识,具体调试我还没做过。"
- "EtherCAT 周期 100μs~1ms 比脉冲+方向延迟低,但具体选型我没实操过。"
- **关键:诚实回答 + 表达学习意愿 + 强调"我做了抽象层,真机来了我能基于抽象层扩展",这才是 13-15K 该有的姿态。**

#### 打卡
- ✅ DAQMonitor-mine 里 `Motion/` 目录 4 个文件手敲完成
- ✅ `MotionControlTests` 5 个测试全绿
- ✅ 能 5 分钟白板画 IAxisController 状态机转移图
- ✅ 能讲清:E-Stop vs Stop / 软限位 vs 硬限位 / 未回零为啥不能绝对定位
- ✅ (选做)雷赛 DMC 模拟器跑通"使能→回零→点位→急停"
- ✅ 简历加运动控制句(基础版或升级版)

#### 硬件采购参考(30 天**不要买**,仅供将来入职后参考)

> ⚠️ **30 天内不要花一分钱买真硬件**。下面是入职后真机学习时的型号参考,让你心里有数,知道公司给你什么档次的卡。

| 档位 | 配置 | 价格 | 用途 / 何时考虑 |
|---|---|---|---|
| **零成本**(30 天用这个) | 雷赛 DMC 模拟器 / 固高 GTS 模拟器 | **0 元** | 30 天学习用,简历可写"模拟器联调" |
| **入门真硬件**(自学不推荐) | 雷赛 DMC1415 单轴脉冲卡 + 42 步进电机 + DM542 驱动器 + 简易滑台 | **2000-3000 元** | 自学深度上手,但缺机械场景性价比低 |
| **进阶真硬件**(自学强烈不推荐) | 固高 GTS-400-PG-4 4 轴卡 + 汇川/松川伺服 + 多轴滑台 + IO 模块 | **6000-10000+ 元** | 自学不值得,公司全套都有 |
| **入职后**(最优,30 天别想) | 公司现成硬件 + 真机 + 老员工带 | **免费 + 工资照拿** | 1 个月真机经验 > 6 个月自学 |

**主流品牌型号**(2026 年市占率):
- **雷赛**(深圳,国产主流):DMC 系列(脉冲卡,入门,1500-2500 元/2 轴)/ DMC-E 系列(EtherCAT,中高端)
- **固高**(深圳,国产主流):GTS 系列(主流,2000-4000 元/4 轴)/ GTS-PG(高性能)
- **正运动**(深圳):ZM 系列,价格中等,API 接近雷赛
- **凌华**(台湾):PCI-1245/1245U,偏高端,半导体内资厂常用
- **国外品牌**:Galil(美国)/ Beckhoff(德国,EtherCAT 顶级,TwinCAT 软件,半导体/锂电高端设备用)

**行业惯例**(对应目标行业):
- **3C 非标自动化**:固高 + 雷赛 平分秋色,简历写"了解固高/雷赛 API"覆盖 80%
- **半导体设备**:固高 + 凌华 + Beckhoff(高端),简历写"了解固高 + Beckhoff 概念"覆盖 60%
- **新能源锂电**:雷赛 + 正运动 + EtherCAT 趋势,简历写"了解雷赛 + EtherCAT"覆盖 70%
- **包装/食品/传统制造**:雷赛入门卡为主,简历写"了解雷赛 DMC"覆盖 80%

#### 时间充足要不要加真硬件?(策略建议)

> 你可能想:"30 天主干 + 模拟运控学完后,如果时间充足,我再加真硬件对接是不是更好?"

**简短答案**:❌ **不推荐 30 天内碰真硬件**。真硬件永远等入职后用公司真机学。

4 个理由:
1. **成本不划算**:入门真硬件最低 2000-3000 元(单轴卡 + 步进 + 滑台),简历同样写"运控了解",面试官**不会因为"你买过真卡"加分**(他看简历是看项目,不是看你银行卡)
2. **学习曲线深**:真硬件 ≠ "模拟器再多花几小时"。需要电气接线、电机参数整定、伺服/编码器调试、强电安全知识,前置学习 1-2 周才能让卡跑起来,**挤爆你 30 天节奏**
3. **30 天本来就紧**:主干 21 天 + 加分包 + 投递/面试,挤时间做真硬件会拖累主干 + 简历质量
4. **入职后免费学**:公司有真机 + 老员工带 + 真机械场景,**1 个月真机经验 > 6 个月自学**,工资照拿

**唯一例外** — 3 个条件全部满足才考虑买:
- 30 天学完后**还没拿到 offer**(投了 50+ 家都没下文)
- 空档期 ≥ 1 个月(短期内不会入职)
- 投的岗位 50%+ 是"非标自动化/3C 装配/半导体设备"运控密集行业(简历被运控卡住)

**如果满足,最便宜方案**(不要买 2000+ 元工业卡):
- **Arduino Mega 2560 + 42 步进电机 + DM542 驱动器 + 简易滑台套装**:淘宝/拼多多 ~300-500 元
- 学习目标:用 C# 写串口/USB 程序控制 Arduino → 步进电机转动,跑通"使能 → 回零 → 点位 → 急停"全流程
- 简历写法:"运控真机实操(嵌入式方案 + C# 串口控制)",成本只有工业卡 1/4,简历效果 80%
- 优势:强电安全要求低(5-24V)、套件成熟、社区资源多

**最优时间线**(强烈推荐):
```
Day 1-27.5:  30 天主干 + 模拟运控(0 元)→ 投递 13K
Day 28-30:   投递 + 模拟面试 + 边面边补
入职第 1 月: 用公司真机学,熟悉具体品牌(雷赛/固高/正运动)
入职第 2-3 月: 简历加"运控真机经验"句,准备 16-18K 涨薪跳
```

> ⚠️ **进度紧可砍**:这是 15K 加分包。Week 1-3 主干没稳优先保主干;**真硬件永远不要自学买,入职后用公司真机学,2-3 个月深度上手 + 工资照拿**。

### Day 28(周日)· 投递 + 模拟面试
- **开始投简历**(BOSS / 智联 / 拉勾 / 猎聘),先投 10-13K 练手,关键词镜像 JD
- 找朋友或对着镜头模拟面试,录音回听;用 [面试逐字稿](面试问答_逐字稿_30题.md) 自测
- 复盘 + 补漏(根据面试反馈)

**代码肌肉**(每天 1h):
- 每天白板 3 题(随机抽,模拟面试官出题)
- 错题集中刷

### Day 29-30 · 真面试 + 边面边补
- 真去面试,每场面试后立刻记录"被问倒的题"
- 当晚针对问倒的题查文档/补代码/补到逐字稿
- 投递提级到 13-15K,争取拿 2-3 个 offer 比较

**代码肌肉**:
- Day 30 **真毕业测试**:不查文档白板写完一个完整 ModbusDevice(含协议解析 + CRC + 异常处理 + IDisposable),30 分钟内
- 任意一段代码 5 秒内能说"这里有什么坑"

---

## 应急预案(进度滞后怎么办)

### 滞后 1 周 → 砍 P2 内容
- 砍 M16(CAN/USB-HID 详学,保留概念)、M14(WinForm 详学)、M18(配方管理)
- 砍 Day 18(OPC UA)/ Day 20(MES)/ Day 21(Recipe)3 个选做日
- 重点保 W1+W2 主干 + W3 必做 3 项 + W4

### 滞后 2 周 → 砍到 W1+W2+W4
- 只做 串口/Modbus/PLC/SQLite/可视化/报警/Prism + 简历面试
- TCP/上云/报表 简历"了解"即可,不写代码
- 这样也能投 10-13K

### 完全来不及 → 项目当"学习项目"投
- 老老实实说"自学项目,做到 M9",投助理岗(8-10K)
- 边工作边补完

---

## 边面试边学策略

**Week 2 末就可以开始投练手**(10-13K),目标不是拿 offer,是:
1. 摸清楚 JD 真实要求(对照 [JD 调研](JD调研_13-15K上位机岗位对照.md) 补漏)
2. 练面试表达(尤其是项目讲法)
3. 知道面试官常挖的坑(记下来背答案,补到 [面试逐字稿](面试问答_逐字稿_30题.md))

**Week 3 末投 13-15K 主力**:
- 这时候项目已经完整,简历硬通货
- 把面试当"免费模考",每场回来补强
- 通常 5-10 场面试后能拿到第一个 offer

---

## 学习节奏(每天 4-5h 怎么分配)

**最优时间块**:
- 上午 1h:看讲义(理论,大脑最清醒)
- 下午 1.5h:写代码(项目实战)
- **傍晚 1h:代码肌肉(白板刷题,形成肌肉记忆)**
- **通勤/排队 10 分钟:间隔重复刷 5-10 张速记卡**(Anki 风格,记 1d/3d/7d/15d 节奏)
- 晚上 0.5h:打卡 + 复盘 + 第二天预览 + **当天复习队列**(看 [间隔重复表](记忆与复习机制_间隔重复版.md))
- 周末加量:5-7h 集中突破

### 🔁 间隔重复(防遗忘的核心机制)
**问题**:只学不复习,7 天后忘 70%。**解决**:用 1d/3d/7d/15d/30d 五段间隔重复(基于艾宾浩斯曲线)。
- 每天打卡块多加 **15-20 分钟复习队列**(不是新学,是复习 N 天前学过的)
- 每周日闭卷自测 30 题(Week 1-4 各 120 题,见 [间隔重复表](记忆与复习机制_间隔重复版.md) 第 2 节)
- 错题进 `错题本.md`(格式见 [间隔重复表](记忆与复习机制_间隔重复版.md) 第 4 节)
- **快忘了信号**(写代码卡壳超 10 秒 / 看到题不知道用什么 / 讲不出为什么)→ 当天加复习一遍

**前端背景的偷懒技巧**:
- C#/WPF 速过(M0 Day 1-6 不要全做练习,看懂就行)
- LINQ 不用学,你 JS 数组方法熟 → 直接迁移
- async/await 不用学,你已经熟
- **节省的时间全压到**:多线程/锁/struct/字节序/CRC(前端没概念的)+ **每天 1h 代码肌肉**

---

## 检查点总览(每周自测)

| 周 | 自测题 | 代码肌肉 | 通过标准 |
|---|---|---|---|
| W1 | 手算 Modbus CRC + 讲 IDevice 抽象 | CRC 5min + Channel 10min + IDevice 15min | 5 分钟内答完,白板过关 |
| W2 | 讲 EF Core 变更追踪 + Prism 模块化 | PointStore 15min + AlarmRule 15min + RelayCommand 5min | 不卡壳,白板过关 |
| W3 | 讲 TCP 粘包怎么解 + MQTT 双向怎么实现 | TcpDevice 粘包 15min + MQTT 双向 15min | 能画图,白板过关 |
| W4 | 项目讲法 STAR + 30 题面试速记 | 5 大件 60min 毕业测试 | 通关模拟面 + 白板过关 |

---

## 最终交付物清单(30 天后你应该有)

- ✅ DAQMonitor 完整项目(7+ 设备/协议类、SQLite、Prism、上云、报表)
- ✅ 33+ 测试全绿(项目原有 28 + 你新加的 5-8)
- ✅ 完整文档(README + 架构 + 协议 + 操作 + 测试报告)
- ✅ 简历定稿 3 版本(13K / 14-15K / 15K+,项目讲法 STAR 化)
- ✅ 面试逐字稿背熟(30 题)
- ✅ GitHub 仓库 commit 历史清晰(每天 commit,面试官看)
- ✅ **代码肌肉**:30 道手写题 + 10 道 Debug 题白板过关,看代码 5 秒知坑

---

## 📚 配套材料导航

| 材料 | 用途 | 何时用 |
|---|---|---|
| [C#_陷阱_前端转上位机必看_深度版.md](C#_陷阱_前端转上位机必看_深度版.md) | 8 大前端→C# 陷阱 | Day 1 必读,Week 1 反复回看 |
| [代码肌肉训练手册_30天刷题版.md](代码肌肉训练手册_30天刷题版.md) | 20 翻译 + 30 手写 + 10 Debug 题库 | **每天 1 小时**,按本路线打卡 |
| [记忆与复习机制_间隔重复版.md](记忆与复习机制_间隔重复版.md) | **1d/3d/7d/15d 间隔重复表 + 每周自测 120 题 + Anki 卡片建议** | **每天通勤 10min 刷 + 每周日闭卷自测** |
| [前端×上位机差异化冲刺_WebView2与工业大屏.md](前端×上位机差异化冲刺_WebView2与工业大屏.md) | **5 天 WebView2+Vue3 大屏方案 + 真实代码 + 面试 10 题** | Day 26-27 落地,简历加分王牌 |
| [面试问答_逐字稿_30题.md](面试问答_逐字稿_30题.md) | 30 题标准答 + 逐字稿 | Week 4 背诵 + 面试前复习 |
| [简历模板_上位机_13-15K.md](简历模板_上位机_13-15K.md) | 3 版本简历 + 投递策略 | Week 4 改简历,每投一家调一次 |
| [JD调研_13-15K上位机岗位对照.md](JD调研_13-15K上位机岗位对照.md) | JD 必考点 + 行业偏好 + 缺口 Top 10 | 投简历前对照,面试前看行业 |
| [审计_整体串联复核_v2.md](审计_整体串联复核_v2.md) | 整体串联审计 + Top 10 必修问题 | 学习中遇到疑问时回看 |

---

> **最后一句**:这不是 30 天速成,是 30 天**集中突破**。前端背景让你代码不慌,但**协议正确性、稳定性、现场排错**这三件事必须从零练。每天 commit,每周自测,边面边补,**每天 1 小时白板刷题形成肌肉记忆**,**30 天后你会发现自己已经能跟中级上位机工程师平等对话**。

开始 Day 1 → [C#_陷阱_前端转上位机必看_深度版.md](C#_陷阱_前端转上位机必看_深度版.md)
