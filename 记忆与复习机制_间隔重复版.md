# 记忆与复习机制 · 间隔重复版(Spaced Repetition)🧠

> **优先级定位**:🔴 **必执行** · 这是 30 天路线的"防漏机制"——没有它,Day 1 学的 CRC 到 Day 25 已经忘 80%,你前面 24 天的努力直接打水漂
> **核心问题**:旧 30 天路线只有 Day 7 / 14 / 24 三次"二刷/三刷",间隔太稀疏,Week 1 学完到 Week 2 复习中间已经遗忘 70%,复习等于重学。本机制换 Ebbinghaus 数字版:**1d / 3d / 7d / 15d / 30d 五段间隔重复**
> **技术来源**:🟦 Ebbinghaus 遗忘曲线(1885,实验心理学的开山研究) · 🟧 Anki / Quizlet / SuperMemo 的 SM-2 算法 · 🟧 React `useEffect` 依赖触发模型(类比记忆触发模型)
> **给简历加的能力**:面试时被追问"为什么 0xA001 不是 0x8005"——你 **3 秒内** 答出来,不假思索。这不是"聪明",是**间隔重复训练出来的肌肉记忆**。13-15K 和 8-10K 的差距,70% 在这里
> **前置**:[30 天作战路线](30天作战路线_转行冲刺版.md) · [代码肌肉训练手册](代码肌肉训练手册_30天刷题版.md) · [面试高频知识点速记卡](面试高频知识点_速记卡.md) · [面试问答逐字稿 30 题](面试问答_逐字稿_30题.md)

---

## 为什么需要间隔重复(用前端语言讲)

**核心类比**:`React.useEffect` 的依赖数组——只有 deps 变了,effect 才重跑。**记忆也是一样的**:只有"快忘了才复习"才能让大脑把这条信息标记为"重要、长期保留";每次都趁热复习(像每次 render 都重跑 effect),大脑反而判它"已知、不用存",**复习反而破坏记忆**。

**为什么旧路线的"周末刷上周"不行**:周一学的 CRC,周日复习时你已经忘掉 65%(Ebbinghaus 曲线:24h 后忘 66%,7d 后忘 75%)。复习 = 重学一遍,效率极低,而且因为重学不痛苦,大脑记不住。

**SRS(Spaced Repetition System)为什么有效**:

| 时机 | 大脑反应 | 记忆效果 |
|---|---|---|
| 学完立刻复习(0-2h) | "这个我已经知道了" | 几乎无效(浅层加工) |
| 1 天后复习 | "诶,有点模糊,要想想" | **强记忆**(大脑判定"重要") |
| 3 天后复习 | "得回忆几秒" | **更强记忆**(深度提取) |
| 7 天后复习 | "卡壳了,翻一下笔记" | **长期固化**(迁移到长期记忆) |
| 15-30 天后还能复习起来 | "这题我熟" | 永久掌握 |

**Anki / Quizlet 的底层就是这个**:算法在"你快忘了"那一刻把卡片推给你,复习成本最低、收益最高。Anki 用 SM-2 算法,SuperMemo 用 SM-18,本质都是 Ebbinghaus 曲线的工程化。

**前端工程师的天然优势**:你已经习惯"配 useEffect deps",配间隔重复就是同样的事——给每个知识点挂"复习触发器",到点自动触发。30 天 = 30 个 effect,每天 15-20 分钟跑一遍。

---

## 第 1 节 · 30 天间隔重复排程表(核心产出)📅

> **怎么读这张表**:
> - **【新学】** 当天学的新内容(讲义 / 项目 / 代码肌肉 / 速记卡)
> - **【复习队列】** 用 1d/3d/7d/15d/30d 倒推出来的今天必须复习的内容
>   - 例:Day 5 复习队列 = Day 4 学的(隔 1d)+ Day 2 学的(隔 3d)
> - **目标时间**:复习队列每天 15-20 分钟,不允许超时(超时 = 你没掌握,记错题本)
> - **判据**:复习题能"3 秒内反应答案"算过关;卡壳超 5 秒 = 失败,记错题本,隔 1d 重刷

### Week 1(Day 1-7)· 主干打底 + 第一轮复习起步

| Day | 【新学】学习 + 项目 + 代码肌肉 | 【复习队列】1d / 3d / 7d / 15d 倒推 | 目标时间 |
|---|---|---|---|
| **1 周一** | 学习: [C# 陷阱](C#_陷阱_前端转上位机必看_深度版.md) 8 大陷阱 / [M0](M0_每日讲义_深度版.md) Day 1-6 速过<br>项目: clone + 跑通 DAQMonitor + 读 IDevice.cs<br>代码肌肉: A1-A4(翻译题)+ C1(async void)<br>速记卡: Q1-Q3(上位机基础) | **无**(第一天没有复习) | 学习 2h + 项目 1.5h + 代码 1h |
| **2 周二** | 学习: [M1 串口通信](M1_串口通信_深度版.md) 全文<br>项目: 写 ModbusDevice(虚拟串口)<br>代码肌肉: **B1(CRC16,5min 白板)** + A5-A8<br>速记卡: Q4-Q6(并发异步) | **【1d】** Day 1 内容:A1-A4 翻译题默写一遍 + C1 复述 async void 三大坑 | 复习 15min |
| **3 周三** | 学习: [M2 Modbus](M2_Modbus_深度版.md) 全文 + 手算 CRC<br>项目: ModbusDevice 跑通虚拟从站<br>代码肌肉: B1 二刷(限时 4min)+ C2(.Result 死锁)+ C10(字节序)<br>速记卡: Q7-Q9(串口通信) | **【1d】** Day 2:B1 默写一遍(目标 4min)<br>**【速记 1d】** Q4-Q6 默背 | 复习 18min |
| **4 周四** | 学习: [M3 PLC](M3_PLC_深度版.md) 全文<br>项目: 写 PlcDevice(S7.Net)<br>代码肌肉: **B2(Channel 生产消费者,10min)** + C3(竞态)+ C4(event 没退订)<br>速记卡: Q10-Q12(Modbus/粘包/PLC) | **【1d】** Day 3:B1 三刷 + 字节序口诀<br>**【速记 1d】** Q7-Q9 默背 | 复习 18min |
| **5 周五** | 学习: [M3 PLC](M3_PLC_深度版.md) Day 2 + 硬件替代<br>项目: PlcDevice 编译通过<br>代码肌肉: B2 二刷(限时 8min)+ A9-A12<br>速记卡: Q13-Q14(工程量/EF Core) | **【1d】** Day 4:B2 默写一遍<br>**【3d】** Day 2 学的:**B1 第四刷** + C1+C10 复述<br>**【速记 1d】** Q10-Q12 默背 | 复习 20min |
| **6 周六** | 学习: [M0 Day 7 并发](M0_每日讲义_深度版.md) + 硬件替代方案<br>项目: 4 设备(Sim/Serial/Modbus/PLC)同时跑<br>代码肌肉: **B3(IDevice 实现,15min)** + A13-A16<br>速记卡: Q15-Q17(报警/OPC UA/控件) | **【1d】** Day 5:B2 三刷<br>**【3d】** Day 3:**B1 + C2(.Result)** 全套复述<br>**【速记 3d】** Q1-Q6 全部默背一遍(盖答案自问) | 复习 20min |
| **7 周日** | 学习: [M19 Day 1 VS 调试](M19_问题排查与调试_深度版.md) + Wireshark<br>项目: 条件断点实操 + 抓 Modbus TCP 字节<br>代码肌肉: **B1+B2+B3 闭卷自测**(限时 4min/8min/12min)<br>**🎯 周日闭卷 30 题(Week 1 版,见第 2 节)** | **【1d】** Day 6:B3 默写<br>**【3d】** Day 4:**B2 + C3+C4** 全套<br>**【速记 7d】** Q1-Q17 系统过一遍(第一次 7d 间隔)<br>**【Week 1 闭卷 30 题】** | 复习 30min + 自测 30min |

**Week 1 检查点**:周日闭卷 30 题正确率 ≥ 80%。低于 70% → Day 8 新学内容砍半,补救 Week 1。

---

### Week 2(Day 8-14)· 数据 + 可视化 + Prism

| Day | 【新学】学习 + 项目 + 代码肌肉 | 【复习队列】1d / 3d / 7d / 15d 倒推 | 目标时间 |
|---|---|---|---|
| **8 周一** | 学习: [M4 数据持久化](M4_数据持久化_深度版.md) 全文<br>项目: 读+理解 PointStore + AppDbContext + 历史查询<br>代码肌肉: **B11(PointStore 内存+SQLite,15min)** + A1-A10 二刷<br>速记卡: Q18-Q21(工程素养) | **【1d】** Day 7 闭卷错题复刷<br>**【速记 1d】** Q15-Q17 默背 | 复习 15min |
| **9 周二** | 学习: [M4](M4_数据持久化_深度版.md) Day 2 + SQLite Browser<br>项目: 历史查询方法<br>代码肌肉: **B4(Retry 指数退避,10min)** + C5 二刷 + C6(struct 拷贝)<br>速记卡: Q22-Q23(简历讲法) | **【1d】** Day 8:B11 默写<br>**【3d】** Day 6:**B3 + A13-A16** 复述<br>**【速记 1d】** Q18-Q21 默背 | 复习 18min |
| **10 周三** | 学习: [M5 实时可视化](M5_实时可视化_深度版.md)<br>项目: LiveCharts2 实时曲线 + 自定义仪表<br>代码肌肉: **B9(RelayCommand,5min)** + **B10(INPC,5min)**<br>速记卡: 二刷 Q1-Q12(盖答案) | **【1d】** Day 9:B4 默写<br>**【3d】** Day 7:**B1+B2+B3 闭卷再刷**(关键,7d 巩固)<br>**【速记 3d】** Q1-Q17 系统过 | 复习 20min |
| **11 周四** | 学习: [M6 报警日志](M6_报警引擎日志_深度版.md) 全文<br>项目: AlarmEngine 回滞验证 + Serilog 配置 + dotnet counters<br>代码肌肉: **B5(AlarmRule 回滞,15min)**<br>速记卡: 二刷 Q13-Q23 | **【1d】** Day 10:B9+B10 默写<br>**【3d】** Day 8:**B11 + C5+C6** 复述<br>**【速记 1d】** 二刷 Q1-Q12 错的题 | 复习 20min |
| **12 周五** | 学习: [M9 工程素养](M9_工程素养_测试DI容错_深度版.md) 速过<br>项目: Prism.DryIoc 安装 + Bootstrapper 改造<br>代码肌肉: **B8(Bootstrapper DI,10min)** + **B12(HttpClient Polly,10min)**<br>速记卡: 三刷 Q1-Q10 | **【1d】** Day 11:B5 默写<br>**【3d】** Day 9:**B4 + C5 二刷**<br>**【7d】** Day 5:**B2 + Q1-Q6** 第一次 7d 间隔,看还记不记得 | 复习 20min |
| **13 周六** | 学习: [M8.5 Prism](M8.5_Prism企业级MVVM_深度版.md) 精读<br>项目: 拆 View + RegionManager + EventAggregator<br>代码肌肉: **B17(EventAggregator,10min)** + **B18(ViewModelLocator,5min)**<br>速记卡: 三刷 Q11-Q23 | **【1d】** Day 12:B8+B12 默写<br>**【3d】** Day 10:**B9+B10 + Q15-Q17**<br>**【速记 7d】** Q1-Q23 全部盖答案自问 | 复习 25min |
| **14 周日** | 项目: Prism 重构完成 + Region 全套<br>代码肌肉: **Week 1+2 错题二刷(限时 50%)**<br>**🎯 周日闭卷 30 题(Week 2 版,见第 2 节)** | **【1d】** Day 13:B17+B18 默写<br>**【3d】** Day 11:**B5 + Q13-Q14**<br>**【7d】** Day 7:**Week 1 闭卷 30 题错的复刷**(关键)<br>**【Week 2 闭卷 30 题】** | 复习 30min + 自测 30min |

**Week 2 检查点**:周日闭卷 30 题正确率 ≥ 80%;**Week 1 闭卷题二次正确率 ≥ 90%**。

---

### Week 3(Day 15-21)· TCP + 上云 + 报表

| Day | 【新学】学习 + 项目 + 代码肌肉 | 【复习队列】1d / 3d / 7d / 15d 倒推 | 目标时间 |
|---|---|---|---|
| **15 周一** | 学习: [M11 TCP Socket](M11_TCP_Socket_深度版.md) 全文<br>项目: 读+理解 TcpDevice + 模拟 TCP Server<br>代码肌肉: **B16(TcpDevice 粘包,15min)** + C7(TCP 缓冲)+ C8(闭包)<br>速记卡: Q4-Q12 三刷 | **【1d】** Day 14 闭卷错题<br>**【速记 1d】** 三刷 Q1-Q10 | 复习 15min |
| **16 周二** | 学习: [M11](M11_TCP_Socket_深度版.md) Day 2 状态机<br>项目: TcpDevice 真粘包处理 + 模拟设备通<br>代码肌肉: **B6(帧解析 AA55,10min)** + C9(async lambda)二刷<br>速记卡: Q13-Q23 三刷 | **【1d】** Day 15:B16 默写<br>**【3d】** Day 13:**B17+B18**<br>**【速记 7d】** Q1-Q23(每周一通) | 复习 20min |
| **17 周三** | 学习: [M7 OPC UA + MQTT](M7_OPCUA_MQTT_深度版.md) + [M12 工程量](M12_工程量转换与多数据库_深度版.md)<br>项目: MqttPublisher 双向 + EngineeringConverter + MQTTX 验证<br>代码肌肉: **B13(MQTT 双向,15min)** + **B24(EngineeringConverter,5min)**<br>速记卡: 闭卷 Q1-Q23 | **【1d】** Day 16:B6 默写<br>**【3d】** Day 14:**Week 2 闭卷错题**<br>**【速记 1d】** 闭卷 Q1-Q23 | 复习 20min |
| **18 周四** | 学习: [M9.5 性能压测](M9.5_性能压测与长跑稳定性_深度版.md)<br>项目(选): OpcUaClient + BenchmarkDotNet + 1h 长跑<br>代码肌肉: **B14(OPC UA 异步,15min)**<br>速记卡: 全闭卷(目标 80%+) | **【1d】** Day 17:B13+B24 默写<br>**【3d】** Day 15:**B16 + C7+C8**<br>**【7d】** Day 11:**B5 + M6 报警原理**<br>**【速记 15d ⭐】** Day 3:**B1 + C1+C10**(第一次 15d 间隔,关键考验) | 复习 25min |
| **19 周五** | 学习: [M10 报表](M10_报表_深度版.md) + [M8 工程化](M8_工程化收尾_深度版.md)<br>项目: ClosedXML 导 Excel + Inno Setup 安装包<br>代码肌肉: **B19(IDialogService,10min)** + **B20(Serilog 中间件,10min)**<br>速记卡: 全闭卷 | **【1d】** Day 18:B14 默写<br>**【3d】** Day 16:**B6 + C9**<br>**【速记 7d】** Q1-Q23(每周) | 复习 20min |
| **20 周六** | 学习: [M17 MES](M17_工业安全与MES对接_深度版.md)<br>项目(选): MesApiClient + Polly + JWT + 白名单<br>代码肌肉: **B21(白名单,10min)** + **B22(AuditLogger,10min)**<br>速记卡: 全闭卷 | **【1d】** Day 19:B19+B20 默写<br>**【3d】** Day 17:**B13+B24**<br>**【7d】** Day 13:**B17+B18 + Prism 知识点** | 复习 25min |
| **21 周日** | 学习: [M18 配方](M18_配方管理_深度版.md) + [M19](M19_问题排查与调试_深度版.md) Day 2-5 复习<br>项目(选): RecipeManager + dotnet gcdump 实操 + Wireshark 抓协议<br>代码肌肉: **B23(Recipe 配方,15min)**<br>**🎯 周日闭卷 30 题(Week 3 版,见第 2 节)** | **【1d】** Day 20:B21+B22 默写<br>**【3d】** Day 18:**B14 + 性能压测知识**<br>**【7d】** Day 14:**Week 2 闭卷错题二刷**<br>**【15d ⭐】** Day 6:**B3 + C3+C4**(15d 关键考验)<br>**【Week 3 闭卷 30 题】** | 复习 35min + 自测 30min |

**Week 3 检查点**:周日闭卷 30 题 ≥ 80%;**Week 1 闭卷题三次正确率 ≥ 95%**(已经接近永久记忆)。

---

### Week 4(Day 22-30)· 文档 + 简历 + 面试 + 毕业测试

| Day | 【新学】学习 + 项目 + 代码肌肉 | 【复习队列】1d / 3d / 7d / 15d 倒推 | 目标时间 |
|---|---|---|---|
| **22 周一** | 学习: [文档能力](文档能力_设计协议操作测试文档_深度版.md) + [M15](M15_工程协作与联调_深度版.md) Day 1<br>项目: 写 README + 架构 + 协议 + 操作 + 测试报告 4 份文档<br>代码肌肉: **B25(PerformanceCounter,10min)** + **B26(IDisposable,10min)** | **【1d】** Day 21 闭卷错题<br>**【3d】** Day 19:**B19+B20**<br>**【7d】** Day 15:**B16 + TCP 粘包原理**<br>**【速记 7d】** 全部 Q1-Q23(每周) | 复习 25min |
| **23 周二** | 项目: 文档完善 + PlantUML 时序图<br>代码肌肉: **B27(CancellationToken,5min)** + **B28(LazyCache,5min)** + **B29(MemoryCache,5min)** + **B30(单元测试,10min)** | **【1d】** Day 22:B25+B26 默写<br>**【3d】** Day 20:**B21+B22 + MES 知识**<br>**【7d】** Day 16:**B6 + 帧解析原理** | 复习 25min |
| **24 周三** | 学习: 速记卡全集闭卷 + [面试逐字稿 30 题](面试问答_逐字稿_30题.md) 精读 10 题<br>产出: 简历 13K 版改写 + 项目 STAR 讲法背第 1 遍<br>代码肌肉: 闭卷抽 5 题(2 翻译 + 2 手写 + 1 Debug) | **【1d】** Day 23:B27-B30 默写<br>**【3d】** Day 21:**Week 3 闭卷错题**<br>**【7d】** Day 17:**B13+B24 + MQTT/工程量**<br>**【15d ⭐】** Day 9:**B4 + Retry 原理**(关键) | 复习 25min |
| **25 周四** | 学习: 逐字稿 30 题精读后 10 题<br>产出: 简历 14-15K 版 + 15K+ 版 + 5 个"难点故事"打磨<br>代码肌肉: **毕业测试预演**(B1+B2+B5+B16+B13 五大件,目标 60min) | **【1d】** Day 24:抽 5 题默写<br>**【3d】** Day 22:**B25+B26**<br>**【7d】** Day 18:**B14 + OPC UA 原理**<br>**【速记 7d】** 全部 Q1-Q23 | 复习 25min |
| **26 周五** | 投递: BOSS / 智联 / 拉勾 / 猎聘,先投 10-13K 练手 5-10 家<br>代码肌肉: 每天白板 3 题(随机抽) | **【1d】** Day 25:毕业预演错的题<br>**【3d】** Day 23:**B27-B30**<br>**【7d】** Day 19:**B19+B20 + Serilog 原理** | 复习 15min |
| **27 周六** | 模拟面试: 对镜头自问自答录音回听 + 逐字稿自测<br>代码肌肉: 错题集中刷 + 白板 3 题 | **【1d】** Day 26:白板 3 题<br>**【3d】** Day 24:**抽 5 题默写**<br>**【7d】** Day 20:**B21+B22 + MES/审计**<br>**【15d ⭐】** Day 12:**B8+B12 + DI/Polly 原理** | 复习 25min |
| **28 周日** | 复盘: 模拟面试录音整理 + 补漏<br>**🎯 周日闭卷 30 题(Week 4 综合版,见第 2 节)** | **【1d】** Day 27:白板 3 题<br>**【3d】** Day 25:**毕业预演五大件**<br>**【7d】** Day 21:**Week 3 闭卷 30 题**<br>**【15d ⭐】** Day 13:**B17+B18 + Prism 原理**<br>**【Week 4 闭卷 30 题】** | 复习 35min + 自测 30min |
| **29 周一** | 真面试(边面边补):每场面试后立刻记录被问倒的题<br>代码肌肉: 针对性补刷(被问什么补什么) | **【1d】** Day 28:闭卷错题<br>**【7d】** Day 22:**B25+B26**<br>**【速记 7d】** 全部 Q1-Q23 + 30 题逐字稿 | 复习 20min |
| **30 周二** | **🎯 毕业测试**:不查文档白板写完一个完整 ModbusDevice(含协议解析+CRC+异常处理+IDisposable),30min 内<br>**🎯 综合闭卷 30 题**(从 Week 1-4 各抽 7-8 题组成,见第 2 节) | **【1d】** Day 29:被问倒的题<br>**【3d】** Day 27:**白板 3 题**<br>**【7d】** Day 23:**B27-B30**<br>**【15d ⭐】** Day 15:**B16 + TCP 粘包**(15d 关键考验)<br>**【30d ⭐⭐ 终极考验】** Day 1:**C# 8 大陷阱全复述**(30 天前学的,现在还能讲清 = 永久记忆) | 复习 40min + 毕业测试 1h |

**Day 30 毕业判据**:
- ✅ 五大件白板 60min 内完成(B1+B2+B5+B16+B13)
- ✅ 综合闭卷 30 题正确率 ≥ 85%
- ✅ **Day 1 学的 C# 8 大陷阱还能 3 秒反应答案**(30d 间隔通过 = 永久记忆)
- ✅ 任意一段代码 5 秒内能说"这里有什么坑"

---

## 第 2 节 · 每周日闭卷自测 30 题(共 120 题)📝

> **怎么用**:每周日打开本节对应那一周,盖住答案,3 秒内回答。**卡壳超 5 秒 = 失败**,标"快忘点"。
> **快忘点** = 面试官最爱追问的点,你最容易答不出的点。每个快忘点都标了为什么面试官爱追。
> **题号格式**:W1.01 = Week 1 第 1 题。

### Week 1 闭卷 30 题 · 主干打底(C# / Modbus / 串口 / 通道)

#### C# 陷阱(5 题)

**W1.01** `List<Point>` 改字段(`list[0].X = 99`)为什么会丢?
> 答:struct 值类型,索引器返回拷贝,改拷贝原对象没动。
> **快忘点**:面试官追问"struct 性能一定比 class 好吗?"→ 错,>16 字节拷贝成本陡增。

**W1.02** `async void` 为什么是炸弹?
> 答:异常无法 catch + 调用方无法 await + 进程崩溃。唯一合法场景是 WPF 事件处理器。
> **快忘点**:追问"库代码里 async void 怎么改"→ 一律 async Task,内部包 try-catch。

**W1.03** UI 线程调 `.Result` 为什么死锁?
> 答:`.Result` 阻塞 UI,async 内部 await 要回 SynchronizationContext(UI 线程),两边互等。
> **快忘点**:追问"库代码怎么避免"→ 加 `ConfigureAwait(false)`。

**W1.04** `Count++` 在多线程下为什么会丢数?
> 答:读-改-写三步非原子,用 `Interlocked.Increment(ref _count)`。
> **快忘点**:追问"为什么 lock 也行但慢"→ lock 有上下文切换成本。

**W1.05** 闭包捕获 for 循环变量(`for(i...) Task.Run(()=>i)`)为什么全打 5?
> 答:Lambda 捕获变量引用,循环结束 i=5,C# 5+ 已修但老代码要 `var local = i`。
> **快忘点**:追问"foreach 也踩吗?"→ C# 5+ foreach 已修,for 没修。

#### Modbus / 协议(5 题)

**W1.06** Modbus RTU 帧格式?
> 答:`[从站地址 1B][功能码 1B][数据 NB][CRC16 低字节][CRC16 高字节]`。
> **快忘点**:CRC 低字节在前(很多新人搞反)。

**W1.07** Modbus CRC16 初值为什么是 0xFFFF,不是 0?
> 答:让全 0 数据也产生非零校验,否则全 0 数据 CRC=0 看不出错。
> **快忘点**:追问"多项式为什么 0xA001"→ 0x8005 的位反转。

**W1.08** Modbus 寄存器是大端还是小端?CRC 呢?
> 答:寄存器值大端,CRC 低字节在前(混合标准,最坑)。
> **快忘点**:面试官故意问"CRC 也是大端吧"→ 错,这是陷阱。

**W1.09** 寄存器地址 40001 对应协议里的什么?
> 答:40001 是引用地址(文档写法),协议里功能码 03 的"寄存器地址"从 0 开始,40001 → 0。
> **快忘点**:追问"为什么有 4 区段(0/1/3/4)"→ Modbus 规范:线圈/输入线圈/输入寄存器/保持寄存器。

**W1.10** 粘包/半包的本质是什么?怎么解?
> 答:串口/TCP 是字节流无消息边界。解法:帧头 + 长度域 + 缓冲重组 + CRC 校验。
> **快忘点**:追问"找不到帧头怎么办"→ 清空缓冲,否则内存无限涨(生产事故源)。

#### PLC(3 题)

**W1.11** S7.Net 连 PLC,`IsConnected` 返回 true 就一定通吗?
> 答:不,只表示 TCP 通。PLC 进 STOP 模式后 `IsConnected=true` 但 `LastErrorCode != 0`。
> **快忘点**:追问"怎么真判断"→ 看 `LastErrorCode` + 心跳读已知点位。

**W1.12** `DB1.DBW0` / `DB1.DBD4` / `DB1.DBX0.0` 区别?
> 答:DBW = Word(2 字节)、DBD = Double Word(4 字节)、DBX = 位(1 bit)。
> **快忘点**:追问"读浮点用哪个"→ DBD,但 PLC 端要声明 Real 不是 DInt。

**W1.13** S7-1200 默认优化访问的 DB 块能用 S7.Net 读偏移吗?
> 答:不能,必须右键 DB 块取消"优化访问"才能按偏移读。
> **快忘点**:追问"为什么"→ 优化访问用符号寻址,偏移不固定。

#### EF Core(3 题)

**W1.14** EF Core 为什么推荐 `IDbContextFactory<T>` 不直接注入 `DbContext`?
> 答:DbContext 不是线程安全,并发 SaveChangesAsync 抛异常。Factory 每次返回新实例。
> **快忘点**:追问"AddDbContext + Scoped 行不行"→ WPF 没请求作用域,Factory 更稳。

**W1.15** `SaveChangesAsync` 高频调用为什么慢?怎么优化?
> 答:每条都开事务 + 网络往返。改批量:`AddRange` + 单次 `SaveChanges`,性能提 10-50 倍。
> **快忘点**:追问"SQLite 单写者怎么处理"→ 用 Channel 串行化所有写。

**W1.16** `AsNoTracking()` 什么时候用?
> 答:只读查询用,跳过变更追踪提升性能。写场景必须用 tracking。
> **快忘点**:追问"全局禁用 tracking 怎么做"→ `UseQueryTrackingBehavior(NoTracking)`。

#### Prism / MVVM(3 题)

**W1.17** MVVM 三层职责?
> 答:View(XAML 界面) / ViewModel(状态+逻辑,INotifyPropertyChanged) / Model(数据)。
> **快忘点**:追问"ViewModel 能引用 View 吗"→ 不能,违背 MVVM。

**W1.18** `SetProperty(ref _x, value)` 内部做了什么?
> 答:① 比较新旧值(EqualityComparer)② 不等则赋值 ③ 触发 PropertyChanged 事件。
> **快忘点**:追问"为什么不直接 `_x = value`"→ 不触发事件 UI 不更新。

**W1.19** `ICommand.CanExecute` 返回 false 会怎样?
> 答:绑定的 Button 自动 `IsEnabled=false`(灰显)。
> **快忘点**:追问"CanExecute 什么时候重算"→ `CommandManager.InvalidateRequerySuggested()` 或显式 `RaiseCanExecuteChanged`。

#### TCP(3 题)

**W1.20** TCP `ReceiveAsync` 返回 0 表示什么?
> 答:对端正常关闭(FIN),不是错误,必须 return 触发重连。
> **快忘点**:追问"返回 0 抛异常吗"→ 不抛,静默返回 0。

**W1.21** TCP 心跳和接收循环为什么要并行 Task?
> 答:接收循环 await ReceiveAsync 阻塞,心跳要定时发送,串行会卡。
> **快忘点**:追问"用 LinkedTokenSource 干嘛"→ 一个 cancel 同时停心跳和接收。

**W1.22** 重连退避为什么加抖动(jitter)?
> 答:避免多客户端同时重试撞服务端(thundering herd),抖动 = 随机 + baseDelay。
> **快忘点**:追问"指数退避上限"→ 必须封顶,否则 `Math.Pow(2, 20)` 溢出。

#### MQTT / 上云(3 题)

**W1.23** MQTT QoS 0 / 1 / 2 区别?
> 答:0=至多一次(火警丢)、1=至少一次(可能重复)、2=恰好一次(性能差)。
> **快忘点**:追问"工业现场选哪个"→ 1(默认),2 性能差慎用。

**W1.24** MQTT ClientId 重复会怎样?
> 答:后连的踢先连的(broker 默认行为)。
> **快忘点**:追问"怎么避免"→ `MachineName + Guid` 保证唯一。

**W1.25** MQTT 通配符 `+` 和 `#` 区别?
> 答:`+` 单层(`device/+/data` 匹配 device/A/data 不匹配 device/A/B/data),`#` 多层。
> **快忘点**:追问"`#` 必须在主题最后吗"→ 是,MQTT 规范要求。

#### 报警 / 性能(2+2 题)

**W1.26** 报警为什么要回滞(hysteresis)?
> 答:阈值边界抖动时报警反复触发/消除。回滞:超上限才报,降到下限以下才解除。
> **快忘点**:追问"回滞带怎么定"→ 经验值,通常是量程的 2-5%。

**W1.27** `dotnet counters` 监控什么指标?
> 答:GC / ThreadPool / 内存 / CPU / 工作集。生产 7×24 跑必看。
> **快忘点**:追问"GC Gen 2 计数涨说明什么"→ 大对象进老代,可能内存泄漏。

**W1.28** 7×24 跑 3 天内存从 100MB 涨到 1GB,怎么排查?
> 答:`dotnet gcdump` 抓快照对比,找对象数量异常增长(event 没退订 / Channel 没消费)。
> **快忘点**:追问"怎么验证修好了"→ 再跑 3 天看曲线平稳。

#### 调试(1 题)

**W1.29** 条件断点和数据断点区别?各自适合什么场景?
> 答:条件断点 = 表达式为真时断(类似 `if(x)debugger`)。数据断点 = 内存地址被改时断(适合"字段莫名被改")。
> **快忘点**:追问"数据断点对 class 字段有效吗"→ 无效,只能监视值类型字段地址。

#### 综合(1 题)

**W1.30** 讲讲你 DAQMonitor 项目的架构(1 分钟 STAR)?
> 答:分层 Core(IDevice 抽象 + Channel 并发 + 报警 + 报表 + 心跳)+ UI(WPF/MVVM + 自研控件)+ Tests(56 测试)。换设备 UI 零改动,支持串口/Modbus/PLC/CAN/USB。
> **快忘点**:面试官追问"最难的一点"→ 准备好粘包 / UI 卡顿 / 内存泄漏任选一个真实例子。

---

### Week 2 闭卷 30 题 · 数据 + 可视化 + Prism 深化

> **注意**:Week 2 的题比 Week 1 深 1 层,且**包含 Week 1 的核心题复刷**(标 🔁)。

#### C# 陷阱深挖(5 题)

**W2.01** 🔁 W1.02 升级:`async void` 内的异常,AppDomain.UnhandledException 能 catch 吗?
> 答:能捕获但不能阻止进程崩溃(除非 `TaskScheduler.UnobservedTaskException` 配置)。
> **快忘点**:追问".NET Core vs .NET Framework 行为差"→ Core 默认不崩(可配),Framework 默认崩。

**W2.02** `ConfigureAwait(false)` 在 WPF 和库代码里分别该不该加?
> 答:WPF UI 代码不用(要回 UI 线程更新),库代码必须加(避免 SynchronizationContext 捕获,防死锁)。
> **快忘点**:追问"ASP.NET Core 还需要吗"→ 不需要(无 SyncContext)。

**W2.03** `lock(this)` 为什么不行?
> 答:`this` 是公开引用,外部代码可以 lock 同一对象 → 死锁 + 隐蔽耦合。用 `private readonly object _gate = new()`。
> **快忘点**:追问"`lock(typeof(T))` 行吗"→ 也不行,typeof 返回的 Type 对象是公开的。

**W2.04** 🔁 W1.04 升级:`Interlocked.CompareExchange` 怎么实现无锁 CAS?
> 答:CPU 原生指令(`lock cmpxchg`),比较 + 交换一次性原子完成。失败重试。
> **快忘点**:追问"CAS 的 ABA 问题"→ 加版本号(`Interlocked` 的 `ref int version` 模式)。

**W2.05** `IDisposable` + finalizer 什么时候一起用?
> 答:只有封装非托管资源(IntPtr/文件句柄/COM)才需要 finalizer 兜底。纯托管不需要。
> **快忘点**:追问"SafeHandle 是干嘛的"→ 已包 finalizer,优先用而不是自己写。

#### Modbus / 通信深化(5 题)

**W2.06** 🔁 W1.06 升级:Modbus RTU 帧之间要等多久?
> 答:3.5 字符静默时间(9600bps 约 4ms)。否则接收方分不清帧边界。
> **快忘点**:追问"Modbus TCP 也要吗"→ 不用,TCP 用 MBAP 头里的长度域定位。

**W2.07** Modbus 异常码 0x01-0x04 含义?
> 答:0x01 非法功能码 / 0x02 非法数据地址 / 0x03 非法数据值 / 0x04 从站设备故障。
> **快忘点**:追问"异常响应帧怎么识别"→ 功能码最高位置 1(如 0x83 = 0x03 异常)。

**W2.08** RS485 一主多从,主站轮询时间怎么算?
> 答:每从站响应时间 × 从站数 + 帧间隔。如 10 从站 × 50ms = 500ms 一轮,1s 轮询 2 次。
> **快忘点**:追问"从站太多轮询不够怎么办"→ 拆多个总线 / 提波特率 / 用 TCP。

**W2.09** 🔁 W1.10 升级:粘包缓冲区找不到帧头为什么必须清空?
> 答:设备掉线发垃圾数据时缓冲区无限涨,最终 OOM 崩进程。
> **快忘点**:追问"清空会丢真数据吗"→ 不会,真数据有 AA55 帧头,清的是垃圾。

**W2.10** 串口 `BytesToRead` 等够数据时为什么用 `Thread.Sleep(10)` + 超时,不直接 `Read()`?
> 答:`SerialPort.Read` 没数据会阻塞卡死,无法超时退出。轮询 BytesToRead + Sleep 可控。
> **快忘点**:追问"生产怎么写"→ 用 NModbus4 库的 `async Task<ushort[]>` 异步。

#### EF Core 深化(3 题)

**W2.11** PointStore 内存索引 + SQLite 双写为什么不一致?
> 答:设计上接受"内存是最新,SQLite 是历史",落盘失败丢这条历史业务可接受。
> **快忘点**:追问"强一致怎么做"→ 加事务 + 写完再返回,但采集慢。

**W2.12** `await using var db = factory.CreateDbContextAsync()` 为什么必须 `await using`?
> 答:DbContext 实现 `IAsyncDisposable`,同步 Dispose 占 ThreadPool(高频场景炸)。
> **快忘点**:追问"IAsyncDisposable 和 IDisposable 区别"→ 异步释放能 await 内部异步清理。

**W2.13** EF Core 迁移(Migrations)和 `EnsureCreated` 区别?
> 答:EnsureCreated 一次性建表,改字段不生效。Migrations 支持增量演进,生产推荐。
> **快忘点**:追问"什么时候用哪个"→ Demo 用 EnsureCreated,生产用 Migrations。

#### Prism / MVVM 深化(3 题)

**W2.14** Prism 的 `EventAggregator` 比自己写的多什么?
> 答:UI 线程自动 marshal + 订阅过滤(`filter: m => m.X > 5`)+ `KeepSubscriberReferenceAlive` 选项。
> **快忘点**:追问"为什么默认弱引用"→ 防 VM 订阅没退订导致内存泄漏。

**W2.15** Prism Region 和 Navigation 怎么协作?
> 答:Region 是 UI 占位符(ContentControl),Navigation 切换 Region 里显示哪个 View。
> **快忘点**:追问"View 怎么注册到 Region"→ `RegisterViewWithRegion("MainRegion", typeof(MyView))` 或 `RequestNavigate`。

**W2.16** `ViewModelLocator.AutoWireViewModel="True"` 怎么找到 VM?
> 答:命名约定:`MyApp.Views.MainView` → `MyApp.ViewModels.MainViewModel`。
> **快忘点**:追问"约定能改吗"→ `ViewModelLocationProvider.SetDefaultViewTypeToViewModelTypeResolver(...)`。

#### TCP / 性能深化(3+2 题)

**W2.17** 🔁 W1.22 升级:指数退避公式?
> 答:`delay = baseDelay × 2^(attempt-1) + random(0, baseDelay)`。attempt 从 1 开始。
> **快忘点**:追问"为什么 attempt <= maxRetries 用 when"→ 最后一次失败要抛,不能进 retry 分支。

**W2.18** `MemoryStream.CopyTo` 为什么高频小包性能炸?
> 答:byte-by-byte 循环,每帧 GC 压力大。改 `Array.Copy` + 重置 Position。
> **快忘点**:追问"更优解"→ `System.IO.Pipelines`(PipeReader/Writer),专为大流量。

**W2.19** 高频采集(1000 点/秒)UI 怎么不卡?
> 答:① 后台线程采集 ② Channel 攒批 ③ 每 200ms 批量 UI 刷新 ④ Dispatcher 切 UI 线程。
> **快忘点**:追问"ObservableCollection 直接 Add 会怎样"→ 跨线程异常 + 每条触发 UI 刷新卡死。

**W2.20** `dotnet trace` 和 `dotnet counters` 区别?
> 答:counters = 实时指标(GC/CPU);trace = 采样火焰图(cpu-stack),找热点方法。
> **快忘点**:追问"什么时候用 PerfView"→ Windows 旧场景 / ETW 深挖。

**W2.21** BenchmarkDotNet 怎么测 PointStore 性能?
> 答:`[MemoryDiagnoser]` 标注 + `[Benchmark]` 标方法 + `dotnet run -c Release`,看 Gen0/Gen1/Gen2 + Mean + Allocated。
> **快忘点**:追问"`[MemoryDiagnoser]` 干嘛"→ 测每次调用分配多少内存,找 GC 压力源。

#### 报警 / 调试(2+1 题)

**W2.22** 🔁 W1.26 升级:回滞带的 HighThreshold 和 LowThreshold 谁大?
> 答:HighThreshold > LowThreshold(高报场景)。LowThreshold 是"回落到这个值以下才解除"。
> **快忘点**:追问"低报场景反过来吗"→ 是,LowThreshold < HighThreshold(低报解除要升回去)。

**W2.23** AlarmEngine 触发事件的边沿检测怎么做?
> 答:存 `wasActive`,只在 `!wasActive && nowActive` 触发 Triggered,`wasActive && !nowActive` 触发 Cleared。
> **快忘点**:追问"不边沿检测会怎样"→ 状态不变也触发,UI 反复刷新报警。

**W2.24** Wireshark 抓 Modbus TCP 怎么过滤?
> 答:`modbus` 或 `tcp.port == 502`。看 Function Code / Reference Number / Register Values。
> **快忘点**:追问"RTU 串口怎么抓"→ 用串口监视器(ComSpy / PortMon),Wireshark 抓不到串口。

#### MQTT / 综合(3+3 题)

**W2.25** 🔁 W1.25 升级:MQTT `WithCleanSession(false)` 为什么重要?
> 答:配合 QoS 1,broker 在断线期间缓存消息,重连补发。否则离线消息全丢。
> **快忘点**:追问"持久会话 ClientId 怎么处理"→ 必须固定(不能用随机 Guid),否则 broker 找不到旧 session。

**W2.26** 🔁 W1.30 升级:你项目最难的一点是什么?(STAR 法 1 分钟答)
> 答:挑粘包 / Channel 并发 / 内存泄漏任选一个真实例子:Situation → Task → Action → Result。
> **快忘点**:追问"如果重来怎么改"→ 必须有反思(说明你成长型思维)。

**W2.27** `IDevice` 抽象为什么这么设计?换设备为什么 UI 零改动?
> 答:IDevice 定义 Connect/Disconnect/Read/Write + DataReceived 事件。UI 订阅事件,Bootstrapper 改一行换设备实现。
> **快忘点**:追问"接口 vs 抽象类"→ IDevice 接口(无共享代码),DeviceBase 抽象类(共享 RaiseData)。

**W2.28** DI 容器为什么用 Singleton 注册 PointStore?
> 答:全局共享状态(实时数据缓存),所有线程读同一份。Scoped 用于"请求级"对象(WPF 没请求概念)。
> **快忘点**:追问"Singleton 注入 DbContext 会怎样"→ 炸(并发),所以用 IDbContextFactory。

**W2.29** 单元测试怎么 mock IDevice 验证 BatchReady 事件触发?
> 答:`Mock<IDevice>` + `SetupAdd(d => d.DataReceived += It.IsAny<>())` 捕获订阅,手动 Invoke 触发。
> **快忘点**:追问"`Verify(d => d.Connect(), Times.Once)` 干嘛"→ 验证方法调用次数。

**W2.30** 🔁 Week 1 全套抽 3 题复刷(W1.06 / W1.10 / W1.26 任选)
> 答:口述答案 + 快忘点。
> **快忘点**:这是 Week 2 复习 Week 1 的核心机制(7d 间隔重复)。

---

### Week 3 闭卷 30 题 · TCP + 上云 + 报表 + 综合

> Week 3 题比 Week 2 又深 1 层,且包含 Week 1-2 关键题复刷。

#### C# / 异步深挖(5 题)

**W3.01** `Task.FromResult(42)` 和 `Task.Run(() => 42)` 区别?
> 答:FromResult 同步完成(已完成的 Task,无线程开销);Task.Run 调度到线程池跑。
> **快忘点**:追问"什么时候用 FromResult"→ 已有结果的异步签名伪装,如 mock。

**W3.02** `IAsyncEnumerable<T>` 是什么?前端类比?
> 答:异步流(`async IAsyncEnumerable<T> Stream()` + `yield return`)。类比 Node Readable Stream / JS `for await...of`。
> **快忘点**:追问"和 Channel 区别"→ IAsyncEnumerable 拉模型(pull),Channel 推模型(push)。

**W3.03** `ValueTask<T>` 比 `Task<T>` 强在哪?
> 答:值类型,避免 Task 对象分配(高频热路径同结果可复用)。但只能 await 一次。
> **快忘点**:追问"什么时候用 ValueTask"→ 高频 + 多数同步完成的 API(如带缓存的读)。

**W3.04** 🔁 W2.01 复刷:async void 异常路径(详细机制)?
> 答:3 秒内回答完整;卡壳 = 失败,记错题本。
> **快忘点**:同 W2.01。

**W3.05** `CancellationTokenSource.CreateLinkedTokenSource` 用途?
> 答:合并多个 token(超时 + 用户取消 + 父任务取消),任一触发都取消。
> **快忘点**:追问"linkedCts 必须 Dispose 吗"→ 必须,内部有 Timer 链接。

#### Modbus / PLC 深化(5 题)

**W3.06** 🔁 W1.08 复刷:Modbus 寄存器大端 + CRC 小端(混合标准)。
> 答:3 秒内答完。
> **快忘点**:同 W1.08。

**W3.07** Modbus 浮点字节序(ABCD / CDAB / BADC / DCBA)现场怎么确认?
> 答:**抓帧**:让设备发一个已知值(如 1.0),看字节顺序。**不能假设** ABCD,工业现场 CDAB 最常见。
> **快忘点**:追问"1.0 在 ABCD 下字节是什么"→ `0x3F800000`(IEEE754),即 3F 80 00 00。

**W3.08** 多个 Modbus 从站共享一个串口怎么调度?
> 答:必须串行(同一时刻只能一个从站在响应),用调度队列排队请求。**不能并发**。
> **快忘点**:追问"轮询顺序怎么定"→ 关键从站优先,或按 slave ID 顺序。

**W3.09** 🔁 W1.11 复刷:PLC `IsConnected` 不可全信。
> 答:3 秒答完。
> **快忘点**:同 W1.11。

**W3.10** S7.Net 同步 API 在 C# 怎么异步化?
> 答:`Task.Run(() => plc.Read(addr))` 包装。或换 S7NetPlus.Async 扩展 / Sharp7。
> **快忘点**:追问"为什么 S7.Net 不直接 async"→ 历史原因,API 老旧。

#### EF Core / 持久化(3 题)

**W3.11** SQLite 高频写(1000 条/秒)怎么压?
> 答:批量 `AddRange` + 单次 SaveChanges + 用 Channel 串行化 + WAL 模式。
> **快忘点**:追问"WAL 模式怎么开"→ `PRAGMA journal_mode=WAL`,允许并发读 + 单写。

**W3.12** EF Core 跟踪大对象图(1000 个 entity)性能差,怎么办?
> 答:用 `AsNoTracking()` 或 `AsNoTrackingWithIdentityResolution()`,跳过变更字典。
> **快忘点**:追问"`Detach` 单个 entity 怎么做"→ `db.Entry(e).State = Detached`。

**W3.13** 跨数据库(SQLite → SQL Server → MySQL)怎么平滑切换?
> 答:EF Core 抽象层 + `UseSqlite` / `UseSqlServer` / `UseMySQL` 切换 Provider。
> **快忘点**:追问"SQL 方言差异怎么办"→ 用 LINQ(避免裸 SQL),或 `Database.SqlQueryRaw<T>` 时分库写。

#### Prism / MVVM 深化(3 题)

**W3.14** Prism 模块化(Module)和 Region 区别?
> 答:Module = 功能包(DLL 级别隔离);Region = UI 占位符(View 切换)。Module 注册 View 到 Region。
> **快忘点**:追问"什么时候要拆 Module"→ 团队多人 + 独立部署 + 按需加载(减小主程序)。

**W3.15** Prism DialogService 和自己写的 IDialogService 区别?
> 答:Prism 的支持自定义弹窗 UI(注册到容器 + `ShowDialog("ConfirmDialog", parameters, callback)`)。
> **快忘点**:追问"VM 怎么接收 Dialog 返回值"→ callback `r => { /* r 是 true/false */ }`。

**W3.16** 🔁 W2.16 复刷:ViewModelLocator 命名约定。
> 答:3 秒答完。
> **快忘点**:同 W2.16。

#### TCP / 性能深化(3+2 题)

**W3.17** TCP 粘包状态机 `needResync` 信号什么时候置位?
> 答:扫到帧头但下一字节不对(如 AA 后不是 55),置位 → 调用方 `RemoveAt(0)` 丢 1 字节继续扫。
> **快忘点**:追问"为什么只丢 1 字节不丢整段"→ 可能 AA 是真帧头(被噪声污染前一字节),丢整段会丢真数据。

**W3.18** 🔁 W2.18 复刷:`MemoryStream.CopyTo` 性能坑。
> 答:3 秒答完。
> **快忘点**:同 W2.18。

**W3.19** 长跑 1 小时内存平稳的标准是什么?
> 答:工作集不持续涨(允许锯齿,GC 周期回收),GC Gen 2 对象数稳定,无大对象(>85KB)持续分配。
> **快忘点**:追问"锯齿是什么"→ GC 回收的正常表现,每代回收后内存降。

**W3.20** `Interlocked.Exchange(ref _x, 0)` 在 PerformanceCounter 里干嘛?
> 答:原子读 + 重置,避免上报和计数竞态(普通读 + 赋值两步中间会被插队)。
> **快忘点**:追问"为什么不用 lock"→ Interlocked 是 CPU 原子指令,lock 是内核态切换慢 100 倍。

**W3.21** 🔁 W2.21 复刷:BenchmarkDotNet 用法。
> 答:3 秒答完。
> **快忘点**:同 W2.21。

#### MQTT / 报警 / 报表(3+2+2 题)

**W3.22** 🔁 W1.23 复刷:MQTT QoS 三档。
> 答:3 秒答完。
> **快忘点**:同 W1.23。

**W3.23** MQTT 高频发布(1000 条/秒)怎么压?
> 答:批量 `PublishAsync` 循环 / 用 `PublishAsync(IAsyncEnumerable<>)` 流式 / Payload 用 MessagePack 替代 JSON。
> **快忘点**:追问"为什么 MessagePack"→ 比 JSON 小 50-80%,反序列化快 10 倍。

**W3.24** 🔁 W2.22 复刷:回滞带 HighThreshold / LowThreshold 关系。
> 答:3 秒答完。
> **快忘点**:同 W2.22。

**W3.25** 报警去抖除了回滞还有什么方法?
> 答:时间窗口(N 秒内 M 次超阈值才报)+ 滑动平均(过滤毛刺)+ 中值滤波(去极值)。
> **快忘点**:追问"这些方法组合用吗"→ 是,工业现场常回滞 + 时间窗口双保险。

**W3.26** ClosedXML 导 Excel 大数据(10 万行)慢?
> 答:用 `LoadFromCollection` 批量插入,不要逐 cell 写。或用 OpenXML SDK 的 SAX 流式。
> **快忘点**:追问"超过 Excel 行数(104 万)怎么办"→ 分 sheet 或换格式(CSV / Parquet)。

**W3.27** Excel 报表模板怎么填充?
> 答:ClosedXML `workbook.Worksheet("Sheet1").Cell("A1").Value = ...` 或用占位符 `{{NAME}}` 批量替换。
> **快忘点**:追问"图表怎么生成"→ ClosedXML 原生支持 Line/Bar/Pie,从数据范围自动生成。

#### 调试 / 安全 / 综合(1+1+3 题)

**W3.28** `dotnet gcdump` 和 `dotnet dump` 区别?
> 答:gcdump = 只抓堆对象(轻量,用于找泄漏);dump = 全进程快照(用 windbg/lldb 分析)。
> **快忘点**:追问"gcdump 何时抓两个对比"→ 跑 5min 抓一次,跑 30min 再抓一次,对比对象增长。

**W3.29** JWT 在工业 MES 对接里干嘛?
> 答:无状态认证(token 内含签名 + 过期),HttpClient 每请求带 `Authorization: Bearer <token>`,服务端验签不查库。
> **快忘点**:追问"token 过期怎么续"→ RefreshToken 模式,过期前用 refresh 换新 token。

**W3.30** 🔁 Week 1+2 综合复刷:W1.30 + W2.26 项目 STAR 讲法 + 难点故事。
> 答:3 秒内开讲,流畅完整。
> **快忘点**:这是 Week 3 检验 Week 1-2 是否真固化的关键。

---

### Week 4 闭卷 30 题 · 综合 + 面试冲刺(终极版)

> Week 4 是**面试前最后一关**,题目全部模拟面试官真实追问,深度最高。包含 Week 1-3 核心题三刷。

#### C# 终极追问(5 题)

**W4.01** 🔁 W1.01-W1.05 + W2.01-W2.05 综合:C# 8 大陷阱无脑过(前端转上位机必看)
> 答:struct 拷贝 / async void / .Result 死锁 / 闭包循环变量 / Count++ 竞态 / lock(this) / IDisposable+finalizer / ConfigureAwait。每个 3 秒答完。
> **快忘点**:面试官最爱从这 8 个里随机抽。

**W4.02** GC 三代(0/1/2)机制 + 大对象堆(LOH)什么时候触发?
> 答:Gen 0 = 短命对象(<85KB),Gen 1 = 缓冲,Gen 2 = 长命,LOH = ≥85KB 大对象(直接进 Gen 2)。
> **快忘点**:追问"为什么大对象直接 Gen 2"→ 复制成本太高,LOH 不压缩不动。

**W4.03** `Span<T>` / `Memory<T>` 是什么?为什么有?
> 答:连续内存的抽象(数组 / 字符串 / native),零拷贝切片。Span 栈 only(不能字段),Memory 可堆。
> **快忘点**:追问"`MemoryMarshal.Cast<byte, int>` 干嘛"→ 类型重解释,UDP/协议解析常用。

**W4.04** 🔁 W3.03 复刷:ValueTask vs Task。
> 答:3 秒答完。
> **快忘点**:同 W3.03。

**W4.05** `record` / `record struct` / `class` 怎么选?
> 答:不可变值对象用 `record`(默认引用 + 值相等),性能敏感小对象用 `record struct`,可变业务对象用 `class`。
> **快忘点**:追问"record 的 `with` 表达式"→ 创建副本只改某字段,基于 `init` 属性。

#### Modbus / PLC 终极(5 题)

**W4.06** 🔁 W1.06-W1.09 + W3.06-W3.07 综合:Modbus RTU 帧全细节(CRC/字节序/地址偏移)
> 答:3 秒答完,不卡壳。
> **快忘点**:CRC 低字节在前、寄存器大端、40001→地址 0,三个不能混。

**W4.07** Modbus RTU vs ASCII 区别?
> 答:RTU = 二进制 + CRC16(高效,主流);ASCII = 16 进制字符(:开头,CRLF 结尾)+ LRC 校验(可读性,基本淘汰)。
> **快忘点**:追问"LRC 怎么算"→ 补码校验和(`256 - (sum & 0xFF)`)。

**W4.08** 🔁 W1.11-W1.13 + W3.10 综合:PLC 全套(S7.Net / DB 块 / 优化访问)
> 答:3 秒答完。
> **快忘点**:同 W1.11-W1.13。

**W4.09** 三菱 / 欧姆龙 / 西门子通信协议区别?
> 答:三菱 = MC 协议(二进制/ASCII);欧姆龙 = FINS(UDP/TCP);西门子 = S7(ISO-on-TCP)。或用 HslCommunication 统一。
> **快忘点**:追问"HslCommunication 优势"→ 一把梭多协议,缺点是闭源商业(基础免费)。

**W4.10** OPC UA 比 Modbus 强在哪?
> 答:类型系统(对象/方法/事件)+ 内置安全(签名加密)+ 订阅模式(变化推送)+ 信息模型(每个变量有 unit/range/description)。
> **快忘点**:追问"OPC UA 缺点"→ 重(.NET 实现 5MB+)+ 复杂(信息模型学习曲线陡)。

#### EF Core / 数据终极(3 题)

**W4.11** 🔁 W2.11-W2.13 + W3.11-W3.12 综合:EF Core 全套(IDbContextFactory / 跟踪 / 批量)
> 答:3 秒答完。
> **快忘点**:同 W2.11-W2.13。

**W4.12** Dapper vs EF Core 怎么选?
> 答:Dapper = 轻量 SQL 映射,性能好可控;EF Core = ORM,LINQ 写快,性能差 2-5 倍。简单查询用 Dapper,复杂关系用 EF Core。
> **快忘点**:追问"混用怎么搞"→ 同项目都装,CUD 走 EF Core,复杂报表 SQL 走 Dapper。

**W4.13** 时序数据(每秒 1 万点)用什么库?
> 答:SQLite 撑不住(单写者),换 InfluxDB / TimescaleDB(PostgreSQL 扩展)/ TDengine(国产,工业主流)。
> **快忘点**:追问"为什么不用 MySQL"→ B+树索引对时间序列写放大严重,时序库用 LSM-tree 写吞吐高 10 倍。

#### Prism / TCP / MQTT 终极(3+3+3 题)

**W4.14** 🔁 W2.14-W2.16 + W3.14-W3.15 综合:Prism 全套(EventAggregator / Region / Module / DialogService)
> 答:3 秒答完。
> **快忘点**:同 W2.14-W2.16。

**W4.15** 🔁 W1.20-W1.22 + W3.17 综合:TCP 全套(ReceiveAsync 0 / 心跳并行 / 退避抖动 / needResync)
> 答:3 秒答完。
> **快忘点**:同 W1.20-W1.22。

**W4.16** TCP `SocketAsyncEventArgs` vs `ReceiveAsync` 区别?
> 答:SAEA 是高性能 IOCP 模式(对象池复用,无 GC 压力);普通 `ReceiveAsync` 简单但每调用分配。万连接以上用 SAEA。
> **快忘点**:追问"`System.IO.Pipelines` 在哪"→ 更高层抽象,管道式读写,ASP.NET Core 内部用。

**W4.17** 🔁 W1.23-W1.25 + W3.22-W3.23 综合:MQTT 全套(QoS / ClientId / 通配符 / CleanSession / 高频发布)
> 答:3 秒答完。
> **快忘点**:同 W1.23-W1.25。

**W4.18** MQTT Broker 自建(Mosquitto) vs 云(EMQX) vs 商业 怎么选?
> 答:自建 Mosquitto(轻量,内网);EMQX(集群,百万连接);商业 AWS IoT(贵 + 合规)。工业现场常 Mosquitto 内网 + 备份。
> **快忘点**:追问"Broker 挂了怎么办"→ 集群 + 客户端本地队列(SQLite)回放。

**W4.19** OPC UA `Subscription` 和 `MonitoredItem` 关系?
> 答:Subscription = 发布通道(PublishingInterval);MonitoredItem = 订阅的节点(SamplingInterval)。一个 Subscription 装多个 MonitoredItem。
> **快忘点**:追问"几千节点怎么压"→ 1 个 Subscription 装 1000 个 Item,不要 1000 个 Subscription。

#### 报警 / 性能 / 调试终极(2+2+1 题)

**W4.20** 🔁 W1.26 + W2.22 + W3.24-W3.25 综合:报警回滞全套(HighThreshold / LowThreshold / 边沿检测 / 去抖方法)
> 答:3 秒答完。
> **快忘点**:同 W2.22-W3.25。

**W4.21** 报警分级(Critical / Warning / Info)怎么落库 + 通知?
> 答:Critical = 短信 + 电话 + 邮件;Warning = 邮件 + 企业微信;Info = 仅日志。落 SQLite 不同表或同表 Level 字段。
> **快忘点**:追问"报警风暴(1 秒 100 条)怎么压"→ 去重 + 分级聚合(同点位 1 分钟内只发 1 次)。

**W4.22** 🔁 W1.27-W1.28 + W2.20-W2.21 + W3.19-W3.21 综合:性能全套(counters / gcdump / trace / BenchmarkDotNet / 长跑标准)
> 答:3 秒答完。
> **快忘点**:同 W2.20-W3.21。

**W4.23** 🔁 W1.29 + W2.24 + W3.28 综合:调试全套(条件断点 / 数据断点 / Wireshark / gcdump / dump)
> 答:3 秒答完。
> **快忘点**:同 W1.29-W3.28。

**W4.24** 工业现场"偶发性掉线 30 秒自动恢复",怎么定位?
> 答:① 物理层(网线 / 接地 / EMC 干扰)② 链路(抓包看 FIN 还是 RST)③ 应用层(超时配置 / 重试退避)④ 日志(Serilog TraceId 关联)。
> **快忘点**:追问"30 秒是哪个超时"→ 多半是 OS TCP keepalive(默认 2 小时,但配 30s 也可能)或应用层心跳超时。

#### 简历 / 项目 / 综合(3+3 题)

**W4.25** DAQMonitor 项目 1 分钟 STAR 讲法?
> 答:S(转行,缺项目经验)T(30 天做真能跑的 SCADA)A(分层架构 + 6 协议 + 56 测试)R(可演示 / 面试官挖不倒)。
> **快忘点**:面试官追问"用了什么设计模式"→ 接口 / 工厂 / 观察者 / 策略,挑 2 个具体讲。

**W4.26** 🔁 W1.30 + W2.26 + W3.30 终极:5 大件白板 60min(B1+B2+B5+B16+B13)?
> 答:模拟面试场景,白板写完。**卡壳任一题 = 失败**。
> **快忘点**:这是 Day 30 毕业测试预演,必须一次过。

**W4.27** 为什么从前端转上位机?(必问,准备好 3 个理由)
> 答:① 上位机技术栈深(协议 / 实时 / 并发)天花板高;② 工业互联网风口;③ 5 年前端经验可迁移(WPF/XAML ≈ HTML/CSS,MVVM ≈ React,异步习惯好)。
> **快忘点**:追问"为什么不选后端"→ 后端竞争激烈 + 互联网收缩,工业稳定 + 缺人。

**W4.28** 期望薪资 13-15K 的依据?
> 答:① JD 调研(同岗位中位数)② 项目可演示(超 80% 应届)③ 协议 / 并发 / 调试深度(超 8-10K 助理岗)④ 学习能力(30 天独立完成)。
> **快忘点**:追问"低于 13 接受吗"→ 看公司(大厂 12K 可以,小厂坚持 13K+)。

**W4.29** 1 年后想做到什么水平?(成长型思维)
> 答:① 独立负责中型项目(从协议对接到部署)② 带新人 / Code Review ③ 深挖 1-2 个垂直领域(OPC UA / 时序数据库 / 工业安全)。
> **快忘点**:追问"3 年呢"→ 技术专家 / 架构师方向,或转 PM(懂技术的产品)。

**W4.30** 🔥 **毕业终极题**:从 Day 1 学到现在,你觉得最关键的 3 个知识点是什么?
> 答:**自己回答**(没有标准答案,看你是否真内化)。建议答:① IDevice 抽象(架构思维)② Channel 并发(性能核心)③ 协议字节序 / CRC(工业本质)。
> **快忘点**:面试官最爱这个,看你思维深度。

---

## 第 3 节 · Anki 卡片制作建议 📇

### 工具选择

| 工具 | 平台 | 优势 | 劣势 | 推荐度 |
|---|---|---|---|---|
| **AnkiDesktop** | Win/Mac/Linux/手机 | 免费、开源、SM-2 算法、插件丰富、支持图片音频 | UI 丑、学习曲线陡 | ⭐⭐⭐⭐⭐ |
| Quizlet | 全平台 | UI 美观、易上手 | 免费版功能少、算法不如 Anki | ⭐⭐⭐ |
| RemNote | 全平台 | 笔记 + 卡片一体化 | 收费、社区小 | ⭐⭐ |
| **自建 Markdown** | VS Code | 已有讲义直接复用、Git 管理 | 无算法、靠手动 | ⭐⭐⭐⭐(备用) |

**结论**:**主推 AnkiDesktop**(免费 + 算法强 + 跨平台同步)。

- 下载:https://apps.ankiweb.net/(Windows 免费)
- 手机:AnkiDroid(Android 免费)/ AnkiMobile(iOS 168 元,可先用网页版)
- 同步:免费 AnkiWeb 账号 2GB 空间,够用

### 卡片格式(四件套)

每张卡片必须 4 个字段,缺一不可:

```
【正面】问题(短、具体、面试官会怎么问)
【反面】答案(一句话,3 秒能答完)
【为什么】底层原理(让面试官"嗯,你真懂")
【踩坑警告】实战坑(证明你做过,不是背书)
```

### 5 个具体卡片样例(从代码肌肉题里挑)

#### 卡片 1(来自 B1)

```
【正面】手写 Modbus CRC16,初值为什么是 0xFFFF 而不是 0?
【反面】让全 0 数据也产生非零校验,否则全 0 数据 CRC=0 看不出错。
【为什么】CRC 的本质是"多项式除法余数",初值 0 会让前导 0 字节不影响余数 → 失去检错能力。
   初值 0xFFFF 等价于"开头先 XOR 一个 0xFFFF",保证前导 0 也能改余数。
【踩坑警告】① 多项式必须 0xA001 不是 0x8005(Modbus 用反向多项式)
           ② 8 次循环不是 16 次
           ③ Modbus 协议 CRC 低字节在前(很多新人搞反)
           ④ 5 分钟白板写不出 = 你背得不够熟
```

#### 卡片 2(来自 B2)

```
【正面】Channel<T> 为什么比 ConcurrentQueue<T> 更适合生产者消费者?
【反面】Channel 是异步无锁(单 reader / writer 时),ConcurrentQueue 是全局锁。
【为什么】Channel 内部用 Segment 链表 + Volatile 读,SingleReader=true 时完全无锁。
   配合 `await ReadAllAsync()` 自然支持异步消费,不阻塞线程。
   ConcurrentQueue 的 `TryDequeue` 是同步阻塞,高频场景吃 ThreadPool。
【踩坑警告】① 必须 `Writer.Complete()` 否则消费者永远 await
           ② 必须 `CancellationToken` 否则退出卡死
           ③ `SingleReader=true` 一定要配对(多个消费者会乱)
           ④ 不要用 `async void` 处理消费异常(会吞)
```

#### 卡片 3(来自 B16)

```
【正面】TCP 粘包状态机遇到"假帧头"(AA 后不是 55)怎么办?
【反面】只移除 1 字节(AA),从下一字节继续扫 0xAA,不要整段丢弃。
【为什么】AA 可能是真帧头被噪声污染的前一字节,丢整段会丢真数据。
   丢 1 字节最坏情况是丢一个垃圾字节,数据完整性最高。
【踩坑警告】① `RemoveAt(0)` 是 O(N),高频场景用 ArrayPool + 环形缓冲
           ② 缓冲找不到 AA 必须清空(否则内存爆)
           ③ CRC 校验范围要对(头 + 长 + 载荷,不含 CRC 本身)
           ④ `RemoveRange(0, idx + total)` 不是 `RemoveRange(idx, total)`
```

#### 卡片 4(来自 C1)

```
【正面】`bus.DataArrived += async (s, e) => await SaveAsync(e)` 为什么是炸弹?
【反面】Lambda 是 `async void`(EventHandler 签名),SaveAsync 抛异常时进程直接崩,没 Task 能 catch。
【为什么】async void 的异常直接抛到 SynchronizationContext,WPF 默认会让进程崩溃。
   编译器生成的状态机无法被外部观察,取消/超时/异常全部"消失"。
【踩坑警告】① 改同步入队:bus.DataArrived += OnData;然后在 OnData 里 _ = Task.Run(...)
           ② 更优:用 Channel<T> 串行化(异步消费,不阻塞事件源)
           ③ WPF Button_Click 是 async void 唯一合法场景,但内部必须 try-catch + 日志
           ④ 库代码见到 async void 99% 是 bug
```

#### 卡片 5(来自 B5)

```
【正面】报警回滞的 HighThreshold 和 LowThreshold 谁大?低报场景反过来吗?
【反面】高报:High > Low(80 / 70,超 80 报,降到 70 以下解);低报:Low < High(20 / 30,低 20 报,升到 30 以上解)。
【为什么】回滞本质是"两个阈值中间是迟滞区",防止边界抖动反复触发。
   高报场景:Low 是"解除阈值",必须在 High 之下才有效果。
   低报场景反之。
【踩坑警告】① 必须边沿检测(只在 wasActive ≠ nowActive 触发事件),否则反复刷 UI
           ② 回滞带大小 = 量程 2-5%(温度量程 100℃ → 带 2-5℃)
           ③ 报警风暴(1 秒 100 条)要聚合去重(同点位 1 分钟 1 次)
           ⑤ 锁要细(只锁 Evaluate + 事件触发,不要锁整个 OnSample)
```

### 每天怎么刷

| 时段 | 场景 | 张数 | 目标 |
|---|---|---|---|
| 早上通勤 | 地铁 / 公交 | 10 张 | 唤醒记忆 |
| 中午排队 | 食堂 / 电梯 | 5 张 | 碎片利用 |
| 晚上睡前 | 床上 | 5 张 | 巩固 + 反思 |
| **总计** | | **20 张/天** | 15-20 分钟搞定 |

**纪律**:
1. 卡片"快忘了才复习"(Anki 算法自动推),不要每天全刷
2. 失败 3 次的卡片标"重难点",手动加备注
3. 每周日凌晨把本周错题本里的题加进 Anki(下一周自动刷)
4. 面试前 1 小时刷 30 张(进入"考试模式")

---

## 第 4 节 · 错题本规范 📒

### 路径与格式

**文件路径**:`f:\00_project\上位机学习\错题本.md`(每天写,git commit)

**每条错题格式**:

```markdown
## 2026-08-04 周一

### ❌ 错题 #001 - B1 Modbus CRC16
- **日期**:2026-08-04
- **题号**:B1(代码肌肉训练手册)
- **错因分类**:🟢 语法 📝
- **错因细节**:把 0xA001 写成 0x8005,正向多项式忘了位反转
- **正确答案**:`crc ^= 0xA001`(Modbus 用反向多项式)
- **快忘点**:面试官追问"为什么 0xA001"必答"0x8005 位反转"
- **二刷日期**:2026-08-05(1d 后)
- **三刷日期**:2026-08-07(3d 后)
- **四刷日期**:2026-08-11(7d 后)
```

### 错因分类与标色

| 标色 | 分类 | 图标 | 占比(经验) | 说明 |
|---|---|---|---|---|
| 🔴 | **异步** | ⚡ | 30% | async void / .Result 死锁 / ConfigureAwait / Thread vs Task |
| 🟡 | **边界** | 🪟 | 25% | null / 空集合 / 除零 / 整数溢出 / 数组越界 |
| 🟢 | **语法** | 📝 | 20% | C# 关键字 / LINQ / 模式匹配 / 类型转换 |
| 🔵 | **性能** | 🚀 | 15% | GC / 内存分配 / 锁 / 异步性能 / LINQ 延迟求值 |
| 🟣 | **逻辑** | 🧠 | 10% | 算法错 / 状态机错 / 业务理解错 |

**用法**:每周日统计各色占比,**红色多** → 重点补异步专项;**黄色多** → 补防御编程;**蓝色多** → 看 [M9.5 性能压测](M9.5_性能压测与长跑稳定性_深度版.md)。

### 3 个错题样例(从 C1/C2/C5 改写)

#### 错题样例 1(C1 改写)

```markdown
## 2026-08-05 周二

### ❌ 错题 #002 - C1 async void 吞异常
- **日期**:2026-08-05
- **题号**:C1(代码肌肉训练手册 Part C)
- **错因分类**:🔴 异步 ⚡
- **错因细节**:看到 `bus.DataArrived += async (s, e) => await SaveAsync(e)` 没反应过来这是 async void,
  以为是 async Task,5 分钟找 bug 没找到
- **正确答案**:Lambda 签名 `void(object, EventArgs)`,加 async 变成 `async void`(不是 async Task!),
  异常直接抛到 SynchronizationContext,进程崩。改同步入队 + Task.Run 包 try-catch,或用 Channel<T>。
- **快忘点**:EventHandler 签名强制 void,加 async 就是 async void,这是 C# 编译器"特殊照顾"的灾难。
- **二刷日期**:2026-08-06
- **三刷日期**:2026-08-08
- **关联讲义**:[C# 陷阱](C#_陷阱_前端转上位机必看_深度版.md) 陷阱 4 · [M7 修正版](M7_OPCUA_MQTT_深度版.md)
```

#### 错题样例 2(C2 改写)

```markdown
## 2026-08-06 周三

### ❌ 错题 #003 - C2 .Result 死锁
- **日期**:2026-08-06
- **题号**:C2
- **错因分类**:🔴 异步 ⚡
- **错因细节**:WPF UI 线程调 `LoadFromApiAsync().Result`,以为是"阻塞等结果",
  没意识到 LoadFromApiAsync 内部 await 要回 UI 线程(SynchronizationContext),两边互等 = 死锁
- **正确答案**:UI 事件处理器一律 async void 包 try-catch + await。库代码加 ConfigureAwait(false) 避免捕获 SyncContext。
  绝对不要 `.Result` / `.Wait()` / `.GetAwaiter().GetResult()`(后两个一样死锁)。
- **快忘点**:面试官最爱问"为什么 .Result 死锁,Task.Run(() => xxx.Result).Wait() 不死锁?"
  → 后者在 ThreadPool 线程,无 SyncContext,不互等。
- **二刷日期**:2026-08-07
- **三刷日期**:2026-08-09
- **关联讲义**:[C# 陷阱](C#_陷阱_前端转上位机必看_深度版.md) 陷阱 4
```

#### 错题样例 3(C5 改写)

```markdown
## 2026-08-08 周五

### ❌ 错题 #004 - C5 DbContext 线程不安全
- **日期**:2026-08-08
- **题号**:C5
- **错因分类**:🔵 性能 🚀 + 🟣 逻辑 🧠
- **错因细节**:以为 Singleton 注入 DbContext 跟 Singleton 注入 PointStore 一样安全,
  没意识到 DbContext 内部有变更追踪字典 + 连接,多线程并发 SaveChangesAsync 抛
  `InvalidOperationException: A second operation was started on this context`
- **正确答案**:用 `IDbContextFactory<T>` + `await using var db = factory.CreateDbContextAsync()`,
  每次调用独立 DbContext,用完即释放。WPF 没请求作用域,Factory 比 Scoped 更直观可控。
- **快忘点**:面试官追问"为什么 IDbContextFactory 而不是 AddDbContext + Scoped?"
  → ASP.NET Core 有请求作用域自然适用,WPF / 服务这种没"请求"概念的场景,Factory 显式控制生命周期更稳。
- **二刷日期**:2026-08-09
- **三刷日期**:2026-08-11
- **关联讲义**:[M4 数据持久化](M4_数据持久化_深度版.md) · [B11 PointStore](代码肌肉训练手册_30天刷题版.md)
```

### 错题本纪律

1. **当天错当天记**:不要攒,隔天就忘细节
2. **每周日统计**:看哪色错题最多,下周专项突破
3. **二刷必须闭卷**:不能看答案,3 秒答不出 = 还没掌握
4. **三刷四刷加入 Anki**:让算法自动推复习时机
5. **面试前 1 天**:全错题本过一遍(目标 30 分钟内)

---

## 第 5 节 · "快忘了"信号 + 立即复习触发器 🚨

### 5 大"快忘了"信号

| 信号 | 表现 | 说明 |
|---|---|---|
| 🔴 **写代码卡壳超 10 秒** | 手写 B1 时忘了 `0xA001` 怎么写 | 肌肉记忆退化了 |
| 🟡 **看到题不知道用什么** | 问"TCP 粘包怎么解",你想不到状态机 | 概念链接断了 |
| 🟠 **讲不出"为什么"** | 知道用 Channel,但讲不清为什么不用 ConcurrentQueue | 表层记忆,没内化 |
| 🔵 **细节说不全** | 知道回滞,但讲不清 HighThreshold 和 LowThreshold 谁大 | 模糊记忆 |
| 🟣 **类比前端讲不出** | 知道 MVVM,但说不出"ViewModel 像 React 的 useState" | 跨域链接断 |

### 立即复习触发器(信号 → 行动)

```
检测到信号(任一)
    ↓
立即查 30 天排程表(第 1 节)
    ↓
看今天"复习队列"有没有这道题
    ├─ 有 → 提前复习,标"信号触发"
    └─ 无 → 当天加复习一遍(加 15 分钟)
            ↓
            复习后能否 3 秒答?
            ├─ 能 → 标"信号已恢复",正常继续
            └─ 不能 → 记错题本(标红),明天再复习一遍
```

### 实战场景模拟

**场景 1**:Day 18 你在写代码,B14 OPC UA 卡壳了想不起 `Subscription` 和 `MonitoredItem` 关系。
- **触发**:🔴 信号 1
- **行动**:打开本文档第 1 节 Day 18 行,看复习队列里没 B14 → 加 15 分钟复习 → 看速记卡 W4.19 → 3 秒能答 → 继续。

**场景 2**:Day 25 你在准备面试,模拟时讲不出"为什么 IDevice 抽象这么设计"。
- **触发**:🟠 信号 3
- **行动**:查 Day 25 行 → 复习队列有 Day 18(B14)+ Day 23(B27-B30)→ 没直接对应 → 加复习 [M9 工程素养](M9_工程素养_测试DI容错_深度版.md) IDevice 章节 → 默背 W2.27 → 讲清 → 记错题本(防再忘)。

**场景 3**:Day 30 你毕业测试白板 B16 TcpDevice 粘包,写到 `needResync` 卡了 10 秒。
- **触发**:🔴 信号 1
- **行动**:这是毕业测试,不能查文档 → 标"未达标" → 记错题本 → Day 30 复习队列里有 Day 15(B16)→ 重写一遍 → 直到 5 秒能写出。

### 信号统计(每周日闭卷自测后做)

| 信号类型 | 本周触发次数 | 主要在哪题触发 | 下周专项 |
|---|---|---|---|
| 🔴 卡壳超 10s | ___ | ___ | ___ |
| 🟡 不知用什么 | ___ | ___ | ___ |
| 🟠 讲不出为什么 | ___ | ___ | ___ |
| 🔵 细节说不全 | ___ | ___ | ___ |
| 🟣 类比讲不出 | ___ | ___ | ___ |

**判据**:任一信号一周触发 ≥ 3 次 = 该知识点没固化,**下周每天加 5 分钟专项刷**。

---

## 第 6 节 · 与 30 天路线的集成 🔗

### 每天 4 块变 5 块(新增复习队列)

旧版 30 天路线每天 4 块:学习 + 项目 + 代码肌肉 + 打卡。**本机制插入第 5 块**:

```
每天 = 学习(2h)+ 项目(1.5h)+ 代码肌肉(1h)+ 【复习队列 15-20min】+ 打卡(0.5h)
        ↑                                                            ↑
        新学内容                                                      间隔重复(本机制)
```

**总时长**:4-5 小时(复习队列占 15-20min,占比 5-7%,**性价比极高**)。

### 关键日历节点

| 日期 | 类型 | 内容 |
|---|---|---|
| **每周日** | 闭卷自测 30 题 | Week 1/2/3/4 各一版,共 120 题 |
| **Day 7** | Week 1 闭卷 + 7d 间隔首考 | 检验 Day 1 学的 C# 陷阱是否真记住 |
| **Day 14** | Week 2 闭卷 + 15d 间隔首考 | Day 1 学的内容第一次 15d 考验 |
| **Day 21** | Week 3 闭卷 + 7d 二次考 | Week 1 闭卷题二次复刷(目标 95%+) |
| **Day 28** | Week 4 综合闭卷 + 15d 二次考 | Day 13 学的 Prism 内容 15d 考验 |
| **Day 30** | **🔥 毕业测试** | 不查文档白板五大件 60min + 综合闭卷 30 题 + **30d 间隔终极考验**(Day 1 学的 C# 陷阱) |

### Day 30 毕业测试详情

**上午(1.5h)**:
- **五大件白板**(60min):B1 CRC16(5min)+ B2 Channel(10min)+ B5 AlarmRule(15min)+ B16 TcpDevice 粘包(15min)+ B13 MQTT 双向(15min)。每题独立计时,超时也算完成但要标"未达标"
- **任意一段代码 5 秒内说坑**(10min):让朋友 / 镜头随机指 5 段代码,你 5 秒内说"这里有什么坑"

**下午(1h)**:
- **综合闭卷 30 题**(从 Week 1-4 各抽 7-8 题):盖答案,3 秒内答完,正确率 ≥ 85%
- **30d 间隔终极考验**:Day 1 学的 C# 8 大陷阱还能 3 秒答完 → **永久记忆达成**

**毕业判据**(全部满足):
- ✅ 五大件白板 60min 内完成,代码能跑
- ✅ 综合闭卷 30 题正确率 ≥ 85%
- ✅ Day 1 学的 C# 8 大陷阱 3 秒答完(30d 间隔通过)
- ✅ 错题本累计错题 ≤ 50 条(超过说明掌握度不够)
- ✅ 5 段代码坑 5 秒内说全

**未达标处理**:延期 3-5 天,集中补弱项,再考一次。

### 与 30 天路线的同步策略

| 30 天路线模块 | 本机制对应章节 | 协作方式 |
|---|---|---|
| [Week 1 主干打底](30天作战路线_转行冲刺版.md) | 本机制 Week 1 排程 | 路线决定"学什么",本机制决定"何时复习" |
| [代码肌肉 Part D](代码肌肉训练手册_30天刷题版.md) | 本机制 30 天排程的"代码肌肉"列 | 路线给题号,本机制给复习节奏 |
| [速记卡 Q1-Q23](面试高频知识点_速记卡.md) | 本机制每天的"速记卡"行 | 路线给卡片,本机制给倒推时机 |
| [面试逐字稿 30 题](面试问答_逐字稿_30题.md) | 本机制 Week 4 闭卷题 | Week 4 才开始背诵(避免太早背忘) |

**冲突处理**:如果某天复习队列 + 新学时间超 5 小时,优先砍新学内容(Project / 讲义),保复习队列(因为复习是巩固,新学是增量,巩固比增量重要)。

### 边面试边学期间的复习调整

进入 Week 4 后期(Day 26+),开始真面试,**复习策略调整**:

| 面试反馈 | 当晚复习动作 |
|---|---|
| 被问倒的题 | 立刻记错题本 + 当晚必复习 + 第二天加刷 |
| 答得模糊的题 | 当晚复习 + 加进 Anki |
| 答得好的题 | 跳过(已固化) |
| 完全没准备过的题 | 加进错题本 + 找讲义补 + Week 4 末统一刷 |

**原则**:**每场面试后当晚必复盘**,不复盘 = 浪费这场面试的"暴露弱点"价值。

---

## 📚 配套材料导航

| 材料 | 用途 | 何时用 |
|---|---|---|
| [30 天作战路线](30天作战路线_转行冲刺版.md) | 总览 + 每天新学内容 | 每天 |
| [代码肌肉训练手册](代码肌肉训练手册_30天刷题版.md) | B1-B30 + C1-C10 题库 | 每天代码肌肉 + 复习队列 |
| [面试高频知识点速记卡](面试高频知识点_速记卡.md) | Q1-Q23 通勤刷 | 每天速记卡列 |
| [面试问答逐字稿 30 题](面试问答_逐字稿_30题.md) | Week 4 背诵 + 面试前复习 | Week 4 开始 |
| [C# 陷阱讲义](C#_陷阱_前端转上位机必看_深度版.md) | 8 大陷阱深度 | Day 1 必读 + 反复回看 |
| [M19 问题排查](M19_问题排查与调试_深度版.md) | 调试方法 + 工具 | Day 7 / 21 复习 |
| 本文档 | 间隔重复排程 + 闭卷自测 + 错题规范 | 每天复习队列 + 每周日自测 |

---

## 🎯 最终判据(30 天后你应该有的)

- ✅ **30 天间隔重复完整执行**(每天 15-20min 复习队列,无中断)
- ✅ **4 次周日闭卷自测**(120 题累计正确率 ≥ 85%)
- ✅ **错题本 ≤ 50 条**(每条都四刷通过)
- ✅ **Anki 卡片 ≥ 100 张**(每天 20 张刷过)
- ✅ **5 大件白板 60min**(CRC / Channel / AlarmRule / TcpDevice / MQTT)
- ✅ **Day 1 学的 C# 8 大陷阱 30d 后还能 3 秒答**(永久记忆)
- ✅ **任意代码 5 秒说坑**(真技术硬实力判据)

---

> **最后一句**:这不是"额外的复习任务",是**让前 24 天的努力不打水漂**的保险。
> 没有本机制,你 Day 1 学的 CRC 到 Day 25 已经忘 80%,Week 4 面试时连 5 大件都写不全。
> 有了本机制,Day 30 你能在白板上 60min 写完五大件 + 3 秒答 30 题综合闭卷 + 30 天前学的陷阱还能脱口而出。
> **这就是 13-15K 和 8-10K 的差距,70% 在这里**。
>
> 开始 Day 1 → [30 天作战路线](30天作战路线_转行冲刺版.md) + 本文第 1 节 Week 1 排程表
