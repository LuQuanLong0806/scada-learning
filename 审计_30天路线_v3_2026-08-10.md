# 审计 · 30 天作战路线第二轮(v3)· 知识点连贯性 / 正确性 / 深度

> **触发**:用户反馈"仔细检查知识点连贯性、正确性、深度"
> **审计日期**:2026-08-10
> **审计方法**:4 个并行 agent 分别审"讲义代码一致性 / 技术正确性 / 学习闭环 / 15K 深度",采样 100 行/讲义 × 6 个讲义
> **本轮已修复**:P0(4 处闭环断裂) + P1(路径错误 + MQTT/EF Core 概念补强)已在 30 天路线主文档直接修改
> **本报告作用**:记录 P2 留作后续补的讲义级缺口(改讲义工作量大,留给你第二轮学习时补)

---

## ✅ 本轮已修(P0 + P1,直接进 30 天路线)

### P0 闭环断裂(4 处)
| 位置 | 问题 | 修复 |
|---|---|---|
| Day 27.5 简历话术 | "运动控制实操"有诚信风险(只讲状态机不讲插补,投非标岗被拆穿) | 简历话术降级为"运控抽象层设计 + 模拟器联调",加"❌ 千万别这么写"清单 + 诚实回答模板 |
| Day 4 PlcDevice 测试 | S7.Net 的 `Plc` 是 sealed,Moq 不能直接 mock,测试卡死 | 加"先抽 `IPlcClient` 接口"步骤,`PlcDevice` 依赖接口不是 `Plc` 类 |
| Day 12 Bootstrapper 断档 | Day 12 说"改造 Bootstrapper.cs",但用户从未写过这个文件 | Day 11 末尾加 30 分钟"先写非 Prism 版 Bootstrapper",Day 12 才能"改造"它 |
| Day 17 MQTT air-gapped 冲突 | 打卡项"PLC 真的写入"在断网车间不可验收 | 改"本地 Mosquitto + MQTTX 发命令 → SimulatedDevice 改值"闭环,不依赖外网/真 PLC |

### P1 路径错误(讲义引用的代码文件实际不存在或路径错)
| Day | 错路径 | 真路径 |
|---|---|---|
| Day 1 | `Devices/SensorPoint.cs` | `Models/SensorPoint.cs` |
| Day 2-3 | `Modbus/ModbusRtuMaster.cs` | 已重构进 `Devices/ModbusDevice.cs` + `Protocol/ModbusFrameParser.cs`(无独立 Master 类) |
| Day 2-3 | `Modbus/Crc16.cs` | `Protocol/Crc16.cs` |
| Day 6 | `IAcquisitionPipeline.cs` + `AcquisitionService.cs` + `DeviceManager.cs` | 全部合并在 `Acquisition/AcquisitionPipeline.cs` 一个类 |
| Day 21 | `Recipe/RecipeManager.cs` | `Recipes/RecipeService.cs` + 3 个相关类 |

### P1 概念补强(15K 必考,讲义缺口,直接进 30 天路线)
- **Day 8-9 EF Core 4 缺**:为什么不能单例 / AsNoTracking / 并发 Token `[Timestamp]` / 生产迁移脚本(`--idempotent` 而非 `Migrate()`)
- **Day 17 MQTT 3 缺**:ManagedMqttClient vs MqttClient / Topic 通配符 `+`/`#` / QoS 0/1/2 三档语义
- **Day 26 缺失命令**:补 `dotnet new web` 创建 `DaqMonitor.Web` 的完整命令链
- **Day 27.5 状态机 E-Stop 注释**:严格 PLCopen 应进 Stopping/ErrorStop 中间态,学习版简化,加面试被追问的诚实回答

---

## ⚠️ P2 留给后续补(讲义级缺口,改 M2/M3/M4 工作量大)

### 1. M2 Modbus 深度补 3 点(15K 必考)
- **CRC16 反射多项式**:讲算法时只给 0xA001,没说**为什么是 0xA001 而不是 0x8005**。补一句:"Modbus 用 LSB-first 位移实现,采用反射多项式,0x8005 反转后 = 0xA001"。面试官追问"为啥反转"才答得上来
- **TCP vs RTU 并发性差异**:讲 TCP/RTU 差异时只说"去 CRC + MBAP 头",**漏了并发性**:TCP 走 6 字节 MBAP 头含事务元 ID,**支持并发请求**(多请求同时发,响应按 ID 匹配);RTU 单总线串行,**必须排队轮询**。15K 必问
- **TCP 粘包解法缺第三种**:M11 只讲"长度前缀 / 分隔符",**漏了"固定长度法"**(每帧定长)。面试常追问"还有别的吗"

### 2. M3 PLC 深度补 3 点(15K 半导体设备岗必问)
- **Snap7 vs S7.Net 对比**:M3 只用 S7.Net,**完全没提 Snap7**。补:Snap7 是 C++ 原生绑定,性能更高 / 支持 300/400/1200/1500 全系 / 国内厂商更常用;S7.Net 纯托管 C#,仅 200/1200/1500
- **1200 vs 1500 通信差异**:1500 PUT/GET 默认开启、1200 默认关闭;1500 有 Profinet IRT、1200 只有 RT;1500 DB 块默认优化、1200 早期版本绝对寻址
- **优化块访问机制**:M3 一句"勾掉优化访问"带过,**没讲机制**:符号寻址 vs 绝对偏移、TIA 内部按符号哈希存储、S7.Net v3 的 `ReadClass` 才能读优化块

### 3. M4 EF Core 注册方式自相矛盾(技术正确性)
- **L72 推荐 `AddDbContextFactory`**,**L113 注释又写"`AppDbContext`(Scoped 生命周期)"**。两种注册混着说会让面试官问"你到底用哪种"懵
- 修复:统一表述 — 高并发采集用 Factory、Web 请求用 Scoped;讲义改一处即可

### 4. M9 / 逐字稿 多线程 4 选 1 缺失(15K 必考)
- **Monitor vs Mutex vs SemaphoreSlim 怎么选**:Monitor 进程内轻量、Mutex 跨进程、SemaphoreSlim 异步资源限流 — 讲义没对比
- **Channel vs BlockingCollection**:Channel 异步原生 awaitable、BlockingCollection 同步阻塞消费线程,Channel 是 .NET Core 后续首选 — 讲义没对比
- **SynchronizationContext 下 await 线程切换**:WPF ConfigureAwait(true) 回 UI 线程、false 不回 — C# 陷阱提了 ConfigureAwait 但没讲 SC
- 修复:在 [面试问答_逐字稿_30题.md](面试问答_逐字稿_30题.md) Q4-Q6 区域补 4 题(每题 200 字)

### 5. 运动控制概念外延(Day 27.5)
Day 27.5 只讲 PLCopen 抽象,**没讲插补/伺服步进/EtherCAT**。**这不是 bug 是设计取舍** — 只学 1 天不可能全懂,**靠"诚实回答模板"覆盖**(已在 Day 27.5 简历话术一节加诚实降级 + 面试被追问的回避话术)。如果想再深一层,补:
- 直线插补要 2 轴 + 速度同步;圆弧插补要圆心 + 半径 + 起止角 + 进给速度
- 伺服闭环(编码器反馈,不丢步) vs 步进开环(易失步,成本 1/3)
- EtherCAT 周期 100μs~1ms vs 脉冲+方向最高 500kHz(无实时反馈)

---

## 📊 4 个维度的总评

| 维度 | 评分 | 一句话结论 |
|---|---|---|
| **代码-讲义一致性** | 6/10 | Day 1/2-3/6/21 路径错(本轮已修);仍有 `Auth/` `Health/` `Diagnostics/` `CanDevice` 等代码存在但路线未引用,属"加分可选"非必修 |
| **技术正确性** | 8/10 | 0 硬错误;Modbus CRC / async void / .Result 死锁 / DbContext 非线程安全 / PLCopen 命名全对;M4 注册方式矛盾是唯一明显 bug |
| **学习闭环** | 7/10 | Week 1=8/10、Week 2=6/10(Bootstrapper 断档已修)、Week 3=5/10(MQTT air-gapped 已修)、Week 4=8/10;Day 27.5=9/10 |
| **15K 深度** | 6/10 | 13K 稳过、15K 悬崖边;运动控制(已诚实化)、Snap7、EF 迁移、Monitor 4 选 1 是 4 个深度漏洞 |

---

## 💡 给用户的 3 条最高优先级建议

1. **投简历前**:把 Day 27.5 简历话术改成"基础版"(只写 Simulated),投非标自动化岗位被问插补时**老实说"做了抽象层,具体插补算法可入职后补"**,不要吹精通
2. **Week 1 学到 Day 4 时**:严格按"先抽 `IPlcClient` 再写 `PlcDevice`"步骤来,否则 Day 5 测试卡死你会以为代码错了去找半天
3. **Week 4 投 15K 岗位前**:补 4 件事(每件 30 分钟)— ① Snap7 vs S7.Net 看官方 wiki;② EF Core 迁移脚本试跑一次 `--idempotent`;③ Monitor/Mutex/SemaphoreSlim 选型表抄一遍;④ Modbus TCP 事务 ID 并发特性看 M2 补充章节

---

## 关联文档
- 主路线:[30天作战路线_转行冲刺版.md](30天作战路线_转行冲刺版.md) — 已应用本轮 P0+P1 修复
- 第一轮审计:[审计_整体串联复核_v2.md](审计_整体串联复核_v2.md)
- 15K 完整性:[审计_15K完整性复核.md](审计_15K完整性复核.md)
- 讲义质量:[审计_讲义质量评估.md](审计_讲义质量评估.md)
